
> lero-al@0.1.0 check:css-vars
> node scripts/check-css-var-resolvability.mjs

🔍  check:css-vars — owned custom properties (globals.css @theme/@theme inline/:root): 297
    Arm A (shipped CSS) — owned names referenced: 93
    Arm B (src/**/*.{css,tsx,ts}, excl. globals.css) — owned names referenced: 68
    Dynamic var() construction sites: 8 raw, 0 in-class (prefix could name an owned token)
    Ownership snapshot: version 1, 297 name(s) (scripts\css-var-ownership-snapshot.json)
    Blind spots (R6): a name deleted from globals.css BEFORE the snapshot's first commit is invisible to drift — the snapshot only remembers what it was told to; a dynamically-built var(--prefix${…}) construction site is covered only by the existing prefix rule (R6/§3.4), unchanged by this task; a reference living outside src/**/*.{css,tsx,ts} (Arm B's own glob) is never scanned by either arm.

ℹ️   0 fallback-bearing owned/dropped reference(s) — non-blocking (R10):

✅  check:css-vars — 0 violations, 0 in-class dynamic sites, 0 dropped-name references, 0 snapshot drift.
EXIT_CODE=0
