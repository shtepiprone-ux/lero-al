
> lero-al@0.1.0 check:card-track-monotonicity:verify
> node scripts/check-card-track-monotonicity.mjs --verify-gate

check-card-track-monotonicity.mjs --verify-gate

── Arm (a): negative — real tree, expect every in-scope story clean ──
check-card-track-monotonicity.mjs — asserting the real tree

Selector extraction failed: expected exactly 1 asset matching /^MantineListingCardTrack-.*\.css$/ in C:\Claude_Code_Projects\lero-al\storybook-static\assets, found 0: []
scope: not discovered — selector extraction failed

cannot see: a breakpoint not in the width list; locales other than en; a track rendered only after interaction; a drop that recovers between two sampled widths not at a declared breakpoint; a Story whose track mounts only after its Storybook render reaches finished.
❌ Arm (a) FAILED — the unmodified tree already reports a drop. Gate is broken or the tree has drifted.

❌ Arms (b)/(c) cannot run — selector extraction failed: expected exactly 1 asset matching /^MantineListingCardTrack-.*\.css$/ in C:\Claude_Code_Projects\lero-al\storybook-static\assets, found 0: []

── Arm (d): selector fail-closed — runGate itself, via a non-matching asset pattern (§17.5) ──
  check-card-track-monotonicity.mjs — asserting the real tree

  Selector extraction failed: expected exactly 1 asset matching /^DOES-NOT-EXIST-TASK815-.*\.css$/ in C:\Claude_Code_Projects\lero-al\storybook-static\assets, found 0: []
  scope: not discovered — selector extraction failed
  
cannot see: a breakpoint not in the width list; locales other than en; a track rendered only after interaction; a drop that recovers between two sampled widths not at a declared breakpoint; a Story whose track mounts only after its Storybook render reaches finished.
✅ Arm (d) PASS — runGate returned exit 1 and printed the cannot see: line.

── Arm (e): scope fail-closed — discovery with a title predicate matching nothing ──
❌ Arm (e) cannot run — real selectors unavailable.

── Post-plant re-check: negative arm again, confirming full restore ──
check-card-track-monotonicity.mjs — asserting the real tree

Selector extraction failed: expected exactly 1 asset matching /^MantineListingCardTrack-.*\.css$/ in C:\Claude_Code_Projects\lero-al\storybook-static\assets, found 0: []
scope: not discovered — selector extraction failed

cannot see: a breakpoint not in the width list; locales other than en; a track rendered only after interaction; a drop that recovers between two sampled widths not at a declared breakpoint; a Story whose track mounts only after its Storybook render reaches finished.
❌ Tree NOT fully restored — a plant leaked.

EXIT_CODE=1
