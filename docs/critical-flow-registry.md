# Critical Flow Registry — single source of truth for regression coverage

> **Part of Epic RS — Regression Shield.** This registry enumerates every P0/P1 critical flow in the
> project. **A task that touches any flow listed here is bound by the regression-coverage P0 rule**
> (`docs/agent-contract.md` clause 15): it must add a regression test or update the existing one, and may
> not close without proof the old behavior still works.
>
> **How to use:** find the flow you are about to touch → read its happy + failure path → ensure its
> required regression test exists and passes BEFORE you change anything (record the green baseline) → after
> your change, the same test (updated if behavior legitimately changed) must pass → flip / keep the coverage
> status and paste the command in your session log. If the flow is missing, ADD a row in the same task.
>
> **Coverage status legend:** ✅ covered by an automated regression test · 🟡 partial (gate exists but not
> the full happy+failure assertion) · ❌ no automated coverage yet (manual only) · ⏳ scheduled in a named slice.
>
> **Owner-task** = the task/epic that built or last meaningfully changed the flow (for traceability).

## P0 — Auth lifecycle (Slice 2 / Task 441)

| Flow | Route / component / action | Owner task | Happy path | Failure path | Required regression test | Command | Coverage |
|---|---|---|---|---|---|---|---|
| Login (email/password) | `AuthSheet.tsx` → `signIn` (`lib/auth/browser.ts:22`) | D-series / **441** | valid creds → session + redirect | wrong creds → localized error, no session | smoke: login ok + login wrong-creds | `npx vitest run src/lib/auth/__tests__/browser.smoke.test.ts` | ✅ (Task 441: vitest smoke — login ok, login wrong-creds error propagated, logout, logout global-scope; planted-violation: stub signIn to return error → happy-path session assertion FAILS) |
| Signup | `AuthSheet.tsx` → `signUpWithCaptcha` (`auth/actions/captcha.ts:12`) | D.x / **441** | valid → confirm email sent | dup email / weak pw / captcha fail → typed error | smoke: signup ok + dup-email fail + captcha fail (empty + turnstile reject) | `npx vitest run src/modules/auth/actions/__tests__/signUpWithCaptcha.smoke.test.ts` | ✅ (Task 441: vitest smoke — happy, empty-captcha, turnstile-reject, dup-email; planted-violation: stub signUp success instead of error → signup_failed assertion FAILS) |
| **Signup confirmation → session/header** | signup confirm link → `/auth/confirm` → `/{locale}/auth/verified` | **446** | confirm link → state CONSISTENT: header authenticated iff session exists, and `/auth/verified` copy matches the real session state (no "you can use all features" while guest) | expired/used token → localized error (`verified_error_title/body`) + Login CTA, no false success; confirm-but-sync-fail → `verified_nosession_body` + Login CTA; double-click → same expired handling | (1) route: Set-Cookie present on success redirect + (2) page: syncFailed guard prevents success copy while guest | `npx vitest run src/app/auth/confirm/__tests__/route.test.ts "src/app/[locale]/auth/verified/__tests__/page.test.tsx"` | ✅ (Task 446 rework: Storybook contract-clean — `VerifiedCard` single-source, no STRINGS hardcode, no locale pin; 15 tests (9 route + 6 page invariant), 2 planted-violation FAILs confirmed; `screenshots:assert --fast` 924/924 PASS sq/en/uk/it × 320/375/390 2026-06-16) |
| Recovery request | `AuthSheet.tsx:210` → `requestPasswordResetWithCaptcha` (`captcha.ts:48`) | 121/122 / **441** | email → neutral success (no enumeration) | captcha fail → error; unknown email → still neutral | smoke: request neutral-success + non-enumeration assert + captcha-fail + supabase-error | `npx vitest run src/modules/auth/actions/__tests__/requestPasswordReset.smoke.test.ts` | ✅ (Task 441: vitest smoke — neutral success, non-enumeration (unknown email → same ok:true), captcha fail, supabase error; planted-violation: unknown email → error response → non-enumeration assertion FAILS) |
| Recovery link → reset | hook `buildConfirmUrl` (`auth-email-hook/route.ts:191`) → reset page (`ResetPasswordClient.tsx`) | 224 / **439** / **441** | link lands on reset form; set new pw; verify only on user gesture; usable ≥15 min | scanner GET does NOT burn token (URL → reset-password, NOT /auth/confirm); expired/used → localized + request-new CTA | smoke: (1) hook sends resetUrl → /auth/reset-password NOT /auth/confirm; (2) component mounts WITHOUT calling verifyOtp (tokenHash branch + legacy no-tokenHash → getSession only); (3) submit-path → verifyOtp called only on user gesture with correct token_hash/type; (4) success path → link usable post-mount; (5) expired/used token on submit → expired state + request-new CTA | `npx vitest run src/app/api/auth-email-hook/__tests__/recoveryUrl.smoke.test.ts src/modules/auth/components/__tests__/ResetPasswordClient.smoke.test.ts` | ✅ (Task 441 rework: 8 vitest smokes — hook recovery URL → reset-password NOT /auth/confirm (3 tests); ResetPasswordClient (5 tests): mount no verifyOtp on tokenHash branch + no-tokenHash → getSession only, submit → verifyOtp on user gesture with correct args, success path → link usable post-mount, expired/used → expired state + request-new CTA; prod-validated 2026-06-16 Task 439; planted-violation A: route recovery to /auth/confirm → URL assertion FAILS; planted-violation B: verifyOtp on mount → 4 ResetPasswordClient assertions FAIL) |
| Logout | `signOut` (`lib/auth/browser.ts:56`) | D-series / **441** | session cleared, header reflects guest | — | smoke: logout clears session (default scope + global scope) | `npx vitest run src/lib/auth/__tests__/browser.smoke.test.ts` | ✅ (Task 441: covered in browser.smoke.test.ts — signOut called, global scope forwarded) |
| Self-retriggered SIGNED_OUT loop guard | `handleAuthEvent` SIGNED_OUT branch (`lib/auth/controller.ts:190`) | **449** | SIGNED_OUT while authenticated → one server verification → settles unauthenticated; localStorage-cleared-but-cookies-valid → re-verifies and stays authenticated | self-retriggered SIGNED_OUT while already unauthenticated → guard returns immediately, no fetch; guest first paint + SIGNED_OUT → no /api/auth/me poll | Scenario 9 cases 1–4: loop broken (exactly 1 fetch), no-fetch when already unauth, localStorage-cleared re-verify, genuine sign-out settles with 1 verification | `npx vitest run src/lib/auth/__tests__/controller.test.ts -t "Self-retriggered SIGNED_OUT"` | ✅ (Task 449: 4 vitest regression cases; planted-violation: guard removed → 3/4 FAIL — case 1 loops→error, case 2 fetches when shouldn't, case 4 loops→error; case 3 passes because server returns user so coreSignOut never reached) |
| OAuth (Google) | `signInWithOAuth` + `/auth/callback/route.ts` | D-series | PKCE code → session + profile ensured | code error → locale-aware login redirect | documented manual-only: requires real provider + browser session; no mock path without full PKCE roundtrip | MANUAL — cannot be faked in Vitest without live OAuth provider | ❌ (manual-only documented in Task 441; OAuth + magic-link exempt per kickoff contract) |
| Magic link | hook `magiclink` → `/auth/confirm` | 122/224 | token-hash verify → session | invalid/expired → login redirect | documented manual-only: requires real email + live Supabase token | MANUAL — token consumed server-side, cannot be replicated with static stub | ❌ (manual-only documented in Task 441; exempt per kickoff contract) |
| Email change | `initiateEmailChange`/`consumeEmailChangeToken` → `/auth/confirm-email` | custom / **441** | token consumed → email updated (auth.admin.updateUserById called) | expired/consumed/invalid token → typed errorCode | smoke: happy + expired + consumed + invalid token | `npx vitest run src/modules/cabinet/actions/__tests__/deleteOwnAccount.smoke.test.ts` | ✅ (Task 441: 4 vitest smokes — happy (updateUserById called), invalid (not found), consumed (consumed_at set), expired (expires_at in past); planted-violation: remove consumed_at guard → consumed assertion FAILS) |
| **Self-delete + email reuse** | `deleteOwnAccount` (`cabinet/actions/index.ts:235`) + `ProfileTab.tsx` | 12-19 / **439** / **441** | hard-delete auth user → **same email signs up again** | auth-delete fail → error, NOT success; profile-delete fail → no auth.admin.deleteUser call | smoke: delete happy (deleteUser called), auth-fail → profile_deleted_auth_failed NOT success, profile-fail → delete_failed + no deleteUser, unauthorized | `npx vitest run src/modules/cabinet/actions/__tests__/deleteOwnAccount.smoke.test.ts` | ✅ (Task 441: 4 vitest smokes — happy (auth.admin.deleteUser called = email freed), auth-delete fail → profile_deleted_auth_failed NOT success, profile-delete fail → delete_failed + no deleteUser, unauthorized; prod-validated 2026-06-16 Task 439; planted-violation: auth-delete fails but action returns {} → error assertion FAILS) |
| **Authenticated header hydration — NotificationBell SSR shell** | `Header.tsx` (`notificationSlot`) → `NotificationBell.tsx` → `NotificationBellView.tsx`, route `/{locale}` authenticated | **599** | authenticated `/{locale}` (header right-cluster: LocaleSwitcher + UserMenu + NotificationBell) hydrates with ZERO console hydration errors; server HTML ↔ hydrated DOM `useId` target ids on the LocaleSwitcher/UserMenu Mantine Menus are identical | `ssr:false` dynamic import or `if (loading) return null` reintroduced on `NotificationBell` → server↔client tree asymmetry on the header right-cluster → Mantine `useId` target-id hydration mismatch | **Authoritative (Task 601):** `src/components/layout/__tests__/header-hydration-id-parity.test.tsx` — deterministic jsdom dual-phase `renderToString`→`hydrateRoot` harness asserting on React's `onRecoverableError`; catches the exact bug class (planted-violation proven 3/3). Secondary/supplementary: `check:hydration` extended (Task 599) to navigate `/en`+`/uk` AUTHENTICATED whenever a session is supplied, `notRealCoverage` without one (never a false PASS) — dev-only, noise-floor-affected, kept as a broader console-error net, NOT the source of truth for this bug. | **Authoritative:** `npm run test:header-hydration-id-parity` (CI-safe, no server needed). Secondary: `HYDRATION_GATE_STORAGE_STATE=<path> BASE_URL=http://localhost:PORT npm run check:hydration -- --with-admin` — **MUST target a running `next dev` server, NEVER `next start`/production** (React strips hydration-mismatch console warnings from production builds) | ✅ (Task 599, 2026-07-15 — fix landed + native happy-path observed (owner `next dev` + captured session → authenticated `/en`+`/uk` PASS 7/7; manual clickthrough all locales clean; 10 rendered PNGs) **BUT regression coverage is NOT yet real — a GATE BLIND SPOT was found (owner, 2026-07-15):** with the violation replanted the page threw an unrelated corrupted-`.next` runtime error (`Cannot find module '../chunks/ssr/[turbopack]_runtime.js'`) and `check:hydration` STILL reported PASS — the gate scans `console` for hydration patterns only and ignores the navigation HTTP status + `pageerror`/Next error-overlay, so it gives a FALSE PASS on ANY hard-errored page. Whether it reliably catches the actual `useId` mismatch is therefore UNPROVEN natively. → **Task 600 opened** to harden the gate (fail on non-200 / `pageerror` / error-overlay presence) AND prove it catches a real planted hydration mismatch in a clean `.next` env. fix landed — `Header.tsx` static import replaces `dynamic(...,{ssr:false})`; `NotificationBell.tsx` always renders the `NotificationBellView` shell instead of `return null` while loading; SSR-safety guard confirmed no render-time `window`/`document`/`localStorage`/browser-Supabase access anywhere in the tree. Gate-logic self-test (`check:hydration:admin-config`, CI-safe, no server) confirms the new authenticated-homepage route plan is correctly gated (session→navigable, no-session→`notRealCoverage`, never a false PASS) — states 4–5, both green. **Sandbox-observed (not a verdict, agent-contract clause 14 — Cowork sandbox is a screen only):** `next dev` with a real captured Supabase session showed a clean differential with the pre-fix `ssr:false` code restored — guest `/en` PASS 3/3 vs authenticated `/en` FAIL 3/3 across 3 consecutive runs. Post-fix, a freshly-restarted sandbox `next dev` still showed authenticated-route failures in a minority of runs (indistinguishable in this sandbox from pre-existing dev-mode flakiness that also hits guest-only routes with no bell, e.g. `/uk`/`/sq`/`/listings`, on unrelated unchanged code). Rendered proof: authenticated header visually byte-identical at 10 breakpoint×locale combos (uk@320/375/390 + sq/en/it@375 + sq/en/uk/it@1280), `docs/sessions/2026-07-15-task599-assets/`, no layout shift, bell chrome unchanged. **OPEN — requires owner NATIVE `next dev` run to close:** (1) fix clean ≥3/3 consecutive runs on the authenticated-homepage routes, (2) replant `ssr:false` → authenticated routes FAIL while guest stays clean, (3) revert → PASS again. Reopen criterion if the native run still shows authenticated failures WITH the fix applied: capture which `id` attributes mismatch and whether the SERVER-rendered HTML shows the guest or authenticated header shape — if the server HTML itself is wrong (guest shape) that is a SEPARATE, larger `useUser()`/`AuthContext` SSR-consistency bug, not this fix.) **Task 600 (2026-07-15) — gate blind spot closed, still 🟡 pending the same owner native run:** `checkRoute` hardened — now FAILs on a non-OK `goto` response (≥400), any uncaught `pageerror`, or the Next dev error-overlay dialog present in the DOM (selector `#nextjs__container_errors_label` inside `<nextjs-portal>`'s shadow root, verified empirically against a real running Next 15 dev instance — confirmed present only with a genuine thrown error, absent on a clean 200 page; the `<nextjs-portal>`/`devtools-indicator` element itself is NOT a valid signal, it's always present in dev). `RUNTIME_ERROR_PATTERNS` added alongside (not replacing) `HYDRATION_PATTERNS`. New CI-safe, deterministic `check:hydration:error-page` self-test (3/3 exact-match runs: HTTP 500 page → FAIL, uncaught-`pageerror` page → FAIL, clean page → PASS) proves the hardening is not a no-op — this is the authoritative, non-flaky proof for the hardening itself. **Sandbox-observed (not a verdict) with the hardened gate, clean `.next`:** fixed code gave a clean 3/3 PASS on the authenticated-homepage rows on the first clean-compile run (the hardening also correctly caught an unrelated genuine `/en/admin/users` HTTP 404 in every run — real-world proof it's not a no-op beyond the synthetic self-test); replanting both original violations then gave a mixed signal across repeated runs (1 authenticated FAIL out of 3), confounded by the same ambient sandbox dev-mode flakiness on unrelated guest routes documented in the Task 599 entry above — consistent with, not contradicting, that finding. The exact owner-native commands (clean `.next`, fresh session, ≥3 runs each direction) are unchanged from the Task 599 entry above and remain the closing gate for BOTH this row and Task 600. **Task 601 (2026-07-15) — owner ran the Task 600 AC5 native proof; result CONFOUNDED, row stays 🟡.** On native `next dev` + a real captured session: with the FIX applied `check:hydration` FAILed on unrelated bell-less routes (`/en/listings`, `/sq`, `/uk`) with a generic "some attributes… didn't match" warning; with `ssr:false` REPLANTED it went all-PASS 3/3. The FAILs anti-correlated with the code and tracked server warmth (cold-compile runs FAILed, warm runs passed) ⇒ **the dev-mode Turbopack hydration-warning noise floor is NATIVE, not sandbox-only** — `check:hydration`'s console-scan cannot isolate this specific header `useId` mismatch (and React strips the warnings from prod entirely). → **Task 601 opened** for a deterministic, warning-independent proof: compare the header Menu **target ids in the raw SSR HTML vs the hydrated DOM** (the `useId` offset is a physical id divergence, detectable directly) against a prod build with a captured session; planted `ssr:false` must diverge the ids → FAIL, reverted → PASS ≥3/3. This row flips to ✅ on that green planted-violation proof. Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_601_HydrationIdParityDeterministicProof.md`. **Task 601 continued (2026-07-15) — the settled-DOM id-parity primary approach FAILED via a STOP-AND-ASK, then the orchestrator-authorized dual-phase `onRecoverableError` harness SUCCEEDED — row flips to ✅.** First attempt (`scripts/check-header-id-parity.mjs`, comparing header Menu-target ids in the raw SSR HTML vs the SETTLED hydrated DOM, run natively against a real `next build`+`next start` with a captured session) was empirically proven to be a false-positive generator by 3 convergent native experiments: (1) settled-DOM comparison FAILed identically on BOTH the fixed HEAD code and the `ssr:false`-replanted buggy code — Mantine's `useId` hook (`@mantine/hooks/esm/use-id/use-id.mjs`) unconditionally overwrites every target's id with a fresh `randomId()` via `useIsomorphicEffect` shortly after ANY mount, erasing the SSR-matching id before any external tool can read it; (2) a MutationObserver capturing the full id-mutation history showed an identical single-coalesced-mutation pattern in both cases (browser mutation coalescing erases the transient hydration-corrected value); (3) direct console-warning capture under `next dev` with the bug replanted showed zero hydration warnings across 4 consecutive runs including a cold `.next` cache. STOP-AND-ASK raised per the kickoff's own escape hatch; the owner/orchestrator accepted the finding, rejected premature closure, and authorized a **deterministic jsdom dual-phase `renderToString`→`hydrateRoot` vitest harness** asserting on React's own `onRecoverableError` callback instead (immune to both the dev noise floor and prod warning-stripping by construction — no console, no Next server, no Turbopack). New test: `src/components/layout/__tests__/header-hydration-id-parity.test.tsx` renders the REAL `HeaderView`/`LocaleSwitcher`/`UserMenu`/`NotificationBell` via `HeaderView`'s own `notificationSlot` prop seam (the exact real production wiring, satisfying the mandatory faithfulness check — the bell sits at its real tree position, sibling-shifting `UserMenu`'s `useId` fork encoding exactly as `dynamic(ssr:false)` does). **Planted-violation proof (clause 15):** asymmetric tree (bell absent server / present client, the pre-Task-599 shape) → `onRecoverableError` fires 3/3 runs, captured error text confirmed showing the SSR-rendered `UserMenu` target's own `aria-haspopup="menu"` / `id="mantine-_R_ann_-target"` attributes hydration-matched against the client's positionally-shifted NotificationBell button — an exact, independently-verified reproduction of the original Task 599 symptom. Symmetric tree (bell present both phases = current fixed HEAD shape) → `onRecoverableError` never called, 3/3 stable; symmetric guest-shaped control (bell absent both phases) → never called. `npm run test:header-hydration-id-parity` — CI-safe, jsdom-only, deterministic, zero flakiness across repeated runs. The earlier settled-DOM script is KEPT (not deleted) as a narrower structural-drift detector (its own `--verify-gate` self-test still proves that narrower claim) with its header comment rewritten to document this finding and redirect to the vitest test for the useId-mismatch bug class specifically. Session: `docs/sessions/2026-07-15-task601-header-hydration-id-parity-deterministic-proof.md`. |

## P0 — Admin lifecycle (Slice 4 / Task 443)

| Flow | Route / component / action | Owner task | Happy path | Failure path | Required regression test | Command | Coverage |
|---|---|---|---|---|---|---|---|
| Admin users list loads | `/admin/users` | HH | list renders, no console/hydration error | — | smoke + hydration gate | `BASE_URL=http://localhost:3000 npm run check:hydration --with-admin` (owner-run with auth cookies; admin routes require HYDRATION_GATE_COOKIES) | 🟡 (436: gate script live, requires owner auth run to fully cover; Task 443 note: owner-run hydration gate — requires live server + real auth cookies + real user UUID; deterministic Vitest smoke not feasible without product change; Slice 6 / Task 445 scope) |
| Admin user detail loads | `/admin/users/[id]` (`AdminUserProfile.tsx`) → its `DatePicker` (`src/components/shared/DatePicker.tsx`) | 434, **565** | renders, no hydration/date-format mismatch; **Task 565:** `DatePicker`'s weekday row/month-year header/today label render byte-identical server↔client at every locale, no `sq`/other-locale English fallback | — | hydration gate on this exact route (MUST include `/admin/users/[id]`) — live-authenticated capture is owner-run only (no Sonnet-session auth cookies available); **Task 565:** vitest mounts the REAL `DatePicker` OPEN under real `sq`/`uk` `NextIntlClientProvider`s with `Intl.DateTimeFormat` monkey-patched to throw, asserting the localized weekday/month/today-label text still renders | `HYDRATION_ADMIN_USER_ID=<uuid> BASE_URL=http://localhost:3000 HYDRATION_GATE_COOKIES='[...]' npm run check:hydration --with-admin` · `npx vitest run src/components/shared/__tests__/DatePicker.localization.test.tsx` | 🟡 (Task 448: HYDRATION_ADMIN_USER_ID-parametrized; when unset → NOT-REAL-COVERAGE/skip; full ✅ requires owner auth run with real user UUID; Task 443 note: deterministic Vitest smoke not feasible — Slice 6 / Task 445 scope). **Task 565 (2026-07-09) — ✅ DatePicker localization ICU-independence covered:** root cause = same Chromium-lacks-`sq`-ICU class as Tasks 562/563/564, at `DatePicker`'s 3 remaining `Intl.DateTimeFormat(locale,...)` call sites (weekday row, month/year header, today label — the last runtime-locale `Intl.*` call sites in the whole app per the Task 564 grep sweep). Fixed by sourcing all 3 from static `common.calendar_*` i18n data — `calendar_weekdays_short`/`calendar_months` (nominative, reused unchanged) + a NEW `calendar_months_formatting` array (day-bound/genitive month form, needed for `uk`'s `9 липня` today label, which nominative `calendar_months` cannot supply) added to all 4 `messages/*.json` (2123 keys × 4, `check:i18n` green). 2 RTL tests, all green; planted-violation verified for EACH of the 3 call sites individually (reverted to raw `Intl.DateTimeFormat`, under the broken-Intl simulation the mount genuinely crashes for each), reverted → 2/2 PASS. `grep -rn "new Intl\.(Number\|DateTime)Format(" src/` now returns ONLY `PasswordChangedEmail.tsx` (×2, justified, hardcoded `sq-AL`, server-only email render) — every runtime-locale `Intl.*` call site in the app is eliminated. |
| User status / role / account-type change | `updateUserProfileFull` (`modules/admin/actions/index.ts`) | 427 / **448** | change persists + `user_status_history` / `user_change_log` row written | permission denied → `{ error: 'Forbidden' }` | smoke: status-change writes history row; profileType-change writes change-log row; forbidden/unauthorized paths | `npm run test:admin` · `npx vitest run src/modules/admin/actions/__tests__/updateUserProfileFull.smoke.test.ts` | ✅ (Task 448: vitest smoke — status-change writes history, profileType-change writes change-log, forbidden + unauthorized paths; planted-violation FAIL confirmed) |
| Clear history (success) | `clear_user_history()` RPC (`admin/actions`) | 246 | rows cleared, audited | rpc failure → `clear_failed` + console.error logged | smoke: vitest unit, action contract | `npm run test:admin` · `npx vitest run src/modules/admin/actions/__tests__/clearHistory.smoke.test.ts` | ✅ (Task 436: vitest smoke — happy path + rpc-failure diagnosable) |
| Clear history (no-op race) | same | 432 | `{cleared:0}` → neutral info toast | — | smoke: no-op neutral toast (cleared=0 path) | `npm run test:admin` · `npx vitest run src/modules/admin/actions/__tests__/clearHistory.smoke.test.ts` | ✅ (Task 436: vitest smoke — cleared=0 no-op path covered) |
| Hard-delete user (admin) | `hardDeleteUser` (`admin/actions/index.ts:564`) | **443** | listings archived → row deleted → `auth.admin.deleteUser` called = email freed | forbidden (both variants) / Unauthorized / delete_failed (no auth call) / profile_deleted_auth_failed | smoke: happy (ordering: archive→delete→authDelete, authDelete called once) + forbidden (false + throw) + Unauthorized + delete_failed + profile_deleted_auth_failed; planted-violation noted | `npm run test:admin` · `npx vitest run src/modules/admin/actions/__tests__/hardDeleteUser.smoke.test.ts` | ✅ (Task 443: vitest smoke — 6 tests; happy: ordering invariants + auth.admin.deleteUser called once = email freed; forbidden both variants + Unauthorized = zero side effects; profile-fail = no auth call; auth-fail = diagnosable console.error; planted-violation FAIL confirmed) |
| Verify / revoke agent (table action) | `toggleUserVerified` (`modules/admin/actions`) ← `AdminUsersTable.tsx` (Task 483) | **483** | click verify → `toggleUserVerified(userId, true)` + `toast.success('verify_success')`; click revoke → `toggleUserVerified(userId, false)` + `toast.success('revoke_success')`; per-row `opacity-50` loading guard prevents double-submit | server error on toggle → no success toast; detail link `/admin/users/[id]` intact; navigate() called correctly for tab/role/status/pagination | RTL smoke: verify-button calls `toggleUserVerified(id, true)`, revoke-button calls `toggleUserVerified(id, false)`, toast.success with correct key, detail link href, navigate for tab/role/status/pagination; loading rowClassName=opacity-50; empty state; mobile full-width style guard | `npx vitest run src/components/admin/__tests__/AdminUsersTable.smoke.test.tsx` | ✅ (Task 483 MM.1: RTL smoke — 14 tests; verify calls `(id,true)`, revoke calls `(id,false)`, no-toast on server error, detail href, tab/role/status/page navigate, prev disabled on p1, next disabled on last, loading opacity-50 class applied+cleared, mobile filter width=100%, desktop no forced width, empty state, rows render, location_request badge; planted-violation: drop `await toggleUserVerified(...)` → verify-handler test FAILS — mockToggleUserVerified never called) |
| Admin-table pagination navigation | `MantinePagination` (`design-system/mantine/patterns/MantinePagination.tsx`) ← `MantineAdminSurfacePattern.tsx` | **535** | click a page number / Prev / Next → `onChange(page)` fires with the correct page; Prev disabled on page 1, Next disabled on the last page; controls render single-line (`flex-wrap:nowrap`+`overflow:hidden`) at every `total`/`value`, never wrapping to a 2nd row or h-scrolling, even at 320px with `total` in the hundreds (asymmetric shed-to-fit) | `total<=0` → renders nothing, no crash; `total=1` → renders `Prev·1·Next`, both edges disabled, no crash | vitest: `computeShedRange` full-level equivalence with `@mantine/hooks` `usePagination` (9 total/active combos) + asymmetric-shed unit tests (drop-trailing, drop-leading mirror, floor) + RTL: Next/Prev `onChange` value, Prev/Next disabled at boundaries, `flex-wrap:nowrap`+`overflow:hidden` computed-style invariant across 4 total/value combos, `total<=0`/`total=1` no-crash | `npx vitest run src/design-system/mantine/patterns/__tests__/MantinePagination.smoke.test.tsx` | ✅ (Task 535: 22 vitest tests — 9 equivalence + 4 asymmetric-shed + 3 onChange/boundary + 5 CSS-invariant + 1 no-crash; planted-violation: `flex-wrap:'nowrap'`→`'wrap'` → all 4 CSS-invariant tests FAIL (`expected 'wrap' to be 'nowrap'`), reverted → 22/22 PASS) |
| Admin add-location sub-panel | `LocationCombobox.tsx` (`regions`+`onAddLocation` sites: `AdminUserProfile.tsx:1042`, `AdminUserCreate.tsx:281`) → `addLocation` (`modules/admin/actions/index.ts:615`) | **553, 554, 555** | toggle opens the panel; region trigger shows its placeholder ("Region"/"Регіон" — Task 555 D2 fix); Add (enabled only when name+region both set) → `onAddLocation({name_al, region_id})` → `{id}` → new city selected (`onChange(newId)`), panel closes + resets | `{error}`/no `id` → panel STAYS open, adding resets, localized generic error shown (the raw error CODE, e.g. `add_failed`, is never displayed); Add disabled while name/region missing OR while `adding`; Cancel → panel closes, fields reset, no persist call; main-field icon never overlaps text (Task 555 D1 fix — canonical `input-chrome.css`) | RTL smoke: toggle open/close + Cancel-resets-no-call; Add disabled-until-both-set; Add calls `onAddLocation` with the exact `{name_al, region_id}` shape; success → `onChange(newId)` + panel closes; failure → panel stays + localized error (not the raw code). Persisted rendered proof (Task 554): `Mantine/Primitives/LocationComboboxSubPanel` story, sub-panel forced open, 16 cells × sq/en/uk/it. Human visual proof (Task 555, §18.9): icon↔text gap + region placeholder confirmed at 320/375/390/1280 × sq/en/uk/it (geometry gate alone does NOT prove this — see §18.9) | `npx vitest run src/components/shared/__tests__/LocationCombobox.smoke.test.tsx` · `npm run screenshots:assert -- --mantine-only` | ✅ (Task 553: 4 RTL tests; planted-violation: drop the `!addRegionId` guard from Add's `disabled` condition → "Add is disabled until both name and region are set" test FAILS (`toBeDisabled()` on an enabled button), reverted → 4/4 PASS. Task 554: persisted Phase-0 story, 478/496 PASS 0 FAIL after adding it. Task 555: fixed D1 icon-overlap [canonical `input-chrome.css` guard, was checking the wrong DOM element] + D2 missing region placeholder — both owner-caught, geometry-gate-invisible per §18.9; human-inspected rendered proof at 6 breakpoint/locale combos across 4 icon-bearing consumers, not just the unchanged 478/496 count) |
| Phone entry (registration / profile / admin) | `PhoneField.tsx` → `AuthSheet.tsx` (registration), `ProfileTab.tsx` (cabinet), `AdminUserProfile.tsx`/`AdminUserCreate.tsx` (admin phone+whatsapp, ×2 sites each) → E.164 written to `phone`/`whatsapp` columns via each form's own submit action | **556** | user selects a country (compact trigger + full-width dropdown/sheet, searchable across sq/en/uk/it names) → national number typed/pasted → `onChange` emits `{national, dialCode, iso2, e164}` with a correct rebuilt E.164 on every country AND national change | invalid/mismatched paste → `onPasteError(errorKey)` fires, field content unchanged, non-phone characters filtered on type; `error` prop shows the §6e red border + wrapped message on the national field only (country trigger unaffected) | RTL smoke: country-change rebuilds E.164; national-change emits E.164; rejected paste fires `onPasteError` + does not mutate. UI-agnostic logic baseline: `phone.test.ts` (56 tests — validation/paste-normalization/placeholders/country-list, unchanged by this migration). Persisted rendered proof: `Mantine/Primitives/PhoneField` story (default + error state) | `npx vitest run src/components/shared/__tests__/PhoneField.smoke.test.tsx src/lib/phone/__tests__/phone.test.ts` · `npm run screenshots:assert -- --mantine-only` | ✅ (Task 556: presentational swap — legacy `Combobox`/`Input`/`Label` → `MantineCombobox`/Mantine `TextInput`/`InputLabel`, all phone LOGIC byte-identical; 3 RTL smoke tests; planted-violation: commented out `emit(...)` in `handleNationalChange` → "national-number change emits an updated E.164 value" test FAILS (0 calls, expected 1), reverted → 3/3 PASS; `phone.test.ts` 56/56 baseline unaffected) |
| Listings date-range filter | `RangeDatePicker` (`design-system/mantine/patterns/RangeDatePicker.tsx`) ← wired into `src/components/shared/FiltersPanel.tsx` (homepage, batch-Apply model) + `src/modules/listings/components/ListingsFilters.tsx` (listings page, URL-immediate model), replacing the two legacy single-date `DatePicker` `date_from`/`date_to` fields on each surface (**Task 559**); calendar month/weekday names sourced from `messages/*.json` `common.calendar_*` static data, NOT `Intl.DateTimeFormat` (**Task 562** — Chromium's bundled ICU has no `sq` locale data, verified via `Intl.DateTimeFormat.supportedLocalesOf(['sq'])` → `[]` in-browser vs. Node's full ICU; `sq` silently fell back to English) | **558, 561, 559, 562** | desktop: click start day (Apply enabled immediately, Task 561 D1), optionally click end day → **Apply** commits `onChange({from, to: to ?? from})` with correct ISO + closes; mobile: tap start (Confirm enabled immediately), optionally tap end → **Confirm** commits `onChange({from, to: to ?? from})` + closes; day-tap alone NEVER fires `onChange` on either path; mobile fixed-header month/year dropdowns (Task 561 D2) jump-scroll the list and reflect the in-view month; **Task 559:** each surface's `value={{from: date_from, to: date_to}}`, `onChange` writes BOTH `date_from`+`date_to` back atomically (`FiltersPanel`'s local-draft `update()` / `ListingsFilters`' URL `updateParams()`, one call each — not two sequential writes), `maxDate={today}` preserved from the legacy fields | single day then Apply/Confirm → `{from, to: from}` (Task 561 D1 — never a half-open range, never a no-op); end-before-start → swapped so `from<=to` (never emits inverted); a day past `maxDate`/before the effective min is `disabled` and clicking it does NOT stage a new endpoint; `disablePastDates` (Task 561 point 5) disables/hides every day before today on BOTH breakpoints (desktop: cell `disabled`; mobile: the whole past month SECTION is absent from the window, not rendered-then-disabled) — selectable/reachable when omitted; Cancel (desktop) / backdrop-dismiss (mobile) discards the staged range — fires nothing, panel closes; invalid incoming `value.from` guarded to empty (placeholder shown, no crash); trigger clear-X commits `onChange({undefined,undefined})` without opening the panel; **Task 559:** a URL/values already carrying `date_from`+`date_to` hydrates the trigger correctly (deep-link, SSR authority unchanged); clearing removes BOTH `date_from`+`date_to` from state/URL in the SAME call, never one-then-the-other; **Task 562:** the calendar's month/weekday labels render in the active locale's OWN language for all four locales — no silent English fallback for any locale, on either breakpoint | RTL smoke: (1) desktop pick from+to then Apply fires ONE `onChange({from,to})`; (2) end-before-start is swapped; (3) desktop single day → Apply → `{from,to:from}`; (4) a day past `maxDate` is disabled + clicking it does not stage (Apply still commits the UNCHANGED pre-seeded range, not weakened to "stays disabled"); (4b) same on a fresh/empty selection — Apply stays disabled; (5) Cancel discards staged + fires nothing; (6) mobile tap-only fires nothing, only Confirm commits; (7) mobile single day → Confirm → `{from,to:from}`; (8) `disablePastDates` disables a day (desktop) / excludes the whole past section (mobile), selectable/present when omitted; (9) trigger clear-X commits empty without opening; (10) invalid `value.from` guarded. Companion primitive-level smoke: `MantinePopover`'s `fullWidthTrigger`/`close()` additive props (click-to-open regression risk — controlled-mode Popover.Target attaches no click handler of its own; fixed by cloning the trigger). **Task 559 consumer-wiring smoke** (mounts the REAL `FiltersPanel`/`ListingsFilters`, not a stand-in harness): (1) FiltersPanel hydrates the trigger from `values.date_from`/`date_to`; (2) FiltersPanel picking a range + the panel's own "Apply filters" button commits BOTH params atomically via `onChange`/`onApply`; (3) FiltersPanel Reset clears both (existing `handleReset` semantics, clears ALL filters); (4) ListingsFilters hydrates the trigger from a URL deep-link; (5) ListingsFilters picking a range calls `router.push` with BOTH params in ONE call; (6) ListingsFilters clearing removes both params. **Task 562 localization smoke** (mounts the REAL `RangeDatePicker` open under a REAL `sq`/`it` `NextIntlClientProvider`, reads the rendered text — not a re-derivation of the same data the component reads): (1) `sq` right-month label + weekday row render Albanian ("Korrik"/"Gusht" + "hën", never "July"/"Mon"); (2) `it` right-month label + weekday row render Italian ("Luglio"/"Agosto" + "lun", never "July"/"Mon") | `npx vitest run src/design-system/mantine/patterns/__tests__/RangeDatePicker.smoke.test.tsx src/design-system/mantine/patterns/__tests__/MantinePopover.smoke.test.tsx src/components/shared/__tests__/filtersRangeDatePicker.smoke.test.tsx src/design-system/mantine/patterns/__tests__/RangeDatePickerLocalization.test.tsx` | ✅ (Task 558: initial 7 RTL tests (RangeDatePicker) + 6 RTL tests (MantinePopover). **Task 561** (owner-rejected Task 558 mobile render, 2026-07-08 — fixed 6 defects: duplicate mobile month label, weekday-above-title ordering, Apply/Confirm disabled on single date, no mobile year nav, past dates unreachable + no `disablePastDates` prop, unpinned mobile Confirm): rewrote the maxDate test (was asserting the now-obsolete "Apply stays disabled" — D1 makes that a false claim; rewritten to assert the disabled cell does not stage) + added 6 tests (single-day desktop/mobile commit, `disablePastDates` ×2 desktop/mobile) → 14/14 RTL tests (RangeDatePicker) green; planted-violation re-verified: removed the `isBefore(day, staged.from)` swap branch in `pickDay` (untouched by Task 561) → "end-before-start is swapped" test FAILS (`onChange` called with `{from:DAY_20,to:DAY_10}`, inverted), reverted → 14/14 PASS. **Task 559** (consumer wiring, 2026-07-08): replaced the two legacy `DatePicker` `date_from`/`date_to` instances with ONE `RangeDatePicker` on both `FiltersPanel` and `ListingsFilters`; 6/6 new integration RTL tests green; planted-violation: dropped the `date_to: next.to` half of `FiltersPanel`'s `onChange` handler → the atomic-commit test FAILS (`onApply` called with `date_to: undefined` instead of the picked date), reverted → 6/6 PASS. **Task 562** (calendar-body localization fix, 2026-07-08): root cause verified directly — Node's ICU has full `sq` data (`Intl.DateTimeFormat('sq',{month:'long'})` → "korrik") but Chromium's bundled ICU does not (`Intl.DateTimeFormat.supportedLocalesOf(['sq'])` → `[]` in-browser, the only runtime that renders this popover); replaced all `Intl.DateTimeFormat` month/weekday-name calls with static `common.calendar_months`/`calendar_months_short`/`calendar_weekdays_short`/`calendar_month_year_suffix`/`calendar_summary_order` data (5 new i18n keys × 4 locales, `check:i18n` parity green at 2122 keys) — en/it/uk output verified byte-identical to the prior `Intl` output (extracted via Node reference, matched exactly), only `sq` output changes (fixed); 2 new RTL tests (sq/it render localized, non-English text) added, all 14 pre-existing `RangeDatePicker` tests + 6 `filtersRangeDatePicker` tests still green (no regression); planted-violation: hardcoded `useCalendarLocaleData` to always return the English arrays → both new sq/it tests FAIL (`getByText`/`getAllByText('hën'\|'lun')` find nothing), reverted → 2/2 PASS) |
| Listings filter controls — leaf sub-components + shell (Mantine) | `FilterRangeInputs` (price/area/floor/floors_total ranges) + `FilterMultiToggle` (condition/heating/wall/offer/layout/purchase multi-select) + `FilterRoomsRow` (room-count multi-select) + `FiltersPanel.tsx` shell (`MantineDrawer` overlay, property/market-type toggle grids, listing-id input, Apply/Reset footer) ← `FiltersPanel` is consumed only by `src/components/shared/HeroSearchView.tsx` (Task 568 split `HeroSearch.tsx` into a thin container + this prop-driven view); `ListingsFilters.tsx` composes the 3 leaf components directly (no `FiltersPanel` dependency); **Task 568:** `HeroSearchView`'s own sale/rent tab pair + filters-trigger + search Buttons (the last legacy-primitive holdouts in the search stack) also migrated to Mantine `Button`; **Task 571:** the filters trigger adopts the canonical `MantineCountButton` (replacing the raw `Button` + hand-rolled corner badge Task 568 shipped) and gains a component-scoped `iconOnlyBelow={860}` collapse; **Task 572:** `HeroSearchView`'s 4 search-bar controls (type/location/filters/Search) flattened into ONE `flex flex-wrap md:flex-nowrap` container so Search alone wraps to a second row in the 640–767px band (was crushing the Location combobox illegible at ~720px — owner-reported) | **566, 567, 568, 571, 572** | `FilterRangeInputs`: typing in min/max fires `onMinChange`/`onMaxChange` with the exact typed string (Mantine `TextInput`, §6e chrome, `h-11`, even-split row); `FilterMultiToggle`/`FilterRoomsRow`: clicking an option fires `onToggle(value)`, multiple can be active simultaneously (Mantine `Button` toggles, §6a chrome — selected `variant="filled"` brand `#EC5447` / unselected `variant="default"` bordered); **Task 567:** overlay = `MantineDrawer` (`side="right"`, `size="sm"`, `<640` inherited full-width bottom sheet), header title+badge in the Drawer `title` slot, Apply/Reset in the Drawer `footer` slot, built-in Drawer close replaces the custom header X; property-type + market-type single-select grids = Mantine `Button` `variant="light"` (soft-tint, theme-derived) selected / `variant="default"` unselected — visually distinct from the leaf components' `filled` multi-select chips; listing-id = Mantine `TextInput` §6e; **Task 568:** `HeroSearchView`'s sale/rent tabs → Mantine `Button` `unstyled` (bespoke hero-gradient chrome preserved verbatim, joined-inner-corner radius + `max-sm:flex-1 max-sm:min-w-0` 50/50 full-width from Task 570 preserved), clicking a tab fires `onListingTypeChange(type)`; filters trigger → Mantine `Button` (`variant="filled"` when `activeFiltersCount>0` / `"default"` §6a bordered when idle), `SlidersHorizontal` `leftSection`, label `hidden sm:inline` (icon-only <640), corner active-count badge markup preserved verbatim, `onClick`→`onOpenFilters()`; search button → Mantine `Button` `variant="filled"` §6a, `Search` `leftSection`, `flex-1`, `onClick`→`onSearch()` | empty value → placeholder shown, no crash, emitting `''` unchanged (parent maps `''→undefined`); nothing selected → all chips render `default`; long uk/it toggle labels wrap, never clip, no h-scroll at 320; **Task 567:** Reset clears ALL filters via existing `handleReset` (panel stays open); backdrop/Esc/built-in-X dismiss discards the batch-apply draft without committing; `contentReady` idle-mount gate preserved; **Task 568:** `activeFiltersCount===0` → corner badge NOT rendered, filters trigger idle `variant="default"`; icon-only filters trigger <640 keeps `aria-label` as its accessible name; long uk/it tab labels (`Оренда`/`Me qira`) wrap, never clip, zero `scrollWidth>clientWidth` at 320 in any locale; `HeroSearch`'s public no-props API + `HeroSearchClient.tsx`/`src/app/[locale]/page.tsx` untouched (grep-confirmed); **Task 571:** at ≥860px the filters trigger shows `[icon] Advanced filters [count]` (canonical `MantineCountButton`, matching the `CountButton` story's filled+count look byte-for-byte — it IS that primitive); below 860px it collapses to `[icon][count]` (icon-only exemption, documented, ≥44px) so the location combobox regains the width the full label was stealing; count=0 → no badge in EITHER state; the collapse mechanism is `useMediaQuery` (SSR-safe, `getInitialValueInEffect:true`, same mechanism as `MantineDialogDrawerPattern`/`responsiveBottomSheet`) — no global Tailwind screen/Mantine breakpoint added; **Task 572:** at 640–767px (`sm`) row 1 = `[type] [location — roomy, legible placeholder] [⚙ n icon-only]`, row 2 = `[Search — full width]`; `<640` and `≥768` (`md`) layouts are byte-identical to pre-572 (stacked 3-row / single-row respectively) | RTL smoke: `FilterRangeInputs` typed-string passthrough (min+max) + value render; `FilterMultiToggle`/`FilterRoomsRow` `onClick`→`onToggle(value)` wiring + selected renders `data-variant="filled"` vs unselected `"default"`; `'5+'` label for room value 5. **Task 567** (`filtersPanelShell.smoke.test.tsx`, mounts the REAL `FiltersPanel`): Apply fires `onApply` with composed `local` values; Reset fires `onChange({})` (all cleared); property-type chip click toggles `data-variant="light"`/`"default"` correctly (select/re-select-clears/`"All types"`-clears); market-type same; listing-id typed string reaches `onApply` (incl. empty→`undefined`); the built-in `MantineDrawer` close (`.mantine-Drawer-close`) fires `onClose`. Baseline: `filtersRangeDatePicker.smoke.test.tsx` (consumes these leaf components via the real `FiltersPanel`/`ListingsFilters`) stays green, confirming no regression to the parent consumers. Persisted rendered proof: `Mantine/Primitives/FilterControls` story (Task 566) + `Mantine/Primitives/FiltersPanelShell` story (Task 567, real `FiltersPanel` rendered open, property/market grids pre-seeded with 1 selection each) + `Mantine/Primitives/HeroSearch` story (Task 568, real `HeroSearchView` rendered with fixture props, `activeFiltersCount=2` showing the filled trigger + corner badge). **Task 568** (`heroSearch.smoke.test.tsx`, mounts the REAL `HeroSearch` container end-to-end): Search button click → `router.push('/{locale}/listings?type=sale')`; Rent tab click then Search → `type=rent`; filters-trigger click renders `FiltersPanel` (listing-id input anchor); corner badge absent with no filters, shows the count after typing a listing-id + clicking Apply. **Task 571** (same file, extended): filters button is a `MantineCountButton` — the active count now renders as a DESCENDANT of the button (`rightSection`) not a sibling in a `relative` wrapper; a dedicated `useMediaQuery`-forced test proves the `iconOnlyBelow={860}` collapse is genuinely wired (label hides, `aria-label` accessible name + `onClick`/FiltersPanel-open survive) | `npx vitest run src/components/shared/__tests__/filterLeafComponents.smoke.test.tsx src/components/shared/__tests__/filtersPanelShell.smoke.test.tsx src/components/shared/__tests__/filtersRangeDatePicker.smoke.test.tsx src/components/shared/__tests__/heroSearch.smoke.test.tsx` · `npm run screenshots:assert -- --mantine-only` | ✅ (Task 566: presentational swap — legacy `@/components/ui/input`/`@/components/ui/button` → Mantine `TextInput`/`Button` (Task 556 `PhoneField` precedent), all filter LOGIC (parent-owned number parsing, multi-select array semantics, URL/state wiring) byte-identical, zero consumer edits (`FiltersPanel.tsx`/`ListingsFilters.tsx` untouched); 8 RTL smoke tests; planted-violation ×3 (dropped `onChange`→`onMinChange` wiring, dropped `onClick`→`onToggle` wiring on both toggle components) → the corresponding assertion genuinely FAILed each time (0 calls, expected 1), reverted → 8/8 PASS each time; `filtersRangeDatePicker.smoke.test.tsx` baseline unaffected. **Task 567** (2026-07-09): shell rebuilt on `MantineDrawer` + Mantine `Button`/`TextInput` — legacy `@/components/ui/sheet`/`@/components/ui/button`/`@/components/ui/input` fully removed, custom header close-X control REPLACED by the canonical `MantineDrawer` built-in close (documented relocation, not a removal — clause 4), `size="lg"`/`"xl"` (banned, Task 520) dropped from the market-type grid + footer buttons in favor of the theme default + `fullWidth`; property/market selected state moved from a hand-rolled `bg-primary/10 text-primary border-primary/30` className to the theme-derived `variant="light"`; 8 new RTL tests, planted-violation (dropped `onClick={handleApply}`) genuinely FAILed 3 assertions (Apply + both listing-id tests, which commit via Apply), reverted → 8/8 PASS; `filtersRangeDatePicker.smoke.test.tsx` (6 tests) re-verified green post-migration; zero consumer edits — `HeroSearch.tsx` (`FiltersPanel`'s one consumer) untouched, confirmed via grep. **Task 567 round-2** (2026-07-09, owner-rejected round-1 corrective — 4 defects the green round-1 gates missed): (1) canonical `theme.ts` Button `label` `wordBreak:'break-word'`→`'normal'`+`overflowWrap:'break-word'` (global, no more mid-word splits) + market-type row `sm:flex-wrap`+`w-full sm:w-auto` (wraps, never squeezes); (2) canonical `MantineDrawer`/`responsiveBottomSheet` header gains a `gray-3` bottom border when `title` is present (both desktop Drawer + mobile bottom-sheet); (3) new canonical `MantineCountButton` primitive (`patterns/MantineCountButton.tsx`) renders the active-count inline in the Button `rightSection` as a variant-aware `Badge` (white/brand pill on `filled` hosts, canonical gray-2/gray-7 pill on `default`/other hosts — an invisible white-on-white regression was caught and fixed same-day), replacing the round-1 absolute-corner-badge hack; (4) `MantineDrawer`/`responsiveBottomSheet` footer is now a pinned, non-shrinking flex sibling (NOT `position:sticky` — two sticky attempts were tried and rejected after live `screenshots:assert` caught genuine `element-overlap`, documented in the session log) alongside a genuine `flex:1;overflow-y:auto` scroll region, so Apply/Reset never scroll away. All 4 fixes are RTL-covered with **8 new planted-violation transcripts** (one per fix × desktop/mobile blast-radius pairs for Fixes 2/4, plus the variant-aware-chip proof for Fix 3) — each genuinely FAILs on the reintroduced defect and reverts to green; `filtersPanelShell.smoke.test.tsx` grew 8→15 tests, `MantineCountButton.smoke.test.tsx` grew 5→7. A geometry-checker limitation (naive `getBoundingClientRect()` overlap check doesn't exempt `overflow:auto`-clipped content, unlike the existing `outside-container` check) was found, a fix drafted and verified, then reverted and flagged for independent orchestrator review rather than self-merged into shared verification infrastructure; a scoped, precedented `GEOMETRY_ALLOWLIST` entry (matching the file's existing `PasswordInput`/`RangeDatePicker` entries) unblocks this story instead. Final: `screenshots:assert --mantine-only` 576 total/550 PASS/0 FAIL/26 AMBIGUOUS (pre-existing, unrelated), `FiltersPanelShell` 16/16 PASS, `CountButton` 16/16 PASS; §18.9 human-visual set captured at all 6 required combos incl. the mandatory scrolled-to-bottom footer frame. **Task 568** (2026-07-10, last legacy-primitive holdout in the search stack): `HeroSearch.tsx` split into a thin container (owns `useLocations`/`useRouter`/state/`handleSearch` URL-building — byte-identical) + a new pure-presentational `HeroSearchView.tsx` (all JSX + the 4 migrated Mantine `Button`s), avoiding any Storybook hook-mocking/live-Supabase risk (owner decision, supersedes an earlier Vite-alias plan); sale/rent tabs → `unstyled` Mantine `Button` with explicit per-corner Tailwind radius utilities (never the bare `rounded-*` shorthand, which Mantine's own radius CSS var can win back on ties) giving joined-inner/rounded-outer corners, Task 570's `max-sm:flex-1 max-sm:min-w-0` 50/50 full-width preserved; search-card top-right radius made responsive (`rounded-b-2xl sm:rounded-tr-2xl`) so the full-width mobile tab strip sits flush without squaring off the unchanged desktop content-width chip look; filters trigger → `variant="filled"`/`"default"` + `leftSection` icon + verbatim corner badge; search button → `variant="filled"` + `leftSection`; 4 new RTL tests, planted-violation (dropped the search button's `onClick`) genuinely FAILed both search-dependent assertions (0 `router.push` calls), reverted → 4/4 PASS; pre-existing 566/567/`filtersRangeDatePicker` smoke (33 tests total across the search stack) re-verified green post-migration). **Task 571** (2026-07-10, completes the held Task 568 — owner rejected 568's closure because the filters button used a raw `Button` + hand-rolled absolute corner `<span>` badge instead of the canonical `MantineCountButton`, and below 860px the full-text label stole width from the location combobox): `MantineCountButton` (`patterns/MantineCountButton.tsx`) gains an `iconOnlyBelow?: number` prop — unset (all other consumers, e.g. `FiltersPanel`'s Apply button) renders byte-identically; when set, below the threshold the label/`children` hides while `leftSection` + the count badge stay, compact `px="xs"` padding, ≥44px touch target preserved via the theme's existing `minHeight:'2.75rem'`. Threshold is `useMediaQuery` with the hook's own `getInitialValueInEffect:true` default made explicit — the SAME SSR-safe mechanism already used by `MantineDialogDrawerPattern`/`responsiveBottomSheet`/`MantineAdminSurfacePattern`/`MantineDataTableToCards`/`RangeDatePicker`/`AdminUsersTable`; no global Tailwind `screen` or Mantine theme breakpoint added. `HeroSearchView.tsx`'s filters button replaced the raw `Button` + absolute corner `<span>` + `relative` wrapper + `hidden sm:inline` span with `MantineCountButton` (`leftSection`, `count={activeFiltersCount}`, `iconOnlyBelow={860}`, `onClick={onOpenFilters}`, `aria-label`). `CountButton.stories.tsx` `Default` gained a 4th `Stack` section (no new export) demonstrating the collapse — verified via a standalone rendered capture at 680/768/810/860/960px × sq/en/uk/it: label absent <860, present ≥860, in BOTH the `CountButton` story's demo instance and the REAL `HeroSearchView` filters button, on every locale. `MantineCountButton.smoke.test.tsx` grew 7→13 tests (byte-identical-unset guard, collapsed label-hidden+icon+badge+aria-label-kept, at/above-threshold full-render, count=0 collapsed no-badge, ≥44px touch target, onClick-fires-collapsed); planted-violation (dropped the `iconOnlyBelow != null &&` guard) genuinely FAILed the byte-identical-guard test, reverted → 13/13 PASS. `heroSearch.smoke.test.tsx` grew 4→5 tests + rewrote the badge test for the new inline-descendant DOM shape; planted-violation (dropped `iconOnlyBelow={860}` from `HeroSearchView.tsx`) genuinely FAILed the collapse test (label found when it should be absent), reverted → 5/5 PASS. `screenshots:assert --mantine-only` (fresh `storybook-static` rebuild, confirmed via bundle grep for `iconOnlyBelow`): `CountButton` 16/16 PASS, `HeroSearch` 16/16 PASS, `FilterControls` 16/16 PASS, `FiltersPanelShell` 16/16 PASS (sq/en/uk/it × 320/375/390/1024); the full 592-cell Mantine-only run hit a late-run Playwright browser crash unrelated to this task's stories (alphabetically after T*, zero non-crash failures — `grep` confirmed) — documented as an environment limitation, not a regression. **Task 572** (2026-07-10, owner-reported @720px: Location combobox crushed illegible because filters+Search stayed grouped on one row): flattened `HeroSearchView`'s action `<div className="flex gap-2">` wrapper so all 4 controls (type/location/filters/Search) are direct siblings of ONE `flex flex-wrap md:flex-nowrap gap-2` container; Search alone gets `grow shrink basis-0 sm:basis-full md:grow-0 md:basis-auto` so it wraps to its own full-width row 2 in the 640–767px (`sm`) band while filters (`shrink-0`) stays on row 1 with type/location — `<640` and `≥768` (`md`) unchanged. 1 new RTL smoke test (DOM-structure: the 4 controls are direct children of one flex-wrap container, not nested in the old action `<div>`) — `heroSearch.smoke.test.tsx` grew 5→6 tests, 6/6 PASS; planted-violation (reinstated the old nested `<div className="flex gap-2">` around filters+Search) genuinely FAILed the new structural test (`toHaveClass` mismatch: got `"flex gap-2"` not `"flex flex-wrap gap-2"`), reverted → 6/6 PASS. Pre-existing 33 tests across `filterLeafComponents`/`filtersPanelShell`/`filtersRangeDatePicker` smoke suites re-verified green (35/35 total incl. heroSearch), zero regression. `screenshots:assert --mantine-only` (fresh `storybook-static` rebuild, confirmed via bundle grep for `flex-wrap md:flex-nowrap`): 566/592 PASS, 0 FAIL, 26 AMBIGUOUS (pre-existing, unrelated — Combobox/RangeDatePicker/Tabs stories only); `HeroSearch` 16/16 PASS, `CountButton` 16/16 PASS, `FilterControls` 16/16 PASS, `FiltersPanelShell` 16/16 PASS (sq/en/uk/it × 320/375/390/1024, confirms `<640`/`≥768` byte-identical preservation). The 640–767 in-band behavior itself (outside the standing gate's 320/375/390/1024 sample) was verified with a one-off Playwright capture script (not persisted — ad hoc verification only) at 320/375/390/560/600/640/680/700/720/767/768/810/860/960/1024px × sq/en/uk/it (60 screenshots): geometry-based row-count assertion (`getBoundingClientRect` top-coordinate clustering of the 4 controls) confirms 3 rows `<640`, exactly 2 rows at 640–767 (Search alone on row 2), 1 row `≥768`, consistently across all 4 locales; visually confirmed via direct screenshot inspection at uk@320 (unchanged 3-row stack), uk@700 (row 1 `[Всі типи][Місто або село...][⚙ 2]`, row 2 full-width `Пошук`, Location placeholder fully legible — the exact defect being fixed), uk@767/768 (boundary snap-back), uk@860 (filters label reappears, no overflow/clip with the longest uk string) and uk@1024 (single row, Location fills, Search content-width). **Task 573 (2026-07-10) — the 640–767 band now has a PERSISTED, CI-runnable gate cell, replacing the retired Task-572 one-off script as the authoritative proof:** `scripts/check-stories-rendered.mjs`'s Mantine gate gained a per-story `MANTINE_STORY_EXTRA_VIEWPORTS.HeroSearch` `band-700` viewport (700×812, keyed off the same discovered `componentName`, never a hardcoded story id) — every OTHER Mantine primitive story stays exactly 320/375/390/1024 (unchanged). At the new 700px cell, a HeroSearch-only, `640<=width<768`-only DOM assertion (`cell.assertions.heroSearchWrapInBand`) reads the 4 search-bar controls' `getBoundingClientRect().top` and hard-fails (never retried into a pass, mirrors `fullWidthControlsAtMobile`) unless the Search button's top is strictly below the Location field's top (Search wrapped to row 2); command: `npm run screenshots:assert -- --mantine-only`. **Task 671 (2026-07-28, de-Tailwind only — zero behavioral/logic diff):** `FiltersPanel.tsx`'s remaining 27 raw elements (24 `div`+1 `p`+2 `span`) and 32 Tailwind `className` values migrated onto a new canonical `MantineFilterSection` pattern (`src/design-system/mantine/patterns/MantineFilterSection.tsx`, `p="lg"`, `gray-3` top divider, uppercase `Text` label) plus direct Mantine props for the title/footer/grids (`SimpleGrid`, `Flex direction={{base:'column',sm:'row'}}`, responsive `w`); file now greps 0 raw elements / 0 `className` (was 27/32). The 17 section wrappers derive their own `withDivider` from a `sectionVisibility` map keyed to the live `shows()` predicate (never a hardcoded index-0), reproducing the prior `divide-y` semantics (border on every section except the first VISIBLE one) — the kickoff's own named highest-risk defect. `filtersPanelShell.smoke.test.tsx` grew 15→18: the one class-coupled test (`:203`, asserted literal `sm:flex-wrap`/`w-full sm:w-auto` Tailwind class substrings) was rewritten to assert the actual Mantine mechanism (a `<style data-mantine-styles="inline">` sibling tag keyed to the row/button's own generated class, containing `flex-direction:column`/`flex-wrap:nowrap` at base and `flex-direction:row`/`flex-wrap:wrap`/`width:auto` inside `@media(min-width: 40em)` — verified by direct render inspection, not re-derived from source); 3 new tests cover the D3 divider derivation (first-visible-section undivided; a later section divided; the same holds after a real `shows()`-changing property-type selection via a added 'land' fixture type whose schema hides market_type/rooms/floor/etc.; `contentReady===false` renders Drawer header+footer only, empty body, no orphan divider). Planted-violation (hardcoded `isFirstVisible` to always return `true`) genuinely FAILed both new (a)/(b) assertions (expected `''`, got the gray-3 border), reverted → 18/18 PASS. All 3 sibling suites (`filterLeafComponents`/`filtersRangeDatePicker`/`heroSearch`) re-verified green untouched (38/38 total). New pattern enrolled in `mantine-migration-scope.json` (append-only, 11→13) + proven by a new `Patterns/Mantine/FilterSection` story (reuses existing `storybook.mantine.form_section_location/details/contact`/`action_filter`/`action_add_new`/`home_section_cta` keys — zero new i18n keys, `check:i18n` 2215/2215/2215/2215 parity unchanged); `check:story-coverage` 13/13. `check:design-tokens` 44 violations both before/after (identical set, zero in any file this task touched). `check:hydration` against a fresh `next build`+`next start` (NOT `next dev` — Turbopack's dev HMR cache produced a flaky one-off mismatch alternating between `/sq`/`/uk` across dev-mode reruns, consistent with the project's standing documented Turbopack caveat) — 4/4 PASS, reproduced twice. `useHomepageFilters.ts`/`HeroSearchView.tsx`/`HeroSearch.tsx`/`page.tsx` zero diff (confirmed via `git diff --stat`). Command (same as Task 567/571/572): `npx vitest run src/components/shared/__tests__/filtersPanelShell.smoke.test.tsx src/components/shared/__tests__/filterLeafComponents.smoke.test.tsx src/components/shared/__tests__/filtersRangeDatePicker.smoke.test.tsx src/components/shared/__tests__/heroSearch.smoke.test.tsx` **Task 675 (2026-07-28, revision of Task 671 — orchestrator review found 3 defects + 2 record errors, code not reverted, amended in place):** section-label micro-heading colour pinned to `c="gray.5"` (`#667085`, `theme.ts:11` gray tuple index 5) instead of the unpinned `c="dimmed"` Task 671 shipped (`#475467`) — owner decision D4, clause 16a (no TailAdmin row existed for a 12px uppercase micro-heading; `tt="uppercase"` grepped to exactly 1 hit before the decision); Drawer title `<Text fw={600} size="md">` gained `component="span"`, removing the `<p>` cannot be a descendant of `<p>` React warning Task 671 introduced on the mobile bottom-sheet path (the sibling `<div>` cannot be a descendant of `<p>` warning is pre-existing, unrelated, reserved as Task 677); `isFirstVisible` renamed `withTopDivider` at its declaration + all 17 call sites, predicate (`key !== firstVisibleSection`) unchanged; Task 671's kickoff + session log's divider before-hex corrected (the originally-recorded value never rendered; measured `#E5E5E5`) → `#D0D5DD` after. `npx vitest run` (same 4 suites) 39/39 (38 + 1 new DOM-nesting test); planted-violation (`component="span"` dropped) genuinely FAILed the new test AND reproduced the `<p>`-in-`<p>` warning on the mobile path, reverted → 39/39 PASS, git status confirms no residual plant. `screenshots:assert -- --mantine-only`: `FiltersPanelShell` + `FilterSection` both 16/16 PASS, fresh-capture label colour measured `#667085`. `build` 40/40. Also downgraded Task 671's own session log: R7/R8 to `PARTIALLY IMPLEMENTED` (the harness's `MANTINE_VIEWPORTS` is 4 widths, not the 14-width canon — Task 678 reserved) with an owner native visual waiver recorded (480/560/680/768 × sq/en/uk/it); R13 to `NOT VERIFIABLE` (the Storybook fixture renders raw English enum labels in every locale via `usePropertyTypes()`'s `buildFallback()`, never the real `messages/*.json` labels — Task 679 reserved). Command: same as above. |

## P0/P1 — Listings lifecycle (Slice 3 / Task 442)

| Flow | Route / component / action | Owner task | Happy path | Failure path | Required regression test | Command | Coverage |
|---|---|---|---|---|---|---|---|
| Listings display — price + date formatting (SSR/CSR parity) | `ListingCard`'s `PriceBlock` (`modules/listings/components/ListingCard.tsx`) → `formatPrice`/`formatCount`/`formatDate`/`formatDateTime`/`formatListingDate` (`src/lib/formatters.ts`) | **563, 564** | price/old-price/per-sqm/converted-original-price AND the listing-card date stamp are byte-identical on server and client at every locale (`en`/`uk`/`sq`/`it`), no hydration warning on `/[locale]/listings` or the homepage | a runtime whose ICU cannot format the active locale (e.g. some browsers lack `sq` locale data entirely — `Intl.NumberFormat`/`Intl.DateTimeFormat.supportedLocalesOf(['sq'])` → `[]` in Chromium, verified directly) must still produce the correct output, not a silently-different fallback | vitest: literal-byte assertions for `formatPrice`/`formatCount` (Task 563) and `formatDate`/`formatDateTime`/`formatListingDate` (Task 564) across all 4 locales + a direct simulation of a broken `Intl.NumberFormat`/`Intl.DateTimeFormat` (monkey-patched to throw) asserting the exact expected literal still comes out | `npx vitest run src/lib/__tests__/price-format-ssr-parity.smoke.test.ts src/lib/__tests__/date-format-icu-independence.smoke.test.ts src/lib/__tests__/date-format-ssr-parity.smoke.test.ts` | ✅ (Task 563: root cause verified directly — Chromium's bundled ICU has no `sq` locale data at all (`supportedLocalesOf(['sq'])` → `[]`), so `Intl.NumberFormat('sq',...)` silently fell back to a different (comma) grouping than the Node server's correct (space) grouping — the exact same class of gap as Task 562's calendar-name fix, but for `formatPrice`/`formatCount`'s digit grouping; `it` was never actually broken. Fix: `formatPrice`/`formatCount` no longer call `Intl.NumberFormat` at all — grouping computed by a pure `groupDigits()` helper from a static `NUMBER_GROUPING` table, verified byte-for-byte against Node's output across 76 combinations; `ListingCard`'s hardcoded `new Intl.NumberFormat('en')` also routed through `formatCount(...,'en')`. 22 vitest tests, planted-violation genuinely FAILed then reverted. **Task 564 (2026-07-09) closes this row's flagged follow-up:** `formatDate`/`formatDateTime`/`formatListingDate` had the identical live-`Intl.DateTimeFormat(locale,...)` root cause at 3 more call sites (numeric date/time + the listing-card date stamp). Fix: `formatDate`/`formatDateTime` compose output manually from `Date` parts + a static per-locale `DATE_FORMAT` table (order/separator/12h-vs-24h/day-period markers, extracted from Node's full-ICU output, verified byte-identical across 336 timestamps × 4 locales × 3 formatters = 4032 comparisons, 0 mismatches); `formatListingDate` composes from the Task-562 static `common.calendar_months_short`/`calendar_month_year_suffix`/`calendar_summary_order` i18n data (reused, not duplicated) instead of a live `Intl.DateTimeFormat` call. `NotificationItem`'s `resolvePriceChangeBody` (raw `Intl.NumberFormat(locale)`) also routed through `formatCount`. 10 new vitest tests (`date-format-icu-independence.smoke.test.ts` + `NotificationItem.priceChange.smoke.test.tsx`), all green; planted-violation (reverted `formatListingDate` to raw `Intl.DateTimeFormat`, and `resolvePriceChangeBody` to raw `Intl.NumberFormat`) → both genuinely FAILed, reverted → green. `grep -rn "new Intl\.(Number|DateTime)Format(" src/` sweep: only 2 justified exceptions remain — `src/components/shared/DatePicker.tsx` (legacy single-date picker, superseded by `RangeDatePicker`/Task 557 migration not yet executed; same bug class, flagged not fixed to avoid throwaway work on a component about to be replaced) and `PasswordChangedEmail.tsx` (hardcoded `'sq-AL'`, server-only email render — never hydrated in a browser, no SSR/CSR boundary exists to diverge across). Live-app rendered proof (Playwright, real dev server, 2 runs): `/sq/listings` date now renders `"17 qer 2026"` + price `"45 000 ALL"`, **zero hydration-related console messages on any of the 4 locale routes** (the previously-observed Base-UI Tabs/CompositeRoot `id` mismatch on `/it` did not reproduce in this session — separate/intermittent Base-UI issue, unrelated to formatters, still out of scope).) |
| **Listing card rendering — Mantine pattern is the COMPLETE single source of truth (Grid/Latest) + horizontal List view (unchanged)** | `ListingCard.tsx` (`modules/listings/components`) → `MantineListingCardPattern` (`design-system/mantine/patterns`), consumed by `LatestListings.tsx`/`FeaturedListings.tsx`/`RecentlyViewedGrid.tsx`/`SimilarListings.tsx` (vertical, default) and `ListingsShell.tsx` (`variant={view==='list'?'horizontal':'vertical'}` — List/Grid toggle); `ListingCardPattern.stories.tsx` (Task 605) now renders the identical complete card (favorite + photo counter included) as production | **602, 605** | **(605)** the pattern OWNS all card chrome (badges, sold/rented overlay, photo counter, type/title/location, features row, price(+old), per-m²/original-price footer line, premium/archived, hover) from data/props; `image`/`favorite`/`footerActions` are passed in as positioned nodes (real `AppImage`/`FavoriteButton`/copy-id in the app, demo equivalents in the story) — `ListingCard.tsx` is now a thin data-mapper with zero card markup of its own; the Storybook `Default` story is a pixel-complete production-equivalent (favorite heart + photo counter genuinely render, unlike Task 602's shell where the story only exercised its own demo `ActionIcon`); hover on desktop → photo zoom + TailAdmin-cited elevation shadow, VERIFIED ON THE REAL NEXT.JS PAGE (not just Storybook — absorbs Task 604) via computed-style diff, genuinely suppressed under coarse/touch pointer (by design) and under `prefers-reduced-motion`; horizontal branch (List view, `ListingsShell.tsx`) untouched and still renders | plain-price shows no strike; sold/rented/archived/expired badges+overlay unaffected, favorite disabled+labeled when closed; no-image fallback intact (pattern-agnostic, container-owned); photo counter absent when `photoCount` is 0/undefined; hover reverts under `prefers-reduced-motion`, scoped to `(hover:hover) and (pointer:fine)` so touch taps never get stuck-hovered — CONFIRMED via a real coarse-pointer Playwright context (`hasTouch:true, isMobile:true`) that hover genuinely does NOT fire there (the owner's original "no hover" report was this exact environmental artifact — device-toolbar/touch emulation — not a code bug); horizontal branch (List view) must keep rendering after the pattern's completion — this is the exact regression class the task could have introduced | vitest RTL: mounts the REAL `ListingCard` under a REAL `NextIntlClientProvider`+`MantineProvider` — vertical content-parity assertions (type/title/price/location/features/photo-count/copy-id/favorite/nav), reduced-price strike-through present/absent, sold-listing overlay+disabled-favorite, archived badge+dimming (605), no-image fallback, favorite+photoCount+features+footerActions all render via the pattern (605 single-source proof), always-'en' `originalPriceStr` grouping under `sq` locale (hydration-safety), AND a separate horizontal-branch suite (`variant="horizontal"` still renders + still shows the strike via the untouched legacy `PriceBlock`); PLUS a live-`next dev`-server Playwright script (`scripts/task605-qa-listingcard-complete.mjs`, ad hoc, not CI) proving content-completeness (favorite/photoCount/features/footerActions present, no overflow) across a 56-cell route×locale×viewport matrix and the real-page hover computed-style diff | `npx vitest run src/modules/listings/components/__tests__/ListingCard.smoke.test.tsx` | ✅ (Task 602: 8 vitest RTL tests, all green; 2 independent planted-violations verified — (A) dropped the pattern's `priceOld` strike-through branch → reduced-price test genuinely FAILs (`getByText('92,000 EUR')` finds nothing); (B) disabled the `variant==='horizontal'` branch in `ListingCard.tsx` → horizontal-still-renders test genuinely FAILs (`toHaveClass('listing-card--horizontal')` mismatch); both reverted → 8/8 PASS. Real bug found+fixed pre-ship: Tailwind `hover:`/`group-hover:` utility classes on the Mantine `Card` were silently inert (Mantine's own box-shadow is unlayered CSS, always wins over Tailwind's `@layer utilities` regardless of specificity/order) — switched to an unlayered CSS module (`MantineListingCardPattern.module.css`), re-verified via Playwright computed-style diffing: `boxShadow`/`transform` genuinely change on `:hover` and revert to `none` under `prefers-reduced-motion: reduce`. Rendered matrix: 10 breakpoint×locale cells (uk@320/375/390/1280, sq/en/it@375/1280) + hover/no-hover/reduced-motion captures, persisted at `docs/sessions/2026-07-15-task602-assets/`. Session: `docs/sessions/2026-07-15-task602-listingcard-mantine-migration.md`.) **(Task 605, 2026-07-15: owner caught that 602's pattern was a thin shell whose OWN story never rendered the favorite/photo-counter that ship in production — 605 rebuilt the pattern as the complete single source of truth. 10 vitest RTL tests (2 new: archived-badge, favorite+photoCount+features+footerActions-via-pattern), 2 fresh planted-violations verified (photoCount broken → 2 tests genuinely FAIL; horizontal-branch guard disabled → 1 test genuinely FAILs), both reverted → 10/10 PASS. Real-page matrix: 56/56 cells show favorite+photoCount+features+footerActions present with zero overflow on the pattern's own content (the 12 `/listings`-Grid-mobile cells that show FAIL in the raw script output are 100% attributable to a PRE-EXISTING, out-of-scope FilterBar overflow bug, not this task — see Pending Action Items). Real-page hover: desktop pointer genuinely fires (boxShadow `none`→`0px 12px 16px -4px rgba(16,24,40,.08)…`, `transform: none`→`translateY(-2px)`, image `scale(1.05)`); coarse/touch pointer genuinely suppressed (`fired=false`); `prefers-reduced-motion` genuinely suppressed — CONCLUSION: Task 604's premise resolved as Branch A (environmental, not a bug), no hover code change needed, absorbed here with real evidence. Storybook story rebuilt to render the SAME complete card (favorite/photoCount/badges/features/per-sqm footer) at 4 locales — verified visually identical structure to the real page. 8 new i18n keys added (`storybook.mantine.card_{type_label,badge_reduced,badge_archived,overlay_sold,footer_date,favorite_aria_add,favorite_aria_remove,price_per_sqm_1}`, parity-verified 2153 keys × 4 locales). Session: `docs/sessions/2026-07-15-task605-listingcard-pattern-complete-single-source.md`.) **(Task 606, 2026-07-15, STORYBOOK ONLY — no site wiring:** `MantineListingCardPattern` gained a `layout: 'grid' | 'list'` prop (default `'grid'`, byte-identical to HEAD — confirmed via diff, zero removed/changed lines in the grid branch, only additive JSDoc + a new `layout='list'` branch inserted before it). `layout='list'` is a structural port of the legacy `ListingCard.tsx` `variant==='horizontal'` branch: image-left (fixed `w-32`/`sm:w-44`, self-stretch) + info-right column (type-label+inline-favorite top row, 2-line-clamp title, price(+old)+per-sqm row, features row, location+footerActions bottom row) — reuses the SAME props as `'grid'`; `overlay`/`photoCount` are intentionally NOT rendered in `'list'` (the ported legacy design never had them; the `badges` array already conveys sold/rented). **Real bug found+fixed during the port:** Mantine's `Card`/`Paper` root sets `display: block` via unlayered CSS — Tailwind's `flex`/`flex-row` utility classes (in `@layer utilities`) can NEVER override an unlayered declaration (the exact Task 602 cascade-layer lesson, this time for `display` instead of `box-shadow`); with a Tailwind-only `flex flex-row` the image/info children silently stacked VERTICALLY instead of side-by-side (invisible on `layout='grid'` since block-stacking and flex-column-stacking coincide for full-width children, but breaks a row). Fixed with a new unlayered `.listRow { display:flex; flex-direction:row; }` rule in `MantineListingCardPattern.module.css`, verified via Playwright (`getComputedStyle(...).flexDirection`) before/after: before the fix every list row's info column rendered BELOW the image (`imageLeftOfInfo:false` in all 28 cells); after, `imageWidth` matches the expected `128px`(<640)/`176px`(≥640) breakpoint and sits strictly left of the info column in all cells. New pattern-level regression suite `MantineListingCardPattern.smoke.test.tsx` (4 tests: grid-unchanged content sanity, list image-first DOM-order structural marker, list still renders favorite/features/price/footer while photoCount/overlay are correctly absent) — planted-violation (swapped the image/info div order in the list branch) genuinely FAILed the image-first assertion, reverted → 4/4 PASS. `ListingCardPattern.stories.tsx` `Default` (single export, governance §8) gained a labelled List section (6 rows: regular/premium/reduced/sold/no-image/archived) below the unchanged Grid section, on canonical primitives (`Button` `icon-sm`, Mantine `Image` — no raw `<img>`/`<button>`, `eslint`'s `no-restricted-syntax` enforced this during the task); 2 new i18n keys (`storybook.mantine.card_section_{grid,list}`) × 4 locales. Storybook-only Playwright matrix (`scripts/task606-qa-listingcard-list-layout.mjs`): 28/28 cells (4 locales × 7 widths) PASS — grid section unchanged (6 cards), list section renders 6 correctly-ordered horizontal rows, favorite inline (not `position:absolute`), zero page overflow at any width incl. 320/375/390. No site consumer touched (confirmed `git diff --stat` empty across `ListingCard.tsx`/`LatestListings.tsx`/`ListingsShell.tsx`/`FeaturedListings.tsx`/`RecentlyViewedGrid.tsx`/`SimilarListings.tsx`) — wiring both layouts into the site is a separate, later, owner-authorized task. Session: `docs/sessions/2026-07-15-task606-listingcard-pattern-list-layout-variant.md`.) **(Task 607, 2026-07-15: `Patterns/Mantine/ListingCardPattern` gained REAL, committed, CI-blocking rendered coverage via `npm run screenshots:assert -- --mantine-only` — 16/16 PASS (320/375/390/1024 × 4 locales), superseding the throwaway ad-hoc scripts Tasks 605/606 had to write because the gate didn't discover `Patterns/Mantine/*` at all. See `docs/storybook-governance.md` §14.9.18.)** **(Task 608, 2026-07-16: site-wiring HELD half of Task 606 — the `ListingCard.tsx` `variant==='horizontal'` branch (`/listings` List view, `ListingsShell.tsx`) migrated from legacy hand-rolled row markup onto `MantineListingCardPattern layout="list"`, mirroring the vertical branch's thin-mapper split (Task 602/605): thumb `AppImage variant="listing-thumb"` (+fallback), inline `FavoriteButton` (`shrink-0 -mt-0.5 -mr-1`, not absolute), pre-translated badges, pre-rendered feature-icon nodes, footer copy-id+date node — no `overlay`/`photoCount`/`onContact` (the ported legacy list design never had them). Now-dead local `PriceBlock` helper + `Badge`/`cn`/`MapPin` imports removed (compiler+grep verified unused post-change, vertical branch untouched). `ListingCard.smoke.test.tsx` horizontal-branch suite extended 2→5 tests (content-parity: title/price/location/features/copy-id/favorite/nav/no-photo-count; reduced-price strike+badge; sold-disabled-favorite; no-image fallback; archived dimming) — planted-violation (`variant === 'horizontal' && false`) genuinely FAILed 2/2 (fell through to the vertical render, wrong link class + no strike-through), reverted → 13/13 PASS. Real-page rendered matrix (`scripts/task608-qa-listingcard-list-site.mjs`, live `next dev`): 28 cells × 4 locales × 7 breakpoints on `/[locale]/listings` List view — full-width row `<640`, no page overflow, inline favorite, badges present, overlay/photo-count/contact absent, footer actions present, title hover→brand-red confirmed on desktop pointer. Persisted: `docs/sessions/2026-07-15-task608-assets/`. Session: `docs/sessions/2026-07-15-task608-listingcard-list-layout-site-migration.md`.)** |
| Create listing | listing form → create action | Y / **442** | valid → listing created (status:'pending') | validation error → typed | smoke: create ok (status:pending asserted) + validation fail + unauthenticated + insert error + console.error | `npx vitest run src/modules/listings/actions/__tests__/createListing.smoke.test.ts` | ✅ (Task 442: vitest smoke — 4 tests; happy asserts status:'pending' + user_id; validation_failed → no DB; unauthenticated; insert DB error → console.error diagnosable; planted-violation: status:'active' → happy status assertion FAILS) |
| Edit listing | edit side-panel (`Task 238`) | Y/238 / **442** | edit persists; dirty-state save | save error → typed | smoke: edit ok + not_found + validation_failed + unauthenticated + DB update error + console.error | `npx vitest run src/modules/listings/actions/__tests__/updateListing.smoke.test.ts` | ✅ (Task 442: vitest smoke — 5 tests; happy → {slug, status}; not_found → no update; validation_failed; unauthenticated; DB update error → console.error diagnosable; planted-violation: drop not_found guard → TypeError FAILS) |
| Status change | `applyListingTransitionByStatus` / `StatusChangeControl` | 427/I / **442** / **455** | allowed transition persists; **re-stamp:** transition→publicEligible includes fresh `expires_at` | illegal transition blocked; non-publicEligible → no `expires_at` in payload | smoke: pure engine matrix (resolveTransition + canSetStatusPrivileged) + gateway (owner-privileged, forbidden, not_found, same-status invalid_transition) via _db injection; comprehensive gateway coverage also in `applyListingTransition.test.ts`; **re-stamp smoke (Task 455):** payload-capturing mock asserts `expires_at` present for RENEW/APPROVE/PUBLISH→active, absent for ARCHIVE/MARK_AS_SOLD/EXPIRE→non-publicEligible | `npx vitest run src/modules/listings/actions/__tests__/listingStatusChange.smoke.test.ts` · `npx vitest run src/modules/listings/actions/__tests__/reStampExpiresAt.smoke.test.ts` · `npx vitest run src/modules/listings/actions/applyListingTransition.test.ts` | ✅ (Task 442: 13 smoke tests; Task 455: +9 re-stamp tests (6 action-based + 3 privileged-status) + 5 EXPIRE/RENEW gateway tests in applyListingTransition.test.ts + 5 listingConstants unit tests; planted-violation: `shouldReStampExpiresAt` → always false → 6/15 re-stamp tests FAIL; restore → 15/15 PASS) |
| Report listing | `reportListing.ts` → report dialog | 243/BB / **435** / **442** / **460** / **463** | submit success; **admin full management (Task 463):** free any→any status via `reports.status_override`; reopen terminal→pending; hard delete via `reports.delete`; both capability-gated (delegable via Дозволи) | transport/RLS fail → **diagnosable** typed error + console.error; **Task 463 negative:** `reports.manage`-only moderator denied out-of-allowlist transitions + delete; same→same → `invalid_status`; missing row → `not_found`; DB error → `save_failed` + console.error | smoke: report ok (incl. table-name assertion) + save_failed → console.error asserted; **dialog-open wiring (Task 442):** dialog closed on mount; open button shows all 6 reasons; submit invokes reportListingAction(listingId, reason, comment); already_reported → toast.info + dialog closes; **grant check (Task 460):** `check:listing-reports-grants` static gate over SQL source-of-truth; **Task 463:** deleteReportAction smoke (6 tests: capable delete+DB-call-verified, forbidden+no-delete-call, delegation, unauth, not_found, DB error); updateReportStatusAction allowlist/status_override guard (14 tests: allowlist pass×2+update-called, status_override-only×2, out-of-allowlist forbidden×3+update-NOT-called, override pass, same→same+no-update, unauth+no-update, no-capability→forbidden-before-read, CAS-conflict, audit-insert+status-revert-verified); UI capability controls (5 tests: caps true→shown, caps false→hidden, delete confirm→removed+no-reload, cancel→still-present, Esc/backdrop→still-present); planted-violation: drop `reports.status_override` classification → out-of-allowlist tests FAIL; restore → PASS | `npx vitest run src/modules/listings/actions/__tests__/reportListing.smoke.test.ts src/modules/listings/actions/__tests__/deleteReport.smoke.test.ts src/modules/listings/components/__tests__/ReportListingDialog.smoke.test.tsx` · `npx vitest run src/components/admin/__tests__/AdminReportsManager.smoke.test.tsx` · `npm run check:listing-reports-grants` | ✅ (Task 436/448: action smoke — happy + save_failed console.error; Task 442: dialog-open smoke — 4 RTL tests; Task 458 Fix B: +6 per-branch error toast tests (16 total); Task 459: Fix A NOT applied — no prod middleware evidence; **Task 460:** root cause = `42501 permission denied` from Task 275 over-revoke of authenticated INSERT/SELECT; fix = `grant insert, select on public.listing_reports to authenticated` migration + corrected `grant-discipline-audit.sql` + `check:listing-reports-grants` blocking CI gate (planted-violation: missing-grant FAIL + overgrant FAIL, restored PASS); **Task 461:** admin report-details dialog shows listing OWNER (name + profile link `/admin/users/[ownerId]`) distinct from Reporter; **Task 462:** badge removed (raw `profile_types` key fix); 4 RTL tests (owner present + no profile_types text + unknown user_type no raw key + owner null + listing null); planted-violation: re-add badge → `profile_types` assertion FAILS; **Task 463:** full admin report management — `reports.status_override` + `reports.delete` permission keys; allowlist transition guard (moderator: pending→{reviewed,resolved,dismissed}, reviewed→{resolved,dismissed}; out-of-allowlist requires `reports.status_override`); `deleteReportAction` hard-delete with `not_found` detection; UI: status Select + Apply, Reopen quick-action, Delete with confirmation dialog; capability-prop driven visibility; 21 server tests + 11 UI tests; CAS update prevents concurrent race; CAS-guarded audit-failure revert (CRITICAL log on revert-race miss); permission-before-read prevents ID enumeration; typed error toasts (forbidden/unauthorized/conflict/not_found → per-code i18n key); `npx vitest run src/components/admin/__tests__/AdminReportsManager.smoke.test.tsx src/modules/listings/actions/__tests__/deleteReport.smoke.test.ts`) |
| Inquiry / send message | `submitListingInquiry` + `ListingInquiryDialog` | 243 / **442** | sent → owner email; rate-limited | email transient → partial-success UX | smoke: inquiry ok (status:'new' inserted, email fired) + validation + rate_limited (IP-based) + not_found + save_failed + console.error + email_transient partial-success | `npx vitest run src/modules/listings/actions/__tests__/submitListingInquiry.smoke.test.ts` | ✅ (Task 442: vitest smoke — 7 tests; happy asserts status:'new' + email called; validation (short msg + bad email); rate_limited (count≥5 from real IP); not_found; save_failed → console.error diagnosable + no email; email_transient partial-success (insert ok, email fail) + console.error; planted-violation: swallow rate_limited → {} → rate_limited assertion FAILS) |
| Listing expiry reconciliation | `POST /api/cron/listings-expiry` | **455** | lapsed `active + expires_at<now()` → `expired` via engine EXPIRE; idempotent; NULL-expiry reported but NOT mutated (decision #3) | unauthorized → 401; nothing lapsed → 0 expired, no updates; NULL-expiry = report only | smoke: authorized lapsed→expired, nothing-lapsed=0, NULL-reported-not-mutated, unauthorized 401, engine-resolved status | `npx vitest run src/app/api/cron/listings-expiry/__tests__/route.test.ts` | ✅ (Task 455: 5 vitest smoke tests — authorized+lapsed→2 expired, nothing-lapsed→0 + no updates, NULL-expiry→reported count=3 + 0 mutations + note contains "NOT auto-expired", unauthorized→401, engine-resolved status=expired not raw string) |
| **Listing public visibility invariant** | canonical `applyPublicVisibility`/`isListingPubliclyVisible`/`formatVisibility`/`applyPublicEligibleButHidden` (Task 454+456) ← all public reads (`listings/lib/queries.ts`, `api/listings`, `[locale]/listings/page.tsx`, `SimilarListings.tsx`) + cabinet/admin visibility badge + admin audit panel/filter; **LV.2/455:** `expired` status added to policy + semantic layer (HIDDEN group) + transition engine (EXPIRE/RENEW) + re-stamp-on-activation; **LV.3/456:** `formatVisibility` shared formatter (badge↔predicate proof) + `applyPublicEligibleButHidden` complement filter (admin audit counts + filter) + cabinet "Hidden — reason" indicator + admin per-row/detail Visible/Hidden diagnostics | **LV / 454–457** | a listing whose status is policy-`publicEligible` and meets its conditions (active + `expires_at>=now()`) is shown publicly AND labelled "Visible"; the public predicate == the cabinet/admin "visible" badge for the same row, across **every** `ListingStatus` (now 7 incl `expired`); `formatVisibility` output matches `isListingPubliclyVisible` for all 7×3 combinations (badge↔predicate equivalence); `applyPublicEligibleButHidden` selects exactly the eligible+hidden set per reason | active-but-expired / NULL-expiry → hidden with explicit reason (`expired`/`no_expiry`), never silently "Active"; `expired` status → `status_not_public`; re-stamp guarantees no stale `expires_at` on re-activation; no inline `status='active'`/`expires_at` filter bypasses the canonical helper; unknown reason → falls back to full hidden_eligible set | invariant vitest across all 7 statuses (predicate == badge) + `formatVisibility` delegation proof (policy mutation test) + `applyPublicEligibleButHidden` set/reason tests + `check:listing-visibility` grep-gate + LV.1 audit script + LV.2 audit-script drift guard (B1 deep-equal) | `npm run check:listing-visibility` · `npx vitest run src/modules/listings/lib/__tests__/visibility.test.ts` | ✅ (Epic LV complete: LV.1/454 policy+predicate+audit; LV.2/455 7-status coverage (34 tests); LV.3/456 badge↔predicate equivalence (7) + `applyPublicEligibleButHidden` set/reason (6) + surface import gates (6) = 45; LV.4/457 adds blocking `check:listing-visibility` grep-gate + gate self-test (7 bad variants + 3 good + 4 no-false-positive) + per-ListingStatus fragment⇔predicate⇔badge invariant (21 tests) = 66 total; planted-violation FAILs: inline literal on public read → gate FAILS, policy break → invariant FAILS; `governance-pr.yml` blocking) |

## P1 — Notifications display (Task 564)

| Flow | Route / component / action | Owner task | Happy path | Failure path | Required regression test | Command | Coverage |
|---|---|---|---|---|---|---|---|
| Notifications panel — price_change body (SSR/CSR parity) | `NotificationItem.tsx` → `resolvePriceChangeBody` (`oldPrice`/`newPrice` grouping) | **564** | `price_change` notification body renders the old/new price with correct per-locale digit grouping, byte-identical regardless of the runtime's own ICU completeness | a runtime whose ICU lacks the active locale (e.g. `sq` — `Intl.NumberFormat.supportedLocalesOf(['sq'])` → `[]` in Chromium) must not silently fall back to a different (comma) grouping | RTL: mounts the REAL `NotificationItem` under a REAL `sq`/`en` `NextIntlClientProvider` with a `price_change` notification, asserts the rendered body text uses the correct NBSP-grouped (`sq`) / comma-grouped (`en`) literal, and a spy wrapping the real `Intl.NumberFormat` constructor (delegates via `super()`, so `next-intl`'s own internal Intl usage is unaffected) proving `resolvePriceChangeBody` makes ZERO `new Intl.NumberFormat` calls during render | `npx vitest run src/modules/notifications/components/__tests__/NotificationItem.priceChange.smoke.test.tsx` | ✅ (Task 564: root cause — `resolvePriceChangeBody` called `new Intl.NumberFormat(locale)` directly, same Chromium-lacks-`sq`-ICU class as Task 563's `PriceBlock` bug. Fix: routed through `formatCount(value, locale)` (Task 563's ICU-independent formatter). 3 RTL tests: `sq` body = `"50 000 ALL → 45 000 ALL"` (NBSP grouping), `en` body = `"50,000 ALL → 45,000 ALL"` unchanged, and the zero-`Intl.NumberFormat`-calls spy proof; planted-violation (reverted to raw `new Intl.NumberFormat(locale)`) → the spy-count assertion genuinely FAILed (`expected 1 to be 0`), reverted → 3/3 PASS.) |
| Notifications panel — template-driven title/body localization | `useNotifications.ts` (data source) → `NotificationItem.tsx` (render-time resolver, Task 319) | **595 + 596** | a `template_id`-driven notification (`saved_search_match`/`price_change`/`support_created`/`support_resolved`/`support_closed`/`report_resolved`/`report_dismissed`) renders its `t(<template_id>_title/_body)` key in the **viewer's** current locale — the SAME row shows different text switching `/uk` ↔ `/sq` ↔ `/en` ↔ `/it` | the hook's `.select()` omitting `template_id`/`template_params` silently makes `notification.template_id` `undefined` at runtime → `NotificationItem` always takes the non-template `else` branch → renders the stored (fixed-language) `title`/`body` verbatim for EVERY template row, in every locale, with no error surfaced | **Render layer (595):** RTL mounts the REAL `NotificationItem` under REAL `uk`/`sq` `NextIntlClientProvider`s with `template_id='support_created'`, asserts the SAME notification object renders "Скарга на ваш акаунт" (uk) vs "Ankesë për llogarinë tuaj" (sq); a 3rd test strips `template_id` and asserts the component falls back to the stored (wrong-language) title; a 4th test confirms a genuine non-template row (`template_id=NULL`) still renders verbatim, unregressed. **Hook layer (596):** mounts the REAL `useNotifications` against a mocked Supabase query-builder chain, asserts `.select()` is called with a column string containing BOTH `template_id` AND `template_params`, plus a chain-integrity assertion that `.order('created_at', {ascending:false})` and `.limit(PAGE_SIZE)` are still invoked — this is the layer that actually regressed in the original bug and that the 595 render-layer test alone cannot catch (its fixtures bypass the hook entirely) | `npx vitest run src/modules/notifications/components/__tests__/NotificationItem.templateLocalization.smoke.test.tsx` · `npx vitest run src/modules/notifications/hooks/__tests__/useNotifications.smoke.test.ts` | ✅ (Task 595: root cause — `useNotifications.ts`'s `.select('id, user_id, type, title, body, link, is_read, created_at')` omitted the two template columns the `Notification` type already declared (`src/types/database.ts:460-461`, added Task 319) and `NotificationItem` already knew how to consume; the Storybook story masked it because its fixtures pass `template_id` directly, bypassing the hook. Fix: one-line `.select()` addition — `, template_id, template_params`. 4 RTL tests: uk/sq locale-switch proof on the identical notification object, planted-violation (`template_id: null`) genuinely FAILed 2/4 (`Скарга на ваш акаунт`/`Ankesë për llogarinë tuaj` not found, fell back to `stub-stored-title`), reverted → 4/4 PASS; non-template negative-flow row unchanged. **Task 596: closes the render-layer blind spot** — new hook-level test mocks `@/lib/supabase/client`'s query-builder chain (`from→select→order→limit`), asserts the `.select()` argument itself; planted-violation (stripped `template_id, template_params` from the hook's `.select()` string, matching the exact Task 595 bug) genuinely FAILed 1/2 (`expected '...created_at' to deeply equal StringContaining "template_id"`), reverted → 2/2 PASS + product code confirmed byte-identical (`git diff --stat` empty) after the revert. Now BOTH the render layer and the hook `.select()` are guarded.) |

## P0 — Server-action / RLS write paths (Slice 5 / Task 444)

| Flow | Route / component / action | Owner task | Happy path | Failure path | Required regression test | Command | Coverage |
|---|---|---|---|---|---|---|---|
| Archetype A: Anon-allowed public write guard | `submitContactInquiry` (`contacts/actions/index.ts:64`) | **444** | valid payload → insert + email | validation fail / rate-limited → no insert | guard smoke: positive (insert + email) + negative (validation + rate-limit + save_failed diagnosable) | `npm run test:rls-guards` | ✅ (Task 444: vitest smoke — 4 tests; positive: insert + email; negative: validation + rate-limited; diagnosability: insert fail → console.error. Already covered siblings: `reportListingAction` 🟢 Slice 3, `submitListingInquiry` 🟢 Slice 3) |
| Archetype B: Authenticated self-scoped write guard | `updateCabinetProfile` (`cabinet/actions/index.ts:26`) | **444** | authenticated → update via user-scoped client, anchored to caller id | no session → Unauthorized, no write | guard smoke: positive (user-scoped client, ownership) + negative (unauthorized) + client-boundary (admin client NOT used) + diagnosability | `npm run test:rls-guards` | ✅ (Task 444: vitest smoke — 3 tests; positive: user-scoped client + `.eq('id', userId)` ownership; negative: Unauthorized; diagnosability: console.error. Already covered siblings: `deleteOwnAccount` 🟢 Slice 2, `createListing`/`updateListing` 🟢 Slice 3) |
| Archetype C: Admin/moderator write guard | `createCurrency` (`admin/actions/currencies.ts:50`) | **444** | admin/moderator → insert via admin client | unauthenticated / regular user → forbidden, no insert | guard smoke: positive (admin → insert) + negative (unauthenticated + regular user) + diagnosability | `npm run test:rls-guards` | ✅ (Task 444: vitest smoke — 4 tests; positive: admin insert; negative: unauthenticated + forbidden; diagnosability: console.error. Already covered siblings: `updateUserProfileFull` 🟢 Slice 4, `clearHistory*` 🟢 Slice 4, `hardDeleteUser` 🟢 Slice 4) |
| Archetype D: Admin-only (NOT moderator) boundary | `setModeratorPermission` (`admin/actions/permissions.ts:142`) | **444** | admin → upsert + audit event | moderator / unauthenticated → forbidden, no write | guard smoke: positive (admin → upsert + event) + negative (moderator rejected + unauthenticated) + diagnosability | `npm run test:rls-guards` | ✅ (Task 444: vitest smoke — 4 tests; positive: admin upsert + event; negative: moderator → forbidden, unauthenticated → forbidden; diagnosability: upsert fail → console.error; planted-violation FAIL confirmed) |
| DB-level RLS policy enforcement (anon/role/cross-user, live-DB) | all RLS-enabled tables | 270/444 | Postgres RLS blocks illegitimate writes | — | live-DB integration test with real Postgres roles | — | ❌ (Slice 5b, deferred — mocked-client smoke cannot cover; see `docs/rls-write-path-manifest.md` Table 2 for detailed gaps) |

> This is the exact gap that let Task 270's RLS change break the report flow (Task 435) with no test
> catching it. Slice 5 action-guard coverage covers the "someone weakened/removed an action's guard or
> swapped its client" regression class. DB-level RLS (a *policy* change breaking an insert while action
> code is unchanged) is deferred to Slice 5b — see `docs/rls-write-path-manifest.md`.

## P1 — Admin data freshness / moderation visibility (Epic KK / Task 452)

| Flow | Route / component / action | Owner task | Happy path | Failure path | Required regression test | Command | Coverage |
|---|---|---|---|---|---|---|---|
| Admin data freshness (focus/visibility refresh) | `useAdminPageFreshness` + `AdminShell` | **452** | focus/visibility→visible while admin tab open → exactly one throttled `router.refresh()`; URL filters preserved | hidden tab → no refresh; burst → ≤1 refresh in interval; non-admin route → never fires (mount contract) | vitest: (1) focus fires router.refresh once; (2) visibilitychange→visible fires once, hidden→none; (3) burst within interval → 1 call; (4) listeners cleaned on unmount | `npm run test:admin-freshness` | ✅ (Task 452: 5 vitest tests — focus, visible, hidden, throttle-burst, unmount cleanup; planted-violation: throttle removed → burst test FAIL `expected 1 call, got 2`; mount contract verified via grep — imported only by `AdminShell.tsx`) |

## P1 — i18n / hydration / mobile contract (Slice 6 / Task 445)

| Flow | Route / component / action | Owner task | Happy path | Failure path | Required regression test | Command | Coverage |
|---|---|---|---|---|---|---|---|
| i18n parity on key routes | sq/en/uk/it | II / **445** | all keys present, render correct | missing key / leak | static `check:i18n` (parity) + G2 render-parity smoke (runtime message-provider parity covered via test-only harness exercising 8 namespaces × 4 locales; not every key route rendered in jsdom) | `npm run check:i18n` · `npm run test:i18n-hydration` | ✅ (Task 445: static parity via `check:i18n` (1821 keys × 4 locales) + G2 runtime render-parity smoke (8 tests, MISSING_MESSAGE + raw-key-leak assertions); planted-violation: renamed `nav.home` in sq.json → 3/8 FAIL with `MISSING_MESSAGE: Could not resolve nav.home`; both gates blocking in CI) |
| Hydration / console errors — detector + self-test | embedded violation server | 434/436 / **445** | gate FAILS on planted violation | gate broken → exit 0 on violation | `check:hydration:verify` self-test, no server needed | `npm run check:hydration:verify` | ✅ (Task 436: planted-violation FAIL confirmed — 2 patterns detected; CI-safe; Task 445: confirmed blocking in `governance-pr.yml`) |
| Hydration / console errors — live public routes | `/en`, `/en/listings`, `/sq`, `/uk`, listing-detail | 434/448 / **445** | zero hydration/console errors | mismatch → gate FAILS | `check:hydration` on running server | `BASE_URL=http://localhost:3000 npm run check:hydration` · listing-detail via `HYDRATION_LISTING_PATH=/en/listings/<slug>` | ✅ (Task 445: CI piece = `check:hydration:verify` blocking self-test; live public-route evidence owner-run 2026-06-17 — `BASE_URL=http://localhost:3000 HYDRATION_LISTING_PATH=/en/listings/test1-mqidv5is npm run check:hydration` → PASS 5/5 (Homepage en/sq/uk, Listings en, Listing-detail), 0 hydration violations; no booted-Next CI step per owner scope decision) |
| Hydration / console errors — admin routes | `/en/admin/users`, `/en/admin/users/[id]` | 434/448 / **451** | zero hydration/console errors | mismatch → gate FAILS | `check:hydration --with-admin` with real user UUID + storageState | `HYDRATION_GATE_STORAGE_STATE=playwright/.auth/admin-storage-state.json HYDRATION_ADMIN_USER_ID=<uuid> BASE_URL=http://localhost:3000 npm run check:hydration -- --with-admin` | ✅ (Task 451 + rework: repeatable capture harness (`capture:admin-session`) + storageState wiring landed; no-session false-green hole closed (admin routes → SKIP/NOT-REAL-COVERAGE when no session, never navigated unauthenticated); CI piece = auth-less admin-config 3-state self-test (`check:hydration:admin-config`) blocking. Live authenticated run owner-verified 2026-06-17: real storageState + real admin UUID → `check:hydration --with-admin` navigated BOTH admin routes (not SKIP) → Admin users list PASS + Admin user detail `/admin/users/[id]` PASS (exact Task 434 route); Summary PASS 6 / FAIL 0 / SKIP 1 (listing-detail SKIP = HYDRATION_LISTING_PATH unset, covered by row above), 0 hydration violations) |
| Date-format SSR/CSR match | `formatDate` / `formatDateTime` / `formatListingDate` (`src/lib/formatters.ts`) | 434 / **445** | literal-byte output matches across sq/en/uk/it; `formatDateTime` TZ-invariant (UTC vs America/New_York) | format change / TZ pin removed → G1 FAILS | G1 vitest: literal-byte assertions + TZ-invariance child-process test | `npm run test:i18n-hydration` | ✅ (Task 445: 25 G1 tests — 4×3 literal byte + TZ-invariance + 12 edge cases; planted-violation: removed `timeZone:'UTC'` → 5/25 FAIL, TZ child shows `01.01.2026 12:30 p.d.` vs `31.12.2025 07:30 m.d.`) |
| Mobile no-overflow at 320/375/390 | critical admin/user/listing routes | DS/JJ / **445** | no horizontal scroll, full-width <640 | overflow → gate FAILS | `screenshots:assert` on 77 stories (admin/user/listing/overlay) × 14 viewports × 4 locales; uk@320/375/390 mandatory | `npm run screenshots:assert` (full) · `npm run screenshots:assert:fast` (320/375/390 × 4 locales) | ✅ deterministic story-level coverage (Task 445: confirmed 77 Storybook stories covering AdminUserProfile, AdminUsersTable, AdminListingsTable, ListingDetailView (12 explicit mobile-320/375/390 story variants), ListingFormShellView, overlays, notifications — uk@320/375/390 present; existing gate, no new stories added). Live-route overflow evidence at 320/375/390 is owner-run and not yet provided — `screenshots:assert` is Storybook-only, not live routes |
| Header overlays (locale switch · user menu · mobile drawer) | `Header.tsx` + `LocaleSwitcher.tsx` — rendered on every public route | **574** | LocaleSwitcher menu / desktop user menu / mobile hamburger `MantineDrawer` / mobile locale `MantineCombobox` all open, route/switch/logout correctly, close on backdrop+Esc; **zero hydration console error on `/` in any locale** (the specific `base-ui-_R_...` id-mismatch the owner captured 2026-07-10 is eliminated because the base-UI `useId()` source — `ui/dropdown-menu` + `ui/sheet` + legacy `shared/Combobox` — is fully removed from both files) | mismatch reappears if a base-UI overlay primitive is reintroduced into either file → `check:hydration` gate FAILS on `/`, `/sq`, `/uk`, `/it` | this flow is a specific instance of the existing "Hydration / console errors — live public routes" row above (same command, same routes — Header renders on all of them); Task 574 additionally requires a manual before/after console transcript for the specific `DropdownMenuTrigger`/`SheetTrigger` `id="base-ui-_R_..."` pattern, since that exact before-state cannot be regenerated once the base-UI code is removed | `BASE_URL=http://localhost:3000 npm run check:hydration` (same as row above) | ✅ (Task 574, run LIVE this session against a real dev server — not deferred: **Homepage (en) PASS, Homepage (sq) PASS, Homepage (uk) PASS — 0 hydration violations**, the exact route the owner's 2026-07-10 capture showed the `base-ui-_R_...` mismatch on; bug confirmed fixed. `screenshots:assert -- --mantine-only` (fresh Storybook build incl. the new §0 story) → 570/596 PASS/0 FAIL/26 pre-existing-ambiguous, byte-identical to the last approved baseline (zero regression); `Mantine/Primitives/{DropdownMenu,Drawer,Avatar}` each 16/16 pass. Also green: `tsc --noEmit`=0, `eslint` clean, `check:i18n` 2133×4 parity, `check:mojibake` 0 artifacts, `check:file-integrity` 7/7 clean, `check:stories` 111 files/0 violations. Static proof the base-UI `useId()` source is gone: `grep -rn "@/components/ui/dropdown-menu\|@/components/ui/sheet\|@/components/shared/Combobox" src/components/layout/Header.tsx src/components/shared/LocaleSwitcher.tsx` → 0 matches. **Note:** the same live run FAILed on `/listings` (en) — traced to `src/modules/listings/components/ListingsStatusTabs.tsx`, an untouched base-UI Tabs consumer, matching the pre-existing intermittent "Base-UI Tabs/CompositeRoot id mismatch" already documented in row 55 above; NOT caused by this diff (Header renders identically on every route and passed cleanly on all 3 homepage locales tested). That pre-existing issue remains open, unrelated to Task 574.) |
| Listing-detail gallery lightbox stacking (portal + z-index) | `ListingGallery.tsx` → `LightboxView.tsx` (`modules/listings/components`), rendered on `/[locale]/listings/[slug]` | **612** | opening a photo renders a Mantine `fullScreen Modal` portaled to `document.body`, whose managed z-index genuinely paints ABOVE the site header AND the sticky agent contact card at every breakpoint × locale; Prev/Next (buttons + Arrow keys, wrap), thumbnail jump, counter, close (X/Esc/backdrop), scroll-lock+restore, focus-return all preserved; zero hydration error on the route | reverting the lightbox to an inline (non-portaled) `div` — the pre-Task-612 shape — silently traps it behind the header/contact card again (the `--z-*` semantic scale computes `z-index:auto` everywhere, see Task 613) | vitest RTL (`ListingGallery.portal.smoke.test.tsx`): dialog is NOT a descendant of a `position:relative`+`z-index` ancestor wrapper, IS inside a `[data-portal]` node, and its Mantine root carries a declared `--mb-z-index` > a real `z-30` sibling; close/Arrow-key/Prev-Next behavior preserved. PLUS a live-`next dev` Playwright script (`scripts/task612-qa-listinggallery-lightbox-portal.mjs`) proving on the REAL page that the scrim covers both `.site-header` and `.listing-contact`/`.listing-mobile-cta` (via `elementFromPoint` topmost-hit-test) across all 7 canonical breakpoints × 4 locales | `npx vitest run src/modules/listings/components/__tests__/ListingGallery.portal.smoke.test.tsx` | ✅ (Task 612: root cause was TWO-FOLD — (1) the lightbox rendered inline, trapped in the listing-detail ancestor stacking context; (2) `z-toast` (the token the original inline div used) is DEAD CSS — `globals.css`'s `--z-*` scale sits under a Tailwind v4 namespace that never compiles a `z-*` utility (confirmed empirically: `getComputedStyle(...).zIndex` = `"auto"`, zero matching rules in any compiled stylesheet) — so a portal alone would still have lost to the header's real, working `z-30`. Fixed at the root by migrating to a Mantine `fullScreen Modal` (compound `Modal.Root`/`Modal.Content`/`Modal.Body` API, not the `MantineModal` pattern — that one is a centered-card/bottom-sheet shape, wrong for a full-bleed media viewer), whose managed `--mb-z-index` (200) is a real, working value. Extracted `LightboxView.tsx` as the presentational primitive (Container/Presentational split gate). Found+fixed a SECOND cascade-layer bug during implementation (same class as Task 602/606): Mantine's `Paper`/`ActionIcon` set their own unlayered `background-color`/`color`/`position` that silently beat the initial `className="bg-overlay/95"` / `className="absolute ..."` attempt (scrim rendered WHITE not black; Close/Prev/Next buttons rendered `position:relative` — real flex-layout participants — squashing the image to 60px wide) — fixed by driving scrim background and the `ActionIcon` `--ai-bg`/`--ai-color`/`--ai-hover`/`position` via inline `style` (inline always outranks unlayered CSS). 4/4 vitest tests PASS; planted-violation (reverted `Modal.Root`/`Modal.Content` to a plain non-portaled `div`) genuinely FAILed 3/4 (the portal/z-index proof + the two tests whose flow depended on Mantine's async close), reverted → 4/4 PASS. Live-page matrix: 28/28 cells PASS (7 breakpoints × 4 locales) — `bodyChild=true`, header AND sticky-contact-card genuinely covered (`elementFromPoint` topmost-hit-test), zero page overflow. Before/after screenshots persisted at `docs/sessions/2026-07-16-task612-assets/` (before: header + "Sign in to contact the owner" card visibly painted over the lightbox; after: full-bleed black scrim, header/card fully hidden). `check:hydration` on the real listing-detail route (`HYDRATION_LISTING_PATH=/en/listings/11-mr7ucly4`) → PASS, 0 violations (the flip-flopping Homepage/Listings-list failures seen across repeated runs are pre-existing dev-mode Turbopack HMR noise, unrelated — confirmed by re-running 3× with a different route failing each time, none touching `ListingGallery`/`LightboxView`). `tsc`=0, `eslint` clean, `check:file-integrity`/`check:mojibake` clean, full `src/modules/listings` suite 1054/1054 PASS (zero regression).) |

## Rendered-proof precondition (Task 464, 2026-06-19)

**Horizontal-overflow proof and every other visual gate (full-width controls, bottom-sheet popup, button width) is INVALID unless the rendered-proof preconditions pass first.** A cell must pass ALL rendered-proof layers (no loader-only, no blank/empty canvas, no blank screenshot, all semantic anchors found) BEFORE overflow/regression assertions are evaluated. A cell that fails rendered-proof short-circuits — its visual-gate results are not recorded and cannot count as evidence. This ordering is enforced in `scripts/check-stories-rendered.mjs` (Task 464) and codified in `docs/storybook-governance.md` §14.4.1.

## P0 — Storybook rendered-proof gate (Task 464)

| Flow | Route / component / action | Owner task | Happy path | Failure path | Required regression test | Command | Coverage |
|---|---|---|---|---|---|---|---|
| Storybook rendered-proof (loader/empty/blank/anchor + style-integrity + geometry/visual-integrity) | `scripts/check-stories-rendered.mjs` + `scripts/geometry-integrity.mjs` — `screenshots:assert` | **464** / **467** | real story content renders, all declared anchors present, non-blank bitmap, CSS/DS styles applied, all visible interactive elements geometrically intact → cell `verdict='pass'` | Task 464: `loader-only`, `blank-canvas`, `empty-canvas`, `blank-screenshot`, `anchor-missing` → FAIL. **Task 467:** `text-clipped`, `offscreen-control`, `element-overlap`, `outside-container`, `bottomsheet-overflow` → hard FAIL (`verdict='fail'`); `text-clipped-ellipsis` / `ambiguous-offscreen` / `ambiguous-overlap` → third state (`verdict='ambiguous'`, `pass=false`, `ambiguousOnly=true` — never readable as clean PASS); **R4:** `unstyled-render` (controlled 2-signal style-integrity trip: `bodyMargin="8px"` via CSS `revert` + `controlThemed=false` via `data-slot="button"` after `all: revert`; `fontFamily` stays `"Geist, sans-serif"` inherited from SB shell; `sheetsWithRules=true`) → retried up to MAX_ATTEMPTS then hard FAIL (`hardAfterRetries=true`, non-transient). **B3/Round 7:** planted unstyled story uses CSS `revert` cascade (no stylesheet removal, SB infrastructure intact). **B6:** `index.json` read failure aborts the run (fail-closed, never silently drops geometry coverage) | planted-violation fixtures (12 stories): Task 464 (1)–(6); **Task 467:** (7) clipped text → text-clipped; (8) overlap → element-overlap; (9) offscreen → offscreen-control; (10) container-clipped → text-clipped; (11) known-good → PASS; (R1) ambiguous-overlap; (R2) text-clipped-ellipsis; **(C2) container-escape → outside-container; (R4) unstyled-frame → unstyled-render (CSS `revert` 2-signal trip: bodyMargin + controlThemed); (V1-FINAL) sr-only-icon-button → PASS (FP-CLASS A guard: icon + sr-only text, no aria-label); narrow-range-guard → out-of-range at desktop (FP-CLASS B); large-range-guard → out-of-range at mobile (FP-CLASS C).** OLD@`5c2edabae`-PASS / NEW-FAIL **PROVEN (owner-native 2026-06-22)** for ALL 9: ClippedButtonText, OverlappingActions, OffViewportControl, ContainerClipped, KnownGoodControl, **ContainerEscape** (`task467-{old,new}-planted-containerescape-unstyledframe.json`: OLD 12/12 PASS → NEW 12/12 FAIL `outside-container`), **UnstyledFrame** (OLD 12/12 PASS → NEW 12/12 FAIL `unstyled-render` `hardAfterRetries=true`). **F-G/Round 5 retry-order:** NEW UnstyledFrame manifest shows `retryCount=2 + hardAfterRetries=true + unstyled-render` counted in `failed` (primary proof); OLD `--fast` 936/1092 → exit=1 (supplementary early-exit evidence). **AdminCurrenciesManager `default × sq × mobile-375`:** verdict=pass, all checks pass (`task467-admincurrencies-sq-mobile-375-entry.json`). **Full inventory (owner-native final rerun 2026-06-23T08-44):** 240 stories, 6532 cells, 5326 PASS / 758 FAIL / 340 AMBIGUOUS / 108 OUT-OF-RANGE; controlled exit=1 (failed=758 > 0); total FAIL counter reduced 2129 → 758; real Bucket-1 product defects = 422 (96 LDV + 326 siblings); 336 planted-fixture cells intentionally fail as harness probes (not product scope) | `npm run screenshots:assert` (full) · `npm run screenshots:assert -- --fast` (320/375/390 × 4 locales) | ✅ (Task 464 + **Task 467 R1–R7 + G3 owner-native evidence 2026-06-22; V1-FINAL complete:** harness code complete — geometry-integrity + style-integrity layers; three-bucket `verdict` output; fail-closed global enumeration; 12 planted fixtures (incl. SrOnlyIconButton + NarrowRangeGuard + LargeRangeGuard); per-cell `verdict` field on ALL paths incl. early exits (F3); `unstyled-render` in `HARD_FAIL_REASONS`; `self-clipped` DEFERRED (F-H); style-only cells excluded from Bucket 1; V1 blank-screenshot false-positives fixed (domRenderPassed gate); V2 crash guards (exit 2 on uncaught); **V1-FINAL false-positive audit:** FP-CLASS A (sr-only/visually-hidden text → skip in text-clipped walk + hasOnlyScreenReaderText exemption), FP-CLASSES B/C (STORY_VIEWPORT_RANGE per-story viewport-range → per-cell `verdict='out-of-range'` + `viewportMismatch=true` + separate inventory section; `admin-adminsidebar--collapsed-rail` minWidth=1024 added), FP-CLASS D (Leaflet `.leaflet-container` controls excluded from geometry checks); auto-generated per-cell inventory with viewport-mismatch section. **All proofs owner-native verified:** ContainerEscape OLD-PASS→NEW-FAIL(`outside-container`) ✅; UnstyledFrame OLD-PASS→NEW-FAIL(`unstyled-render`, retryCount=2, hardAfterRetries=true) ✅; F-G/Round 5 retry-order (NEW UnstyledFrame manifest = primary proof) ✅; AdminCurrenciesManager `default × sq × mobile-375` verdict=pass ✅; full global inventory 6532 cells / 240 stories (owner-native final rerun 2026-06-23T08-44, exit=1, authoritative; total FAIL 2129 → 758; Bucket-1 product defects = 422; 336 planted-fixture harness probes) ✅. Proof JSON: `docs/sessions/task467-v1final-proof-summary.json` · Inventory: `docs/governance-reports/2026-06-19-task467-storybook-visual-defect-inventory.md`) |

## Maintenance rule

Every bug fixed anywhere in the project, after the fix, adds a regression test to the relevant row here
(coverage → ✅) so it cannot silently return. Every new critical flow built gets a row at creation time.
