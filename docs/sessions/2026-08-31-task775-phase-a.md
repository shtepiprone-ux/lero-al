# Task 775 — `/listings` route chrome → `ListingsPageFrame` (Mantine), Phase A2

**Status: REVISION 2 — RETURNED TO EXECUTOR (Phase A)**

Sprint 68. Kickoff: `tasks/Sprints/Sprint_68_kickoff_prompt_Task_775_Listings_Route_Frame.md` (Revision 2.1, §0a).
Prior session: `docs/sessions/2026-08-30-task775-listings-page-frame.md` (Revision 1, not approved).

The first Phase-A pass stripped the §10.3b banned strings from the three Revision-1 comments and added the
§10.10a–e probe fields. Review then returned it for Phase A2: the interaction pass had to assert exact outcomes,
and evidence had to become append-only. Phase A2 adds only non-visual DOM hooks needed for stable selectors and the
probe/documentation corrections below. Phase B and Phase C are not started — §0a keeps the task in Phase A until
this rework is validated and committed.

## Files changed

| Path | Reason |
|---|---|
| `src/modules/listings/components/ListingsPageFrame.tsx` | Line-20 doc comment reworded — removed `` `.container-wide` `` and `1536px` (Revision 1's exact failure line, §0 row 1) |
| `src/modules/listings/components/ListingsPageFrame.module.css` | Whole file is "new" under §10.3b-1 split (B) — every `container-wide` mention (lines 6–7, and inside the `design-tokens-allow:` marker) removed; the marker rewritten to match §10.3c byte for byte |
| `src/design-system/mantine/theme.ts` | D775-C provenance comment (added lines only) reworded — removed the two `` `.container-wide` `` mentions and the `1536px` mention; still cites `globals.css:705-715` by line number only |
| `src/components/shared/Combobox.tsx` | Phase A2: the existing `role="option"` button receives `data-value={opt.value}`, exposing the component's real option value for a non-localized, non-positional probe selector |
| `src/modules/listings/components/ListingsFilterBar.tsx` | Phase A2: the existing advanced-filters `Button` receives `data-testid="task775-advanced-filters"`, making that exact control unambiguous without localized text or DOM order |
| `scripts/task775-listings-frame-route-probe.mjs` | Extended per §10.10a/b/d/e; Phase A2 gives each of the four §10.10c interactions its own context, asserts the exact URL/state postcondition, uses condition waits rather than sleeps, emits a `failReason` on any mismatch, requires the served-tree and instrument identities, and writes only into a new immutable run directory |

`docs/backlog.md` and this session log are the only other touched paths (both outside the §10.3a implementation-source set).

## §10.3b-1 corpus scan — real output

Re-run in Phase A2 per the kickoff's PowerShell block: added/changed lines from every existing implementation-source
path (including the two hook files), plus the full contents of the three wholly-new files:

```
--- assertion 1 (must print NOTHING) ---
(no output)
--- assertion 2 (must print exactly 1) ---
1
```

Both pass. The single matched `design-tokens-allow:` marker in `ListingsPageFrame.module.css` now reads exactly:

```css
/* design-tokens-allow: padding-block: 0.625rem — preserved 10px breadcrumb-band block padding; no Mantine token matches */
```

— byte-identical to §10.3c.

## Decided routes — unchanged from Revision 1

D775-A = A2, D775-B = B2, D775-C = C1 are not re-opened. No visual styling or route behaviour moved in this session;
the Phase-A2 DOM additions are data attributes used solely by the evidence probe. `theme.ts`'s only change
remains the two additive `spacing` keys (`'2xl': '2rem'`, `'3xl': '3rem'`) and the seven-key
`MantineThemeSizesOverride` augmentation; nothing else in that file changed.

## Validation evidence

| Command | Exit code | Notes |
|---|---|---|
| `node --check scripts/task775-listings-frame-route-probe.mjs` | 0 | Syntax check after the Phase-A2 rewrite |
| `git hash-object scripts/task775-listings-frame-route-probe.mjs` | 0 | `890743d176172af788b0f4584cb7665395d124f3` in the current uncommitted worktree; Phase A is not yet reproducible until the owner commits it |
| `npm.cmd run typecheck` | 0 | Re-run after the two JSX data-attribute additions |
| `npm.cmd run build` | 0 | Owner ran it natively after Phase-A2 edits; compiled successfully, completed all build stages, and `/[locale]/listings` remained `ƒ` |

No live probe run or other §13 gate is claimed in this session. The live route evidence remains a Phase-B/Phase-C
responsibility under §0a.

## §10.10 probe extension — not yet exercised against a live route

Phase A's mandate is to implement the probe, not to run it against a live route. The current probe has not yet been
exercised against a running server — Phase B supplies the immutable `pre-edit` run and Phase C supplies the
immutable `post-edit` run. The three Revision-1 `post-edit` artefacts were moved byte-identically to
`docs/sessions/evidence/task775/revision1/`; no current-probe run exists under `runs/` yet. Consequently no
Phase-D comparison manifest exists, and no `probeHash` comparison is claimed here.

## Assumptions, deviations, limitations

- The probe requires a running production server with seeded data during Phase B/C. That condition has not been
  exercised in Phase A2.
- The source showed that `Combobox` previously exposed no option value in the DOM, so Phase A2 adds exactly
  `data-value={opt.value}`. The advanced-filters button likewise receives one exact `data-testid`. Both selectors
  are non-localized and non-positional. The other selectors use existing `data-slot`, class, role and URL contracts.
  Whether all selectors resolve on the live route remains unverified until the first run; an absent, ambiguous, or
  wrong control records `failReason` and makes the probe exit 1.
- `theme.ts`'s D775-C provenance comment still cites `globals.css:705-715` by line number, per §10.3b's explicit
  requirement that the citation stay line-number-only.

## Findings previously reported, not re-verified this session

Unchanged from the 2026-08-30 session log — `MantineHomeSection.tsx:51`'s `container-wide` dependency, the same
legacy breadcrumb markup on `/favorites` and the listing detail page, the measured 1440 content-vs-chrome gutter
delta, and the `MantineThemeSizesOverride.breakpoints` follow-up question. Not re-measured in Phase A; carried
forward for Phase D.

## Backlog

`docs/backlog.md` names the same current status as this file and §0a: `REVISION 2 — RETURNED TO EXECUTOR (Phase A)`.
It records the Phase-A2 rework rather than claiming Phase B has started.
