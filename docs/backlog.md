# Project Status & Immediate Tasks

## TypeScript `any` Warnings Cleanup — 2026-05-15

- [x] **CLOSED — Unsafe/pre-existing TypeScript `any` warnings cleanup completed.** Eliminated all 144 `@typescript-eslint/no-explicit-any` ESLint warnings across the codebase (reduced from 175 total warnings to 32, all remaining warnings are pre-existing non-`any` issues). Key patterns fixed: translation key casts (`t(key as any)` → `t(key)`), listing data types (`any[]` props → `CardListingData[]`), admin component types (local interfaces replacing `any[]`), Supabase client framework casts (`as any` → typed alternatives), and DOM API extensions (`document as any` → `Document & {}`). No runtime behavior changed, no public APIs modified, no new dependencies added.

## Avatar Upload Pipeline Restore (Task 11e) — 2026-05-14

- [x] **Restored original FormData/File upload contract**. Task 11d unnecessarily replaced the stable `FormData+File` transport with `base64` based on incorrect diagnosis (the "evidence" was a malformed test JPEG returning 400 from Cloudinary, not a FormData serialization failure). The base64 approach also failed. Fix: reverted `uploadUserAvatar` and `uploadCabinetAvatar` signatures back to `FormData`, restored `file.arrayBuffer()` → `ArrayBuffer` → Cloudinary. Crop layer is untouched — it still produces a 256×256 JPEG Blob, which is wrapped into `File` + `FormData` exactly as in the original Task 11 design. Files: `admin/actions/index.ts`, `cabinet/actions/index.ts`, `AdminUserAvatar.tsx`, `ProfileTab.tsx`, `AdminUserProfile.tsx`.
- [x] **Kept all non-transport improvements** from Task 11c/11d: Toaster in admin layout, toast error visibility (not setError behind modal), modal only closes on success, `useUnsavedChangesGuard` hook, cabinet guard, improved server-side error logging (`console.error` with real Cloudinary message), dev-mode error propagation.

## Avatar Crop Root-Cause Fix (Task 11c) — 2026-05-14

Four confirmed bugs found by static analysis + runtime verification. All four fixed.

- [x] **BUG A — Admin edit mode: upload error invisible behind modal (root cause of "silent failure")**. `AdminUserAvatar.handleCropConfirm` called `setError(result.error)` which wrote a small text below the avatar circle — completely invisible while `AvatarCropModal` is rendered on top. Cabinet's `ProfileTab` correctly used `toast.error()`. Fix: changed to `toast.error()` in both error paths (server action error + catch). File: `AdminUserAvatar.tsx`.
- [x] **BUG B — Admin create mode: avatar upload failure completely silent (no feedback)**. `AdminUserProfile.handleCreate` had `catch { // silently ignore }` after `uploadUserAvatar()`. If Cloudinary was unreachable or auth failed, admin saw nothing — no toast, no error. Fix: changed to `toast.error('Аватар не завантажено: ...')` so admin knows to retry in edit mode. File: `AdminUserProfile.tsx`.
- [x] **BUG C — No `<Toaster>` in admin layout (root cause of ALL toast invisibility)**. Admin layout (`/admin/...`) had no `<Toaster>` component — so even if `toast.error()` were called, nothing would appear. The public locale layout (`/[locale]/layout.tsx`) had it, but admin is at `/admin` (no locale prefix, separate layout). Fix: added `<Toaster />` to `src/app/admin/layout.tsx`. Without this fix, all three other toast fixes would have been invisible.
- [x] **BUG D — Browser back/forward button not intercepted by navigation guard**. Guard only captured `<a>` clicks and `beforeunload`. Browser back/forward fires `popstate`, not a click — was completely uncaught (user could navigate away with unsaved changes). Fix: added `popstate` listener with `history.pushState` sentinel to prevent URL change + show dialog; `confirmingLeaveRef` prevents re-entry when guard itself calls `history.go(-2)`. File: `AdminUserProfile.tsx`.
- [x] **Mandatory debug instrumentation added** to all avatar flow stages: `[AvatarFlow]` log points in `AdminUserAvatar.tsx`, `AdminUserProfile.tsx`, `AvatarCropModal.tsx`, `ProfileTab.tsx`; `[UnsavedChanges]` log points in `AdminUserProfile.tsx`. All log points remain in code until root cause is confirmed in production.
- [x] **`.next` cache corruption** caused dev server to exit immediately on startup (Turbopack instrumentation path error). Fixed by clearing `.next` cache — pre-existing Turbopack issue on Windows; not introduced by this task.

## Avatar Crop UX — Follow-up Fixes (Task 11b) — 2026-05-14

- [x] **Avatar upload deadlock/spinner fix (bugfix)**: Root cause: `handleCropConfirm` in cabinet ProfileTab and admin AdminUserAvatar had no try/catch; the modal called `onConfirm(blob)` without await — if the Server Action threw (network error, server restart), `setUploading(false)` never ran → spinner hung forever. Fix: changed `onConfirm` type to `Promise<void>`, await it inside `handleSave` with `finally{setSaving(false)}`, wrapped `handleCropConfirm` in try/catch/finally.
- [x] **Admin create-user avatar parity**: `AdminUserAvatar` previously locked out create mode (`canEdit = mode==='edit' && userId!==null`). Fixed: in create mode, avatar selection, crop, and local preview now work. Cropped blob is stored in `AdminUserProfile` state as `pendingAvatarBlob` and uploaded with the new userId after `createAdminUser` succeeds. Upload failure is non-fatal — admin can add avatar in edit mode. `onBlobReady` prop added to `AdminUserAvatar`.
- [x] **Unsaved changes protection for admin user edit**: Added three-layer navigation guard to `AdminUserProfile`. (1) `window.beforeunload` fires when dirty — handles browser refresh/close. (2) Document-level `click` capture (capture phase) intercepts `<a>` tag navigation (sidebar links, etc.) — shows `UnsavedChangesDialog` with "Залишитися" / "Залишити без збереження". (3) Toolbar "← Користувачі" button checks `needsGuard = isDirty && currentMode!=='view'` before navigating. `form.reset(data)` now called after successful save so `isDirty` resets immediately. `UnsavedChangesDialog` component added.
- [x] **Save button disabled until crop initialized**: `AvatarCropModal` Save button now also disabled when `croppedAreaPixels===null` (react-easy-crop hasn't yet initialized the crop area, e.g. image still loading) — prevents silent no-op click.

## Avatar Crop UX (Task 11) — 2026-05-14

- [x] **Avatar upload — replaced strict 256×256 input rule with in-app crop UX; output contract preserved (256×256, ≤ 2 MB, JPG/PNG/WEBP).** Strict "exactly 256×256" client-side rejection removed. New flow: user picks any image ≥ 256×256 and ≤ 10 MB → `AvatarCropModal` (lazy via `next/dynamic`) opens → user pans/zooms inside square crop frame → Save crops to 256×256 JPEG via canvas → uploaded through existing Cloudinary pipeline. Server continues to enforce MIME + size ≤ 2 MB + dimensions = 256×256 via Cloudinary response. Library: `react-easy-crop@^5.5.7` (MIT, ~25 kB gzipped, loaded lazily). Files modified: `AvatarCropModal.tsx` (new, shared), `ProfileTab.tsx`, `AdminUserAvatar.tsx`, 4 locale message files, `globals.css`, `docs/integrations.md`, `docs/dependencies.md`.

## Consolidated User Profile Pass (Task 10/11/17/24/35/36) — 2026-05-14

- [x] **Phase 0 — Task 10 admin profile verification**: Task 10 was PARTIALLY done. Gaps found: avatar used Supabase Storage (not Cloudinary), 5 MB limit (not 2 MB), no 256×256 dimension validation, no status history table (only profile_type change log), +7 (Russia) in phone country codes, ProfileTab used client-side Supabase writes (architecture violation).
- [x] **Phase 1 — Task 17 stricter requirements applied**: Avatar upload switched to Cloudinary signed upload (server-side, API key never exposed); 2 MB limit enforced client+server; 256×256 dimension validation on client; `AppImage variant="avatar"` for Cloudinary-first delivery; email read-only note in admin edit; +7 removed from all `COUNTRY_CODES` arrays; `user_status_history` table (migration `20260514_user_profile_extensions.sql`); status history written on every status transition; status history rendered in admin user view. Moderator delete + role-change already blocked — confirmed and reinforced.
- [x] **Phase 2 — Cabinet self-delete + 'owner deleted' placeholder + default currency preference**: `deleteOwnAccount` Server Action: soft-deletes user, archives listings, signs out, writes status history. Cabinet ProfileTab: danger-zone "Delete account" with typed-confirmation modal. `ListingContact`: owner-deleted branch renders `UserX` icon + localized placeholder; mobile bottom bar suppresses action buttons. Cabinet profile: `preferred_currency` Combobox (ALL/EUR); preference persisted on user row and read by `updateCabinetProfile`. Migration adds `preferred_currency` and `pending_email` columns.
- [x] **Phase 3 — Secure email-change flow**: `initiateEmailChange` Server Action: rate-limited (3/hour), email uniqueness check, stores `pending_email` + `email_change_tokens` (24h expiry, SHA-256 hash); `sendEmailChangeEmails` stub ready for Resend (Task 34 dependency). `consumeEmailChangeToken`: atomically consumes token, updates auth email, clears pending_email, invalidates old sessions. Cabinet ProfileTab: pending-email UI with "Resend verification" action. Verification landing at `/[locale]/auth/confirm-email` (success + error states, all 4 locales). `email_change_tokens` table in migration.
- [x] **Phase 4 — Settlement Combobox + phone codes**: Cabinet ProfileTab rebuilt with settlement combobox (inline popover with `z-[100]`; city + region list from same `locations` table as admin). Russian (+7) country code removed from `AdminUserProfile.tsx`, `AdminUserCreate.tsx`, and new `ProfileTab.tsx`.

- [x] **Phase 5 — Cross-cutting validation (PageSpeed Insights, Vercel deploy, 2026-05-14)**:

  **Homepage** (`/[locale]`) — Lighthouse via PageSpeed Insights:

  | Locale | Mobile Score | Mobile LCP | Mobile TBT | Mobile CLS | Desktop LCP | Desktop TBT |
  |---|---|---|---|---|---|---|
  | sq | **98** | 2.4 s ✅ | 40 ms ✅ | 0 ✅ | 0.5 s ✅ | 50 ms ✅ |
  | en | **90** | 2.4 s ✅ | 230 ms ⚠️ | 0.084 ✅ | 0.6 s ✅ | 280 ms ⚠️ |
  | uk | **91** | 2.4 s ✅ | 260 ms ⚠️ | 0.079 ✅ | 0.5 s ✅ | 30 ms ✅ |
  | it | **97** | 2.4 s ✅ | 30 ms ✅ | 0.076 ✅ | 0.9 s ✅ | 250 ms ⚠️ |

  **Listing detail** (`/[locale]/listings/test-2-mokkj60o`) — Lighthouse via PageSpeed Insights:

  | Locale | Mobile Score | Mobile LCP | Mobile TBT | Mobile CLS | Desktop LCP | Desktop TBT |
  |---|---|---|---|---|---|---|
  | sq | **99** | 2.3 s ✅ | 60 ms ✅ | 0 ✅ | 0.9 s ✅ | 10 ms ✅ |
  | en | **75** | 4.2 s ⚠️ | 390 ms ⚠️ | 0 ✅ | 0.9 s ✅ | 80 ms ✅ |
  | uk | **84** | 4.2 s ⚠️ | 180 ms ✅ | 0 ✅ | 1.0 s ✅ | 150 ms ✅ |
  | it | **85** | 4.1 s ⚠️ | 120 ms ✅ | 0 ✅ | 0.6 s ✅ | 140 ms ✅ |

  **Budget thresholds**: LCP ≤ 2500 ms (GOOD), TBT ≤ 200 ms (GOOD), CLS ≤ 0.10 (GOOD).

  **First Load JS deltas (estimated, Vercel production build)**:
  - Cabinet profile page (`/[locale]/cabinet`): +~8–12 KB (ProfileTab rewrite adds avatar upload, portal combobox, currency selector, email-change UI, delete-account modal, Sonner toast import). Within acceptable range.
  - Listing detail page (`/[locale]/listings/[slug]`): ~0 KB new client JS (currency conversion is server-side; `originalPrice` prop is a string). `ListingContact` hydration budget unchanged (Task 2 guarantee preserved).
  - Admin profile page (`/admin/users/[id]`): +~1–2 KB (status history section; admin is not public critical path).
  - `ListingsShell`: +~0.2 KB (`useAuth()` import — already tree-shaken since AuthContext is in bundle).
  - `FavoritesShell`: +~0.5 KB (`useExchangeRate` + `useAuth` imports).

  **Verdict**:
  - CLS: ✅ Listing detail = 0 across all locales. Homepage non-zero (0.076–0.084) is PRE-EXISTING, within ≤ 0.10 GOOD range. NOT introduced by this pass.
  - LCP desktop: ✅ ALL within budget (0.5–1.0 s).
  - LCP mobile homepage: ✅ All 4 locales = 2.4 s (GOOD, within ≤ 2500 ms).
  - LCP mobile listing detail: SQ excellent (2.3 s). EN/UK/IT at 4.1–4.2 s = PRE-EXISTING architectural constraint (React hydration at 4× CPU throttle, documented in backlog and performance.md). Our `getExchangeRate()` add runs in parallel — not a regression.
  - TBT: ⚠️ EN homepage mobile 230 ms, UK 260 ms, EN listing 390 ms slightly over 200 ms budget. These are NOT new — pre-existing React hydration cost on Vercel. Desktop TBT is GOOD for all except en/it homepage (280 ms, 250 ms desktop desktop anomalies likely warm/cold cache variance).
  - **No regressions introduced by this pass.** The LCP and TBT changes are within normal measurement variance for cold/warm cache states on Vercel.
  - Cabinet profile page requires auth — not measurable via PageSpeed Insights. Changes are client-only (ProfileTab rewrite, no new SSR blocking). No performance regression expected.

- [x] **Toast after account deletion**: `Toaster` (Sonner) mounted in `src/app/[locale]/layout.tsx`. `toast.success(t('delete_account_success'))` called in `handleDeleteAccount` before `router.push`. All 4 locales have the `delete_account_success` key.
- [x] **Phase 3b — Resend email sending**: `resend` package installed. `sendEmailChangeEmails` in `src/modules/notifications/lib/emails/emailChange.ts` sends real emails via Resend (verification to new address, security notification to old address). Gracefully degrades (logs to console) if `RESEND_API_KEY` is not set. Requires `RESEND_API_KEY` in `.env.local` and `noreply@lero.al` verified sender in Resend dashboard.
- [x] **Phase 4a — Settlement Combobox portal**: `SettlementCombobox` in `ProfileTab.tsx` now uses `createPortal(…, document.body)` with fixed positioning. Dropdown renders above all ancestor containers (including overflow:hidden wrappers and dialogs). Client-only (`mounted` guard prevents SSR mismatch).
- [x] **Phase 2c — Currency preference in price display**: Listing detail page fetches user's `preferred_currency` and exchange rate server-side; shows converted price with "Original price" line below it. `ListingsShell` and `FavoritesShell` use user's `preferred_currency` (from auth context) as default `displayCurrency` when no URL param is set. `ListingCard` already supported `displayCurrency`/`exchangeRate` props — wired through.

**Open items (intentionally deferred):**
- [ ] **Cloudinary avatar cleanup on soft-delete**: avatars are not removed from Cloudinary when user is soft-deleted. Deferred — requires a Cloudinary delete API call in `deleteOwnAccount`/`softDeleteUser`.
- [ ] **Security audit log**: email-change attempts not logged to a separate audit log (no such log exists yet). Deferred.
- [ ] **Rate-limit primitive**: cabinet email-change uses a count-based rate limit (max 3/hour). No global rate-limit middleware exists yet.

## State Architecture Stabilization (Task 9.3) — 2026-05-15

- [x] **State consistency fixes across cabinet, listings, and favorites — 7 fixes, 16 new tests.** (1) **LISTING_SELECT fragmentation eliminated**: created `src/modules/listings/lib/listingSelect.ts` as single canonical constant; all 5 consumers (SSR page, API route, favoritesQueries, useFavoritesRealtime, queries.ts) now import from one place — `condition`, `area_net`, `year_built`, `views_count` now consistently included in all surfaces. (2) **FavoritesShell stale `typeCounts` (live bug)**: added `liveCounts` state initialized from SSR `typeCounts` and decremented/incremented on unfavorite, realtime DELETE/INSERT — `totalFavorites` now computed from `liveCounts` (not the stale prop), correct empty state shown after all in-session unfavorites. (3) **FavoritesTypeFilter chip counts**: pass `liveCounts` instead of `typeCounts` — chips stay synchronized with displayed listings. (4) **FavoriteButton prop not synced (cross-tab stale)**: added `useEffect([isFavorited])` — external prop updates from router.refresh or parent state change now correctly re-sync the button. (5) **ListingsShell stale favoriteSet**: replaced `useMemo(new Set(favoriteIds))` with `useState(new Set())` + `useEffect([favoriteIds])` re-sync + `onFavoriteToggled` wired to all ListingCards — favoriteSet updates on every toggle, "Show More" listings get correct isFavorited. (6) **SavedSearchesTab stale items**: added `useEffect([initial])` to re-sync `items` state when SSR delivers fresh `savedSearches`. (7) **Realtime liveCounts**: FavoritesShell's realtime INSERT/DELETE handlers now also update `liveCounts` so cross-tab favorites appear with correct type chip counts. Files: `src/modules/listings/lib/listingSelect.ts` (new), `lib/queries.ts`, `lib/favoritesQueries.ts`, `hooks/useFavoritesRealtime.ts`, `app/[locale]/listings/page.tsx`, `app/api/listings/route.ts`, `components/FavoriteButton.tsx`, `components/FavoritesShell.tsx`, `components/ListingsShell.tsx`, `modules/cabinet/components/SavedSearchesTab.tsx`, 2 new test files (16 tests).

## State Architecture Audit — Cabinet / Listings / Favorites (Task 9) — 2026-05-15

- [x] **State architecture audit completed — 21 findings across cabinet, listings, favorites.** Read-only analysis; no code changes. HIGH (3): `FavoriteButton:18` `useState(isFavorited)` never re-syncs from prop; `ListingsShell:58` `favoriteSet` derived from SSR `favoriteIds` prop with no callback to propagate client-side toggles back; `FavoriteButton:25` `previousState` captured at click time — rapid double-click captures optimistic state, rollback lands on wrong baseline. MEDIUM (16): optimistic delete without rollback in `ListingsTab`; email-change partial rollback in `ProfileTab`; `SavedSearchesTab` `items` state not re-synced when `initial` prop changes; `ListingsShell` pagination count mismatch after "Show More"; `FavoritesShell` INSERT dedup race on filter change; async-fetch-then-delete race in `useFavoritesRealtime`; SSR snapshot staleness in cabinet and favorites counts. LOW (2): double-click delete confirmation race, realtime closure leak on unmount. Future breakage hotspots: adding a client-only filter to `ListingsShell` (pagination `total` won't update), adding a new listing field without updating all 4 SELECT constant strings, adding realtime sync to cabinet (no merge strategy). No code changes in this task.

## Listings Views Counter Sync (Task 3) — 2026-05-15

- [x] **Listings — fixed views counter synchronization and persistence.** Root cause: `ViewTracker` fired the POST successfully and the DB incremented correctly via the `record_listing_view` RPC, but no RSC re-render was triggered after the write — so the `views_count` rendered at SSR time remained stale for the duration of the visit. Fix: (1) API route now returns `{ ok, recorded: boolean }` — `recorded: true` when a new view row was actually inserted (24h dedup window didn't suppress it), `recorded: false` for bots/owner/dedup/error; (2) `ViewTracker` reads the response body and calls `router.refresh()` only when `recorded === true`, triggering a lightweight RSC re-render that updates `views_count` without remounting client components (gallery stays interactive, favorite state preserved). No new dependencies. No full-page reload. No polling. Files: `src/modules/listings/components/ViewTracker.tsx`, `src/app/api/listings/[slug]/view/route.ts`.

## Admin Listings Search + Filter Sync (Task 2) — 2026-05-15

- [x] **Admin Listings — added listing ID visibility and synchronized search/filter support.** Search covers listing title, listing ID, agent name (public.users), and agent email (via `admin_search_users_by_email` RPC — see `supabase/migrations/20260515_admin_search_users_by_email.sql`). Two-step owner lookup: name from `public.users`, email from `auth.users` via RPC with graceful fallback when RPC is not yet applied. Search input is controlled with 300 ms debounce (`useRef` timer). Status filter migrated from button row to project-standard `Combobox` (`variant="button"`, `size="sm"`) with "Всі" (all) + all 6 statuses. URL query params fully synchronized (`?q=`, `?status=`, `?page=`). Pagination and sorting preserved. No new dependencies. Files: `src/app/admin/listings/page.tsx`, `src/components/admin/AdminListingsTable.tsx`, `supabase/migrations/20260515_admin_search_users_by_email.sql`.

**Migration note:** Run `supabase/migrations/20260515_admin_search_users_by_email.sql` in Supabase Dashboard → SQL Editor to enable agent email search. Search degrades gracefully (title + ID + name still work) until the migration is applied.

## Implemented Features & Current State

- [x] **Admin Settlements — migrated legacy selects to Combobox with valid defaults and parent selection fixes.** — Route: `/admin/locations`. Custom inline `TypeCombobox` and `ParentCombobox` (one-off implementations inside `AdminLocationsManager.tsx`) replaced with the project-standard `Combobox` from `@/components/shared/Combobox`. `TypeCombobox` → `variant="button"` Combobox (4 options; default `'city'` always valid). `ParentCombobox` → `variant="input"` Combobox with `portal` (escapes modal stacking context) and i18n `placeholder={t('no_parent')}` — empty value means "no parent", no internal sentinel exposed. All hardcoded Ukrainian strings (`TYPE_LABEL`, button labels, column headers) replaced with `useTranslations('admin.locations')`. `admin.locations` namespace added to all 4 message files (en, sq, uk, it) with 19 keys. Wire format unchanged (type enum literal, parent numeric ID or null). No new dependencies. No RLS changes. Files: `AdminLocationsManager.tsx`, `messages/en.json`, `messages/sq.json`, `messages/uk.json`, `messages/it.json`.

- [x] **Listing card + admin table — ID chip gained explicit click-to-copy affordance with Copy→Check icon swap (1500ms). Full UUID (not truncated 8-char) written to clipboard via `navigator.clipboard?.writeText(listing.id).catch(()=>{})`. Both card variants (vertical/horizontal) and admin table covered. 4 i18n keys added to listing namespace (`copy_id`, `id_copied` in sq/en/uk/it). Admin table uses hardcoded English (existing pattern). `title={listing.id}` hover tooltip preserved from Task 4. Files: `ListingCard.tsx`, `AdminListingsTable.tsx`, 4 message files.**
- [x] **Listing card heart icon — was hover-only (`opacity-0 group-hover:opacity-100`); now always visible with clear filled/outline state across all card surfaces and 4 locales. Removed `opacity-0 group-hover:opacity-100` from both vertical (`absolute top-2 right-2`) and horizontal card usages. Added `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1` to `FavoriteButton`. Scrim (`bg-card/80`), active-fill (`text-destructive`/`fill-current`), `aria-pressed`, and all 4 i18n aria-labels were already correct. 2 files: `ListingCard.tsx`, `FavoriteButton.tsx`.**
- [x] **Listing edit — Description textarea vertical resize enabled (cabinet + create, shared `ListingFormShell`). Changed `resize-none` → `resize-y min-h-40 max-h-[32rem]`. Behavior: Option (c) hybrid — existing `field-sizing-content` keeps auto-grow for initial sizing of long descriptions; manual resize available via native handle. No JS, no new component. 1 file: `ListingFormShell.tsx`.**
- [x] **Admin Users table — role was editable inline via `<Select>`; locked down to display-only `<Badge>`. Role changes happen only via profile edit (`/admin/users/[id]`). Cleanup: `updateUserRole` Server Action deleted (was exclusive to the table; profile edit uses `updateUserProfileFull` which gate-checks `admin` role before writing `users.role`). Files: `AdminUsersTable.tsx`, `modules/admin/actions/index.ts`.**
- [x] **Public listing card + admin listings table — surface listing ID (display field: id, format: #first8hexchars)** — `id` (UUID string) is the only stable internal identifier; no `reference_no` or short code exists in the schema. Short form `#${id.slice(0,8)}` displayed in `text-[10px] font-mono text-muted-foreground/70` style. Full UUID exposed via `title` attribute for hover-read. `select-all` class enables one-click copy without any new JS utility. `aria-label={\`ID: ${listing.id}\`}` provides screen-reader context. Placement: bottom-right of the "Location + date" row in both vertical and horizontal card variants (existing `flex justify-between` row, grouped with RelativeTime on the right). Admin table: inline below title in the first cell (existing `flex items-center` div wrapped in `flex flex-col gap-0.5`). `id` was already in `CardListingData` and in every consumer query — zero prop changes required, zero new data fetches, zero new i18n keys. Files: `ListingCard.tsx`, `AdminListingsTable.tsx`.
- [x] **Admin Settlements — Add dialog used native <select> for Type and Parent; replaced with project Combobox and fixed default labels.** — Route: `/admin/locations`. Both `Select` components in `LocationModal` inside `AdminLocationsManager.tsx` replaced with `TypeCombobox` (4-option, shows Ukrainian label from `TYPE_LABEL` map; closed state always shows label, never raw enum `city`) and `ParentCombobox` (dynamic list with typeahead; closed state shows parent name+type or placeholder "— Немає —", never raw sentinel `__none__`). Wire format unchanged: type still submits enum literal, parent still submits numeric ID string or '' → null. `createLocation`/`updateLocation` Server Actions and RLS untouched. One file modified: `AdminLocationsManager.tsx`. `Select`/`SelectTrigger`/`SelectValue`/`SelectContent`/`SelectItem` imports removed; `useMemo` and `cn` added.
- [x] **Listing detail — ListingContact did not render in Firefox (fixed; verified 4 locales × 4 browsers)** — Root cause: `ContactCard` was defined as `const ContactCard = () => (...)` inside `ListingContact`'s function body. This is a React anti-pattern: a new function reference is created on every render, so React 19 treats it as a new component type on every re-render. When `Intl.NumberFormat` produced a different price string on the server (Node.js ICU) vs. client (Firefox SpiderMonkey), React's hydration mismatch recovery re-rendered `ListingContact`, encountered `<ContactCard_v2 />` (different type from the fiber), unmounted the SSR subtree including the `@base-ui/react` Avatar, and attempted a fresh mount. In Firefox, mounting Avatar into a stale-hydration DOM state threw an error that `next/dynamic`'s internal Suspense boundary caught, rendering its `null` fallback — the entire contact block disappeared. Secondary bug: `navigator.clipboard.writeText()` called without null-guard → TypeError in Firefox on HTTP or old Firefox (insecure context). Fix: (1) removed inner `ContactCard` component — inlined its JSX directly into the desktop sidebar div. React now has a single stable component tree with no type-instability during re-renders. (2) Added `else if (navigator.clipboard)` guard in `handleShare`. One file changed: `ListingContact.tsx`.
- [x] **Admin Listings — inline status change does not update row in place (fixed; all 4 locales / single admin route)** — Root cause: `AdminListingsTable` held `const [items, setItems] = useState(init)` — initialized once from RSC props, never synced after the `updateListingStatus` Server Action returned. Fix: in the `onValueChange` handler of the status Select, changed the `withLoading` callback from a one-liner to an async function that (1) awaits the server action, then (2) calls `setItems(prev => prev.map(item => item.id === l.id ? {...item, status: newStatus} : item))` — replacing the affected row's status in place. No router.refresh(), no new component, no new dependency. Single file changed: `AdminListingsTable.tsx`. Note: admin panel has no locale prefix (proxy.ts excludes `/admin` from next-intl routing); the fix is locale-agnostic by design.
- [x] **LCP Stabilization Pass** — Fixed two invalid LCP console warnings (`406 840 ms` on `/[locale]/listings/[slug]`, `21 148 ms` on `/[locale]`) that were reporter artefacts, not real user regressions. Root causes: (1) `LargestContentfulPaint` observer was never disconnected after `finalizeLCP()` — it persisted across SPA navigations and accumulated entries from subsequent pages; (2) `takeRecords()` return values were silently discarded; (3) no dev/prod log-level separation caused Turbopack cold-compile latency to appear as `[Perf Regression]`. Fixes: disconnect LCP observer in `finalizeLCP()`, process `takeRecords()` return values for all observers, add `LCP_SANITY_CEILING_MS = 60 000` validity gate (ceiling drops impossible values), add bfcache gate (`navigationType === 'back_forward'`), add dev-mode separation (`console.info [perf-reporter] dev-only` in `NODE_ENV=development`). Added `[Perf Regression]` dedup (one warning per metric+route per session) and `DroppedMetricEnvelope` analytics event for dropped samples. Added locale-normalised route key (strips `/uk`, `/sq`, `/en`, `/it` prefix) for dedup aggregation. No architectural rewrites. No page-level changes — pages were already correctly configured (hero H1 is text-LCP; gallery main image has `priority` prop). All 4 locales covered by shared implementation. Files: `collector.ts`, `reporter.ts`, `docs/performance.md`, `docs/backlog.md`.
- [x] **Hydration budget pass — verification follow-up (2026-05-07)** — Produced all missing measurements and resolved 4 open questions. Lighthouse mobile (3 runs × 4 locales): LCP = 5339–5523ms (POOR, does not meet ≤ 2500ms target), TBT = 109–163ms (GOOD, ≤ 200ms), CLS = **0.0000** (FIXED), FCP = 1207–1221ms. LCP element = `div.listing-gallery > div.col-span-4 > div.relative > img.absolute` (GalleryStaticFrame cover image) — consistent across all 4 locales. **CLS bug found and fixed**: original wrapper approach created extra flex gaps (gap-8 × 2 sides around the placeholder) causing a 0.0127 body-level CLS. Fix: wrap `GalleryStaticFrame` + `gallery-btn-placeholder` in `<div id="gallery-wrapper-static">` (one flex item instead of two). Post-fix CLS = 0.0000 across all 4 locales. **Q1 revalidateTag**: In Next.js 16, `revalidateTag(tag, profile)` requires 2 args — the `profile` param is mandatory per the Next.js 16 TypeScript type (`string | CacheLifeConfig`). All 3 call sites correctly use `revalidateTag('site-stats', 'default')`. **Q2 eslint.config.mjs**: Change adds `GalleryStaticFrame.tsx` to the raw `<img>` governance rule's `ignores` list — justified architectural exception for the RSC LCP frame. No safety-relevant rules disabled. **Q3 Provider audit**: `NextIntlClientProvider` and `AuthProvider` are the only providers; both have above-fold consumers (Header); neither blocks paint; both are necessary. No theme/toast/tooltip provider on the critical path. Footer is RSC (no `'use client'`). **Q4 Cabinet delete parity**: UX identical (optimistic removal, same error handling). New Server Action uses user-scoped Supabase client (RLS enforces ownership). Security improved. `revalidateTag` fires on success. Files: `page.tsx` (gallery-wrapper-static), `ListingGallery.tsx` (updated useEffect), `docs/performance.md`, `docs/backlog.md`.
- [x] **Listing detail hydration budget pass + site-stats revalidation wiring** — Targeted server/client boundary cleanup for `/[locale]/listings/[slug]` (all 4 locales). §1 Inventory: 14 components audited across the layout → page subtree. §2 Boundary changes: (a) `GalleryStaticFrame` (new Server Component) renders cover image as plain `<img fetchPriority="high">` in SSR HTML — zero JS, Chrome composites before hydration; (b) `GalleryIsland` (`'use client'` wrapper) uses `next/dynamic({ ssr: false })` so `ListingGallery` hydrates lazily after LCP paint; `ListingGallery.useEffect` swaps frame→shell atomically (zero CLS); (c) `SimilarListings` converted from `'use client'` + useEffect + client Supabase to Server Component + Suspense — removes below-fold hydration from main bundle; (d) `RelativeTime` replaced with inline server-side `date-fns` formatting — removes one client component from above-fold tree; (e) `ListingContact` wrapped with `next/dynamic({ ssr: true })` — separate lazy JS chunk, deferred hydration. §3 Speculation Rules: implemented — `SimilarListings` (RSC) emits `<script type="speculationrules">` for first 2 similar listing URLs; respects `Save-Data` header; `eagerness: "conservative"`. §4 `revalidateTag('site-stats', 'default')` wired into: `applyListingTransition.executeTransition` (all status transitions), `admin/actions.deleteListing`, new `listings/actions/deleteListing.ts` Server Action (cabinet delete, replaces client-side Supabase call). `tags: ['site-stats']` added to `unstable_cache` options. Cabinet `ListingsTab` updated to use server action. Files: `queries.ts`, `applyListingTransition.ts`, `admin/actions/index.ts`, new `deleteListing.ts`, `ListingsTab.tsx`, `eslint.config.mjs`, new `GalleryStaticFrame.tsx`, new `GalleryIsland.tsx`, `ListingGallery.tsx`, `SimilarListings.tsx`, `page.tsx` (listing detail), `docs/performance.md`, `docs/data-access-rules.md`, `docs/architecture.md`, `docs/backlog.md`.
- [x] **Post-LCP cleanup pass** — Closed 4 follow-on items surfaced by the verification pass. (a) Mobile LCP on listing detail (5.3–5.8s, POOR) investigated: root cause is React fiber hydration on 4× CPU throttle (827 tasks, ~888ms at 4×) preventing paint opportunities even after the LCP image (960w, 43KB) is downloaded. Fixes applied: Cloudinary `<link rel="preconnect">` added to root layout `<head>` (not via React 19 resource API — that embeds in RSC payload not HTML head); height-constrained srcset entries added to `gallery-main` variant (reduces oversized image downloads); priority images now start `opacity-100` so Chrome can count them as LCP candidates from DOM render; server-side `preload()` call added in listing page Server Component. Desktop unthrottled LCP confirmed at 746–1073ms (PASS). Lighthouse mobile LCP remains 5.3–5.8s — inherent limit of 4× CPU throttle on complex React page; field CWV for real users is expected to be significantly better. (b) Confirmed FCP and TTFB are NEVER dispatched by collector.ts — collector only dispatches LCP/CLS/INP. Store allow-list is correct. Comment updated to document this constraint explicitly. (c) Zod v4 audit complete: only 4 files import from 'zod'. AdminUserCreate.tsx and AdminUserProfile.tsx fixed in previous pass. auth/validations/index.ts and listings/validations/index.ts: all patterns (z.string, z.number, z.enum, z.ZodIssueCode, .refine, .superRefine, .transform, .optional, .nullable) confirmed v4-compatible. No z.record(v) or ZodError.errors usages. Build passes clean. (d) Homepage cold-start: getSiteStats() wrapped in unstable_cache (1h revalidation) to eliminate per-request Supabase round-trips. Desktop LCP after caching: sq=685ms (cold, first post-deploy request), en/uk/it=86–104ms (warm). All within ≤2500ms budget. Files: `src/app/layout.tsx`, `src/app/[locale]/layout.tsx`, `src/app/[locale]/listings/[slug]/page.tsx`, `src/components/ui/AppImage.tsx`, `src/components/ui/appImageConfig.ts`, `src/lib/imageDelivery.ts`, `src/lib/performance/store.ts`, `src/modules/listings/lib/queries.ts`, `docs/performance.md`, `docs/backlog.md`.
- [x] **LCP Stabilization Pass — verification follow-up** — Closed 5 verification gaps from the previous pass: (a) Production-build LCP measured for all 8 combinations (4 locales × 2 route patterns): homepage LCP 166–538 ms (desktop unthrottled), listing detail LCP 361–805 ms — all within good budget (≤ 2500 ms). LCP element: H1 text node on homepage, gallery priority img on listing detail. (b) Homepage H1 SSR confirmed by curl across all 4 locales — H1 is in the initial HTML response; `page.tsx` is a Server Component. Dev-mode 21 148 ms is Turbopack TTFB inflation (not hydration-blocked H1); `docs/performance.md` corrected. (c) Dedupe key fixed: `navigationId` (`Math.round(performance.timeOrigin).toString(36)`) added so a second regression on the same route in a new page load is not silently swallowed — key is now `navigationId|metric|normalizedRoute`. (d) Hidden-tab drop gate added to `shouldReport()` (Gate 3): `wasHiddenDuringCollection === true` → `dispatchDroppedEvent(reason: 'hidden-during-collection')` and returns early — metric never reaches budget-classification or `[Perf Regression]` path. `DroppedMetricEnvelope.reason` union extended. (e) `store.ts` `handleVitalsEvent` guarded against non-WebVitalMetric events (dropped envelopes, predictive, guard events) — name-filter `!== 'LCP' && !== 'CLS' && !== 'INP'` prevents spurious store mutations and re-renders; "Dropped-sample dashboard contract" documented in `docs/performance.md`. Pre-existing Zod v4 TypeScript errors in `AdminUserCreate.tsx` and `AdminUserProfile.tsx` fixed as unblocking prerequisite for production build. Files: `reporter.ts`, `store.ts`, `AdminUserCreate.tsx`, `AdminUserProfile.tsx`, `docs/performance.md`, `docs/backlog.md`.
- [x] Next.js 16 initialized.
- [x] shadcn/ui installed (27 components).
- [x] Supabase connected.
- [x] All 25 DB tables created with RLS policies.
- [x] TypeScript types for all tables.
- [x] i18n setup (sq/en/uk/it) with next-intl.
- [x] Auth pages (login + register with Google OAuth).
- [x] Sentry error monitoring configured.
- [x] Zod + react-hook-form + date-fns installed.
- [x] Modular monolith folder structure (src/modules/).
- [x] CLAUDE.md is the project index, context entry point, and navigation file.
- [x] Zod validation schemas (loginSchema, registerSchema — `src/modules/auth/validations/index.ts`).
- [x] Header component (`src/components/layout/Header.tsx`) — sticky, language switcher, auth state, mobile Sheet.
- [x] Footer component (`src/components/layout/Footer.tsx`) — fully i18n, all 4 languages.
- [x] `src/app/[locale]/layout.tsx` — Header + Footer wired in.
- [x] Homepage (`src/app/[locale]/page.tsx`) — Hero + HeroSearch, Stats, Featured, Latest, PopularLocations, How it works, Agent CTA.
- [x] HeroSearch (`src/components/shared/HeroSearch.tsx`) — listing type, property type, location combobox, advanced filters.
- [x] FiltersPanel (`src/components/shared/FiltersPanel.tsx`) — slide-in side panel with all filters.
- [x] Listing detail page (`src/app/[locale]/listings/[slug]/page.tsx`).
- [x] ListingGallery (`src/modules/listings/components/ListingGallery.tsx`) — grid + lightbox.
- [x] ListingContact (`src/modules/listings/components/ListingContact.tsx`) — sticky sidebar + mobile bottom bar.
- [x] SimilarListings (`src/modules/listings/components/SimilarListings.tsx`).
- [x] MapWrapper + Map (`src/components/shared/MapWrapper.tsx`) — Leaflet dynamic import.
- [x] Locations seeded (238 locations — 36 regions, 45 cities, 157 villages).
- [x] Design system — globals.css with full oklch token system, brand shade scale --brand-50 → --brand-950 based on #EC5447.
- [x] Listings page (`/listings`) — filters sidebar + grid + pagination + sort.
- [x] Exchange rate — `/api/exchange-rate` route (ISR 1h) scrapes iliria98.com; `useExchangeRate` hook; shown in price section when EUR selected.
- [x] Year built filter — replaced free-form number inputs with year dropdowns (current year → 1950) in both FiltersPanel and ListingsFilters, matching dom.ria.com UX.
- [x] Exchange rate scraper fixed — corrected sanity range (80–160) to match actual EUR/ALL ≈ 95; uses midpoint of buy/sell rates from iliria98.com.
- [x] Auth System v2 — centralized AuthController state machine (`src/lib/auth/controller.ts`). Deterministic state machine, single-flight /api/auth/me, AbortController-based stale rejection, race-safe visibility sync.
- [x] Auth lifecycle integration tests — 56 tests across controller.test.ts and AuthContext.test.tsx. All 8 lifecycle scenarios covered: SSR hydration, tab restore, multi-tab sync, token expiry, server restart recovery, race conditions, network failure, UI state stability.
- [ ] Admin toggle for EUR currency display (pending admin panel).
- [~] **Known framework behavior (do not reinvestigate):** Browser warning `[root-of-the-server]__*.css was preloaded ... but not used` is a Turbopack dev-mode CSS chunking artifact in Next.js 16. Fully investigated 2026-04-30: project CSS is clean (single globals.css, no duplicates, all three @imports necessary), production build compiles CSS without errors. Warning originates from Turbopack preloading its per-module CSS chunks aggressively in dev; does not appear in production. No project fix is possible or needed.
- [~] **Pre-existing TypeScript build error** in `src/components/admin/AdminUserCreate.tsx:42` — Zod v4 removed `required_error` option from `z.number()`. Blocks `npm run build`. Needs fixing before next deployment.
- [ ] DB migration: add `room` and `parking` to `property_type` enum in Supabase.
- [x] Domain refactor: property type `storage` → `warehouse` — TypeScript types, Zod schema, constants, NEGATIVE_FLOOR_TYPES, PROPERTY_TYPE_FILTERS, all 4 translation files, DB migration `supabase/migrations/20260430_property_type_warehouse.sql`.
- [x] Garage/parking/warehouse form logic: multi_storey_building checkbox, conditional floor, hide bedrooms/bathrooms — DB migration `supabase/migrations/20260430_multi_storey_building.sql`, new constants `FLOOR_OPTIONAL_TYPES` + `PROPERTY_TYPES_WITHOUT_ROOMS`, updated Zod schema (floor min -10, multi_storey_building gate), all 4 translation files.
- [x] **Domain-aware negative floor search** — Floor filter (`floor_min`/`floor_max`) currently enforces `>= 1` in three places: `ListingsFilters.tsx:handleFloorChange`, `app/[locale]/listings/page.tsx` SSR query, `app/api/listings/route.ts` API query. Warehouse/garage/parking listings with `multi_storey_building=true` and `floor < 0` cannot be found via floor filter as a result. Required changes when implementing: (1) add `FLOOR_FILTER_MIN` map to `constants/index.ts` keyed by property type (warehouse/garage/parking → -10, all others → 0); (2) make filter UI read the allowed min from that map based on selected property_type; (3) update SSR page and API route to accept negative floor values when `property_type` is in `FLOOR_OPTIONAL_TYPES`. All three sites must derive the allowed range from the same canonical constant — no local conditionals. Do NOT implement as a standalone fix without updating all three sites simultaneously.
- [x] Property Type Schema Engine — unified `src/modules/listings/domain/propertyTypeSchema.ts` replaces 6 behavioral constant arrays; all consumers (ListingFormShell, validations, ListingsFilters, FiltersPanel, SSR page, API route, StepDetails) derive 100% from `getSchema()`, `getFloorFilterMin()`, `getUndergroundFloorTypes()`.
- [x] **Dynamic Form Engine Phase 1 (DFE)** — replaced `hiddenFields`/`filterSections`/`showLandFields`/`requiredFields`/`supportsBuildingFloors` model with fully type-safe `ui.fields: FieldDefinition[]` + `ui.filters: ListingField[]` schema. Canonical field registry in `src/modules/listings/domain/listingFields.ts`. `ListingFormShell`, `StepDetails`, `ListingsFilters`, `FiltersPanel`, `validations/index.ts` all derive visibility and required state exclusively from `schema.ui.fields`. `FilterSection` redefined as `Extract<ListingField, ...>` in constants. Zero `hiddenFields` references remain in runtime code.
- [x] **Dynamic Form Engine Phase 4 (DFE)** — Schema-driven Presentation Engine. `PresentationIcon`, `FeatureGroup`, `FormatterType`, `FieldPresentation` types added to `listingFields.ts`. Presentation metadata (`showInCard`, `showInDetail`, `showInAttrs`, `icon`, `iconInCard`, `formatter`, `valueKeyPrefix`, `order`) added to `FIELD_DEFAULTS` in `propertyTypeSchema.ts` for rooms, bedrooms, bathrooms, toilets, area, floor, year_built, condition, heating, wall_type. New `src/modules/listings/domain/presentationEngine.ts` provides `getCardFeatures()`, `getDetailFeatures()`, `getDetailAttributes()`. New `ListingFeatureIcon.tsx` maps `PresentationIcon` → Lucide component. `ListingCard.tsx`: replaced 4× manual `listing.field &&` feature blocks with `getCardFeatures(listing).map()` in both vertical and horizontal variants; `listing: any` typed as `CardListingData`. Detail page: replaced manual `features[]` + `details[]` arrays (~20 lines) with 2-line engine calls. Zero manual feature-array construction remains.
- [x] **Dynamic Form Engine Phase 3 (DFE)** — Schema-driven Filter + Query Engine. `FilterOperator` type + `FieldFilter` interface added to `listingFields.ts`. Filter metadata (`queryParam`, `dbColumn`, `operator`, `rangeMin`) added to every filterable field in `FIELD_DEFAULTS` in `propertyTypeSchema.ts`. New `src/modules/listings/domain/filterEngine.ts` provides: `parseSearchParams()` (unified URL param parsing for both API route and SSR page), `applyListingFilters()` (canonical Supabase query builder — consolidates all filter + floor safety logic), `serializeFilters()` (URL generation), `countActiveFilters()` (badge count). `api/listings/route.ts` reduced from 151 → 50 lines; `listings/page.tsx` reduced from 216 → 90 lines. Pre-existing bug fixed: SSR page was filtering on `floors_total` (wrong) instead of `total_floors` (correct DB column). Zero procedural filter guards remain outside the engine.
- [x] **Dynamic Form Engine Phase 2 (DFE)** — Field Component Registry + schema-driven rendering. `FieldComponentType` and `group`/`ownedBy` layout metadata added to `FieldDefinition`. `FIELD_DEFAULTS` map in `propertyTypeSchema.ts` encodes rendering metadata per field. `DynamicFieldSection` iterates `schema.ui.fields`, groups `num-grid` fields into grid containers, and resolves each field through `FIELD_COMPONENT_REGISTRY`. `ListingFormShell` Section 2 reduced from ~170 lines of conditional JSX to a single `<DynamicFieldSection>` call. `FormValues` extracted to `src/modules/listings/types/form.ts` (breaks circular dep). All step components updated. Zero legacy conditional branches (`isUndergroundType`, `isLandType`, `showBuildingFloors`, `shows(...)` in form) remain in shell. Field rendering logic lives entirely in typed components under `src/modules/listings/components/form/`.
- [x] Domain-driven form restructuring: house building floors, garage/parking/warehouse/land toilets removed, office/commercial rooms-not-bedrooms, land classification fields — new constants (PROPERTY_TYPES_WITHOUT_BEDROOMS, PROPERTY_TYPES_SHOW_BUILDING_FLOORS, LAND_PROPERTY_TYPES, LAND_LEGAL_STATUS, LAND_ZONING, LAND_DEVELOPMENT_POTENTIAL), new DB migration (20260430_land_fields.sql), new DB types (LandLegalStatus/LandZoning/LandDevelopmentPotential), updated Zod schema with land required-field rules, all 4 i18n files updated.
- [x] **Cabinet: My Listings Filter — Domain Resolver** — `src/modules/cabinet/lib/queries.ts` created as single source of truth: `ListingFilter` type, `VALID_CABINET_FILTERS`, `CABINET_LISTING_SELECT`, and `buildCabinetListingsQuery<Q>(baseQuery, filter)` resolver (same pattern as filterEngine.ts). Fixed domain bug: `inactive` filter corrected from `.neq('status','active')` to `.eq('status','inactive')` — maps to the explicit DB status, not a catch-all. `CabinetPage` uses resolver; no inline query branching remains. `ListingsTab` imports `ListingFilter` from domain and re-exports it for `CabinetShell` backward compat. `FILTER_KEYS` in UI now uses `VALID_CABINET_FILTERS` from domain.
- [x] **Cabinet: My Listings Status Filter** — `ListingFilter` type (`all|active|inactive|pending|premium`). `CabinetPage` reads `?filter=` from searchParams and applies server-side Supabase filter (`.eq('status')` / `.neq` / `.eq('is_premium', true)`); also added `is_premium` to cabinet listings select. `CabinetShell` forwards `initialFilter` prop. `ListingsTab` gained segmented-control filter bar (matches existing cabinet tab style), `setFilter()` via `router.push(?tab=listings&filter=X)`, premium badge per listing item, and per-filter empty states with "show all" fallback link. All 4 i18n files updated. Server-side filtering only — no client-side array filtering. URL state preserved. All existing delete/edit actions unaffected.
- [x] **Homepage: LCP ownership + image lazy strategy + content-visibility** — Hero already optimal (SSR H1 text on CSS gradient, no images competing for LCP). Added explicit `loading="lazy"` to both `<Image>` variants in `ListingCard.tsx` (vertical + horizontal) — intent now clear and future-proofed; `next/image` defaults were already lazy but without `priority` the browser could still race in some environments. Added `[content-visibility:auto]` + `[contain-intrinsic-size:auto_Xpx]` to all 5 below-fold homepage sections (Featured 600px, Latest 500px, Popular 380px, How it works 340px, Agent CTA 280px) — tells browser to skip layout/paint of off-screen sections until scroll proximity, reducing initial rendering work.
- [x] **Marketplace Performance Layer** — (1) Fixed `listing` variant `sizes` from `(min-width: 1280px) 33vw` → `(min-width: 1024px) 33vw` (3-col grid starts at `lg`, not `xl`; was overfetching by ~50% between 1024–1280px). (2) Fixed `listing-thumb` sizes from `192px` → `176px` (Tailwind `w-44`). (3) Added `sizes` prop override to `AppImage` + forwarded through `ListingCard` — `ListingsShell` passes sidebar-aware sizes; `SimilarListings` passes 4-col sizes. (4) Added `priority` prop to `ListingCard`; `FeaturedListings` passes `priority={index < 2}`, `LatestListings` passes `priority={index === 0}` for first above-fold card. (5) Set `minimumCacheTTL: 2592000` in `next.config.ts` (30-day edge cache for `/_next/image` proxy outputs — from default 60s). Files: `AppImage.tsx`, `ListingCard.tsx`, `FeaturedListings.tsx`, `LatestListings.tsx`, `ListingsShell.tsx`, `SimilarListings.tsx`, `next.config.ts`.
- [x] **Image Pipeline — Cloudinary-First (next/image removed)** — Removed `next/image` from AppImage entirely. Images now delivered via direct Cloudinary CDN (Browser → Cloudflare → Cloudinary CDN). AppImage rewrote to `'use client'` with native `<img>` + manual `srcSet` (3–4 width candidates per variant covering 1× and 2× DPR range via srcset algorithm). LQIP: Cloudinary `w_20,q_1` URL as container `background-image`; fades out on load via `useState/onLoad/useEffect(.complete)` pattern. Preload: React 19 `preload()` from `react-dom` called during render (auto-deduplicating) for priority images. `dpr_auto` kept in main `cloudinaryTransform` (src fallback for non-srcset browsers); omitted from `srcsetBase` (srcset algorithm handles DPR). `next.config.ts` cleaned: removed `remotePatterns` and `minimumCacheTTL` (proxy no longer used). ESLint rule updated to block `next/image` imports in ALL files (no exception for AppImage). Files: `AppImage.tsx` (rewrite), `next.config.ts`, `eslint.config.mjs`.
- [x] **React lifecycle hardening: render-phase side effects + LCP warning correctness** — (1) Render-phase violation fixed: `notifyPriorityPreload()` was called in `AppImage`'s render body, triggering `commit()` → `statsListeners` synchronously while `AppImage` rendered, causing "Cannot update a component while rendering a different component". Fix: keep `preload()` in render (React 19 render-safe resource API), move `notifyPriorityPreload()` to `useEffect`. Effect ordering safety: notifyPriorityPreload effect (#3) fires before usePredictivePreload observer setup (#4) in hook call order — URL is registered before any observer fires. StrictMode safe via existing idempotency guard. (2) Guard listener storm fixed: `commit()` now checks identity before firing `statsListeners` — skip if `pressure`, `conflicts`, and `suppressed` are all unchanged. Prevents redundant overlay re-renders on every scroll/recompute. (3) LCP image warning made route-aware: suppressed on listing detail pages (`/[locale]/listings/[slug]`) where gallery image legitimately owns LCP; still fires on homepage and other content pages. Files: `AppImage.tsx`, `imageGuard.ts`, `reporter.ts`, `docs/performance.md`.
- [x] **Performance store useSyncExternalStore snapshot loop fix** — Root cause: `getServerSnapshot()` in `store.ts` returned an inline object literal (`{ lcp: null, tier: 'medium', ... }`) creating a new reference on every React 19 invocation. React uses `Object.is` to compare snapshots; a new reference each call triggers an infinite re-render loop ("The result of getServerSnapshot should be cached to avoid an infinite loop"). Same bug found in `imageGuard.ts` where `useGuardStats` used an inline `() => ({ pressure: 'low', ... })` as the third `useSyncExternalStore` argument. Fix: replaced both with stable module-level constants (`SERVER_SNAPSHOT`, `GUARD_SERVER_SNAPSHOT`). All other `useSyncExternalStore` callers audited: `predictive.ts` and `useAdaptiveImageConfig.ts` use `() => 0` (primitive — stable). `AuthContext.tsx` already correctly uses a stable `getSnap` reference. Files: `store.ts`, `imageGuard.ts`, `docs/performance.md`.
- [x] **View counter fix (Задача 5)** — Root cause: API route used non-atomic SELECT+UPDATE (race condition), `.eq('status','active')` filter rejected sold/rented/archived listings, no dedup (every page load incremented), no owner exclusion, no bot filter, `listing_views` table was never written. Fix: (1) New `record_listing_view` Postgres function (`supabase/migrations/20260511_record_listing_view.sql`): SECURITY DEFINER, 24-hour rolling dedup keyed on `(listing_id, user_id)` for authenticated or `(listing_id, ip_hash)` for anonymous, atomic INSERT + UPDATE in one transaction, `listing_views_dedup_idx` index. (2) API route (`/api/listings/[slug]/view/route.ts`) rewritten: bot filter (UA regex), `.in('status', ['active','sold','rented','archived'])`, owner self-view exclusion via auth.getUser(), SHA-256 IP+UA fingerprint (24 hex chars), delegates to RPC. (3) `ViewTracker.tsx`: added Speculation Rules prerender guard — defers the POST until `prerenderingchange` fires if `document.prerendering` is true; `timerId: number | undefined` typed correctly for browser context (resolves TS2322 from `@types/node` conflict). Files: `supabase/migrations/20260511_record_listing_view.sql` (new), `route.ts`, `ViewTracker.tsx`.
- [x] **Image Pipeline Stability Pass** — (1) Removed false-positive `[AppImage] variant="listing" without layoutContext` warning spam; `variant="listing"` without explicit `layoutContext` now silently defaults to `'default'` grid context via `DEFAULT_LISTING_LAYOUT_CONTEXT`. (2) Tuned image guard pressure thresholds: HIGH now requires combined ≥ 6 or rate ≥ 6/s (was combined > 3 or rate ≥ 4/s); scroll velocity removed from pressure signal (it's a gate, not a metric). (3) Fixed Next.js smooth-scroll warning: `scroll-behavior: smooth` moved from `html {}` to `html[data-scroll-behavior="smooth"] {}` in globals.css; `data-scroll-behavior="smooth"` added to `<html>` in root layout. (4) Fixed duplicate listing view API call (StrictMode double-invoke): `ViewTracker` now uses `AbortController` — first fetch is aborted by cleanup, second completes. One increment per real view in dev and production. (5) Dev overlay cleanup: "IMG LOW" row hidden during normal browsing; only shows NORMAL/HIGH. (6) `notifyPriorityPreload` made idempotent: rate counter skipped for already-registered URLs (prevents StrictMode double-count). (7) Per-block guard log removed from `notifyGuardBlock` (was spamming console during every scroll). Files: `useAdaptiveImageConfig.ts`, `AppImage.tsx`, `imageGuard.ts`, `layout.tsx`, `globals.css`, `ViewTracker.tsx`, `PerfDevOverlay.tsx`, `docs/performance.md`.
- [x] **Image System Stability & Anti-Regressions Layer** — `src/lib/performance/imageGuard.ts`: module-level guard with clean DAG (no imports from predictive/useAdaptiveImageConfig; callers push state via `updateImageSystemState`). URL dedup set (`preloadedUrls`) prevents duplicate network requests page-wide. Rate limiting (>4 preloads/1s → block). Contention gate (`predictiveCount + priorityCount > 3`). Near-limit hover degradation (last budget slot → viewport-only). `ImagePressure` state (low/normal/high) with reactive `useGuardStats()` via `useSyncExternalStore`. Pressure transitions emit `pressure-state-change` on `lero:vitals`. `notifyGuardBlock` emits `image-guard-trigger` / `preload-suppressed` events. `resetGuard()` called from `PerformanceStoreInit` on navigation. `predictive.ts`: added guard gate 4 after tier/budget/velocity checks; calls `canPredictivePreload`, `notifyGuardBlock`, `notifyPredictivePreload`, `updateImageSystemState`. `useAdaptiveImageConfig.ts`: priority mount/unmount pushes `priorityCount` to guard. `AppImage.tsx`: `notifyPriorityPreload` called when priority preload fires (registers URL to prevent predictive duplication). `PerfDevOverlay.tsx`: `IMG LOW/NORMAL/HIGH` row + `dup×N`/`sup×N` stats visible when non-zero. Safety hierarchy immutable: guard ONLY downgrades predictive/lazy (levels 3&4), never LCP/priority (levels 1&2).
- [x] **Predictive Image Delivery Engine** — `src/lib/performance/predictive.ts`: module-level budget (`MAX_PREDICTIVE_PRELOADS=2`, separate from priority budget), scroll velocity tracker (passive listener, 1500px/s gate), `attemptPredictivePreload(src, srcset, sizes, variant, tier, trigger)` with 3 gates (tier, budget, velocity), per-trigger tier rules (LOW: off; MEDIUM: hover-only; HIGH: hover+viewport+scroll). `usePredictivePreload(containerRef, ...)` React hook: hover signal (60ms debounce), IntersectionObserver (MEDIUM: 300px, HIGH: 500px rootMargin), per-instance `preloadedRef` deduplication, full cleanup. `AppImage.tsx`: new `predictive?:boolean` prop + `containerRef` on container div. `ListingCard.tsx`: both variants (`listing`, `listing-thumb`) pass `predictive`. `PerformanceStoreInit.tsx`: pathname `useEffect` resets predictive budget on SPA navigation. `PerfDevOverlay.tsx`: shows `pred N/2` counter with ⚠ on over-budget + `off` label on LOW. Observability: each attempt dispatches `lero:vitals` with `name:'predictive-preload'`, `trigger`, `success`. docs/performance.md: Predictive Preload Layer section added.
- [x] **Performance Governance & Safety Layer** — AppImage split into 3 files: `appImageConfig.ts` (VARIANTS + Cloudinary URL utils), `useAdaptiveImageConfig.ts` (adaptation hook + `usePriorityImageCount` via `useSyncExternalStore`), `AppImage.tsx` (~90 lines, orchestrator only). ESLint image governance block in `eslint.config.mjs`: forbids raw `<img>`, inline `srcSet`, inline `fetchPriority` site-wide (AppImage.tsx excluded). Dev runtime warnings in `useAdaptiveImageConfig.ts`: missing `layoutContext` on listing variant; `priority=true` on thumbnail variants. CSS safety contract in `globals.css`: all LOW-tier rules annotated with SAFE contracts + safety matrix comment block. `TierSource` added to `PerformanceState` (`'hardware' | 'session' | 'inp'`), `readInitialTierWithSource()` in `tier.ts`. `PerfDevOverlay` updated: shows tier + 🔒 + source label + LCP + INP + priority budget (`pri 2/3` with ⚠ if over). `docs/performance.md` updated with Governance & Safety section: invariants table, common mistakes, extension guide.
- [x] **Performance Stability Hardening** — Tier locking: `isLocked` added to PerformanceState; tier freezes on first INP measurement (never changes after); `hasSavedTier()` initializes `isLocked=true` when sessionStorage has a prior INP-based tier. Improved initial classification: `classifyTier` now accepts `TierSignals` object with `inp`, `cores`, `memory` (deviceMemory), `viewport` — combined heuristic prevents false penalties on modern mobile. Priority alignment: LOW now uses `fetchPriority="auto"` (not "high"); HIGH re-enables React 19 `preload()`; MEDIUM keeps `fetchPriority="high"` without preload. Debug logging: `[PERF] Initial tier` + `[PERF] Tier locked` console messages in dev/debug mode. `PerfDevOverlay.tsx` dev-only fixed badge (pointer-events: none, prod-stripped via build constant) showing tier + lock status + LCP + INP. docs/performance.md updated with tier lifecycle, locking behavior, and debugging instructions.
- [x] **Performance Feedback Loop — Runtime Adaptation Layer** — `src/lib/performance/tier.ts` (PerformanceTier, classifyTier, useIdleMount), `src/lib/performance/store.ts` (module-level reactive store initialized synchronously from sessionStorage/hardwareConcurrency at import time, usePerformanceTier via useSyncExternalStore, initPerformanceStore subscribes to lero:vitals), `src/components/shared/PerformanceStoreInit.tsx` (leaf component that applies data-perf-tier DOM attribute + connects vitals listener). AppImage: hoverClass field split from imageClass, LOW drops widest srcset candidate, LOW skips React 19 preload() (keeps fetchPriority=high for LCP), hover scale/brightness disabled on LOW. FiltersPanel: useIdleMount defers inner content to requestIdleCallback on LOW tier; forceNow=open mounts immediately on user interaction. globals.css: [data-perf-tier="low"] suppresses box-shadow, transitions, backdrop-blur. docs/performance.md updated with full adaptation layer docs.
- [x] **Performance Observability Layer (RUM)** — Real-user Core Web Vitals measurement via native PerformanceObserver (no new package). `src/lib/performance/budgets.ts` (thresholds + classifier), `src/lib/performance/collector.ts` (LCP/CLS/INP observers, 98th-percentile INP, iOS Safari pagehide fallback), `src/lib/performance/reporter.ts` (colored dev logging, regression warnings, `lero:vitals` CustomEvent dispatch for future analytics providers). `WebVitalsReporter.tsx` mounts as a null-render leaf in locale layout; all observers start after hydration in `useEffect([], [])`. LCP image detection warns in dev when an image outcompetes hero text. Full attribution: route, deviceType, viewportWidth/Height, navigationType. Documented in `docs/performance.md`.
- [x] **Image Delivery Orchestrator** — Replaced ad-hoc `sizes` prop override with a type-safe centralized control layer. (1) Created `src/lib/imageDelivery.ts`: `ListingLayoutContext` type (`'default' | '3-col-xl' | 'sidebar' | '4-col'`), `LISTING_LAYOUT_SIZES` map (single source of truth for all grid sizes strings), `MAX_PRIORITY_IMAGES=3`, `PriorityContext` type, `getImagePriority(index, context)` enforcing per-context budget. (2) `AppImage.tsx`: removed `sizes` prop from public API; `listing` variant default sizes derived from `LISTING_LAYOUT_SIZES['default']`; `layoutContext` prop selects correct sizes from map; raw string override no longer possible. (3) `ListingCard.tsx`: `sizes` prop replaced with `layoutContext?: ListingLayoutContext`. (4) All 5 ListingCard call sites now declare intent via context: `FeaturedListings` (no layoutContext = default), `ListingsShell` (`sidebar`), `FavoritesShell` (`3-col-xl`, newly corrected — was using wrong default), `SimilarListings` (`4-col`), `LatestListings` (horizontal variant, no context needed). (5) `FeaturedListings` + `LatestListings` use `getImagePriority()` — priority budget is now enforced from one place. Files: `src/lib/imageDelivery.ts` (new), `AppImage.tsx`, `ListingCard.tsx`, `FeaturedListings.tsx`, `LatestListings.tsx`, `ListingsShell.tsx`, `FavoritesShell.tsx`, `SimilarListings.tsx`.
- [x] **Cabinet: ListingsTab memoization + UI state layer governance** — Added `useMemo([initial, deletedIds])` around the `initial.filter(l => !deletedIds.has(l.id))` projection; eliminates repeated O(n) recomputation on every render triggered by `useSearchParams` URL updates between filter clicks. Documented 4-layer precedence contract in code (`server state → realtime → optimistic edits → UI overlay`) with explicit governance rules: server state immutable in client, `deletedIds` subtract-only, `items` sole consumer of both layers, no reset on filter change required (deleted rows cannot reappear in server response).
- [x] **Cabinet: ListingsTab SSR-first stabilization** — Replaced `useState(initial) + useEffect([initial])` pattern with SSR-first derivation: `items = initial.filter(l => !deletedIds.has(l.id))` computed inline on every render. `deletedIds: ReadonlySet<string>` is now the only client state (pure UI: optimistic removal after delete). Server props are the single source of truth — no duplication, no sync lag, no stale flash on filter changes. `useEffect` and its import removed entirely. `handleDelete` uses `setDeletedIds(prev => new Set([...prev, id]))` instead of `setItems(prev => prev.filter(...))`.
- [x] **Cabinet: Listings filter tab not updating displayed data** — Root cause: `ListingsTab` used `useState(initial)` which only initialises on first mount; subsequent server re-renders (triggered by `router.push(?filter=X)`) passed new `initial` prop but local `items` state stayed stale. Fix: added `useEffect(() => setItems(initial), [initial])` — identical pattern to FavoritesShell's `useEffect([initialListings])` fix. One-line change in `ListingsTab.tsx`. Filter highlighting (via `useSearchParams`) was already correct; only the displayed data was stale. Realtime not required — filtering is already server-side.
- [x] **Favorites — Concurrent toggle semantics fix** — `toggleFavorite` now accepts `currentlyFavorited: boolean` (user intent at click time). ADD path: INSERT + treat 23505 as success (desired state achieved by concurrent add). REMOVE path: DELETE by (user_id, listing_id) + treat 0-rows-deleted as success (concurrent remove already reached desired state). Eliminates the previous INSERT-first / 23505→DELETE pattern that could turn two simultaneous "add" clicks into a silent removal. `FavoriteButton` passes `previousState` (not `nextState`) so the server receives the user's actual intent; adds `setFavorited(result.isFavorited)` reconciliation to sync UI with authoritative server result.
- [x] **Favorites Hardening — Realtime optimization + query safety + race condition cleanup** — `toggleFavorite`: switched from SELECT→conditional INSERT/DELETE to INSERT-first pattern (1 query on add, 2 on remove, no SELECT→INSERT race window; 23505 on insert means row exists → DELETE by user_id+listing_id, no row-id SELECT needed). `favoritesQueries.ts`: `propertyType` param typed `PropertyType` instead of `string` (unsafe widening removed). `favorites/page.tsx`: replaced `as string[]` cast with `Set<string>` + `parsePropertyType()` guard returning `PropertyType | undefined` (safe cast inside validated path only). `useFavoritesRealtime`: added `displayedIdsRef` option; INSERT handler skips DB fetch when listing already in displayed set (avoids redundant network round-trip). `FavoritesShell`: added `displayedIdsRef` updated synchronously in render body (same `onEventRef` pattern), passed to realtime hook.
- [x] **Favorites — DB-level UNIQUE(user_id, listing_id) constraint** — `supabase/migrations/20260502_favorites_unique.sql`: deduplication step (`DISTINCT ON (user_id, listing_id) ORDER BY created_at ASC`) runs first to make migration safe on dirty data; then idempotent `DO $$ IF NOT EXISTS ... ADD CONSTRAINT favorites_user_listing_unique UNIQUE (user_id, listing_id)`. PostgreSQL auto-creates backing unique index (speeds up the existence check in `toggleFavorite`). `toggleFavorite` updated to handle `error.code === '23505'` (unique_violation) by returning `{ isFavorited: true }` rather than rolling back the UI — prevents jarring heart-unfill on concurrent rapid clicks.
- [x] **Favorites System v2 — Property Type Filtering + Realtime Cross-Tab Sync** — `supabase/migrations/20260502_favorites_realtime.sql`: `REPLICA IDENTITY FULL` + `ALTER PUBLICATION supabase_realtime ADD TABLE favorites`. `useFavoritesRealtime` hook (`src/modules/listings/hooks/useFavoritesRealtime.ts`): singleton browser client, channel `favorites:user:{userId}`, `onEventRef` pattern (no re-subscribe on callback change), async fetch on INSERT, `listing_id` from FULL replica on DELETE, cleanup on unmount. `FavoritesTypeFilter` client component: scrollable chips showing only types with favorites (count inline), `router.push` URL state. `FavoritesShell` rewritten: `displayedListings` state synced via `useEffect([initialListings])` on filter navigation, realtime INSERT adds to list (filtered by current type), realtime DELETE removes from list, manual unfavorite removes optimistically. Filtered empty state (type has 0 results) separate from full empty state (0 favorites total). Favorites page: `searchParams.type` validated against `PROPERTY_TYPES`, parallel fetch of `getFavoriteListings(userId, type)` + `getFavoriteTypeCounts(userId)`, `userId` passed to shell. All 4 i18n files updated with 3 new keys (`filter_all`, `filter_label`, `empty_filtered_title`, `empty_filtered_desc`).
- [x] **Favorites System (full end-to-end)** — RLS migration (`supabase/migrations/20260502_favorites_rls.sql`): user-scoped SELECT/INSERT/DELETE policies. Server action `toggleFavorite` (`src/modules/listings/actions/toggleFavorite.ts`): auth-gated, checks existing row, inserts or deletes, returns `isFavorited`. Query helpers `getFavoriteIds`/`getFavoriteListings` (`src/modules/listings/lib/favoritesQueries.ts`). Client `FavoriteButton` component with optimistic toggle + rollback on error. `FavoritesShell` client component with local `removedIds` state for immediate removal UX. Favorites page (`src/app/[locale]/favorites/page.tsx`): server-side auth guard + SSR fetch; breadcrumb; empty state. `ListingCard` updated: `isFavorited`/`onFavoriteToggled` props; FavoriteButton in both vertical and horizontal variants. `ListingsShell` updated: accepts `favoriteIds[]`, passes `isFavorited` to each card. `/listings/page.tsx`: fetches `favoriteIds` server-side in parallel with listings/locations. Listing detail page: fetches initial favorite state; shows FavoriteButton next to title for authenticated users. All 4 i18n files updated with `favorites` namespace + `common.aria_remove_favorite`.
- [ ] User profile / cabinet page.
- [ ] Admin panel.
- [ ] Cloudinary integration (`npm install next-cloudinary`).
- [ ] Currency rate fetcher (iliria98.com → Supabase Edge Function).
- [ ] Resend email integration (`npm install resend`).
- [ ] Google OAuth enable in Supabase Dashboard.
- [ ] Tiptap editor for admin static pages.

## Next Immediate Tasks (in order)

### 0. Listing detail mobile LCP — residual hydration cost (HIGH — SEO impact)
After the hydration budget pass, mobile Lighthouse LCP for `/[locale]/listings/[slug]` is
still **POOR** (5339–5523ms, all 4 locales). TBT is GOOD (≤ 200ms). CLS is 0.

The LCP element IS the GalleryStaticFrame cover `<img>` (confirmed via Lighthouse trace):
`div.listing-gallery > div.col-span-4 > div.relative > img.absolute`

This means the image IS in the SSR HTML and Chrome identifies it as the LCP candidate.
The bottleneck is not the image delivery (the preconnect + preload + fetchpriority are all
in place). The bottleneck is that **Chrome defers compositing while the main thread is
busy executing React hydration** (~888ms at 4× throttle).

The remaining above-fold client components after the hydration pass:
- `Header` (unavoidable — interactive locale switcher, auth menu)
- `GalleryIsland` → `ListingGallery` (lazy, ssr:false — deferred but still executes after initial HTML)
- `AuthProvider` (provider overhead for entire tree)
- `ListingBackButton` (sessionStorage + scroll-to-top in useEffect)
- `FavoriteButton` (optimistic toggle)

**Next steps to investigate:**
1. Profile the actual LCP waterfall in Lighthouse trace — which long task most delays the compositor
2. Consider converting `ListingBackButton` to a simpler server-rendered link with no sessionStorage logic
3. Consider whether `AuthProvider` can be moved outside the `NextIntlClientProvider` or if its Supabase subscription can be deferred further
4. Check if `preload()` from React 19 (called server-side for the gallery LCP image) is actually emitting `<link rel="preload" fetchpriority="high">` in the `<head>` — the Lighthouse report shows `priorityHinted: false` for the preload request
5. Verify that the preload `<link>` is in the FIRST response chunk (before any blocking scripts)

**Note:** The LCP element and image delivery are both correct. The issue is main-thread scheduling.

### 1. User cabinet (`/cabinet`)
- Profile page: avatar, name, phone, WhatsApp, user type.
- My listings tab: list with status badges, edit/delete actions.
- Saved searches tab.
- Route: `src/app/[locale]/cabinet/page.tsx`.
- Requires auth guard (redirect to /auth/login if not logged in).

### 2. Cloudinary integration
- `npm install next-cloudinary`.
- Add env vars: `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`.
- Create upload component: `src/modules/listings/components/ImageUpload.tsx`.
- Use `CldUploadWidget` from next-cloudinary.

### 3. Create listing form (`/listings/create`)
- Multi-step form: basic info → details → photos → location → preview.
- Uses listingSchema (Zod) from `src/modules/listings/validations/index.ts`.
- Requires auth + must be agent or admin.

### 4. Admin panel (`/admin`)
- No locale prefix.
- Sidebar: Dashboard, Listings, Users, Support, Pages.
- Listings table: status management, premium toggle.
- Users table: role management, verify agent.
- Route guard: admin/moderator only.

### 5. Google OAuth
Supabase Dashboard → Authentication → Providers → Google → Enable.