# Project Backlog

> ⚠️ **HARD LIMIT: this file holds ACTIVE state only (~80 lines).** The historical ledger lives in a SEPARATE file: [`docs/backlog-archive.md`](backlog-archive.md).
> Full per-task detail lives in `docs/sessions/` — do NOT paste multi-line per-task summaries here.
> "Last Session" = 2–4 lines max (what changed, what's next). When a task is reviewed/closed, move its summary to ONE row at the TOP of the archive ledger. Violating this is a rule breach.
> See "Backlog & Session Log Rules" in `docs/ai-behavior.md`.

## Last Session

**2026-07-17 — Task 620 (`theme.ts` badge shade-index comment accuracy, index-6→7, zero pixel change) — ✅ IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** Corrected `green`/`yellow`/`red`/`blueLight`/`purple` comments in `theme.ts` to state Badge actually reads index 7 (per Task 619's traced `primaryShade:7` mechanism), not index 6; comment-only, all hex/`colors`/`primaryShade` byte-identical. `tsc`/`check:mojibake`/`check:file-integrity` clean; `screenshots:assert --mantine-only` 925/952 PASS 0 FAIL 27 AMBIGUOUS (same baseline); all 16 Badge-story screenshots individually `cmp`-verified byte-identical to the pre-edit run. Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_620_BadgeShadeCommentAccuracy.md`. Session: `docs/sessions/2026-07-17-task620-badge-shade-comment-accuracy.md`. **BACKLOG LIMIT BREACH (pre-existing).** **Next:** orchestrator review.

**2026-07-17 — Task 619 (dedicated `sale` badge color `#dd0939` for price-reduced, both surfaces) — ✅ IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** New `sale` `MantineColorsTuple` in `theme.ts`; card (`ListingCard.tsx`/`ListingCardPattern.stories.tsx`) + detail (`MantineListingDetailPattern.tsx`) `reduced` badge both remapped `brand`/`red` → `sale`; `sale` swatch added to canonical `Badge.stories.tsx` + `badge_sale` ×4 locales. **Self-caught mid-task correction:** the kickoff's index-6 assumption didn't render `#dd0939` (theme's global `primaryShade: 7` makes Mantine read shade index 7 for EVERY color's Badge light/filled, not 6 — verified against Mantine's own source) — re-derived the tuple with `#dd0939` at index 7; both surfaces now compute exactly `rgb(221,9,57)`. Flags a pre-existing, out-of-scope index-6-vs-7 mismatch on `green`/`yellow`/`red`/`blueLight`/`purple` for Opus to triage. All gates green: `tsc`/`check:i18n`/`check:mojibake`/`check:file-integrity`/`check:stories` clean; `screenshots:assert --mantine-only` 925/952 PASS 0 FAIL 27 AMBIGUOUS (byte-identical baseline, re-run twice); anti-regression plant/revert on the card surface proved genuine divergence. Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_619_SaleBadgeColor.md`. Session: `docs/sessions/2026-07-17-task619-sale-badge-color.md`. **BACKLOG LIMIT BREACH (pre-existing, not introduced this session):** this file is already far over its stated 80-line cap — flagging for Opus consolidation. **Next:** orchestrator review of the index-7 deviation + owner native gate.

**2026-07-17 — Task 618 (premium listing card: uniform 1px full-gold border, remove outer ring/step) — ✅ APPROVED WITH NOTES (orchestrator diff review), pending owner native gate + run.** `.premium` CSS = full-opacity `--badge-premium` border, resting ring `box-shadow` removed (inherits `.card` flat rest); premium hover elevation preserved; `StoryListingCard` premium branch de-ringed. **Owner amendment (ratified):** top gold stripe REMOVED from both `MantineListingCardPattern.tsx` + `StoryListingCard.tsx` — it read as a doubled gold line inside the now full-gold border (orchestrator task-spec defect, kickoff corrected). `screenshots:assert --mantine-only` owner-waived via visual confirmation across breakpoints. Grep: only remaining `shadow-listing-card-ring` consumer is the token def; both consumers removed, token preserved. `check:stories`/`check:i18n`/`check:mojibake` re-verified green by reviewer; `tsc`/`check:file-integrity` reported clean (618 hunks type-inert). Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_618_PremiumCardUniformBorder.md`. **Next:** owner native gate + run; Task 617 still awaiting its own orchestrator review (co-mingled in `MantineListingCardPattern.tsx`).

**2026-07-17 — Task 617 (`MantineListingCardPattern` legacy Badge → Mantine Badge, `filled` variant for photo-overlay legibility) — ✅ APPROVED WITH NOTES (orchestrator review 2026-07-17), pending owner commit.** Owner reproduced ALL gates natively: `tsc` clean, `vitest` 17/17 (incl. real `ListingCard` under `NextIntlClientProvider`), `check:i18n`/`mojibake`/`file-integrity`/`stories` clean, `screenshots:assert --mantine-only` 925/952 PASS **0 FAIL** (27 AMBIGUOUS all pre-existing on unrelated Combobox/RangeDatePicker/NotificationBell/Tabs overlay stories — none on ListingCardPattern/Badge). Notes: no kickoff file (owner-directed follow-up — retro-kickoff recommended for audit); `DemoCard` in pattern story doesn't render purple/yellow (source-verified + covered in primitive Badge story). Original impl detail below.** Owner-directed follow-up to the Task 616 badge review (no kickoff file). `MantineListingCardPattern` (already wired into the LIVE site via `ListingCard.tsx` — confirmed with owner before touching production) still imported legacy `@/components/ui/badge` colored via Tailwind `className`. Swapped to Mantine's own `Badge`; updated `ListingCard.tsx`'s `getBadges()` + both `patternBadges` mappings and the story's `DemoCard` to emit native `color` (new=green, price_reduced=brand, status_sold=blueLight, status_rented=purple [NEW theme color, one TailAdmin-cited stop `#7a5af8` + 9 approximated, `theme.ts §4`], status_archived=gray, status_expired=yellow). Added the two missing colors to the canonical `Mantine/Primitives/Badge` story FIRST. **Defect found from the rendered screenshot (owner-caught):** first pass used the theme default `variant='light'` — translucent, so the underlying photo bled through and killed legibility on every card badge (all sit directly on the listing photo). Fixed: added a `variant="filled"` (opaque) demo section to the `Badge` primitive story first, then hardcoded `variant="filled"` in both of the pattern's Badge render sites — matches the ORIGINAL legacy design's intent (solid fill + white text). `screenshots:assert --mantine-only`: 925/952 PASS, 0 FAIL, 27 AMBIGUOUS (pre-existing, unrelated) — one transient flake on an untouched story confirmed non-reproducing via isolated re-check + full re-run. Anti-regression: (1) grep gate — planted legacy import, caught, reverted; (2) rendered gate — planted `minWidth:900` on the card root → 16 FAIL (all `ListingCardPattern` cells), reverted → 0 FAIL. `tsc`/vitest smoke (17/17, incl. the REAL `ListingCard` under `NextIntlClientProvider`)/`check:i18n`/`check:mojibake`/`check:file-integrity`/`check:stories` all clean; 3 new `storybook.mantine.badge_*` keys ×4 locales, read-back+`JSON.parse` verified. Session: `docs/sessions/2026-07-17-task617-listingcardpattern-mantine-badge-migration.md`. Prior session: `docs/sessions/2026-07-17-task616-listingdetailpattern-production-rebuild.md`. **Next:** orchestrator diff review + owner native gate (both 616 and 617).

## Pending Action Items

| Item | Owner | Notes |
|------|-------|-------|
| 🔐 Re-verify HIBP "Prevent use of leaked passwords" availability on Free tier (Supabase Auth → Sign In/Providers → Password Security). Owner flagged 2026-05-28 as Pro-only. If a Free-tier toggle is now available → enable; if not → enable at Pro upgrade. | Owner | Supabase Security Advisor `auth_leaked_password_protection` WARN. Documented in `docs/integrations.md` → "Supabase Auth Configuration". |
| ✅ **DONE 2026-07-14** — notification localization one-row UPDATE applied in Supabase (`id 634f124c…` → `template_id='support_created'`, sq-fallback title/body). The only legacy `template_id=NULL` row now localizes per viewer locale. Owner still to eyeball-verify by re-opening the bell under `/sq`. | Owner | Data-only fix; current creation code already correct. |
| 🐞 Pre-existing `/listings` Grid horizontal-overflow at <640px (Sonnet-flagged, Task 603 §out-of-scope): FilterBar segmented-control button (`flex-1`) + a `min-w-35` Combobox trigger push `scrollWidth` past the viewport at 320/375/390. Unrelated to `FavoriteButton`. Needs its own task. | Orchestrator | Traced via DOM offender scan; not touched by 603. |
| 🧾 Run all pending emitted commits in PowerShell (single-writer). ✅-APPROVED-but-uncommitted tasks awaiting the owner's run + native gate: Sprint 39 overlay Batch C (513–517, 522), Sprint 38 form-control commits (494/496/497/498/499/500/505/507/508), Epic II (316–323), Epic BB (243/430/458/459/460/461/462 + Supabase SQL applies), Epic DD (246/432), Epic LV (454/456/457), plus 238/425/426/427/434/437/442/443/444/448. Per-task detail + Files-Changed tables live in the archive ledger + session logs. | Owner | Commit-emission policy below; nothing here needs new orchestrator work. |

**🟢 Task 583 (Sprint 44 — Favorites icon all breakpoints) — ✅ APPROVED + COMMITTED + PUSHED (2026-07-13, owner).** `HeaderActions.tsx` Favorites heart visible at all breakpoints; native diff = two `visibleFrom="sm"` removals only, gates green. Session: `docs/sessions/2026-07-12-task583-favorites-icon-all-breakpoints.md`. (Open caveat: the 320px overlap/gap was not orchestrator-eyeballed — screenshots were throwaway; low risk on an icon-only add.)

**🟢 Task 587 (Sprint 44 — `MobileNavDrawer` legacy Button → Mantine core + reusable Link/`transparent` variant + Logout indent fix) — ✅ APPROVED (with Task 588), commit emitted, pending owner native gate + run.** Owner-directed 2026-07-13: the reworked primitive still imported legacy `@/components/ui/button`. Added the TailAdmin-conformant `transparent` (Link) Button variant to `theme.ts` (`§6a-link`, as a `Button.vars` override — see session log for the CSS-var precedence root-cause), migrated all 4 drawer buttons (Login=default/Register=filled/Register-agent+Logout=transparent), fixed the Logout left-indent (flush-left with nav links). All gates green, `screenshots:assert --mantine-only` zero-regression vs Task 579 baseline. Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_587_MobileNavDrawerMantineButtonLinkVariant.md`. Session: `docs/sessions/2026-07-13-task587-mobilenavdrawer-mantine-button-link-variant.md`. Reference row: `docs/tailadmin-style-reference.md §6a-link`.

**🟢 Task 580 (Sprint 44 — `Header` thin-container audit) — ✅ APPROVED (orchestrator source+diff review 2026-07-12), commit emitted, pending owner native gate + run.** Confirm-only: no product code changed; independently verified `Header.tsx` thin, `MobileLocaleSwitcher` grep=0, all 4 primitives hook-free + prop-driven with canonical stories. See Last Session for full detail. Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_580_HeaderThinContainer.md`. Session: `docs/sessions/2026-07-12-task580-header-thin-container-audit.md`.

**🟢 Task 579 (Sprint 44 — `MobileNavDrawer` primitive) — ✅ APPROVED + native gate GREEN (2026-07-12), combined commit with Task 586 run.** Extracted the hamburger drawer body from `Header.tsx` into a controlled prop-driven primitive + canonical Mantine story (single open fixture — see Last Session). Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_579_MobileNavDrawerPrimitive.md`. Session: `docs/sessions/2026-07-12-task579-mobilenavdrawer-primitive.md`.

**🟢 Task 586 (Header desktop hamburger visibility fix) — ✅ IMPLEMENTED by Opus (owner-authorized product-code edit 2026-07-12), native gate GREEN, committed with 579.** `Header.tsx` hamburger `ActionIcon` used Tailwind `className="md:hidden"`, which loses the cascade to Mantine's own `@layer mantine` display styling → the burger rendered on desktop beside the `UserMenu` dropdown (owner-caught via screenshot). Fixed with Mantine-native `hiddenFrom="md"`, matching the `visibleFrom` pattern already used in `HeaderActions.tsx`. Single-line change in `src/components/layout/Header.tsx` (shares the file with 579 — see Last Session for the commit-split note).

**Tasks 578 + 585 (Sprint 44 — `UserMenu` primitive + desktop single-open story) — ✅ APPROVED 2026-07-12, combined commit emitted, pending owner native gate + run.** Detail archived. Kickoffs: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_578_UserMenuPrimitive.md`, `..._Task_585_UserMenuStoryDesktopSingleOpen.md`.

**Task 584 (bottom-sheet divider spacing) — ✅ APPROVED + COMMITTED `e486d8d88` (2026-07-12), native gate green.** Detail archived. Kickoff: `tasks/Sprints/Sprint_39_kickoff_prompt_Task_584_BottomSheetHeaderTitleDividerSpacing.md`.

**🟢 Sprint 38 (MM Phase-1 Batch B — form controls) — COMPLETE.** All primitives done (Button/Label/TextInput/Textarea/error-border/PasswordInput/disabled-field+label+icon/Checkbox/Radio/Switch); 495 Select closed via 508. Remaining = owner-run commits + the two outstanding 499 Switch render cells (uk@375/390). Batch A (486–491) ✅ done. Tracker: `docs/mantine-tailadmin-migration-tracker.md`. Plan: `tasks/Sprints/Sprint_38_MM_Phase1_FormControls.md`.

**🟢 Sprint 39 (MM overlay Batch C) — COMPLETE, owner commits pending.** 509/510 (canonical Select) · 513 (Popover) · 514 (single-source `ResponsiveBottomSheet`) · 515 (DropdownMenu) · 516 (overlay trigger width) · 517 (drag-handle centering) · 518 (NavigationMenu) · 519/520/521 (Modal thread, committed + pushed) · 522 (§18.8 content-height fix, approved) · 523 (`MantineDrawer`) ✅ committed `04cc304db` · 524+526 (`MantineTooltip`, P1.22, implemented incl. wrap-no-clip fix — see Last Session). Batch C (P1.18–P1.22) fully done → next is Batch D. Kickoffs under `tasks/Sprints/Sprint_39_*`.

**🟡 Sprint 40 (TailAdmin conformance — ALL primitives) — OWNER P0, agent-contract clause 16.** Task 525 audit ✅. Task 527 ❌ REJECTED. Task 528 ✅ APPROVED + committed. Task 529 ✅ APPROVED + committed. Task 530 ✅ APPROVED + committed `9d98b8f7f`. Task 531 ✅ ORCHESTRATOR REVIEW PASS, `theme.ts` landed on HEAD `389bf2ae7`. Task 532 ✅ APPROVED, commit emitted, pending owner run. Task 533 ✅ APPROVED + COMMITTED `a39877e3d`. Task 535 ✅ APPROVED + COMMITTED `29607cb93` — CLOSES P1.13, SUPERSEDES+CLOSES Task 534. Task 536 ✅ APPROVED, commit emitted, pending owner run — `_MantineStoryShell` single-source content-column on all 23 stories. Task 537 ✅ APPROVED + COMMITTED `9925ed253` — canonical `MantineCombobox` primitive (P1.21). **Task 539 (EXPANDED) — Mantine primitive stories TailAdmin conformance — ✅ APPROVED (Scope A Progress + Scope B SegmentedControl §6c), commit emitted pending owner run. STOP-AND-ASK RESOLVED: owner chose segmented/pill Tabs → Scope C underline text-color reverted (Task 542), redesign = Task 541. Progress `[loader-only]` gate false-positive fixed via Task 542.** Progress (Scope A, done) + SegmentedControl §6c deferral CLOSED (Scope B) + delta-table audit of all 25 primitives (Scope C, found+fixed 1 more deferral: Tabs text color). Multi-commit per logical change at close. Kickoff: `tasks/Sprints/Sprint_40_kickoff_prompt_Task_539_MantinePrimitiveStoriesTailAdminConformanceAudit.md`. Session: `docs/sessions/2026-07-04-task539-segmentedcontrol-tabs-conformance-audit.md`. **Task 540 — ✅ APPROVED + COMMITTED `571688eb8` (2026-07-03).** `MantineStoryShell` gained a `width` prop (`'full'` default / `'constrained'`); `≥640` `'full'` drops the §6m 1536 cap → full viewport width minus a 16px(`<768`)/24px(`≥768`) gutter (§6m `p-4 md:p-6` cite, zero invented numbers), card chrome + gray-0 bg retained. `Table.stories.tsx` + `Tabs.stories.tsx` pass `width="constrained"` (Task 536 behavior unchanged). `<640` byte-identical. §6m + §8.1 updated recording the owner override. Rendered gate: 381/400 PASS (16 pre-existing Progress/Task-539 `loader-only` fails + 3 known Tabs-swipe ambiguous, both unrelated — reproduced identically before/after my diff, confirming zero regression); planted-violation (`miw={{base:900,sm:0}}`) genuinely FAILed the gate (96/400 PASS) then reverted. Session: `docs/sessions/2026-07-03-task540-story-shell-full-width.md`. Kickoff: `tasks/Sprints/Sprint_40_kickoff_prompt_Task_540_StoryShellFullWidthAbove640.md`. Sprint: `tasks/Sprints/Sprint_40_TailAdmin_Conformance_AllPrimitives.md`. **Task 542 — ✅ APPROVED, commit emitted pending owner run.** Resolved the Progress `[loader-only]` gate false-positive (narrow `LOADER_ALLOWLIST` exemption verified identical to the Button precedent, Progress primitive untouched, `docs/storybook-governance.md` §14.9.8) + reverted the premature Task 539 Tabs underline block (grep-proven gone). Native gate: 397/400 PASS, 0 FAIL, 3 AMBIGUOUS (known Tabs-swipe only, → Task 541). Kickoff: `tasks/Sprints/Sprint_40_kickoff_prompt_Task_542_Task539CommitUnblock_ProgressGate_TabsRevert.md`. Session: `docs/sessions/2026-07-04-task542-progress-gate-tabs-revert.md`. **Task 541 (Tabs segmented/pill redesign + swipe-scroll CORRECTION) — ✅ IMPLEMENTED, HELD for orchestrator review** (see Last Session).

**⏸️ Task 463 (Epic BB — full admin report management) — ON HOLD until the Mantine primitive library (Epic MM) is complete.** Do NOT re-execute yet: it must be rebuilt on the finished primitives, not the legacy ones. Capability-gated any→any status override + reopen + close + hard-delete, delegable via Дозволи (`reports.status_override` + `reports.delete`). Prior legacy-primitive implementation sits in the working tree (`docs/sessions/2026-06-19-task-463-admin-report-full-management.md`). Kickoff: `tasks/Epics/Epic_BB_kickoff_prompt_Task_463_AdminReportFullManagement.md`.

**Task 534 — SUPERSEDED + CLOSED by 535** (kickoff `..._Task_534_PaginationMobileTapTarget.md` retained for history; its ≥44px requirement was implemented as Rule 5 of Task 535's `MantinePagination`; 534 was never executed standalone).

**🟢 Task 588 (Sprint 44 — Button primitive story: showcase `§6a-link` link variant, with-icon AND no-icon + destructive red) — ✅ APPROVED (orchestrator diff review 2026-07-13), commit emitted (with 587), pending owner native gate + run.** Completed 587: `Button.stories.tsx` `Default` gained the link/tertiary section (no-icon + `ArrowRight` with-icon + `LogOut` red destructive); +3 `storybook.mantine.button_link_*` keys × 4 locales (2145×4 parity); `theme.ts` untouched; single `Default` export; hover-probe `bg rgba(0,0,0,0)` rest+hover + `#344054` neutral. Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_588_ButtonPrimitiveStoryLinkVariants.md`. Session: `docs/sessions/2026-07-13-task588-button-primitive-story-link-variants.md`.

**🟢 Task 589 (Sprint 44 — Button `default`/`outline` FULL chrome vars-precedence fix) — ✅ APPROVED (orchestrator diff review 2026-07-13), commit emitted (with 587/588), pending owner native gate + run.** All four discarded vars + `--button-hover` gray-0 relocated to `Button.vars`, gated `color===undefined`; `boxShadow` kept in `styles`; `transparent`/`filled` byte-identical. BEFORE/AFTER probe tables + `outline color="red"` destructive-safeguard row + full `screenshots:assert` (634/660, zero regression) + computed-style proof on FiltersPanel real consumer. Orchestrator-verified against the actual `theme.ts` diff. Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_589_ButtonDefaultOutlineColorVarsPrecedenceFix.md`. Session: `docs/sessions/2026-07-13-task589-button-default-outline-vars-precedence-fix.md`.

**🟢 Task 590 (Sprint 44 — extract `HeaderView` presentational primitive from `Header.tsx` + canonical story) — ✅ APPROVED (orchestrator source+diff review 2026-07-13), commit emitted, pending owner native gate + run.** Closes the last app-shell violation of the Presentational-Primitive Split Gate: `HeaderView.tsx` (pure, prop-driven, overlays+bell as slots, zero data/network hook), `Header.tsx` → thin container (public API unchanged), single-`Default` `Mantine/Primitives/HeaderView` story (guest+authed fixtures, NO hook mock) — the standing rendered coverage the Task 586 desktop-hamburger bug lacked. Owner-authorized mid-task addition: defensive two-row wrap below `min-[390px]` (≥390 byte-identical). See Last Session for verified detail + the two orchestrator gaps flagged for the native run. Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_590_HeaderViewPresentationalPrimitive.md`. Session: `docs/sessions/2026-07-13-task590-headerview-presentational-primitive.md`.

**🟢 Tasks 591 + 592 (Sprint 44 — header `NotificationBell` → Mantine + presentational split; `<Indicator inline>` badge-attach fix) — ✅ APPROVED + COMMITTED (owner native run 2026-07-14, `check:mojibake` GREEN), push pending (12 commits ahead).** 591's migration was structurally sound but shipped a detached badge (owner-caught) → 592's one-line `inline` fix; committed together as the correct migration. Orchestrator opened the rendered PNGs (badge-on-bell verified uk@320/390 + desktop). Detail in archive. Kickoffs: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_591_NotificationBellMantineMigration.md`, `..._Task_592_NotificationBellIndicatorInlineFix.md`. Sessions: `docs/sessions/2026-07-14-task591-notificationbell-mantine-migration.md`, `..._task592-notificationbell-indicator-inline-fix.md`.

**🟡 Task 599 (Sprint 44 — authenticated-header hydration fix) — IMPLEMENTED 2026-07-15, HELD for orchestrator review (see Last Session for full detail incl. the DEV-ONLY gate finding + open reopen criterion).** Kickoff: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_599_HeaderAuthHydrationSSRBellFix.md`. Session: `docs/sessions/2026-07-15-task599-header-auth-hydration-ssr-bell-fix.md`.

**Task numbering — last used: 620. Next free: 621.** (620 = ✅ IMPLEMENTED 2026-07-17, awaiting orchestrator review — `theme.ts` badge shade-index comment accuracy, zero pixel change.) (619 = ✅ IMPLEMENTED 2026-07-17, awaiting orchestrator review — `sale` badge color; 618 = ✅ APPROVED WITH NOTES 2026-07-17; 617 = ✅ APPROVED WITH NOTES 2026-07-17, pending owner commit —
`MantineListingCardPattern` legacy Badge → Mantine Badge (`filled` variant, photo-overlay legibility fix); adds
`purple` theme color + `Badge` primitive story swatches (Info/Purple/Filled); updates the live-site `ListingCard.tsx`
consumer. See Last Session for full detail. Session `docs/sessions/2026-07-17-task617-listingcardpattern-mantine-badge-migration.md`.)
(616 = ✅ IMPLEMENTED 2026-07-17, awaiting orchestrator review —
ALL-Mantine `ListingDetailPattern` production rebuild (D1 `MantineListingGalleryPattern` + D2 `MantineListingContactPattern`
+ rebuilt D3), zero legacy primitives, see Last Session for full detail. Kickoff
`tasks/Sprints/Sprint_44_kickoff_prompt_Task_616_ListingDetailPatternProductionRebuild.md`. Session
`docs/sessions/2026-07-17-task616-listingdetailpattern-production-rebuild.md`.) (615 = ✅ APPROVED + commit emitted 2026-07-16 (pending owner run) —
`MantineListingDetailPattern` contact CTAs never switched to a side-by-side row at `sm+` (inline
`style={{flexDirection:'column'}}` out-specificitied a `styles`-prop `@media` override). Fixed via canonical
`Flex direction={{base:'column',sm:'row'}}` + per-button `flex:1`; mid-task found+fixed a second real defect (flex-item
automatic-minimum-size trap on an unbreakable single-word uk label — fixed via `minWidth:0` on root+`inner`+`label`
styles plus an explicit wrapping `<span>` so long labels wrap instead of overflowing). Self-caught process bug:
`screenshots:assert` doesn't rebuild `storybook-static/`, so `npm run build-storybook` was re-run before every assert.
Verified: 20-cell Playwright probe (320/375/390/768/1024×4 locales) all PASS incl. hardest cell `uk@768`;
`--mantine-only` 889/916 PASS 0 FAIL 27 AMBIGUOUS (byte-identical to Task 609 baseline), `ListingDetailPattern` 16/16;
planted `minWidth:900` anti-regression genuinely FAILed all 16 cells, reverted clean. Kickoff
`tasks/Sprints/Sprint_44_kickoff_prompt_Task_615_ListingDetailCTAFlexDirectionFix.md`. Session
`docs/sessions/2026-07-16-task615-listingdetailpattern-cta-flexdirection-fix.md`.) (614 = ✅ APPROVED 2026-07-16 (orchestrator review, commit emitted, pending owner native gate + run) — added
the missing planted-violation proof for the shared `check-stories.mjs` Check-9 test-file exclusion Task 612 self-merged
(`.test.tsx`/`__tests__` skip): 3 new tests prove `.test.tsx`/`__tests__` files are skipped AND a real non-test
`src/modules` component with the same literal is STILL caught (blind-spot guard); anti-no-op transcript via reverting
the exclusion (both GOOD tests genuinely fail, then revert restores green, `check-stories.mjs` confirmed byte-identical);
also reconciled the pre-existing stale `checksRan===13` completeness assertion to the true hardcoded `14`. Test-file +
docs only; `check-stories.mjs` UNTOUCHED (verified). Kickoff `tasks/Sprints/Sprint_44_kickoff_prompt_Task_614_CheckStoriesCheck9TestFileExclusionProof.md`.
Session `docs/sessions/2026-07-16-task614-check-stories-check9-test-file-exclusion-proof.md`.) (613 = OPENED 2026-07-16 from the Task 612 STOP-AND-ASK —
remove/deprecate the DEAD `--z-*` Tailwind z-index token scale (`globals.css:245-251` is under the `--z-*` namespace
Tailwind v4 never turns into `z-*` utilities → `z-toast`/`z-sticky`/… all compute `z-index:auto`; blast radius ≈ the
single `ListingGallery` `z-toast`, fixed by 612, because overlays moved to Mantine + core `z-30/40/50` + the
`z-[9999]` escape-hatch). Deprecate, do NOT revive; reconcile `ui-rules.md §16/§12`. LOW-PRIORITY cleanup, after 612. Kickoff
`tasks/Sprints/Sprint_44_kickoff_prompt_Task_613_DeadZIndexTokenScaleCleanup.md`, not yet executed. 612 = RE-SCOPED
2026-07-16 (owner `AskUserQuestion`) then **IMPLEMENTED + self-reviewed same session**: migrated the lightbox to a
Mantine `fullScreen Modal` (managed z-index fixes the stacking at root; NOT `MantineModal` pattern) + extracted
`LightboxView` presentational primitive + canonical Mantine story; found+fixed a second cascade-layer bug
(Mantine `Paper`/`ActionIcon` unlayered CSS) via inline `style`; `globals.css` untouched (→613). All gates green,
28/28 live-page + 16/16 Storybook + 4/4 vitest (planted-violation verified), zero regression on the 1054-test
listings suite. Session `docs/sessions/2026-07-16-task612-listinggallery-lightbox-mantine-modal-migration.md`.)
(612 = OPENED 2026-07-16 from an owner bug report —
`ListingGallery` lightbox renders BELOW the site header + sticky contact card because it is NOT portaled
(`fixed inset-0 z-toast` trapped in the page stacking context; `z-toast:100` can't beat root-level chrome from a
nested context); fix = `createPortal` to `document.body`, keep the token, mandatory real-page before/after pixels.
Kickoff `tasks/Sprints/Sprint_44_kickoff_prompt_Task_612_ListingGalleryLightboxPortalZIndex.md`. 609 = ✅ APPROVED
2026-07-16 (orchestrator diff review + owner native gate green, commit emitted, pending owner run) — see Last Session for full detail (root-cause
`gutter={0}`+`pr`/`mb` fix, NOT `overflow-x:clip` — that containment approach was tried and rejected for a new
`text-clipped` false positive; registry entry removed, clears the last Task-607 tracked xfail). Kickoff
`tasks/Sprints/Sprint_44_kickoff_prompt_Task_609_ListingDetailPatternGridGutterOverflow.md`. Session
`docs/sessions/2026-07-16-task609-listingdetailpattern-grid-gutter-overflow-fix.md`. 610 = still reserved,
not yet kicked off (grid/list title-hover asymmetry — 608 notes the grid `group-hover` fix already shipped, so 610
may now be a no-op/verify-only; re-scope before kicking off). 607/608/611 = ✅ APPROVED + COMMITTED + PUSHED
2026-07-16, see archive + Last Session. 611 = resolved Task 607's 2 held stories via a generic gate fix. Kickoff
`tasks/Sprints/Sprint_44_kickoff_prompt_Task_611_GateHeldStoryResolution.md`. 608 = site-wiring for the Task 606
List-layout pattern; kickoff `tasks/Sprints/Sprint_44_kickoff_prompt_Task_608_ListingCardListLayoutSiteMigration.md`.)
(607 = OPENED 2026-07-15 from the Task 606 review —
extend the canonical rendered gate `scripts/check-stories-rendered.mjs` to auto-discover `Patterns/Mantine/*`
(all 13 pattern stories, owner chose full-group via AskUserQuestion), closing the machine-rendered-coverage hole
that forced Task 606's ad-hoc QA script; prefix-LIST discovery, no hardcoded allowlist, STOP-AND-ASK on any
pattern needing an overlay open-trigger or surfacing a real defect (never patch a component/story to force green),
mandatory anti-no-op planted-violation transcript. **IMPLEMENTED 2026-07-15, gate currently RED (exit 1) —
NOT approved.** `ListingDetailPattern` confirmed real (Grid-gutter overflow) → tracked, Task 609.
`AdminSurfacePattern`/`AppShellFoundation` HELD for owner pixel-review (mid-task course-correction — initially
mischaracterized as confirmed defects; screenshot+source review found both likely false-positive/open-trigger,
not confirmed — owner must personally view evidence before any exemption per §18.9). See Last Session +
session `docs/sessions/2026-07-15-task607-patterns-mantine-rendered-gate-coverage.md`. Kickoff
`tasks/Sprints/Sprint_44_kickoff_prompt_Task_607_PatternsMantineRenderedGateCoverage.md`.) (606 = add a `layout: 'grid' | 'list'` variant to
`MantineListingCardPattern` + a List section in its Storybook story — STORYBOOK ONLY, no site wiring; owner directive
2026-07-15: the List-view horizontal card must exist in the SAME single-source pattern before ANY site wiring. Owner
chose one pattern + `layout` variant (not a separate component). Grid layout stays byte-identical (live grid card
untouched); list layout ports the existing legacy `ListingCard` horizontal branch design into the pattern. **Site
implementation of BOTH shapes is HELD as a separate later task until both Storybook stories are owner-approved.** Found+
fixed a real cascade-layer bug during the port: Mantine `Card` sets `display:block` unlayered, so Tailwind `flex flex-row`
never applied (image/info stacked vertically) — same class as Task 602's hover bug, fixed via a new unlayered `.listRow`
CSS-module rule. ✅ IMPLEMENTED, HELD for orchestrator review (2026-07-15) — see Last Session; session
`docs/sessions/2026-07-15-task606-listingcard-pattern-list-layout-variant.md`. Kickoff
`..._Task_606_ListingCardPatternListLayoutVariant.md`. Opened 2026-07-15.) (605 = OWNER P0 — rebuild `MantineListingCardPattern` into the
COMPLETE, data-driven listing card (single source of truth): photo counter/badges/overlay/features/footer/price/premium/
hover all owned by the pattern; favorite/image/copy-id passed as positioned nodes (hook-free split); the Storybook story
finally renders a production-complete card incl. the favorite heart + photo counter it currently lacks; `ListingCard`
collapses to a thin data-mapper; horizontal List view untouched. Root cause of the owner's "story ≠ site" + "story has no
favorite/counter": Task 602 built the pattern as a hollow shell with real content injected via slots the story never
fills. **SUPERSEDES Task 604** (real-page hover verify — absorbed here, resolved as environmental/Branch A with
real evidence, no hover code change); **keeps Task 603** (compact-heart fix, orthogonal). ✅ IMPLEMENTED, HELD for
orchestrator review (2026-07-15) — see Last Session; session `docs/sessions/2026-07-15-task605-listingcard-pattern-complete-single-source.md`.
Kickoff `..._Task_605_ListingCardPatternCompleteSingleSource.md`. Opened 2026-07-15.) (604 = ❌ SUPERSEDED by 605 —
real-page hover verify/fix folded into the full-card rebuild; kickoff `..._Task_604_ListingCardRealPageHoverVerifyFix.md`
retained for history. Was: VERIFY (and fix if broken) the real-page `ListingCard`
hover — elevation + photo zoom — on the ACTUAL Next.js homepage/`/listings` grid, not Storybook; owner reports the
story's hover doesn't appear on the live site. Standing gap: 602 proved hover ONLY in Storybook, 603 measured favorite
geometry only — the real page was never hover-verified. Kickoff branches on real-page reproduction: Branch A (fires on
real desktop pointer → environmental, likely Chrome device-toolbar coarse-pointer suppressing the intended
`(hover:hover)/(pointer:fine)` guard → STOP-AND-ASK, no code change) vs Branch B (genuinely dead on real page → minimal
canonical cascade/overflow fix). Kickoff `..._Task_604_ListingCardRealPageHoverVerifyFix.md`. Opened 2026-07-15 from the
603 review.) (603 = `FavoriteButton` icon-shape mobile full-width fix — ✅
IMPLEMENTED, HELD for orchestrator review (2026-07-15) — see Last Session for full detail; session
`docs/sessions/2026-07-15-task603-favoritebutton-mobile-fullwidth-fix.md`.) (602 = extend `MantineListingCardPattern` additively with
price/reduced-price variants + dom.ria hover, migrate `ListingCard`'s vertical branch onto it, keep the horizontal
List-view branch untouched; kickoff `..._Task_602_ListingCardMantineMigration.md`; session
`docs/sessions/2026-07-15-task602-listingcard-mantine-migration.md`.) (601 = deterministic, dev-noise-immune proof that the authenticated-header `useId` mismatch is caught; opened 2026-07-15 from the Task 600 native run. **Outcome: the primary settled-DOM id-parity approach (`scripts/check-header-id-parity.mjs`) was empirically proven a false positive (Mantine randomizes every Menu-target id post-mount, erasing the SSR value) — STOP-AND-ASK raised and resolved by the owner/orchestrator, who authorized a jsdom `renderToString`→`hydrateRoot` dual-phase harness asserting on React's `onRecoverableError` instead (`src/components/layout/__tests__/header-hydration-id-parity.test.tsx`), which genuinely discriminates the bug — 599/600 registry row flipped to ✅.** Kickoff `..._Task_601_HydrationIdParityDeterministicProof.md`; session `docs/sessions/2026-07-15-task601-header-hydration-id-parity-deterministic-proof.md`.) (600 = harden `check:hydration` — fail on non-200 / `pageerror` / Next error-overlay, add `--verify-error-page` self-test, and prove the gate catches a real `useId` hydration mismatch in a clean `.next` env; opened 2026-07-15 from the Task 599 review after the owner found the gate reports a false PASS on a corrupted-`.next` 500; kickoff `..._Task_600_HydrationGateHardenErrorPage.md`.) (599 = FIX authenticated-header hydration mismatch — `NotificationBell` `dynamic ssr:false` offsets Mantine `useId` on LocaleSwitcher+UserMenu for logged-in users; owner-confirmed SSR-safe fix 2026-07-15; kickoff `..._Task_599_HeaderAuthHydrationSSRBellFix.md`.) (598 = REMOVE the homepage stats block entirely — owner reversed the 597 decision 2026-07-14; delete the stats `<section>` + all 597 artifacts (MantineMetricCard primitive+story, `getSiteStats`+smoke, `get_agent_count` RPC SQL + grant gate, `stats_*` keys ×4, registry row, `check:get-agent-count-grants` script); SUPERSEDES 597; kickoff ready `tasks/Sprints/Sprint_44_kickoff_prompt_Task_598_RemoveHomepageStatsBlock.md`.) (597 = homepage stats → TailAdmin white metric cards (Mantine) — IMPLEMENTED in the working tree but ❌ SUPERSEDED by 598 before commit (owner no longer wants the tiles); kickoff `..._Task_597_HomeStatsMetricCardMantineMigration.md`, session log retained for history.) (596 = hook-level regression test — `useNotifications` `.select()` MUST include `template_id`+`template_params`, with a planted-violation red/green proof; closes the Task-595 coverage gap (595's test guards the renderer, not the hook that changed); orchestrator-opened 2026-07-14, kickoff ready.) (595 = `useNotifications` SELECT must include `template_id`+`template_params` so `NotificationItem`'s render-time localization actually runs — was silently rendering stored fallback for ALL template notifications; owner-caught 2026-07-14 — ✅ APPROVED (fix correct + complete), commit emitted, pending owner native gate + run; regression-coverage gap routed to Task 596.) (594 = remove the duplicated "Сповіщення" heading on the notification mobile bottom sheet — drop the `MantinePopover title` prop in `NotificationBellView.tsx` so the sheet is drag-handle-only and `NotificationCenter`'s own header is the single heading; owner-reported 2026-07-14 — ✅ APPROVED (orchestrator source review), commit emitted, pending owner native gate + run.) (593 = `NotificationCenter` mark-all button left-align label + own-row <390px — owner-flagged from the 592 render, orchestrator-opened 2026-07-14.) (592 = fix `NotificationBellView` detached badge — `<Indicator inline>` — follow-up to REJECTED 591, orchestrator-opened 2026-07-14.) (591 = header NotificationBell stack → Mantine + presentational split + canonical story — owner-directed 2026-07-13, kickoff ready.) (587 = `MobileNavDrawer` legacy Button → Mantine core + reusable Link/`transparent` variant + Logout left-indent fix — owner-directed 2026-07-13, kickoff ready.) (586 = `Header.tsx` desktop hamburger visibility fix — `md:hidden`→`hiddenFrom="md"`, owner-authorized Opus edit 2026-07-12, shares `Header.tsx` with 579.) (585 = `UserMenu.stories.tsx` `play` opens only the admin fixture on desktop — clean non-overlapping rendered proof, story-only, orchestrator-opened 2026-07-12 from the 578 review. 584 = global bottom-sheet header title↔divider vertical-spacing fix, single source `responsiveBottomSheet.tsx`, owner-reported 2026-07-11. 578 = `UserMenu` presentational primitive, desktop user dropdown extracted from `Header.tsx`. 582 = Header Mantine `useId` target-id hydration fix — P0, owner-captured `mantine-_R_…_-target` mismatch on the LocaleSwitcher Menu target; CLOSED as NOT-A-BUG — dev-only Turbopack HMR stale-cache artifact, gone after clean `next build` + fresh `next dev`, `check:hydration` GREEN on `/en` authed; no code change, no new gate. 583 = Favorites heart visible at ALL breakpoints — remove `visibleFrom="sm"` in `HeaderActions.tsx`, owner decision 2026-07-11.) (574 = Sprint 44 Header base-UI overlays → Mantine, fix SSR-id hydration. 575 = `HeaderActions` presentational primitive, Favorites + auth buttons → Mantine. 576 = `LocaleSwitcher` Mantine story + inert-prop cleanup. 577 = Header dedup — ONE canonical adaptive `LocaleSwitcher` at all breakpoints, deleted the redundant mobile combobox (re-scoped 2026-07-11, no longer a separate `MobileLocaleSwitcher` primitive). 581 = `LocaleSwitcher` remove leading Globe icon — owner request, follow-up out of 576's no-behavior-change scope; globe-only, chevron + spinner kept.) (566/567/568 = Sprint 43; 569 = geometry Check-4 clip-awareness; 570 = HeroSearch mobile tab-overflow hotfix; 571 = CountButton icon-only-collapse prop + HeroSearch filters-button adaptation, completes held 568; 572 = HeroSearch Search-button wraps to row 2 in the 640–767 band so Location regains width; 573 = persist Task 572's 640–767 proof as a durable gate — per-story in-band 700px viewport + HeroSearch row-structure assertion in `check-stories-rendered.mjs`.) **579–580 reserved by `Sprint_44_Header_Mantine_Primitives.md`** (MobileNavDrawer primitive, Header thin container).

**🆕 Task 573 (Sprint 43 — durable gate coverage for the HeroSearch 640–767 band) — IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** See Last Session for full detail. Kickoff: `tasks/Sprints/Sprint_43_kickoff_prompt_Task_573_HeroSearchInBandGateCoverage.md`. Session: `docs/sessions/2026-07-10-task573-herosearch-inband-gate-coverage.md`.

**🆕 Sprint 44 (Header / app-shell → Mantine + presentational-primitive decomposition — Epic MM Phase-2) — 574/575/576/577/581/578/585/579 done; 580 (`Header` thin-container audit) ✅ APPROVED 2026-07-12 — sprint fully closed (pending owner native gate + run).** Decomposition landed as **four** primitives (not five — Task 577 deleted the redundant `MobileLocaleSwitcher`), all stories under the canonical `src/stories/mantine/primitives/` → `Mantine/Primitives/*` location (owner-mandated 2026-07-11 after 575's round-1 mis-placement): **574** Header base-UI overlays → Mantine (SSR-id hydration fix). **575** `HeaderActions` (Favorites + guest login/register + bell slot) — round-2 corrected story location. **576** `LocaleSwitcher` own story + dropped inert `align`/`side`/`defaultOpen` + restored `AdminLocaleSwitcher` `MobileBottomSheet` open-state evidence via a real `play` click. **577** Header dedup — ONE canonical adaptive `LocaleSwitcher` at all breakpoints, deleted the redundant mobile combobox. **581** removed the leading Globe icon from `LocaleSwitcher`'s trigger (owner request, chevron+spinner kept). **578** extracted `UserMenu` primitive from `Header.tsx`'s inline desktop user-menu builder+trigger. **579** extracted `MobileNavDrawer` primitive from `Header.tsx`'s inline hamburger drawer body. **580** confirm-only audit: `Header.tsx` already thin, all 4 primitives 16/16 pass, zero regression — see Last Session. Kickoffs: `tasks/Sprints/Sprint_44_kickoff_prompt_Task_57{4,5,6,7,8,9}_*.md`, `..._Task_580_HeaderThinContainer.md`, `..._Task_581_LocaleSwitcherRemoveGlobeIcon.md`. Sessions: `docs/sessions/2026-07-11-task574-header-mantine-overlays.md`, `docs/sessions/2026-07-11-task575-header-actions-primitive.md`, `docs/sessions/2026-07-11-task576-localeswitcher-story-cleanup.md`, `docs/sessions/2026-07-11-task581-localeswitcher-remove-globe-icon.md`, `docs/sessions/2026-07-11-task577-header-single-localeswitcher-dedup.md`, `docs/sessions/2026-07-11-task578-usermenu-primitive.md`, `docs/sessions/2026-07-12-task579-mobilenavdrawer-primitive.md`, `docs/sessions/2026-07-12-task580-header-thin-container-audit.md`.

**🟡 Sprint 43 (FiltersPanel/HeroSearch → Mantine — Epic MM Phase-2 composite) — PLANNED 2026-07-09.** Last legacy-primitive holdouts in the search stack: 5 files on `@/components/ui/*`. Owner decisions (2026-07-09): 3-task split + `MantineDrawer` overlay + toggle-buttons stay Mantine `Button` (no SegmentedControl). **Task 566** (3 leaf sub-components `FilterRangeInputs`→§6e `TextInput`, `FilterMultiToggle`+`FilterRoomsRow`→§6a `Button` toggles) — ✅ APPROVED (2026-07-09, orchestrator diff + rendered-PNG review), commit emitted, pending owner native gate + run. **Task 567** (FiltersPanel shell: Sheet→MantineDrawer + toggles + Input + footer) — round-1 ❌ REJECTED; round-2 corrective ✅ APPROVED (orchestrator diff review 2026-07-09), commit emitted, pending owner run — see Last Session. **Task 569** (geometry Check-4 clip-awareness) — ✅ APPROVED (orchestrator native-run review 2026-07-10), commit emitted, pending owner run; see Last Session. Kickoff `tasks/Sprints/Sprint_43_kickoff_prompt_Task_569_GeometryCheck4ClipAwareness.md`, session `docs/sessions/2026-07-10-task569-geometry-check4-clip-awareness.md`. **568** (HeroSearch → Mantine, last legacy-primitive holdout in the search stack) — ✅ IMPLEMENTED but NOT CLOSED; owner rejected closure → **completed by Task 571** (see below). **Task 571 — ✅ APPROVED (orchestrator diff + independent gate re-run, 2026-07-10), commit emitted, pending owner run — CLOSES held Task 568.** HeroSearch filters button was a raw `Button` + hand-rolled absolute corner badge (non-native vs the CountButton story) AND kept its text <860px, squeezing the location combobox. Fixed: (1) new reusable `iconOnlyBelow?: number` prop on `MantineCountButton` (label collapses to icon+count below the px threshold, component-scoped `useMediaQuery` — NO breakpoint fork, grep-confirmed; owner chose "prop in primitive" + own story section); (2) HeroSearch adopts the canonical `MantineCountButton` with `iconOnlyBelow={860}`. See Last Session for full verification detail. Kickoff: `tasks/Sprints/Sprint_43_kickoff_prompt_Task_571_CountButtonIconCollapse_HeroSearchAdaptation.md`. Plan: `tasks/Sprints/Sprint_43_FiltersPanel_HeroSearch_Mantine.md`. Kickoffs: `tasks/Sprints/Sprint_43_kickoff_prompt_Task_566_FilterLeafComponentsMantine.md`, `tasks/Sprints/Sprint_43_kickoff_prompt_Task_567_FiltersPanelShellMantine.md`, `tasks/Sprints/Sprint_43_kickoff_prompt_Task_568_HeroSearchMantine.md`. Sessions: `docs/sessions/2026-07-09-task566-filter-leaf-components-mantine.md`, `docs/sessions/2026-07-09-task567-filterspanel-shell-mantine.md`, `docs/sessions/2026-07-10-task568-herosearch-mantine.md`.

**🟢 Task 565 (Sprint 42 / ICU sweep tail — legacy `DatePicker.tsx` runtime-ICU fix) — ✅ APPROVED (2026-07-09), commit emitted, pending owner native gate + run — see Last Session.** Kickoff: `tasks/Sprints/Sprint_42_kickoff_prompt_Task_565_DatePickerRuntimeICUFix.md`. Session: `docs/sessions/2026-07-09-task565-datepicker-runtime-icu-fix.md`.

**🟡 Sprint 42 (Booking-style RANGE DatePicker) — 558 ❌ REJECTED → 561 fixes ✅ APPROVED + landed on HEAD → 559 ✅ APPROVED (round-2 re-review), commit emitted, pending owner run.** Plan: `tasks/Sprints/Sprint_42_Booking_Range_DatePicker.md`. **Task 558** built `RangeDatePicker` + the `MantinePopover` `fullWidthTrigger`/`close()` props, rejected on 6 mobile-render defects. **Task 561** fixed all 6 — approved + committed. **Task 559** (see Last Session) wired `RangeDatePicker` into `FiltersPanel`+`ListingsFilters`, replacing the legacy two-field pickers; `disablePastDates` was NOT passed by either surface (both are listings-search contexts — past posting dates stay selectable, matching the preserved `maxDate={today}` behavior; no rental surface exists yet). **Task 560** admin suspension-as-range still DEFERRED (DB/RLS/server action). 557 kickoff retained as §6t/MantinePopover-props reference. **559 ✅ APPROVED round-2; two follow-ups opened from the 559 review: Task 562 (`RangeDatePicker` sq calendar-body localization — root cause = Chromium's ICU has no `sq` locale data; fixed via static `common.calendar_*` i18n data, `it` was never actually broken) — ✅ APPROVED (orchestrator diff review 2026-07-08), commit emitted, pending owner run; Task 563 (`ListingCard`/`PriceBlock` hydration mismatch on `/sq|it/listings`) — ✅ APPROVED (2026-07-09), commit emitted, pending owner native gate + run. Task 564 (runtime-ICU formatter sweep) — ✅ APPROVED (2026-07-09), commit emitted, pending owner native gate + run. **Task 565 (legacy `DatePicker.tsx` runtime-ICU fix) — ✅ APPROVED (2026-07-09), commit emitted, pending owner native gate + run — see Last Session.** Task 560 still deferred.

**(archived) Task 557 (single-date DatePicker migration) — was ✅ KICKOFF READY (unblocked 2026-07-06).** Owner chose the Mantine-core rebuild (keep hand-rolled calendar + `date-fns`; legacy `ui/popover`→`MantinePopover`, `ui/button`→Mantine `Button`, raw day `<button>`→`UnstyledButton`; trigger→§6d/§6e; no new dependency). 5 consumer call sites (`FiltersPanel` ×2, `ListingsFilters` ×2, `AdminUserProfile` ×1), public API stable. **BLOCKER CLEARED:** the earlier "zip has no calendar → needs clause-16a live-capture" premise was WRONG — the zip's dashboard `index.html` renders the full **flatpickr** calendar (same as TailAdmin's Forms page) and `css/style.css` carries its stylesheet. Orchestrator extracted the authoritative zip-cited `tailadmin-style-reference.md §6t` calendar-body row (clause 16a N/A). **All 3 STOP-AND-ASKs owner-resolved 2026-07-06:** (1) trigger → §6d/§6e bordered TailAdmin chrome; (2) **day cells = 39px on mobile bottom sheet too — explicit owner override of the clause-11 ≥44px gate for day cells (documented exemption in §6t)**; (3) today marker → §6t gray-400 border. Kickoff fully updated + ready to hand to Sonnet. Kickoff: `tasks/Sprints/Sprint_41_kickoff_prompt_Task_557_DatePickerMantineMigration.md`.

**🟢 Task 556 (Sprint 41 / Epic MM Phase-2 · PhoneField → Mantine/TailAdmin migration) — ✅ APPROVED (orchestrator review 2026-07-06) + ✅ COMMITTED `7e8d25c59`.** See Last Session above for full detail (both STOP-AND-ASKs owner-resolved and implemented, 4 admin consumer edits, new `dropdownMinWidth` primitive prop, persisted story, registry row, §18.9 human-visual proof incl. live AuthSheet no-clip check). Kickoff: `tasks/Sprints/Sprint_41_kickoff_prompt_Task_556_PhoneFieldMantineMigration.md`. Session: `docs/sessions/2026-07-06-task556-phonefield-mantine-migration.md`. **Next composite: DatePicker or FiltersPanel/HeroSearch themselves.**

**🟢 Sprint 41 (MM Phase-2 Combobox family + PhoneField) — 551 ✅ APPROVED (native gate green) · 552 ✅ diff-pass · 553 ✅ diff-pass · 554 ❌ REJECTED (owner-caught D1/D2) · 555 ✅ APPROVED (D1/D2 fixed, commit emitted) · 556 ✅ APPROVED (commit emitted, pending owner run).** Full detail for 551–555 in [`backlog-archive.md`](backlog-archive.md) + Last Session history; 556 in Last Session above. **Phase-2 Combobox family (PropertyType/Year/Location) + PhoneField now fully migrated — next: DatePicker or FiltersPanel/HeroSearch themselves.** Kickoffs: `tasks/Sprints/Sprint_41_kickoff_prompt_Task_55{1,2,3,4,5,6}_*.md`.

**🟢 Sprint 40 tail (Tasks 527–529, 538, 544–550) — all closed/superseded, commits pending owner run.** Full detail moved to [`backlog-archive.md`](backlog-archive.md) (2026-07-06 tidy). Closes the Phase-1 primitive slice.

**Reserved/deferred:** 310 (Epic HH P4), 311 (Epic HH P5 — partially superseded), 313 (Epic HH P6 Verified Agents — blocked on owner DB-schema approval), 453 (Epic KK.2 — remaining admin managers freshness). Deferred (no task #): **I.3** listing-status helper API migration `(status) → (listing)` — see `docs/domain-rules.md` → "Future ListingStateMachine evolution trigger". Retired numbers (never reuse): 465 (uk→ua migration, cancelled), 466/467/469–481 (legacy-primitive layout work superseded by the Mantine migration).

**Owner decisions still needed (Epic HH):** Verified Agents DB schema sign-off (Task 313) + verified-badge public visibility.

> **🟡 Known console NOISE (not bugs — do not re-triage as P0):** `[PRED] … preloaded`, `[LCP] … route`, `[Vercel Speed Insights] debug`, "speculation rule set … will be ignored", and Turbopack-dev-only CSS-chunk/`*.woff2` preload warnings are dev/debug artifacts (vanish in prod build). OpenTelemetry `import-in-the-middle` warnings resolved by Task 450 (`322c5d599`). Cloudinary LCP "preloaded but not used" resolved by Task 437. **A stale Turbopack `next dev` HMR cache can also emit a one-off React `useId` attribute-mismatch hydration error (`mantine-_R_…-target` / `_R_…`, Task 582) that does NOT survive a clean `next build` + fresh dev restart and does NOT reproduce in prod — before triaging any `useId`/`_R_` hydration id mismatch as a code bug, re-verify with `check:hydration` against a freshly restarted server.**

> **Commit emission policy:** the orchestrator emits explicit-path `git add` / `git commit` per task at review time (never pre-staged batches, never `-A`/`-u`/wildcards); the owner runs them in PowerShell. Each commit is reconstructable from the session log's "Files Changed" table.

> **Acknowledged advisor exceptions** (intentional, no task): `pg_net in public` (deferred); `email_change_tokens` RLS-enabled-no-policy — see `docs/rls-rules.md` → "Acknowledged Advisor Exceptions".

## Frozen / deferred tasks — relevance after Design System (reviewed 2026-06-05)

The global DS work (Task 340 contract + Sprint 32/33, Tasks 372–392) is the canonical mobile/responsive + Storybook layer. Re-assessment of items frozen behind it:

| Item | Verdict | Notes |
|------|---------|-------|
| **Sprint 28** — admin mobile (306/307/306-Fix ✅; 308/309 BLOCKED) | **RE-SCOPE under DS (owner 2026-06-05)** | 308/309 must be re-written to consume `docs/design-system.md` + canonical primitives before any admin migration resumes. |
| **Epic HH Phase 4 (310)** — 12 content/settings admin routes | **Still relevant** | Never migrated; must consume the DS contract. |
| **Epic HH Phase 5 (311)** — admin modal standardisation | **Partially superseded** | Residual = width tiers + destructive-action footer only. |
| **Epic HH Phase 6 (313)** — Verified Agents workflow | **Still relevant** | Blocked on owner DB-schema approval; DS-unaffected. |
| **Epic II (316–323)** — i18n hardening | **Implemented; owner commits pending** | Runtime notif/email/toast/dynamic-key fixes shipped + reviewed. |
| **I.3** listing-status helper API migration | **Deferred (valid)** | Trigger-based; DS-unaffected. |

> **🆕 Standing principle (owner 2026-06-05):** every still-open task consumes the global Design System (`docs/design-system.md`, Task 340) wherever UI/responsive/overlay surfaces are touched — and, since the Mantine freeze (Task 482, 2026-06-24), new UI work uses Mantine per `docs/mantine-responsive-design-system.md`. `docs/rule-index.md` mandates the correct first pre-read per task type.

## Active product backlog — open Epics. Closed/implemented epics → [`backlog-archive.md`](backlog-archive.md)

| Epic | Status | Plan |
|---|---|---|
| **HH — Admin UX System** | OPEN — 310 (P4), 311 (P5 partial), 313 (P6, blocked on owner DB sign-off); 308/309 re-scope vs canonical primitives | [`Epic_HH_…`](../tasks/Epics/Epic_HH_Admin_UX_System.md) |
| **BB — Listing Inquiries: Report & Message** | ⏸️ ON HOLD — Task 463 (full admin report management) held until the Mantine primitive library (Epic MM) is complete; must be rebuilt on finished primitives. Earlier BB tasks (242/243/430/435/458–462) done | [`Epic_BB_…`](../tasks/Epics/Epic_BB_Listing_Inquiries_Report_and_Message.md) |
| **II — Global i18n Hardening** | Implemented + reviewed (316–323); remaining = owner-run commits | [`Epic_II_…`](../tasks/Epics/Epic_II_Global_i18n_Hardening.md) |
| **KK — Admin Data Freshness** | 452 (KK.1) ✅ committed `1994c67cc`; 453 (KK.2) reserved for remaining managers | [`Epic_KK_…`](../tasks/Epics/Epic_KK_Admin_Data_Freshness.md) |
| **MM — Mantine/TailAdmin Restyle** | IN PROGRESS — Sprint 37 Batch A ✅, Sprint 38 Batch B ✅, Sprint 39 overlay Batch C ✅ (owner commits pending); next primitives resume at 523 | [`Epic_MM_…`](../tasks/Epics/Epic_MM_Mantine_UI_Migration.md) · tracker `docs/mantine-tailadmin-migration-tracker.md` |
| **Y · DD · JJ · LV** | CLOSED / implemented — detail in [`backlog-archive.md`](backlog-archive.md) | — |

> **Standing governance (codified in `/docs`):** Notes 18–23 (`ai-behavior.md`) + `agent-contract.md` (P0, clauses 1–15) + `rule-index.md` (task-type pre-reads) + Positive+Negative flow rule (`orchestrator-role.md`). Non-optional acceptance gates on every task.

## Archive

Completed tasks, sprints, and epics live in **[`docs/backlog-archive.md`](backlog-archive.md)**.
