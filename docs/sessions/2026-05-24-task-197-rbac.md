# Task 197 — R.3 — RBAC / Role Permission Management

**Date:** 2026-05-24
**Branch:** main
**Status:** ✅ Complete

---

## §17 UI Pre-flight Checklist

**1. Non-canonical dropdowns:** No new `<select>` or shadcn `Select` in UI. ✅
**2. Ad-hoc control heights:** No new Buttons — only a Switch toggle and Badge. ✅
**3. Z-index on the scale:** No new z-index values introduced. ✅
**4. Overflow-risk rows:** Permission row uses `grid grid-cols-[1fr_auto] items-center`. No overflow risk. ✅
**5. Same-row height:** Switch + ShieldCheck icon in same row — both 16px/auto. ✅
**6. All 7 breakpoints:** `max-w-2xl` container, no layout-specific breakpoints — renders correctly at 320px–2560px. ✅
**7. Touch targets:** Switch component handles touch natively. ✅
**8. 4 locales:** All strings added to sq/en/uk/it. ✅

---

## Schema (owner must run BEFORE deploying)

```sql
-- Table: role_permissions
CREATE TABLE IF NOT EXISTS role_permissions (
  role            text        NOT NULL,
  permission_key  text        NOT NULL,
  allowed         boolean     NOT NULL DEFAULT false,
  updated_at      timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (role, permission_key),
  CONSTRAINT role_permissions_role_check CHECK (role = 'moderator')
);

-- RLS
ALTER TABLE role_permissions ENABLE ROW LEVEL SECURITY;

-- Admins: full read + write
CREATE POLICY "role_permissions_admin_full" ON role_permissions
  USING (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin')
  )
  WITH CHECK (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin')
  );

-- Moderators: read-only (so assertPermission() can load their own permissions)
CREATE POLICY "role_permissions_moderator_read" ON role_permissions
  FOR SELECT
  USING (
    EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'moderator')
  );

-- Seed: preserve current behaviour (moderators could do everything except delete users / change roles)
INSERT INTO role_permissions (role, permission_key, allowed) VALUES
  ('moderator', 'listings.delete',     true),
  ('moderator', 'listings.set_premium',true),
  ('moderator', 'users.create',        true),
  ('moderator', 'users.change_role',   false),
  ('moderator', 'users.soft_delete',   false),
  ('moderator', 'users.hard_delete',   false),
  ('moderator', 'locations.manage',    true),
  ('moderator', 'settings.manage',     true),
  ('moderator', 'legal.manage',        true),
  ('moderator', 'reports.manage',      true)
ON CONFLICT (role, permission_key) DO NOTHING;
```

---

## Permission Keys

| Key | Default | Description |
|---|---|---|
| `listings.delete` | true | Delete any listing |
| `listings.set_premium` | true | Set/unset premium on listings |
| `users.create` | true | Create new admin/moderator users |
| `users.change_role` | false | Change a user's role |
| `users.soft_delete` | false | Soft-delete a user (set deleted_at) |
| `users.hard_delete` | false | Permanently remove a user from system + auth |
| `locations.manage` | true | Create/update/delete locations |
| `settings.manage` | true | Save site settings |
| `legal.manage` | true | Create/update/delete legal pages |
| `reports.manage` | true | Review/resolve/dismiss listing reports |

Admin always has full access regardless of DB rows. Only `moderator` rows are stored.

---

## Files Created

- `src/lib/auth/permissionKeys.ts` — `PERMISSION_KEYS` constant array + `PermissionKey` type
- `src/lib/auth/permissions.ts` — `roleHasPermission()`, `hasPermission()`, `assertPermission()`
- `src/modules/admin/actions/permissions.ts` — `getModeratorPermissions()`, `setModeratorPermission()`
- `src/components/admin/AdminPermissionsManager.tsx` — permission matrix UI (client component)
- `src/app/admin/permissions/page.tsx` — admin page
- `docs/sessions/2026-05-24-task-197-rbac.md` — this file

## Files Modified

- `src/types/database.ts` — added `RolePermission` interface
- `src/scripts/schema-drift-check.sql` — added `role_permissions` (4 cols) to both CTEs + table list
- `src/modules/admin/actions/index.ts` — updated guards:
  - `setListingPremium` / `toggleListingPremium` → `assertPermission('listings.set_premium')`
  - `deleteListing` → `assertPermission('listings.delete')`
  - `createLocation` / `updateLocation` / `toggleLocationFeatured` / `deleteLocation` → `assertPermission('locations.manage')`
  - `saveSettings` → `assertPermission('settings.manage')`
  - `createPage` / `updatePage` / `deletePage` → `assertPermission('legal.manage')`
  - `createAdminUser` → `assertPermission('users.create')`
  - `softDeleteUser` → `hasPermission('users.soft_delete')`
  - `hardDeleteUser` → `hasPermission('users.hard_delete')`
  - role-change block in `updateUserProfileFull` → `roleHasPermission(role, 'users.change_role')`
- `src/modules/listings/actions/reportListing.ts` — updated guard → `hasPermission('reports.manage')`
- `src/components/admin/AdminSidebar.tsx` — added Permissions nav item (group_system)
- `messages/en.json` — `admin.sidebar.item_permissions` + `admin.permissions.*`
- `messages/sq.json` — same
- `messages/uk.json` — same
- `messages/it.json` — same

---

## TypeScript check

```
npx tsc --noEmit → 0 errors
```
