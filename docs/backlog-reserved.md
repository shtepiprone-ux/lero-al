# Backlog — Reserved Numbers (full text)

> Split out of `docs/backlog.md` on 2026-08-11, for the same reason `backlog-archive.md` was: the backlog holds
> **active state**, and a reserved number's full justification is task-design input, not state. Every number here
> is still live and still listed in the backlog's registry — that registry names it and points here; this file
> carries the measurement behind it.
>
> **These rows are the durable record.** Several explicitly say so — 734's per-file census exists because its
> per-line table is local-only (D6), 736 carries a measurement (`84 of 2257 sq strings`) taken once and not
> repeatable from the repo alone. Do not compress them away; move a row into its kickoff when one is written,
> then delete it here.

| # | State | What |
|---|---|---|
| 734 | reserved — filed by 717 | **25 genuine N1 violations inside `src/design-system/mantine`, reserved not remediated (R7).** `typography.ts` (3, `SECTION_HEADING_FZ`) · `input-chrome.css` (7, padding matching `--space-2-5`/`--space-4` exactly) · `MantineListingCardPattern.module.css` (12: 4× `transition` matching `--duration-slow` exactly, 8× `.card:hover` box-shadow duplicating the Mantine-generated `--mantine-shadow-lg` var the sibling `.premium:hover` rule already references) · `MantineDialogDrawerPattern.tsx` + `responsiveBottomSheet.tsx` (1 each, `paddingBottom` matching `--space-2`) · `MantineResponsiveActionFooter.tsx` (1, `zIndex: 100` bypassing the semantically-named `--z-sticky`). Per-file counts above are the durable record; the per-line table is **local-only** (`.screenshots/task717-evidence/I3-classification.md`, D6) so **734 re-derives the census rather than depending on it** — the 711/728 lesson. **Review escalation (2026-08-08):** `MantineResponsiveActionFooter`'s `zIndex: 100` is not a plain tokenization item — it is `--z-toast` on a `position:sticky` element, above `--z-overlay`/`--z-modal`/`--z-popover`, while the repo's own sticky convention is `z-30` (`AdminMobileHeader.tsx:41`, `AdminPageShell.tsx:30`). Latent only because no production consumer passes `sticky`. Correcting it **moves the element down 70 layers**, so that item alone needs a rendered witness and a Q2/Q3 arm, and 734 must state whether the answer is `--z-sticky` or a new token. |
| 735 | reserved — filed by 678 | **Enrol `System/FeaturedListings` via the Task 678 per-story mechanism + a `wide-1440` `MANTINE_STORY_EXTRA_VIEWPORTS` entry**, closing the 4th of 5 `SECTION_HEADING_FZ` use sites (`FeaturedListingsView.tsx` renders the heading inside its own story tree, unlike its `System/LatestListings` sibling — see `storybook-governance.md` §14.9.30, that one is route-level and stays out of scope, owned by 667). Reserved rather than spent in 678 because the kickoff's objective named only `AdminUsersTable` + `HowItWorksSteps` as "the two things" the mechanism unblocks; `System/*` was OQ1, and reserving is an explicitly acceptable disposition. |
| 736 | reserved — **needs a sprint** | **Classify the AdminUsersTable locale-leak findings; do not translate them.** 678 enrolled the story and surfaced 13 leaks — 11 fixture proper nouns, plus sq `role_admin`/`role_moderator`, which 678 escalated as "untranslated". Measured 2026-08-09: **84 of 2257 sq strings (3.7%) are identical to en** (`Premium`, `WhatsApp`, `Email`, `Studio`, `Duplex`, `Panel`…), and `check-locale-leak.mjs:96-101` is Task 624's loanword allowlist whose comment records that it already verified "sq: admin/brand/panel … identical to en in **admin.*/role keys**". Follow 624's own discipline: verify against `messages/*.json`, then allowlist globally **or** via `PER_STORY_TOKENS` (which is where 624 put Studio/Penthouse precisely so a global entry could not mask a real mistranslation elsewhere). Translating them would "fix" strings that are already correct. |
| 738 | reserved, P3 — **needs a sprint** | **The horizontal-scroll blind spot — 729's residual 40.** After 729's vertical band scan, 40 candidates remain unreachable: lightbox thumbnails inside a horizontally-scrolling container. Structurally different from the vertical case (729 scrolls the document; these need their own container scrolled), so 729 named it rather than absorbing it. Same family as 729 and 733: a gate that reports a number which reads as coverage. |
| 746 | reserved, **P1** — **needs a sprint**, filed by 691's review 2026-08-12 | **No control in this repository can observe a hover-state or real-consumer regression on the listing card.** Both of 691's P0 regressions passed every gate for this reason, so the number exists to close the detector gap, not to re-fix 691 — **it is not a blocker for 691's re-review.** Measured during that review: the `--mantine-only` matrix captures **rest state only**, so a `:hover` rule is structurally unobservable; `ListingCardPattern.stories.tsx:22-24` renders `DemoImage`, a plain Mantine `Image` explicitly documented as a stand-in for the real `AppImage`, so `group-hover:*` utilities never enter the captured DOM at all; `check-homepage-grid.mjs` asserts structure and geometry, not interaction state; and 691's own computed-style capture pinned one story, `locale:en` and `1024x4000` (`capture-computed-styles.mjs:92,:96`). **Order matters: define the detector and its false-positive boundary FIRST** — hover capture is inherently flaky (transition timing, `prefers-reduced-motion`, pointer emulation), and a control that cannot state which diffs it must ignore will be disabled the first week. Only then build the failing plants, **three distinct ones**, each of which must genuinely fail before the fix and pass after: ① remove `group` from a Card root → the image's `scale`/`transform` loss is caught; ② strip the `(hover: hover)` guard → the coarse-pointer arm is caught (Task 605 already proved `hasTouch:true, isMobile:true` works on this surface); ③ the probe runs against the **real** `ListingCard` + `AppImage` composition, not a demo stand-in — `mantine-primitives-listingcard--default` already imports the real component and is the obvious host. **Do not fold in 747** — that one is about prose diverging from a ledger and shares no mechanism with this. |
| 747 | reserved, **P1** — **needs a sprint**, filed by 691's review 2026-08-12 | **Hand-written state and counts in markdown can contradict the machine-checked ledger, and nothing detects it.** Not a UI or hover problem — the subject is the projection layer: `review.coverage` in a v4 ledger is validator-derived and cannot lie, but `docs/backlog.md`, `tasks/Sprints/Sprint_NN_*.md` and session logs restate the same facts in prose that no gate reads. **Two measured instances, both from 691, one in each direction.** ⓐ The backlog registry row, the backlog Last Session line and the Sprint 46 Tasks row all said `2 P0` while the ledger's `openP0` was **4**; `check:review-ledger` was green throughout because the ledger itself was correct. The owner caught it by reading both, which is exactly the control this number must replace. ⓑ Task 691's session log §13 asserted a backlog edit (`landed count moved to 5 of 9`) that the diff never contained — recorded as finding `F-M`, and the same class as the four documented state-drift incidents in the backlog's standing notes, one of which cost a duplicate implementation round trip. **Define the machine source-of-truth and a stable assertion format before writing any checker.** A grep for `N P0` is precisely the brittle answer to avoid: counts appear inside historical prose, retracted claims and archive rows that must *not* be rewritten, so the design question is how a live state claim is marked as machine-checkable at all — a fenced block, a front-matter field, a generated region — not how to pattern-match English. **Failing plant:** change only the markdown P0-count or task state, leave the ledger byte-identical, and prove the new control rejects it; then restore and prove it passes. **Do not fold in 746.** |
| 730 | reserved — **Sprint 55**, folds 731 | **The chip-row toggles announce no selected state.** 726 restored `role="group"` + a name on `FilterMultiToggle`/`FilterRoomsRow`, but neither sets `aria-pressed` — selection rides only on the Mantine `variant` (`filled` vs `default`), so AT hears "Condition, group" and then N plain buttons with no on/off. The two consumers 726's own §12/A2 cites as the correct precedent — `FavoritesTypeFilter.tsx:31` and `ListingsTab.tsx:171` — both **do** set `aria-pressed`; 726 shipped half the pattern. 14 live sites (7 `ListingsFilters` + 7 `FiltersPanel`). The same task must soften `storybook-governance.md` §14.9.28's "announce as named groups", which overstates what landed. |
