# Project Backlog

> ⚠️ **HARD LIMIT: this file holds ACTIVE state only (~80 lines).** Historical ledger → [`docs/backlog-archive.md`](backlog-archive.md); full per-task detail → `docs/sessions/`.
> "Last Session" = 2–4 lines. When a task is reviewed/closed, move it to ONE row at the TOP of the archive ledger. See `docs/ai-behavior.md` → "Backlog & Session Log Rules".
> Reconciled against git history 2026-07-17 (`main` == `origin/main`): everything committed is pushed. The former per-task "pending owner run" narrative was stale (its detail already lives in the archive) and has been removed.

## Last Session (2026-07-17)

- **Task 620** — `theme.ts` badge shade-index comments corrected to index 7 (comment-only, zero pixel change; mechanism verified against Mantine `get-primary-shade.mjs`/`get-css-color-variables.mjs`). ✅ APPROVED (orchestrator review) + committed + pushed. Open P3 follow-ups: light-mode qualifier on the new comments, `purple` index-0 comment still imprecise, stale `sale` prose. Session: `docs/sessions/2026-07-17-task620-badge-shade-comment-accuracy.md`.
- **Task 619** — dedicated `sale` `#dd0939` badge color (index 7 per `primaryShade:7`, card + detail surfaces). ✅ APPROVED + committed + pushed.
- **Tasks 614–618** — Sprint 44 listing card/detail Mantine tail (`ListingDetailPattern` rebuild, premium uniform gold border, Badge→Mantine `filled`, CTA flex-direction fix, check-stories Check-9 proof). ✅ all committed + pushed.
- **Governance** — added the STATUS/REPORT MISMATCH reconciliation gate (`CLAUDE.md` + `agent-contract.md` + `orchestrator-role.md` + `orchestrator-procedures.md`).

## Open — needs action

**Not yet executed / re-scope before kicking off:**
- **Task 625** — lands Q0R + 624 as one commit: `governance-pr.yml` `locale-leak` job step now `continue-on-error: true` (migration-window warn-only policy, owner directive 2026-07-19 — script exit code / `check:locale-leak:mantine-only` definition unchanged, `rendered-proof`/`check:story-coverage` stay blocking); `scripts/mantine-migration-scope.json` completed to 6/6 (`FooterView.tsx` added — its story landed in `7bc4550b9` after Q0R was written); Q0R's shared-module/`--mantine-only`/coverage-rewrite work and 624's allowlist/translation/story fixes carried unchanged. R5 (moving `Studio`/`Penthouse`/`Max` from global to per-story allowlist) deferred, then landed by Task 626. ✅ IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW. Session: `docs/sessions/2026-07-19-task625-q0r-624-warnonly-landing.md`.
- **Task 613** — removed the DEAD `--z-*` Tailwind z-index token scale from `globals.css` (0 consumers grep-confirmed; `ui-rules.md` §16/§12 needed no edit, already correct). ✅ IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW. Session: `docs/sessions/2026-07-17-task613-dead-zindex-token-scale-cleanup.md`.
- **Task 621** — Homepage Agent-CTA migrated to canonical Mantine `Button` (new island `AgentCtaButton.tsx`). Owner visual QA found + fixed 3 real defects beyond the kickoff's suggestion (off-menu `size="lg"`, asymmetric icon-side padding, a theme-wide `height:'auto'`-breaks-`inner`-centering bug fixed locally — flagged as a possible sitewide Button follow-up). `screenshots:responsive` found to be Storybook-only, not app-route-capable — kickoff defect, ad-hoc Playwright capture used instead. ✅ IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW. Session: `docs/sessions/2026-07-17-task621-homepage-agent-cta-mantine-button-migration.md`.
- **Task 626** — Task 625 R5 follow-up: moved `Studio`/`Penthouse`/`Max` out of the global `LEAK_ALLOWLIST` into `PER_STORY_TOKENS`, scoped to `mantine-primitives-filterspanelshell` (`Gas`+`Studio`+`Penthouse`+`Max`) and `mantine-primitives-filtercontrols` (`Max`) — the only two canonical Mantine stories that empirically leaked them once unmasked. 0 leaks/exit 0 (full `--mantine-only`, 3 viewports) before and after; planted-violation round-trip on a raw `Penthouse` in `Badge.stories.tsx` caught + restored. No story-bug found. ✅ APPROVED WITH NOTES (orchestrator review 2026-07-19; owner-native full `--mantine-only` run re-confirmed 0 leaks/exit 0 against a fresh build) — pending owner commit. P3 follow-ups (non-blocking): (1) per-story allowlist is locale-blind (`isPerStoryAllowlisted` = `startsWith(prefix) && tokens.includes`), so inside the two scoped stories a real **it**-locale raw `Studio`/`Penthouse` or **sq**-locale raw `Max` mistranslation is still masked — inherent mechanism limit (same as `Gas`), consider a locale-scoped per-story allowlist; (2) housekeeping — a stale `storybook-static` planted-story artifact (leftover Task 625 `PlantedLeakViolationTask625`) surfaced during review; add rebuild hygiene / don't ship planted stories. Session: `docs/sessions/2026-07-19-task626-locale-leak-perstory-studio-penthouse-max.md`.
- **Task 627** — regenerated authoritative `npm run lint` (47 problems: 17 err/30 warn, confirmed) and wrote a per-category inventory + remediation plan in `docs/sb10-lint-debt-inventory.md` (8 categories: SB10 renderer-import ×13, empty-interface ×1, `@ts-ignore`→`@ts-expect-error` ×1, unescaped-entity ×1, `ReportStatus` false-positive ×1 [needs owner config call], `no-explicit-any` ×25 [accepted `warn`-level debt], unused-vars ×4, unused-disable-directive ×1). Found 2 deltas vs. the Task 625 log's prose summary (RangeDatePicker already fixed for Cat.1; 2 categories were previously unmentioned) — flagged, not silently reconciled. No source touched. ✅ IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW. Session: `docs/sessions/2026-07-19-task627-sb10-lint-debt-triage.md`.
- **Task 629** — migrated `HeaderView.tsx` chrome to Mantine `Box component="header"`/`Group`/`Anchor`/`Text` (all `unstyled`); behavior sub-primitives untouched. **Mid-task defect found + fixed:** `@mantine/core/styles.css` ships with no `@layer` wrapper (unlayered), so its own CSS was silently winning over the verbatim Tailwind classNames — contradicts `docs/mantine-responsive-design-system.md §4`'s "Tailwind wins" claim (recommend a Q0 doc-correction follow-up). Fixed via `unstyled` on every `Group`/`Anchor`/`Text`; re-verified via computed-style diagnostics + pixel-identical BEFORE/AFTER captures at 320/375/390/768/1024/1440 × sq/uk. Hydration id-parity 3/3 pass (baseline + post-fix); `screenshots:assert --mantine-only` 16/16 `HeaderView` pass (968 total/941 pass/0 fail/27 pre-existing unrelated ambiguous). ✅ IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW. Session: `docs/sessions/2026-07-19-task629-headerview-chrome-mantine-migration.md`.
- **Task 631** — fixed `System/FeaturedListings` story to render `ViewAllLink` in its header (mirrors real component), per Task 630's kickoff-gap follow-up. **Mid-session, owner-authorized scope widening:** also fixed a real live bug in `ViewAllLink.tsx` (button-root `display:block` losing internal flex-centering with `component={Link}`, same class of bug `AgentCtaButton.tsx`/Task 621 already patched locally) — reused that exact precedent (`styles.root` inline-flex/center), verified on both consumers (Latest `page.tsx`, Featured `FeaturedListings.tsx`) via live-app DOM measurement before/after. `ViewAllLink.tsx` was explicitly out-of-scope in the 631 kickoff; touched only after explicit in-chat owner authorization — flagged for orchestrator attention. Rendered proof: full (non-fast) `screenshots:assert`, 24/24 `system-featuredlistings-*` cells PASS incl. mandatory uk@320. Follow-up recommended (not done): consolidate the duplicated `styles.root` patch into `theme.ts` sitewide. **The `ViewAllLink.tsx` centering fix landed in this worktree is now re-owned by Task 632** (below) — Task 631 itself is to be committed story-only (`FeaturedListings.stories.tsx`). Session: `docs/sessions/2026-07-19-task631-featuredlistings-story-viewall-proofpath-fix.md`. ✅ APPROVED — story-only (orchestrator review 2026-07-20). R5/AC4 rendered proof = owner visual confirmation (sq/en/uk/it@320); machine `screenshots:assert` matrix **waived by owner** (legacy `System/*` stories slated for Mantine migration). `ViewAllLink.tsx` re-owned by Task 632; committed story-only (`FeaturedListings.stories.tsx` + session log). Pending owner commit.
- **Task 632** — added `styles.label: { textAlign: 'right' }` to `src/components/shared/ViewAllLink.tsx` so wrapped labels (`uk`/`sq`@320) right-align instead of centering; preserves the Task 631 `styles.root` vertical-centering fix. This task now owns the entire `ViewAllLink.tsx` diff vs. HEAD (centering + alignment); no consumer/`theme.ts`/`AgentCtaButton.tsx`/story edit. typecheck/check:stories/check:i18n/check:mojibake all green. **Owner directed skipping `screenshots:assert` mid-session** ("я візуально підтверджую, що тепер кнопка виглядає так як треба") — rendered Q2 evidence is owner real-time visual confirmation, not a machine-captured matrix; flagged for orchestrator. Session: `docs/sessions/2026-07-20-task632-viewalllink-wrapped-label-right-align.md`. ✅ APPROVED WITH NOTES (orchestrator review 2026-07-20). Owner visual confirmation @320 (wrapped label right-aligns, vertical centering intact); machine matrix **waived by owner**. typecheck (owner-native 0 err) + check:stories/check:i18n/mojibake green (reviewer re-ran stories+mojibake). Owns full `ViewAllLink.tsx` diff vs HEAD. P3 follow-up: consolidate the vertical-centering `styles.root` into `theme.ts` (dup with `AgentCtaButton.tsx`). Pending owner commit.
- **Task 630** — migrated both homepage "view all" links (Latest in `page.tsx`, Featured in `FeaturedListings.tsx`) from legacy `buttonVariants({variant:'ghost',size:'sm'})` to the canonical, already-gated Mantine `Button variant="transparent"` (§6a-link) via one new shared island `src/components/shared/ViewAllLink.tsx`; `buttonVariants`/`cn` fully removed from both files, no other `buttonVariants` site touched. Q3 evidence: 56-cell (14 widths × 4 locales) programmatic matrix 56/56 PASS (href/label/variant/transparent-bg/≥44px/no-overflow); hover-no-fill proof; 48 stress-set screenshots + hydration 4/4 PASS. **Kickoff evidence-path gap found:** the named `System/FeaturedListings` story never rendered the real component's `view_all` control at all — substituted a real `/{locale}` app-route capture instead (stronger proof), story untouched. ✅ IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW. Session: `docs/sessions/2026-07-19-task630-homepage-viewall-mantine-transparent-button.md`.
- **Task 633** — AuthSheet Slice 1 (shell-only): replaced shadcn `Sheet`/`SheetContent`/`SheetHeader`/`SheetTitle`/`SheetDescription` with canonical `MantineDrawer` in `AuthSheet.tsx`; all 4 views' fields/state/logic byte-unchanged, only 5× wrapper `px-4` neutralized (Drawer's own inset). **Mid-session defect found+fixed (own code):** the kickoff's literal `<Text>` title composition nested invalidly inside `MantineDrawer`'s own `<h2>`(desktop)/`<Text>`(mobile) title wrappers → real hydration error; fixed with `Text component="span"`. typecheck/check:stories/check:i18n/check:mojibake green; all 5 named critical-flow smokes + `test:header-hydration-id-parity` green (unchanged). Rendered: 64-cell live-app Playwright capture (4 views × {320,375,390,desktop-1280} × 4 locales, `AuthSheet` has no story) — no clip/overflow, mandatory uk@320/sq@320 present, zero hydration errors tied to this component (residual unrelated dev-mode `useId` noise on Header/UserMenu, pre-existing/documented). Session: `docs/sessions/2026-07-20-task633-authsheet-shell-mantinedrawer.md`. ✅ IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW.
- **Task 634** — AuthSheet Slice 2a (Button-only): all 11 legacy shadcn `Button` usages converted to canonical `@mantine/core` `Button` (variant/size/`fullWidth`/`loading` map); zero handler/state change; all gates + 5 regression smokes green. Evidence gap (not a defect): B6/B9 not independently rendered. Session: `docs/sessions/2026-07-20-task634-authsheet-button-mantine-migration.md`. ✅ IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW. Committed (`107f2fcf2`).
- **Task 635** — AuthSheet Slice 2b (Input/Label→`TextInput`): all 5 legacy shadcn `Input` fields (+4 paired `Label`s) converted to canonical `@mantine/core` `TextInput` (label folded into `label` prop, wrapper divs removed); `@/components/ui/label` import + its 5 remaining consumers untouched. Zero handler/state/`id` change (F4/F5 `onSharedChange` cross-view sync verified live). All gates + 5 regression smokes green. Slice 2b of the AuthSheet migration; 2c (`PasswordInput`)/2d (`Alert`)/2e (`Combobox`/`LocationCombobox`) remain open. Session: `docs/sessions/2026-07-20-task635-authsheet-input-label-mantine-textinput.md`. ✅ IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW.

**Deferred / on hold:**
- **Task 560** — admin suspension-as-date-range (DB/RLS/server action). Deferred.
- **Task 463** (Epic BB) — full admin report management. ON HOLD until the Mantine primitive library (Epic MM) is complete; must be rebuilt on finished primitives. Kickoff `tasks/Epics/Epic_BB_kickoff_prompt_Task_463_AdminReportFullManagement.md`.
- **Epic HH** — 310 (P4 content/settings routes), 311 (P5 modal standardisation, partial), 313 (P6 Verified Agents, blocked on owner DB-schema sign-off); 308/309 re-scope onto canonical primitives.
- **Task 453** (Epic KK.2) — remaining admin-manager freshness. Reserved.

## Pending Action Items (owner)

| Item | Notes |
|---|---|
| 🔐 Re-verify HIBP "leaked password protection" availability on Supabase Free tier (Auth → Password Security); enable now if available, else at Pro upgrade. | Security Advisor `auth_leaked_password_protection` WARN. `docs/integrations.md`. |
| 👁️ Eyeball-verify notification localization under `/sq` (data-only fix applied 2026-07-14; creation code already correct). | Re-open the bell under `/sq`. |
| 🐞 `/listings` Grid horizontal overflow <640px (FilterBar segmented `flex-1` + `min-w-35` Combobox push `scrollWidth` past the viewport at 320/375/390). Needs its own task. | Traced via DOM offender scan; out of Task 603 scope. |
| 🖋️ Verified Agents DB schema sign-off (Task 313) + verified-badge public visibility. | Epic HH blocker. |

**Task numbering — last used: 635 (622/623 not used as plain numbers — `Q0R`/`623R` are lettered
task IDs outside the plain numeric sequence; flagging for orchestrator reconciliation). 628 reserved for the
SB10 lint-debt fix (Task 627's follow-up). Next free: 636.**

## Reserved / deferred / retired

- Reserved: 310, 311, 313 (Epic HH), 453 (Epic KK.2).
- Deferred (no #): **I.3** listing-status helper API `(status) → (listing)` — see `docs/domain-rules.md` → "Future ListingStateMachine evolution trigger".
- Retired (never reuse): 465 (uk→ua migration, cancelled); 466/467/469–481 (legacy-primitive layout, superseded by the Mantine migration); 534 (superseded+closed by 535); 597/604 (superseded by 598/605).

## Active Epics — open (closed epics → archive)

| Epic | Status | Plan |
|---|---|---|
| **HH — Admin UX System** | OPEN — 310 (P4), 311 (P5 partial), 313 (P6, blocked on owner DB sign-off); 308/309 re-scope onto canonical primitives | [`Epic_HH_…`](../tasks/Epics/Epic_HH_Admin_UX_System.md) |
| **BB — Listing Inquiries: Report & Message** | ⏸️ ON HOLD — Task 463 held until Epic MM primitives complete; earlier BB tasks (242/243/430/435/458–462) done | [`Epic_BB_…`](../tasks/Epics/Epic_BB_Listing_Inquiries_Report_and_Message.md) |
| **II — Global i18n Hardening** | Implemented + reviewed (316–323); committed + pushed | [`Epic_II_…`](../tasks/Epics/Epic_II_Global_i18n_Hardening.md) |
| **KK — Admin Data Freshness** | 452 (KK.1) done; 453 (KK.2) reserved for remaining managers | [`Epic_KK_…`](../tasks/Epics/Epic_KK_Admin_Data_Freshness.md) |
| **MM — Mantine/TailAdmin Restyle** | IN PROGRESS — Sprints 37–44 landed + pushed (form controls, overlays, TailAdmin conformance, Combobox/DatePicker family, Header/app-shell decomposition, listing card/detail). Next primitives per tracker | [`Epic_MM_…`](../tasks/Epics/Epic_MM_Mantine_UI_Migration.md) · tracker `docs/mantine-tailadmin-migration-tracker.md` |

## Standing notes

> **Frozen/deferred (reviewed 2026-06-05):** Sprint 28 admin-mobile 308/309 must be re-scoped onto the DS + canonical primitives before resuming; Epic HH P4/P5/P6 per the table above; I.3 deferred (valid). Every open task consumes the global Design System (`docs/design-system.md`, Task 340) wherever UI/responsive/overlay is touched, and since the Mantine freeze (Task 482) new UI uses Mantine (`docs/mantine-responsive-design-system.md`); `docs/rule-index.md` sets the per-task pre-read.

> **🟡 Console NOISE (not bugs — do not re-triage as P0):** `[PRED] … preloaded`, `[LCP] … route`, `[Vercel Speed Insights] debug`, "speculation rule set … will be ignored", Turbopack dev-only CSS-chunk/`*.woff2` preload warnings — all dev/debug artifacts, gone in prod. OpenTelemetry `import-in-the-middle` resolved by Task 450 (`322c5d599`); Cloudinary LCP "preloaded but not used" by Task 437. A stale Turbopack `next dev` HMR cache can emit a one-off React `useId` `mantine-_R_…-target` hydration error that does NOT survive a clean `next build` + fresh dev restart and does NOT reproduce in prod — re-verify with `check:hydration` against a freshly restarted server before triaging any `_R_` id mismatch as a code bug (Task 582).

> **Commit emission policy:** the orchestrator emits explicit-path `git add`/`git commit` per task at review time (never `-A`/`-u`/wildcards, never pre-staged batches); the owner runs them in PowerShell. Before emitting, reconcile `git status --short` + the real diff + the session `Files Changed` table (STATUS/REPORT MISMATCH gate). Each commit is reconstructable from the session log.

> **Acknowledged advisor exceptions** (intentional, no task): `pg_net in public` (deferred); `email_change_tokens` RLS-enabled-no-policy — see `docs/rls-rules.md` → "Acknowledged Advisor Exceptions".

> **Standing governance:** `ai-behavior.md` Notes 18–23 + `agent-contract.md` (P0, clauses 1–15) + `rule-index.md` (task-type pre-reads) + the Positive/Negative flow rule (`orchestrator-role.md`). Non-optional acceptance gates on every task.

## Archive

Completed tasks, sprints, and epics live in **[`docs/backlog-archive.md`](backlog-archive.md)**.
