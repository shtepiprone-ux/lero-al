# Project Backlog

> ⚠️ **HARD LIMIT: this file holds ACTIVE state only (~80 lines).** The historical ledger lives in a SEPARATE file: [`docs/backlog-archive.md`](backlog-archive.md).
> Full per-task detail lives in `docs/sessions/` — do NOT paste multi-line per-task summaries here.
> "Last Session" = 2–4 lines max (what changed, what's next). When a task is reviewed/closed, move its summary to ONE row at the TOP of the archive ledger. Violating this is a rule breach.
> See "Backlog & Session Log Rules" in `docs/ai-behavior.md`.

## Last Session

**2026-06-25 — Task 485 (MM.1b) 📋 KICKOFF READY — Admin table TailAdmin-FAITHFUL composition (orchestrator).** Owner rejected the rendered quality: Task 484 put TailAdmin tokens in the theme, but the admin table still looks amateur vs the CRM reference because the **structure/composition was not replicated** (tokens ≠ layout). Orchestrator extracted the EXACT TailAdmin table-block anatomy from the owner's files (`html/crm.html`) → `docs/tailadmin-style-reference.md` §6b: table is **card-wrapped** (`rounded-2xl` border gray-200 bg-white p-5/6), thead `bg-gray-50` + `border-y gray-100`, cells `px-6 py-3` (24×12) `whitespace-nowrap`, composite user cell (avatar h-10 + name 14/medium/gray-700 + subtitle 12/gray-500), pill semantic status badge, right-aligned actions. Task 485 = replicate this 1:1 in `MantineDataTableToCards` (desktop) + `MantineAdminSurfacePattern` + `AdminUsersTable`; becomes the template for all admin tables. Bar = **visual parity with reference, proven by rendered screenshots** (not compile gates). Kickoff: `tasks/Epics/Epic_MM_kickoff_prompt_Task_485_AdminTable_TailAdmin_Faithful_Composition.md`. Task numbering — last used: 485. Next free: 486.

**2026-06-25 — Task 484 (MM.0) ✅ IMPLEMENTED — TailAdmin visual standard.** Sonnet implemented §1b token matrix: Outfit font (Next font loader + Storybook CDN), spacing xs=8/sm=12/md=16/lg=20/xl=24 px, radius lg=8(controls)/2xl=16(Card)/pill(Badge), gray+green+yellow+red TailAdmin palettes, Card flat-border no-shadow, Table verticalSpacing=md/horizontalSpacing=lg, pill Badges fw=500, SegmentedControl radius=lg, Button/Input minH=44px. `MantineDataTableToCards` Card props cleaned (radius/shadow/padding from theme); Table header fw 600→500; outer card Stack gap="sm"; `MantineAdminSurfacePattern` Paper cleaned; AdminUsersTable inactive→'red'; docs §6 rewritten + §7.1 table rhythm + §7.2 primary spacing + §16 table gate updated. **Gates: tsc=0, check:i18n=1939×4, check:stories=74 files 0 viol, check:design-tokens=0, RTL smoke 20/20. Awaiting orchestrator diff review.** Task numbering — last used: 484. Next free: 485.

**2026-06-25 — Task 483 (MM.1) ⏸ PAUSED — awaiting 484 orchestrator review + owner visual confirm before commit.**

**2026-06-24 — Task 482 ✅ APPROVED after REWORK #2 (orchestrator), commit emitted — pending owner run — "Mantine Responsive UI Design System — Foundation".** REWORK #1 was FAILED on a render-blocker: Mantine **v9.4.0** `import { useEffectEvent } from 'react'` (stable, React ≥19.2.0) crashed EVERY Storybook story — `(0, import_react19.useEffectEvent) is not a function` — because `@storybook/nextjs-vite` resolves `react` to Next 15.5.18's vendored canary `19.2.0-canary-0bdb9206-20250818` (lacks the export; would also crash the real app). Green `tsc`/`build-storybook` were NOT render proof (clauses 12–13). **REWORK #2 fix verified at source:** Mantine pinned `^8.3.18` (all 5 pkgs `8.3.18` on disk; `@mantine/core` esm = **0** `useEffectEvent` imports → canary React moot); rendered proof real (orchestrator viewed `DialogDrawerPattern`@320 full-width brand trigger + `CardGrid`@1440 4-up cards, no error overlay; Sonnet Playwright 224/224, 28 shots `.screenshots/mantine-rework2/`); delta clean (2 product files: `MantineListingDetailPattern` v8 `Grid gutter` + comment fixes; **no Tailwind breakpoints / `.container-wide`** in any pattern). Owner native gates green: `tsc=0 · check:stories PASS (74 files, 0 viol)`. **Commit = FULL Task 482 foundation, EXCLUDING the two unrelated Epic BB files** (`Task_462`/`Task_463` kickoffs). `scripts/design-tokens-allowlist.json` was undisclosed in Sonnet's Files Changed table — owner glanced/confirm Mantine `#EC5447` brand-token entries only. Deeper §16a/inventory/locale-parity audit spot-verified; full pass optional post-commit. Rework file: `tasks/kickoff_prompt_Task_482_REWORK2_Mantine_useEffectEvent_RenderBlocker.md`. Session log: `docs/sessions/2026-06-24-task482-mantine-foundation.md`. Task numbering — last used: 482. Next free: 483.

**2026-06-23 — Task 466 ✅ COMPLETE (Sonnet executor).** LDV Bucket-1 hard defects fixed: **168/168 LDV cells PASS** in `screenshots:assert` full run (2026-06-23T21-18; 6532 total · 5435 PASS · 661 FAIL · 108 OUT-OF-RANGE · 328 AMBIGUOUS · exit=1). 661 FAIL = 336 planted probes + 325 non-LDV product cells. Defect A (badge `outside-container`/`element-overlap`): wrapped gallery badge in `absolute`-positioned div (shrinks to content); canonical `<Button>` inside is `max-sm:w-full` of the wrapper, not of the gallery → no left-edge escape. Moved to `top-3 right-3` to clear contact bar Y range. Defect B (CTA overlaps): removed `ListingMobileCTA` (dual-bar eliminated); restructured `listing-contact-mobile` WA to own full-width row at <640, Phone to `icon-xl` in price row; moved "All photos (N)" above gallery on mobile (`sm:hidden`) to clear contact bar Y range. Added `contactListingId` prop (staff-preview contact actions) + `!listingArchived` guard. Gates: `tsc=0 · lint=0-new · check:stories=green · check:i18n=green · harness unchanged`. Session log: `docs/sessions/2026-06-23-task466-listingdetailview-mobile-fix.md`. **✅ APPROVED (orchestrator 2026-06-23), commit emitted — pending owner native verification + run.** Orchestrator source-verified the 3 product files (Read tool): Defect A fix = badge in shrink-to-content `absolute` wrapper so canonical `<Button>` `max-sm:w-full` applies to the ~90px wrapper, not the gallery; Defect B = dead dual-bar `ListingMobileCTA` removed, single `ListingContact` bar (WA full-width row + Phone `icon-xl` documented exemption) + "All photos" above gallery on mobile; behavior preserved (`contactListingId` keeps phone/WA in staff-preview, `listingId` still gates favorite/save/report, `!listingArchived` guard relocated, inquiry dialog intact, `pb-44`); clause-15 "Mobile no-overflow" registry flow satisfied by green LDV `screenshots:assert`. **Owner native must confirm before/at commit: (1) `git diff --stat HEAD -- scripts/check-stories-rendered.mjs scripts/geometry-integrity.mjs` = empty (harness unchanged); (2) explicit-path diff = only the intended files (exclude the two unrelated Epic_BB 462/463 kickoff dirties); (3) file-integrity clean.** Siblings 469–481 reserved. `ListingMobileCTA.tsx` now dead code → optional cleanup follow-up.

**2026-06-23 — Task 467 V1-FINAL closure evidence.** Direct `$LASTEXITCODE` capture: **`exit=1`** (consistent with `failed=758 > 0`). Previous `exit=0` was `tee`-pipeline artifact, now superseded. Owner-native final rerun 2026-06-23T08-44: **6532 cells, 5326 PASS / 758 FAIL / 340 AMBIGUOUS / 108 OUT-OF-RANGE** (evidence: `docs/sessions/task467-owner-final-rerun.exit.txt`). Total FAIL counter: 2129→758 (−1371); real Bucket-1 product scope = 422 cells (96 LDV + 326 siblings), with 336 planted-fixture cells intentionally failing as harness probes. Proof JSON (`task467-v1final-proof-summary.json`): 15/15 invariants passed (2026-06-22 run; 2026-06-23 owner-native rerun supersedes counts/timestamp). Inventory regenerated. Session log corrected (no `exit=0` / "no hard defects" claims). Governance updated (four-bucket verdict model, unconditional out-of-range). **✅ APPROVED & CLOSED (orchestrator, 2026-06-23):** verified at source — sr-only/visually-hidden detection real at `geometry-integrity.mjs:155–161`; inventory `dialog-close` 688→4 (0 in Bucket 1); 8/8 Bucket-1 FP checks = 0; `exit=1`; sr-only + narrow/large viewport-range guards green; −1371 reduction matches the FP-class estimate. Registry Coverage flipped **🟡→✅**. Explicit-path commit emitted (12 Task-467 files; 468's ASSERT-IDs ride along in the shared `check-stories-rendered.mjs` per 468 closure). `.gitignore` (`.codex/` + `docs/chat-gpt-reports/`) = separate `chore` commit. Of the 758 total FAIL cells, **422 are real Bucket-1 product defects** (336 are planted-fixture cells proving harness liveness — 6 planted stories × 56 cells each, intentionally fail, NOT product scope). The 422 Bucket-1 defects → follow-up layout tasks (Task 466 + per-component), NOT gate failures. Owner-native rerun complete.

**2026-06-22 — Task 468 — ✅ COMMITTED `8ffd82d53` (31 files) + ORCHESTRATOR-APPROVED (gate/taxonomy scope); `build-storybook` green.** Owner native verdict: `check:stories` **PASSED — 60 files, 0 violations, all 13 checks + stale-allowlist**; `check-stories.test.ts` **88/88** incl. planted-FAIL/PASS for Checks 3/4/12/13 + `checksRan===13` (gate proven real, not no-op); `tsc --noEmit` clean. Orchestrator diff-reviewed real files: audit greps = 0 width/locale-name/proof-family exports; all remaining bare-viewport exports allowlisted (incl. `ContainerWide` edge-case caught); `isAllowlisted` path-normalized + per-check-scoped; Check 4 old narrow matcher replaced (no double-count), `locale="en" />` continuation handled. **Outstanding for full closure:** `npm run build-storybook` ✅ GREEN (2026-06-22, owner native — prebuild `check:stories` 0 viol/13 checks, build "completed successfully"; benign `use client`/sourcemap Vite warnings only). The single remaining verification — `ASSERT_STORIES ⊆ index.json` 0-stale — lives in `scripts/check-stories-rendered.mjs`, which is SHARED with Task 467 and goes **wholesale into the Task 467 commit** (NOT hunk-split — hunk 1/17 is 467 geometry+crash-guards, file is 467-dominant; 468's ASSERT IDs ride along). So 468's AC11 (ASSERT rewrite) formally lands + is verified in the 467 commit/review. Rendered authority still deferred to Task 467 per owner P0. Session log: `docs/sessions/2026-06-22-task468-storybook-story-dedup.md`. _(Prior status: 📋 KICKOFF READY, REWORKED; HARD-BLOCKED on Task 463 freeze — 463 component now frozen/committed, only its kickoff doc remains dirty.)_ Orchestrator audited all **283 exports / 56 files** (verified live-tree counts): **36** numeric width-suffixed + **24** non-numeric viewport-named exports that FAIL Check 11 (identifier-token: `Tablet`×11 + ListingGrid{Desktop,Mobile,HugeDesktop} + RVS/HugeDesktop + FilterBar{TabletStack,MobileStack,AllLocalesDesktop} + button{MobileSafe,ControlRowRhythm_Desktop} + input/MobileForm + Reports/DialogOwnerRow_Desktop + AdminSidebar/Desktop + EmptyState/MobileEmptyState; allowlisted overlay/scroll modes excluded) + **16** hardcoded-locale instances = **15 `globals.locale` pins** (LDV 9 + Support 3 + NumInput 3) **+ 1 `meta.args.locale:'uk'`** (AdminReportsManager — a prop, missed by globals-only checks) are provable viewport/locale duplicates. **Corrected coverage model:** only `ASSERT_STORIES` get auto 14×4; other stories get {320,375,390}×4 (Phase 2, non-fast) — the toolbar (not auto-coverage) is what makes per-viewport exports redundant. Plan: 283→**≤238** exports, **ListingDetailView 14→3**, collapse AdminReportsManager 16→6 (+ remove `meta.args.locale` → toolbar-reactive) + AdminPermissionsManager 4→1, rewrite **`ASSERT_STORIES` 94→76** with **0 stale/phantom** (incl. `listinggrid--desktop`→`--default`, RM 5 MANDATORY scenario IDs), add blocking `check-stories.mjs` **check 11** (viewport token **anywhere** in name vs documented post-audit real-mode allowlist — blocks bare `Tablet`/`Desktop`/`HugeDesktop` AND prefix `TabletStack`/`MobileStack`) + **check 12** (hardcoded locale: `globals.locale` pins AND `args.locale`/`locale="…"` props; multiline/key-order/all-locale; **replaces Check 4**) over `**/*.stories.{ts,tsx}` at **zero baseline**. Rework (v5) fixes from owner review: scoped **`eslint.config.mjs`** in (story selector + `src/**/*.stories.ts` glob); Group 6 `ListingsTab` now three-way (not 2-class); forbidden-name count **24**, Group 10 = **15 candidates / 12 unallowlisted**; **three-way classification** `PURE_DUPLICATE`/`RESPONSIVE_PROOF_DUPLICATE`/`REAL_MODE` (ListingGrid Desktop/Mobile/HugeDesktop have different renders → RESPONSIVE_PROOF_DUPLICATE: consolidate into one responsive canonical + delete, not just rename); **file-scoped allowlist** (`{file,export,reason}` + stale-entry check — no global-name bypass); Check 11 = **identifier-token** match (not substring; `WorldwideResults` safe); target reframed to **zero forbidden names** (exact count after Group 6/10). v3: 16th pin = `args.locale` (Reports toolbar-reactive) + Check 12 covers props; coverage claim corrected (only ASSERT get 14×4). v2: atomic STOP, effective-locale hash, §8b/§15.3 fix, lint, Group 10 audit. **Owner P0 cycle:** Task 463 functional FROZEN+reviewed → Task 468 atomic → Task 467 REWORK → full rendered-proof + final 463 closure; **no Task-468 `screenshots:assert` matrix is authoritative.** Inventory is **CANDIDATE** until Group 6/10 verdicts. **🔁 Kickoff amended v6 (2026-06-22, owner fold-in — NOT a new task, no 469):** new gate checks **renumbered to align with the LIVE `check-stories.mjs` (which already ends at Check 11)** → viewport/width = **Check 12**, duplicate-family = **Check 13**, and the locale work **broadens existing Check 3 (locale-NAME export families `Uk*/Sq*/It*/En*/Ukrainian*/…`) and Check 4 (locale pins+props) IN PLACE**; **two new bans added** (locale-name families + `Proof*/Demo*/Filtered*/Canonical[0-9]*`), both zero-baseline/preventive with exactly ONE allowlist entry `AdminListingsTable/FilteredPending`; `checksRan`→13. Corrected coverage model retained (only `ASSERT_STORIES` get 14×4; non-ASSERT = 467 geometry-only {320,375,390}×4). Owner P0 sequencing UNCHANGED (blocked on 463 freeze; atomic; precedes authoritative 467 rerun). **Kickoff-quality rework (same v6 change):** companion inventory report banner-superseded to v6 numbering (old Check 11/12 → translated); **ESLint-scope clause** added (`check-stories.mjs` = authoritative allowlist-aware gate; ESLint = static best-effort `no-restricted-syntax` only, allowlist-non-conflicting, **no custom plugin, no flat-config block override** — extend the story-block glob to `src/**/*.stories.ts` only); Positive-flow baseline reworded to "existing Checks 1–11 green before adding 3/4/12/13". Kickoff: `tasks/kickoff_prompt_Task_468_StorybookStoryDedup_CanonicalGrouping_BlockingGate.md`. Companion: `docs/governance-reports/2026-06-20-task468-storybook-story-dedup-before-after-inventory.md`. Audit: `docs/governance-reports/2026-06-20-task468-storybook-story-dedup-before-after-inventory.md`. (Prior — Task 464 COMMITTED `5c2edabae`, 465 retired, 467 kickoff-ready, 463 blocked-on-467 — in archive + Next Tasks.)

## Pending Action Items

| Item | Owner | Notes |
|------|-------|-------|
| 🔐 Re-verify HIBP "Prevent use of leaked passwords" availability on Free tier (Supabase Auth → Sign In/Providers → Password Security). Owner flagged 2026-05-28 as Pro-only. If a Free-tier toggle is now available → enable; if not → enable at Pro upgrade. | Owner | Supabase Security Advisor `auth_leaked_password_protection` WARN. Documented in `docs/integrations.md` → "Supabase Auth Configuration". |

## Next Immediate Tasks

> **🔴 RUN ORDER (owner P0, updated 2026-06-16):** **(1)** Owner runs the emitted Task 446 commit (✅ APPROVED — Branch C fix + Storybook clause-13 rework). **(2)** Owner also runs any remaining pending commits (440/registry/backlog governance; Epic II commits; 432/427/etc.). **(3)** Task 439 original scope PRODUCTION-VALIDATED → closeable on owner native diff confirm. **(4)** Regression Shield Slice 1 (436) and/or Slice 2 (441); 441's recovery-prefetch + self-delete-email-reuse cells can flip (439 landed); 446's Signup-confirmation row = ✅ (Task 446 closed). **(5)** Task 437 ✅ APPROVED — owner runs the emitted 437 commit.

**Sprint 35 (Epic JJ tokens + global responsive rework) — FULLY COMMITTED, nothing pending.** All slices + Epic JJ landed on `main` (408 `6a0b9e623`, 419 committed, 420 `19c39740d`, 421 `44227e995`, 422 `4561a6e82`, 407 `84037666f`). Per-task detail + commit SHAs in [`backlog-archive.md`](backlog-archive.md); session logs in `docs/sessions/`.

**Epic II — Global i18n Hardening: Task 316 COMMITTED (`65a97a8cc`); Task 317 ✅ APPROVED (commit emitted, pending owner run); Task 423 ✅ APPROVED (commit emitted); Task 318 DONE (audit); Task 319 ✅ OWNER-APPROVED (commit pending + manual migration — `docs/sessions/2026-06-13-task319-notification-locale-binding-fix.md`); Task 320 ✅ APPROVED — product fix + QA-hardening addendum, combined commit emitted (pending owner run); Task 424 ✅ APPROVED (commit emitted, pending owner run); Task 323 ✅ APPROVED (commit emitted, pending owner run).** Task 317 output: `scripts/check-i18n-dynamic.mjs` + manifest + baseline + `docs/i18n-rules.md`. Task 423 hardened the same scanner's manifest/baseline validation — zero data edits. Task 318: `docs/governance-reports/2026-06-13-notification-locale-audit.md` (root cause named). Task 319: Model C render-time fix landed. Task 320: Bucket 1 namespace fix + Bucket 3 `floors_total` label, baseline now `{}`. Task 424: NotificationBell/NotificationCenter `w-80` anchored popup → §26.2 full-width bottom sheet at <640 via canonical `Popover` — ✅ APPROVED, commit emitted, `docs/sessions/2026-06-14-task424-notification-bottom-sheet.md`. **Task 323** (`tasks/Epics/Epic_II_kickoff_prompt_Task_323.md`): `check:i18n-dynamic` wired as a BLOCKING CI step in `governance-pr.yml` — done, `docs/sessions/2026-06-14-task323-ci-i18n-dynamic-gate.md`. **Epic II (316–323) is now fully implemented and reviewed; remaining work = owner-run commits only.** Next: **owner runs all pending Epic II commits.**

**Task numbering — last used: 452. 452 ✅ COMMITTED `1994c67cc` (pushed `origin/main`) after REWORK — orchestrator diff-verified on real files (Epic KK Slice KK.1 — B1 fixed: leading-edge-only throttle, `setTimeout`/`timerRef` removed, new trailing-refresh test (FAILS on old hook, PASSES on fixed); B2 fixed: inventory zero "Needs check", `AdminPagesManager` L241 + `AdminLegalManager` L117 FALSE ✅ corrected → `useEffect([init])` added, `AdminPopularLocationsManager` resolved (uses props directly), `AdminSettings`/`AdminFooterManager` honestly deferred (nested form = non-trivial); 6 managers re-synced total; 6/6 tests green; tsc=0; file-integrity clean). Log: `docs/sessions/2026-06-17-task452-admin-data-freshness.md`. 433 ⏸️ DEFERRED / NON-REPRODUCIBLE (owner native check 2026-06-15: `npm run dev` clean, routes compile, pages 200, no globals.css/Tailwind error — do NOT patch globals.css/Tailwind config). 434 ✅ APPROVED (commit emitted). 435 ✅ DIAGNOSIS COMPLETE after documentation-accuracy rework — hypothesis-ranked diagnosis pending owner D1/D2/D3 discriminators; follow-up fix task must start with D1/D2/D3 capture before applying or approving the middleware fix. Report: `docs/governance-reports/2026-06-15-task435-report-listing-submit-rootcause.md`. Log: `docs/sessions/2026-06-18-task-435-report-listing-diagnosis.md`. 436 ✅ IMPLEMENTED + REWORKED via 448 — Slice 1 foundation (4 guards: clearHistory + reportListing smoke, hydration/console gate + self-test, RLS-change + actionable-error-toast governance rules); never committed standalone, lands in the combined 436+448 Slice-1 commit (pending owner run). 437 ✅ APPROVED, commit emitted (preload-not-used perf hygiene — removed redundant middleware HTTP Link header 640w preload that mismatched mobile 2× DPR srcset selection; HTML `<link rel="preload" imageSrcSet=...>` in ListingDetailView retained; orchestrator verified final middleware.ts + imageDelivery.ts, owner native build/tsc/grep clean). Log: `docs/sessions/2026-06-18-task437-preload-not-used-perf-hygiene.md`. 438 USED (AdminTable `<thead>` whitespace hydration fix). 439 ✅ PRODUCTION-VALIDATED / CLOSEABLE (P0 auth lifecycle; commits `f84f0ce40` + `3cd13c816`; owner native diff + §7 checklist). 440 ✅ DONE (Epic RS foundation — commit pending). 441 ✅ IMPLEMENTED (RS Slice 2 — 25 vitest auth-lifecycle smokes; awaiting orchestrator review). 442 ✅ APPROVED (RS Slice 3 — listings-lifecycle smoke: 5 new files + reportListing included, 39 tests; test:listings CI gate; PREVENTION-only; orchestrator verified test code + real product behavior; commit emitted, pending owner run + native gate). 443 ✅ APPROVED (RS Slice 4 — admin-lifecycle smoke: hardDeleteUser 6 tests + test:admin CI gate, 18 tests total; orchestrator verified test code against real reverted index.ts + CI wiring; commit emitted, pending owner native git status + run). 444 ✅ APPROVED (RS Slice 5 — action-level RLS write-path GUARD harness: 15 vitest guard smokes (4 archetypes) + `docs/rls-write-path-manifest.md` (65+ actions) + test:rls-guards blocking CI gate; DB-level RLS honestly ❌ → Slice 5b; orchestrator verified test assertions against real product actions + CI + registry; commit emitted, pending owner native gate + run). 445 ✅ APPROVED + OWNER NATIVE-VERIFIED, commit emitted (RS Slice 6 — i18n/hydration/mobile PREVENTION gates: G1 date-format SSR/CSR parity (25 tests, TZ-invariance child-process), G2 i18n render-parity (8 tests, 8 namespaces × 4 locales), `test:i18n-hydration` + `check:i18n` wired as blocking CI steps, `check:hydration:verify` confirmed blocking, G4 mobile-overflow 77-story coverage confirmed; planted-violations G1 5/25 FAIL + G2 3/8 FAIL, reverts clean; native gates all green incl. live public-route hydration PASS 5/5 (real slug) → public-hydration registry row 🟡→✅; admin live-route → Slice 6b). Log: `docs/sessions/2026-06-17-task445-i18n-hydration-mobile-shield.md`. Slice 5b (live-DB RLS canary) reserved, unnumbered until scheduled. 446 ✅ APPROVED — commit emitted (signup-confirm → /auth/verified Branch C fix + Storybook clause-13 rework: storyT/VerifiedCard, 4 toolbar-reactive stories, 924/924 fast assert, 15 tests). 447 ✅ DONE — raw-enum scanner false-positive fix (AC1/AC2 guards + exported helper + 12 regression tests). 448 ✅ APPROVED (Epic RS Slice 1 REWORK — listing-detail hydration route + status-change→history-entry smoke + report table-name assertion + registry/AC2 honesty; orchestrator verified all 7 ACs against real working-tree files + product code; commit emitted, pending owner run). 449 ✅ IMPLEMENTED (P0 bug fix — infinite `/api/auth/me` loop: added `unauthenticated` guard in `handleAuthEvent` SIGNED_OUT branch of `src/lib/auth/controller.ts`; 4 Scenario 9 regression tests in `controller.test.ts` (44/44 green); planted-violation 3/4 FAIL confirmed; critical-flow-registry row added; awaiting orchestrator review). Log: `docs/sessions/2026-06-17-task449-auth-me-signout-loop.md`. 450 ✅ COMMITTED `322c5d599` (chore — `package.json` global `overrides` pins `import-in-the-middle` to `3.0.1`, approved orchestrator exception after scoped override proven infeasible; all copies deduped, `npm run dev` zero warnings, tsc + build clean, instrumentation compiles; no product/`next.config`/Sentry change). Log: `docs/sessions/2026-06-17-task450-dedupe-import-in-the-middle.md`. 451 ✅ COMMITTED `c3f372775` (pushed `origin/main`) after REWORK + OWNER LIVE-VERIFIED; registry row 🟡→✅ (RS Slice 6b — admin runtime hydration harness + no-session false-green fix). Pure `planRoutes()` gates admin routes on `hasSession` (no session → SKIP/NOT-REAL-COVERAGE, never navigated/PASS); G-B `--verify-admin-config` asserts all 3 session states via the same `planRoutes()`. Owner live run 2026-06-17 (real storageState + real admin UUID): admin list PASS + admin detail `/admin/users/[id]` PASS (both navigated), Summary PASS 6/FAIL 0/SKIP 1, 0 violations → admin hydration registry row ✅. Log: `docs/sessions/2026-06-17-task451-admin-runtime-hydration-harness.md`. Epic II uses reserved 316–323.** **Task numbering — last used: 457.** **Epic LV CLOSED:** 454 LV.1 ✅ (commit emitted) · 455 LV.2 ✅ COMMITTED + production-validated · 456 LV.3 ✅ APPROVED (commit emitted) · 457 LV.4 ✅ APPROVED after rework (commit emitted) — blocking CI invariant gate + per-status invariant test + registry flip ✅. All 4 slices implemented + reviewed; remaining = owner-run commits. 453 reserved for Epic KK.2. **Task 458 (Epic BB — report-listing submit FIX): Fix B ✅ APPROVED (commit emitted, pending owner native gate + run); Fix A PENDING owner D1/D2/D3 capture before ANY `middleware.ts` change.** Kickoff `tasks/Epics/Epic_BB_kickoff_prompt_Task_458_ReportListingSubmitFix.md`. Fix B = per-branch 4-locale error toasts (incl. distinct `report_err_suspended`) + 6 regression tests in `ReportListingDialog.smoke.test.tsx`; registry row scoped toast-branch-only; `middleware.ts` untouched. **Task 459 (Epic BB — Fix A follow-up) ✅ APPROVED as docs-only close (commit emitted, pending owner run)** (`tasks/Epics/Epic_BB_kickoff_prompt_Task_459_ReportListingMiddlewareFixA.md`): Fix A NOT applied — synthetic D1-Network probe found no middleware redirect/rewrite on POST+Next-Action; D2/D3 not captured; no prod evidence for middleware corruption ⇒ `middleware.ts` untouched, no middleware test. Conclusion "most consistent with dev-only stale action ID, not fully proven". Fix B (458) covers UX. **Epic BB report-listing submit thread: 435 diag → 458 Fix B → 459 Fix A (transport, CLOSED) → 460 DB-grants fix (proven root cause).** **Task 460 (Epic BB — `listing_reports` authenticated grants) ✅ APPROVED, commit emitted (pending owner run + Supabase SQL apply)** (`tasks/Epics/Epic_BB_kickoff_prompt_Task_460_ListingReportsAuthenticatedGrants.md`): owner prod-like repro proved `save_failed` = `42501 permission denied for table listing_reports` (NOT middleware — network was a healthy `POST 200 text/x-component`); root cause = Task 275 audit over-revoked `insert,select` from `authenticated`. Shipped: grant migration + corrective `grant-discipline-audit.sql` edit + blocking CI grant-regression check (`check:listing-reports-grants`, planted missing+overgrant FAIL) + registry row; 459 NOT reopened, no middleware/dialog/locale change. See Last Session for the verified detail. **Epic BB report-listing thread (435→458→459→460) fully implemented + reviewed; remaining = owner-run commits + SQL apply.** **Task 461 (Epic BB — admin report-details OWNER row) ✅ APPROVED, commit emitted (pending owner run)** — detail in [`backlog-archive.md`](backlog-archive.md). **Task 462 (Epic BB — report-details owner-row cleanup) — ✅ APPROVED, commit emitted (pending owner run):** orchestrator diff-verified the real files — removed account-type badge + raw `profile_types` key from owner row; `tu`/`clampUserType`/`KNOWN_USER_TYPES` deleted; `Badge` import preserved (Status rows); layout `justify-between`; RTL tests updated (no-`profile_types` + owner-id href assertions); planted-violation 2/4 FAIL→PASS; gates green (tsc=0, check:i18n 1849, check:stories, vitest 4/4, screenshots:assert --fast 924/924, uk@320/375/390). See Last Session. Log: `docs/sessions/2026-06-18-task-462-admin-report-owner-row-cleanup.md`. Task 463 (Epic BB — full admin report management) — 📋 KICKOFF READY (REVISED 2026-06-19 after orchestrator review): capability-gated (server-side `hasPermission`, NOT UI-hide) free any→any status change + reopen + close + HARD delete with confirmation dialog + list-removal without full reload. **Permission model = DELEGABLE via Дозволи (owner decision 2026-06-19), not "hard admin-only":** new keys `reports.status_override` (replaces the too-narrow `reports.reopen`; classifies via a moderator ALLOWLIST `pending→{reviewed,resolved,dismissed}`/`reviewed→{resolved,dismissed}` — all else needs the override key) + `reports.delete`, both default-OFF for moderator, admin-auto + admin-grantable in `AdminPermissionsManager`; MANDATORY `admin.permissions.keys.*`/`descriptions.*` i18n×4 (else Дозволи raw-key leak); UI gated by `canOverrideReportStatus`/`canDeleteReports` capability props (NOT `isAdmin`); `not_found` (not no-op) on missing row; tests prove manage-only moderator denied AND granted moderator allowed + planted-violation; possible `report_actions` FK-cascade SQL. `tasks/Epics/Epic_BB_kickoff_prompt_Task_463_AdminReportFullManagement.md`. **🔁 RE-EXECUTION (owner decision 2026-06-19): kickoff revised to bake in the COMMITTED Task 464 rendered-proof gate — pins EXACT story IDs + anchor testids (`admin-reports-manager`/`status-override-section`/`reopen-btn`/`delete-btn` + `admin-permissions-manager`/`perm-row-reports_status_override`/`perm-row-reports_delete`) the committed `5c2edabae` gate already references; stories must render real content + pass `screenshots:assert` (anchors found, no overflow@320), authoritative run = owner native on committed tree. Note: committed 464 gate currently DEPENDS on this uncommitted 463 work — tree stays inconsistent until 463 lands.** Prior 463 implementation present in working tree (`docs/sessions/2026-06-19-task-463-admin-report-full-management.md`); awaiting Sonnet re-execution to this spec.** **Task 464 (Storybook rendered-proof false-green fix) — ✅ COMMITTED `5c2edabae`** (45 files). 5-layer fail-closed gate (loader-only/blank-canvas/empty-canvas/blank-screenshot/anchor-missing + rendered-proof-before-visual-gates) in `check-stories-rendered.mjs`; owner-native 1068/1068 PASS. AdminReportsManager layout excluded (Task 463 scope). Task 466 = product-layout follow-up. See Last Session. **Task 465 (`uk → ua` locale-code migration) — ❌ CANCELLED by owner 2026-06-19** (more problems than profit; never started). Kickoff file `tasks/kickoff_prompt_Task_465_LocaleCode_uk_to_ua_Migration.md` removed; locale code stays `uk`. Number 465 retired (not reused). **Task 467 (Storybook rendered-proof harness — systemic FALSE-NEGATIVES, owner P0 REWORK 2026-06-19) — ✅ CODE COMPLETE + V1-FINAL FP audit (2026-06-22): FP-CLASS A (sr-only ~688), B/C (viewport-mismatch ~50), D (Leaflet) fixed; Bucket 1 now contains only real product defects; awaiting owner-native re-run + orchestrator review** (`tasks/kickoff_prompt_Task_467_StorybookGeometryVisualAssertions.md`): the Task 464 gate (`5c2edabae`) reports 1068/1068 PASS while owner-native screenshots show MULTIPLE visibly broken stories (clipped controls, off-viewport, action-row overlap, truncated button/tab/select text, bottom-sheet content loss — `NotificationCenter` is only one example). 467 = HARNESS ONLY: global element-geometry/visual-integrity layer on EVERY story at 320/375/390 × sq/en/uk/it (text-clipped/self-clipped/offscreen-control/outside-container/element-overlap/bottomsheet-overflow), fail-closed + intentional-overlap allowlist + planted generic-class proofs + authoritative defect inventory. **Owner decision 2026-06-19: SPLIT** — 467 repairs the harness + emits the inventory; product-layout fixes it uncovers are separate follow-up tasks (folding in Task 466). Acceptance is NOT a green matrix (the failing run IS the deliverable). **🔴 Until 467 is committed AND the committed tree passes the repaired harness, `screenshots:assert` is NOT authoritative proof — Task 463 rendered-proof reliance is BLOCKED.** (V1–V4 complete 2026-06-22; full run 7756 cells exit=1; awaiting orchestrator review + commit.) **Task 468 (Storybook story de-dup + canonical grouping + blocking width/locale-pin gate) — 📋 KICKOFF READY, BLOCKED on Task 463, precedes Task 467** (`tasks/kickoff_prompt_Task_468_StorybookStoryDedup_CanonicalGrouping_BlockingGate.md`; audit `docs/governance-reports/2026-06-20-task468-storybook-story-dedup-before-after-inventory.md`). See Last Session. **Task numbering — last used: 482 (465 retired; 469–481 reserved 2026-06-23 for Task 466 Bucket‑1 product-layout siblings — see `tasks/Task_466_SIBLINGS_Bucket1_Product_Layout_Followups.md`; 482 = Mantine Responsive UI Design System — Full Audit/Foundation/Storybook Rebuild/UI Migration MASTER program — kickoff ready; Tailwind Plus path CANCELLED, 482 reused, NO 483 reserved per owner). Next free (new): 483.**

> **🟡 Separate known terminal warning (NOT a bug, do not chase under 434):** OpenTelemetry `import-in-the-middle can't be external` version-mismatch warnings on `npm run dev`. Unrelated to the Task 434 hydration mismatch. **Resolved by Task 450** (✅ COMMITTED `322c5d599` 2026-06-17 — global `package.json` `overrides` dedupes `import-in-the-middle` to `3.0.1`; `npm run dev` now zero IITM warnings).

> **🟡 Known console NOISE (not bugs — do not re-triage as P0):** `[PRED] … preloaded`, `[LCP] … route`, `[Vercel Speed Insights] debug`, and "speculation rule set … will be ignored" lines are dev/debug artifacts. CSS chunk `[root-of-the-server]__*.css` + font `*.woff2` preload warnings are Turbopack-dev-only artifacts (vanish in prod build). **Task 437 ✅ APPROVED (commit emitted)** — Cloudinary LCP image "preloaded but not used" fixed by removing the redundant middleware HTTP Link header (640w href-only, mismatched on mobile 2× DPR → 960w srcset selection); the HTML `<link rel="preload" imageSrcSet=...>` in ListingDetailView (RSC, first streaming flush) handles all viewports correctly. Log: `docs/sessions/2026-06-18-task437-preload-not-used-perf-hygiene.md`.

**Task 432 (Epic DD follow-up — distinct "nothing to clear" feedback for the no-op clear-history race) — ✅ APPROVED & CLOSED, commit emitted (pending owner run).** Surfaced `cleared_row_count` through the `clearHistory` action (`{ cleared: 0 }` no-op vs `{ cleared: n }`) and added a neutral `toast.info(feedback.clear_history_noop)` branch in both `AdminUserProfile.tsx` handlers + the new 4-locale key. No new UI control/layout. **AC7 verified by owner runtime test (uk no-op neutral toast confirmed 2026-06-15).** tsc/lint/vitest/i18n/file-integrity all green. Kickoff: `tasks/Epics/Epic_DD_kickoff_prompt_Task_432_ClearHistoryNoOpToast.md`. Log: `docs/sessions/2026-06-15-task432-clear-history-noop-toast.md`.

**Task 431 (Epic DD follow-up — schema-drift guard registers `history_clear_events`) — ✅ CLOSED (full detail → [`backlog-archive.md`](backlog-archive.md)).** Added `HistoryClearEvent: 'history_clear_events'` to `check-schema-drift.mjs` `INTERFACE_TABLE_MAP` (+ no-`.from()` coverage-exception comment, Task 265 `search_vector` precedent) and regenerated `schema-drift-check.sql` (32→33 tables, 305→315 cols). Native drift run = empty after Task 246 migration applied (zero drift). (The no-op race success toast from Task 246 is intentionally NOT bundled — separate Task 432 if wanted.)

**Task 427 (Epic I, ✅ APPROVED 2026-06-15, commit emitted — pending owner run; see Last Session):** fixed the owner-reported defect where `sold`/`rented`/`archived` listings opened the PUBLIC VIEW (not the editor) from admin "Редагувати", and only "Архівувати" was offered. Supersedes the never-implemented **Task 355**. Owner decisions (2026-06-15): admin/mod (any listing) AND owner (own listing) get FULL control — edit at any status, delete at any status, and direct any-status→any-status transitions at any time (owner pending→active self-approve intentionally allowed). Implemented: privileged any-status resolver in `listingTransitionEngine.ts` (base matrix + semantic helpers unchanged); `applyListingTransitionByStatus` widened to `privileged = isOwner || canAdminEditListing`; `checkEditPermission` drops `not_editable`; `AdminListingsTable` STATUS_ACTIONS engine-derived (Note 14); owner cabinet `StatusChangeControl` shows full privileged set. Kickoff: `tasks/Epics/Epic_I_kickoff_prompt_Task_427_AdminOwnerFullEditAndStatusAccess.md`. Log: `docs/sessions/2026-06-15-task427-admin-owner-full-edit-and-status-access.md`.

**Task 426 (governance, ✅ COMMITTED — `c4a711ab4` + review commit; working tree clean, verified 2026-06-15):** "canonical-first / no-duplicate-class" AC clause in the Canonical Task Template (`docs/ai-behavior.md:862`) + matching orchestrator review-checklist item (`docs/orchestrator-role.md:237`). Origin: Task 425 review (over-strict literal-local-diff AC where canonical `Button` already satisfied). Kickoff: `tasks/kickoff_prompt_Task_426_canonical_first_ac_clause.md`. Log: `docs/sessions/2026-06-15-task426-canonical-first-ac-clause.md`.

**Tasks 428 + 429 (governance/tooling — `check:mojibake` UTF-8 double-encoding gate) — ✅ COMMITTED `e7396b0c8`.** Gate + Albanian/Cyrillic signature coverage + allowlist hardening. Full detail archived → [`backlog-archive.md`](backlog-archive.md).

**Reserved/deferred:** 310 (Epic HH P4), 311 (Epic HH P5 — partially superseded), 313 (Epic HH P6 Verified Agents — blocked on owner DB-schema approval), 316–323 (Epic II P1–3), 238 (Epic Y; ✅ APPROVED, commit emitted pending owner run), 425 (Epic Y; ✅ APPROVED, commit emitted pending owner run — zero-diff finding), 243 (Epic BB; ✅ APPROVED, commit emitted pending owner commit run; Supabase SQL applied ✅), 246 (Epic DD; ✅ APPROVED after RPC rework, commit emitted pending owner NATIVE verification + SQL run). **CLOSED:** 351/352/353 (superseded by global DS). Deferred (no task #): **I.3** listing-status helper API migration `(status) → (listing)` — see `docs/domain-rules.md` → "Future ListingStateMachine evolution trigger".

**Owner decisions still needed (Epic HH):** Verified Agents DB schema sign-off (Task 313) + verified-badge public visibility.

> **Commit emission policy:** the orchestrator emits explicit-path `git add` / `git commit` per task at review time (never pre-staged batches, never `-A`/`-u`/wildcards); the owner runs them in PowerShell. Each commit is reconstructable from the session log's "Files Changed" table.

> **Acknowledged advisor exceptions** (intentional, no task): `pg_net in public` (deferred); `email_change_tokens` RLS-enabled-no-policy — see `docs/rls-rules.md` → "Acknowledged Advisor Exceptions".

## Frozen / deferred tasks — relevance after Design System (reviewed 2026-06-05)

The global DS work (Task 340 contract + Sprint 32/33, Tasks 372–392) is the canonical mobile/responsive + Storybook layer. Re-assessment of items frozen behind it:

| Item | Verdict | Notes |
|------|---------|-------|
| **Sprint 28** — admin mobile (306/307/306-Fix ✅; 308/309 BLOCKED) | **RE-SCOPE under DS (owner 2026-06-05)** | 308/309 must be re-written to consume `docs/design-system.md` + canonical primitives before any admin migration resumes. |
| **Epic HH Phase 4 (310)** — 12 content/settings admin routes | **Still relevant** | Never migrated; must consume the DS contract. |
| **Epic HH Phase 5 (311)** — admin modal standardisation | **Partially superseded** | Residual = width tiers + destructive-action footer only. |
| **Epic HH Phase 6 (313)** — Verified Agents workflow | **Still relevant** | Blocked on owner DB-schema approval; DS-unaffected. |
| **Epic II (316–323)** — i18n hardening | **Still relevant** | DS only hardened *Storybook* i18n; runtime notif/email/toast/dynamic-key untouched. |
| **Epic Y (237, 238)** · **BB (243)** · **DD (246)** | **Still relevant** | Independent product features; DS-unaffected. |
| **I.3** listing-status helper API migration | **Deferred (valid)** | Trigger-based; DS-unaffected. |

> **🆕 Standing principle (owner 2026-06-05):** every still-open task consumes the global Design System (`docs/design-system.md`, Task 340) wherever UI/responsive/overlay surfaces are touched. `docs/rule-index.md` already mandates `design-system.md` as the first pre-read for UI/layout/admin task types — re-scope any pre-DS plan to it before execution.

## Active product backlog — open Epics (Y/BB/DD/HH/II/JJ; verified 2026-06-05). Closed epics → [`backlog-archive.md`](backlog-archive.md)

| Epic | Tasks | Source notes | Plan |
|---|---|---|---|
| **Y — Listing Form & Lifecycle UX** | **238** ✅ APPROVED (commit emitted) · **425** ✅ APPROVED (commit emitted) (236, 237 ✅, 239 ✅) | Y.3 edit side-panel + status control + dirty-state save ✅ — `docs/sessions/2026-06-14-task238-listing-form-side-panel.md`; 425 = `StatusChangeControl` mobile submit full-width already satisfied (Task 421) — `docs/sessions/2026-06-14-task425-statuschangecontrol-mobile-note-submit.md` | [`Epic_Y_…`](../tasks/Epics/Epic_Y_Listing_Form_and_Lifecycle_UX.md) |
| **BB — Listing Inquiries: Report & Message** | **243** ✅ APPROVED (commit emitted, pending owner commit run; Supabase SQL applied ✅ — "No rows returned") · **430** ✅ APPROVED & CLOSED (commit emitted, pending owner run) — drift-guard now covers `listing_inquiries`; owner native SQL run = "No rows returned" (no drift) (242 ✅) | BB.2 listing→owner inquiry — Path A shipped: new `listing_inquiries` table (idempotent SQL in session log), `submitListingInquiry` action (server-side owner-email resolve, IP rate-limit, typed errors), `listingInquiry.ts` Resend sender, shared `ListingInquiryDialog` (full-width bottom sheet <640), desktop + mobile triggers. Rework closed SQL idempotency + `email_transient` partial-success UX + `sm:max-w-sm` sheet fix + open-dialog matrix. Log: `docs/sessions/2026-06-15-task-243-listing-inquiry-flow.md`. Kickoff: [`Epic_BB_kickoff_prompt_Task_243_ListingInquiryFlow.md`](../tasks/Epics/Epic_BB_kickoff_prompt_Task_243_ListingInquiryFlow.md). | [`Epic_BB_…`](../tasks/Epics/Epic_BB_Listing_Inquiries_Report_and_Message.md) |
| **DD — Admin Audit & History Hygiene** | **246** ✅ APPROVED after RPC rework — **SQL Sections 1–3 APPLIED natively 2026-06-15** (table + `clear_user_history()` RPC + permission seed; "Success. No rows returned"); drift-check empty via Task 431. Commit pending owner run. | DD.1 admin can clear change history (gated + audited, per-row + per-entity); atomic `clear_user_history()` RPC; RLS via service_role-only GRANT (Task 275) + `hasPermission('audit.clear_history')`. Log: `docs/sessions/2026-06-15-task246-admin-clear-history.md` | [`Epic_DD_…`](../tasks/Epics/Epic_DD_Admin_Audit_and_History_Hygiene.md) |
| **HH — Admin UX System** | 310 (P4), 311 (P5, partial), 313 (P6) | 308/309 page-migration need remains — re-scope vs canonical primitives | [`Epic_HH_…`](../tasks/Epics/Epic_HH_Admin_UX_System.md) |
| **II — Global i18n Hardening** | 316–323 (planned) | P1 audit + scanner → P2 remediation → P3 CI gate | [`Epic_II_…`](../tasks/Epics/Epic_II_Global_i18n_Hardening.md) |
| **JJ — Design Variables (single-source tokens)** | 401–408 ✅, 407 strict flip landed — **Epic JJ CLOSED** | Project-wide `@theme` token layer + strict no-raw-value gate (report-mode → blocking, now blocking) | [`Epic_JJ_…`](../tasks/Epics/Epic_JJ_Design_Variables_Single_Source.md) |
| **KK — Admin Data Freshness / Moderation Visibility** | **452** ✅ KICKOFF READY (KK.1) · 453 reserved (KK.2) | Shared `useAdminPageFreshness` (router.refresh on focus+visibility, throttled) mounted once in `AdminShell` → all SSR admin pages refresh on focus/tab-visible, filters preserved, no reload/polling/realtime. Multi-admin stale-data safety. | [`Epic_KK_…`](../tasks/Epics/Epic_KK_Admin_Data_Freshness.md) |
| **LV — Listing Public Visibility Integrity** (CLOSED) | **454** LV.1 ✅ (commit emitted) · **455** LV.2 ✅ COMMITTED + prod-validated · **456** LV.3 ✅ APPROVED (commit emitted) · **457** LV.4 ✅ APPROVED after rework (commit emitted — blocking CI invariant gate) | ONE status→visibility policy (ALL statuses, not just `active`) + `applyPublicVisibility`/`isListingPubliclyVisible` single source replacing 5+ duplicated public-read predicates; reconcile lapsed `expires_at` so `active` can't silently = invisible; surface Visible/Hidden+reason in cabinet/admin; blocking CI invariant gate. Fixes owner's "Active but not in /listings" bug class. | [`Epic_LV_…`](../tasks/Epics/Epic_LV_Listing_Public_Visibility_Integrity.md) · [`Sprint_36_…`](../tasks/Sprints/Sprint_36_Listing_Public_Visibility_Integrity.md) |

> **Standing governance (codified in `/docs`):** Notes 18–23 (`ai-behavior.md`) + `agent-contract.md` (P0, clauses 1–14) + `rule-index.md` (task-type pre-reads) + Positive+Negative flow rule (`orchestrator-role.md`). Non-optional acceptance gates on every task.

## Archive

Completed tasks, sprints, and epics live in **[`docs/backlog-archive.md`](backlog-archive.md)**.
