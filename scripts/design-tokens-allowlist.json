{
  "src/design-system/mantine/theme.ts": "Mantine v9 createTheme() input — theme.ts's color tuples (gray/green/yellow/red/blueLight/purple/sale/orange) and every styles/vars/defaultProps literal from line 154 sit inside the single createTheme({...}) call (verified: no literal in this file sits outside it). Mantine's createTheme() API requires raw hex colors and rem/px values; it does not accept CSS custom properties (it generates --mantine-color-*/--mantine-font-size-*/--mantine-spacing-* etc. from these raw inputs internally). Task 717 narrowed this from a directory-wide exemption to this exact file — the directory's other files carry their own scoped keys or per-line markers instead.",
  "src/design-system/mantine/typography.ts": "RESERVED for Task 734 (genuine N1 violation, not remediated here per Task 717 R7) — SECTION_HEADING_FZ's three literals (a hand-copied responsive font-size triad, 'preserved from the prior hand-copied literal, not re-derived' per this file's own header comment) are consumed as a Title fz prop by 5 external files. This is NOT a createTheme() input and NOT a pattern inline style — neither clause of the old blanket directory exemption ever covered it. Task 734 must tokenize or justify each value individually.",
  "src/design-system/mantine/notification-chrome.css": "Three distinct non-tokenized values, none a createTheme() input or inline-style case (this is a stylesheet): a comment-text hex reference whose live declaration already uses a Mantine color variable; the §6r-LIVE live-captured compact toast max-width (no spacing-scale token maps to it); and two measured header.site-header clearance offsets (Task 723), not design-scale values.",
  "src/design-system/mantine/pagination-chrome.css": "Two non-tokenized values, neither a createTheme() input or inline-style case (this is a stylesheet): a hairline border width inside a shorthand declaration (no border-width token exists in globals.css), and the 44px touch-target floor (established project convention, matches theme.ts's own rem-exemption note for minHeight).",
  "src/design-system/mantine/scrollarea-chrome.css": "Single comment-text hex reference — the live declaration already consumes a Mantine color variable; the detector's color pattern is not CSS-comment-aware for .css files.",
  "src/design-system/mantine/skeleton-chrome.css": "Two comment-text hex references on one line — the live declaration already consumes a Mantine color variable; the detector's color pattern is not CSS-comment-aware for .css files.",
  "src/design-system/mantine/patterns/MantineAppShellFoundation.tsx": "Single 44px touch-target floor on a NavLink styles prop — established project convention, matches theme.ts's own rem-exemption note for minHeight.",
  "src/design-system/mantine/patterns/MantineCombobox.tsx": "Inline SVG checkmark icon geometry (14px width/height) — no icon-size token exists in this project; non-tokenized affordance, same category as drag-handle geometry.",
  "src/design-system/mantine/patterns/MantineSelect.tsx": "Inline SVG checkmark icon geometry (16px width/height) — no icon-size token exists in this project; same category as MantineCombobox.tsx's icon.",
  "src/design-system/mantine/patterns/RangeDatePicker.tsx": "Two non-tokenized values: a local stacking-context z-index (day-cell above its own range-highlight sibling, not a member of the global z-* layer scale), and a pill-radius geometric idiom (any sufficiently large value produces full rounding; no live var(--mantine-radius-pill) usage exists anywhere in src/ to point to as an established reachable reference).",
  "src/design-system/mantine/patterns/MantineResponsiveActionFooter.tsx": "RESERVED for Task 734 (genuine N1 violation, not remediated here per Task 717 R7) — the sticky action-footer z-index bypasses the semantically-matching --z-sticky token (named specifically for sticky elements) and numerically coincides with the unrelated --z-toast. Task 734 must resolve which token this should reference.",
  "src/design-system/brand.ts": "Canonical brand color source (Task 661 single-source refactor) — the one MantineColorsTuple literal that Mantine's createTheme() requires as raw hex input (Mantine's API does not accept CSS custom properties). theme.ts, globals.css's --mantine-color-brand-* aliases, and the email BRAND_PRIMARY/BRAND_HOVER constants all import from this single file instead of duplicating the raw hex — this file is the token source, not a bypass of it.",
  "src/modules/notifications/lib/emails": "HTML email templates — email clients (Gmail, Outlook, Apple Mail) do not support CSS custom properties; all style values must be literal inline styles",
  "src/modules/notifications/lib/sendTemplatedEmail.ts": "HTML email layout template using BRAND_ACCENT const and inline hex literals; CSS custom properties are unsupported in email HTML",
  "src/app/api/auth-email-hook/route.ts": "Supabase auth email hook — generates inline HTML email for email_change events; CSS custom properties are unsupported in email HTML",
  "src/modules/auth/components/AuthSheet.tsx": "Official Google brand SVG fill colors (#4285F4/#34A853/#FBBC05/#EA4335) from Google Identity Guidelines — brand colors are fixed by Google policy and cannot be replaced with design tokens",
  "src/components/ui/appImageConfig.ts": "Next/Image sizes media-descriptor strings + inline SVG blur placeholder color — neither can reference CSS custom properties",
  "src/components/shared/PerfDevOverlay.tsx": "Dev-only performance diagnostic overlay — guarded by process.env.NODE_ENV !== 'development' (build-time constant); returns null and is tree-shaken from production bundles; z-[9999] and text-[10px] are non-production UI"
}
