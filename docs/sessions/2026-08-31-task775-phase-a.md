# Task 775 — `/listings` route chrome → `ListingsPageFrame` (Mantine)

**Status: REVISION 3 — READY FOR CURRENT-TREE VERIFICATION**

Sprint 68. Kickoff: `tasks/Sprints/Sprint_68_kickoff_prompt_Task_775_Listings_Route_Frame.md`.

## Revision 3 owner decision

Task 775 is a frontend migration and now uses current-tree verification only. The earlier Revision-2 baseline design
required a detached `c864431d0` worktree, a separate probe worktree, pre/post comparison and failure waivers. That
process was removed because it proved historical claims rather than the current frontend implementation.

No baseline worktree, `pre-edit` probe, pair manifest or historical waiver remains a Task-775 requirement. Revision-1
probe JSON and PNGs are retained byte-identically under `docs/sessions/evidence/task775/revision1/` as history only.

## Implementation already committed

- `90f1412d8` introduced the route frame, story, messages, theme keys and initial evidence.
- `4254f3897` hardened the interaction probe, added two non-visual selector hooks and moved the Revision-1 probe
  artefacts without altering them.
- The owner ran `npm.cmd run build` after those changes: exit 0, with `/[locale]/listings` emitted as dynamic (`ƒ`).
- `node --check scripts/task775-listings-frame-route-probe.mjs` and `npm.cmd run typecheck` previously exited 0.

## Current verification contract

The task-owned probe now captures one immutable `current` run:

```powershell
$env:BASE_URL = 'http://127.0.0.1:3001'
node scripts/task775-listings-frame-route-probe.mjs current task775-final-<unique-id>
```

It writes `runs/<unique-id>/route-probe.current.json`, including `gitCommit`, `probeHash`, 28 route cells and the
four asserted interactions. A unique run ID prevents overwrite.

Sonnet's remaining work is exactly §8 of the Revision-3 kickoff: run the required current-tree checks, start the
current production build, take one `current` route-probe run, and report exact results. The optional global
diagnostics are reported separately; they are not converted into baseline-comparison evidence.

## Scope boundary

Task 775 does not change `ListingsSortBar` or other listings controls. Any route-probe overflow observation below
640px is a Task-772 finding, not a Task-775 acceptance decision.
