# Project Backlog

> ⚠️ **HARD LIMIT: this file holds ACTIVE state only (~80 lines).** The historical ledger lives in a SEPARATE file: [`docs/backlog-archive.md`](backlog-archive.md).
> Full per-task detail lives in `docs/sessions/` — do NOT paste multi-line per-task summaries here.
> "Last Session" = 2–4 lines max (what changed, what's next). When a task is reviewed/closed, move its summary to ONE row at the TOP of the archive ledger. Violating this is a rule breach.
> See "Backlog & Session Log Rules" in `docs/ai-behavior.md`.

## Last Session

**2026-06-16 — Task 446 ✅ APPROVED — commit emitted (pending owner run).** Branch C fix: Next.js 15 does NOT propagate `cookies().set()` onto `NextResponse.redirect()`; fixed by inlining `createServerClient` from `@supabase/ssr` in `/auth/confirm/route.ts` with `setAll()` writing session tokens directly onto the redirect response. `/auth/verified` now has error + sync-fail guards; new `VerifiedBridge.tsx` re-syncs on mount; 3 i18n keys × 4 locales (parity 1821). Storybook rework (clause-13, owner option A): `VerifiedPage.stories.tsx` rebuilt on `storyT()` + shared `VerifiedCard.tsx` (no STRINGS hardcode, no `globals.locale` pins), 4 toolbar-reactive exports; `generateMetadata` error-state title fixed; tautological route test removed (15 real tests). Owner-native: tsc=0, lint=0, check:i18n 1821, check:stories 0, `screenshots:assert --fast` 924/924, vitest 15/15; runtime AC1/AC3 PASS (header authenticated, `/uk/cabinet` reachable). Full 14-viewport render waived (owner). Next: owner runs the emitted commit.

## Pending Action Items

| Item | Owner | Notes |
|------|-------|-------|
| 🔐 Re-verify HIBP "Prevent use of leaked passwords" availability on Free tier (Supabase Auth → Sign In/Providers → Password Security). Owner flagged 2026-05-28 as Pro-only. If a Free-tier toggle is now available → enable; if not → enable at Pro upgrade. | Owner | Supabase Security Advisor `auth_leaked_password_protection` WARN. Documented in `docs/integrations.md` → "Supabase Auth Configuration". |

## Next Immediate Tasks

> **🔴 RUN ORDER (owner P0, updated 2026-06-16):** **(1)** Owner runs the emitted Task 446 commit (✅ APPROVED — Branch C fix + Storybook clause-13 rework). **(2)** Owner also runs any remaining pending commits (440/registry/backlog governance; Epic II commits; 432/427/etc.). **(3)** Task 439 original scope PRODUCTION-VALIDATED → closeable on owner native diff confirm. **(4)** Regression Shield Slice 1 (436) and/or Slice 2 (441); 441's recovery-prefetch + self-delete-email-reuse cells can flip (439 landed); 446's Signup-confirmation row = ✅ (Task 446 closed). **(5)** Task 437 stays DEFERRED.

**Sprint 35 (Epic JJ tokens + global responsive rework) — FULLY COMMITTED, nothing pending.** All slices + Epic JJ landed on `main` (408 `6a0b9e623`, 419 committed, 420 `19c39740d`, 421 `44227e995`, 422 `4561a6e82`, 407 `84037666f`). Per-task detail + commit SHAs in [`backlog-archive.md`](backlog-archive.md); session logs in `docs/sessions/`.

**Epic II — Global i18n Hardening: Task 316 COMMITTED (`65a97a8cc`); Task 317 ✅ APPROVED (commit emitted, pending owner run); Task 423 ✅ APPROVED (commit emitted); Task 318 DONE (audit); Task 319 ✅ OWNER-APPROVED (commit pending + manual migration — `docs/sessions/2026-06-13-task319-notification-locale-binding-fix.md`); Task 320 ✅ APPROVED — product fix + QA-hardening addendum, combined commit emitted (pending owner run); Task 424 ✅ APPROVED (commit emitted, pending owner run); Task 323 ✅ APPROVED (commit emitted, pending owner run).** Task 317 output: `scripts/check-i18n-dynamic.mjs` + manifest + baseline + `docs/i18n-rules.md`. Task 423 hardened the same scanner's manifest/baseline validation — zero data edits. Task 318: `docs/governance-reports/2026-06-13-notification-locale-audit.md` (root cause named). Task 319: Model C render-time fix landed. Task 320: Bucket 1 namespace fix + Bucket 3 `floors_total` label, baseline now `{}`. Task 424: NotificationBell/NotificationCenter `w-80` anchored popup → §26.2 full-width bottom sheet at <640 via canonical `Popover` — ✅ APPROVED, commit emitted, `docs/sessions/2026-06-14-task424-notification-bottom-sheet.md`. **Task 323** (`tasks/Epics/Epic_II_kickoff_prompt_Task_323.md`): `check:i18n-dynamic` wired as a BLOCKING CI step in `governance-pr.yml` — done, `docs/sessions/2026-06-14-task323-ci-i18n-dynamic-gate.md`. **Epic II (316–323) is now fully implemented and reviewed; remaining work = owner-run commits only.** Next: **owner runs all pending Epic II commits.**

**Task numbering — last used: 446. 433 ⏸️ DEFERRED / NON-REPRODUCIBLE (owner native check 2026-06-15: `npm run dev` clean, routes compile, pages 200, no globals.css/Tailwind error — do NOT patch globals.css/Tailwind config). 434 ✅ APPROVED (commit emitted). 435 kickoff written + sharpened (report-listing transport-failure DIAGNOSE-ONLY). 436 kickoff WRITTEN (RS Slice 1 regression-protection, 4 guards — prevention only). 437 kickoff WRITTEN (preload-not-used perf hygiene — low severity, gated on prod-build repro). 438 USED (AdminTable `<thead>` whitespace hydration fix). 439 ✅ PRODUCTION-VALIDATED / CLOSEABLE (P0 auth lifecycle; commits `f84f0ce40` + `3cd13c816`; owner native diff + §7 checklist). 440 ✅ DONE (Epic RS foundation — commit pending). 441 kickoff WRITTEN (RS Slice 2 auth-lifecycle smoke). 442–445 reserved for RS Slices 3–6 (listings/admin/RLS-perm-harness/i18n-hydration-mobile). 446 ✅ APPROVED — commit emitted (signup-confirm → /auth/verified Branch C fix + Storybook clause-13 rework: storyT/VerifiedCard, 4 toolbar-reactive stories, 924/924 fast assert, 15 tests). Epic II uses reserved 316–323.** **Next free (new): 447.**

> **🟡 Separate known terminal warning (NOT a bug, do not chase under 434):** OpenTelemetry `import-in-the-middle can't be external` version-mismatch warnings on `npm run dev`. Unrelated to the Task 434 hydration mismatch; tracked as known-noise only.

> **🟡 Known console NOISE (not bugs — do not re-triage as P0):** repeated `The resource <url> was preloaded using link preload but not used within a few seconds…` warnings (Cloudinary LCP image, `_next/static` CSS chunk `[root-of-the-server]__*.css`, `*.woff2` font), plus `[PRED] … preloaded`, `[LCP] … route`, `[Vercel Speed Insights] debug`, and "speculation rule set … will be ignored" lines. These are the project's preload machinery + dev/Turbopack artifacts — almost all vanish in a production build. Consolidated under **Task 437**, gated on prod-build reproduction. NOT functional defects; the only genuinely broken user action is the Task 435 report-submit transport failure.

**Task 432 (Epic DD follow-up — distinct "nothing to clear" feedback for the no-op clear-history race) — ✅ APPROVED & CLOSED, commit emitted (pending owner run).** Surfaced `cleared_row_count` through the `clearHistory` action (`{ cleared: 0 }` no-op vs `{ cleared: n }`) and added a neutral `toast.info(feedback.clear_history_noop)` branch in both `AdminUserProfile.tsx` handlers + the new 4-locale key. No new UI control/layout. **AC7 verified by owner runtime test (uk no-op neutral toast confirmed 2026-06-15).** tsc/lint/vitest/i18n/file-integrity all green. Kickoff: `tasks/Epics/Epic_DD_kickoff_prompt_Task_432_ClearHistoryNoOpToast.md`. Log: `docs/sessions/2026-06-15-task432-clear-history-noop-toast.md`.

**Task 431 (Epic DD follow-up — schema-drift guard registers `history_clear_events`) — ✅ CLOSED (full detail → [`backlog-archive.md`](backlog-archive.md)).** Added `HistoryClearEvent: 'history_clear_events'` to `check-schema-drift.mjs` `INTERFACE_TABLE_MAP` (+ no-`.from()` coverage-exception comment, Task 265 `search_vector` precedent) and regenerated `schema-drift-check.sql` (32→33 tables, 305→315 cols). Native drift run = empty after Task 246 migration applied (zero drift). (The no-op race success toast from Task 246 is intentionally NOT bundled — separate Task 432 if wanted.)

**Task 427 (Epic I, ✅ APPROVED 2026-06-15, commit emitted — pending owner run; see Last Session):** fixed the owner-reported defect where `sold`/`rented`/`archived` listings opened the PUBLIC VIEW (not the editor) from admin "Редагувати", and only "Архівувати" was offered. Supersedes the never-implemented **Task 355**. Owner decisions (2026-06-15): admin/mod (any listing) AND owner (own listing) get FULL control — edit at any status, delete at any status, and direct any-status→any-status transitions at any time (owner pending→active self-approve intentionally allowed). Implemented: privileged any-status resolver in `listingTransitionEngine.ts` (base matrix + semantic helpers unchanged); `applyListingTransitionByStatus` widened to `privileged = isOwner || canAdminEditListing`; `checkEditPermission` drops `not_editable`; `AdminListingsTable` STATUS_ACTIONS engine-derived (Note 14); owner cabinet `StatusChangeControl` shows full privileged set. Kickoff: `tasks/Epics/Epic_I_kickoff_prompt_Task_427_AdminOwnerFullEditAndStatusAccess.md`. Log: `docs/sessions/2026-06-15-task427-admin-owner-full-edit-and-status-access.md`.

**Task 426 (governance, ✅ COMMITTED — `c4a711ab4` + review commit; working tree clean, verified 2026-06-15):** "canonical-first / no-duplicate-class" AC clause in the Canonical Task Template (`docs/ai-behavior.md:862`) + matching orchestrator review-checklist item (`docs/orchestrator-role.md:237`). Origin: Task 425 review (over-strict literal-local-diff AC where canonical `Button` already satisfied). Kickoff: `tasks/kickoff_prompt_Task_426_canonical_first_ac_clause.md`. Log: `docs/sessions/2026-06-15-task426-canonical-first-ac-clause.md`.

**Tasks 428 + 429 (governance/tooling — `check:mojibake` UTF-8 double-encoding gate) — ✅ COMMITTED `e7396b0c8`.** Gate + Albanian/Cyrillic signature coverage + allowlist hardening. Full detail archived → [`backlog-archive.md`](backlog-archive.md).

**Reserved/deferred:** 310 (Epic HH P4), 311 (Epic HH P5 — partially superseded), 313 (Epic HH P6 Verified Agents — blocked on owner DB-schema approval), 316–323 (Epic II P1–3), 238 (Epic Y; ✅ APPROVED, commit emitted pending owner run), 425 (Epic Y; ✅ APPROVED, commit emitted pending owner run — zero-diff finding), 243 (Epic BB; ✅ APPROVED, commit emitted pending owner commit run; Supabase SQL applied ✅), 246 (Epic DD; ✅ APPROVED after RPC rework, commit emitted pending owner NATIVE verification + SQL run). **CLOSED:** 351/352/353 (superseded by global DS). Deferred (no task #): **I.3** listing-status helper API migration `(status) → (listing)` — see `docs/domain-rules.md` → "Future ListingStateMachine evolution trigger".

**Owner decisions still needed (Epic HH):** Verified Agents DB schema sign-off (Task 313) + verified-badge public visibility.

> **Commit emission policy:** the orchestrator emits explicit-path `git add` / `git commit` per task at review time (never pre-staged batches, never `-A`/`-u`/wildcards); the owner runs them in PowerShell. Each commit is reconstructable from the session log's "Files Changed" table.

> **Acknowledged advisor exceptions** (intentional, no task): `pg_net in public` (deferred); `email_change_tokens` RLS-enabled-no-policy — see `docs/rls-rules.md` → "Acknowledged Advisor Exceptions".

## Frozen / deferred tasks — relevance after Design System (reviewed 2026-06-05)

The global DS work (Task 340 contract + Sprint 32/33, Tasks 372–392) is the canonical mobile/responsive + Storybook layer. Re-assessment of items frozen behind it:

| Item | Verdict | Notes |
|------|---------|-------|
| **Sprint 28** — admin mobile (306/307/306-Fix ✅; 308/309 BLOCKED) | **RE-SCOPE under DS (owner 2026-06-05)** | 308/309 must be re-written to consume `docs/design-system.md` + canonical primitives before any admin migration resumes. |
| **Epic HH Phase 4 (310)** — 12 content/settings admin routes | **Still relevant** | Never migrated; must consume the DS contract. |
| **Epic HH Phase 5 (311)** — admin modal standardisation | **Partially superseded** | Residual = width tiers + destructive-action footer only. |
| **Epic HH Phase 6 (313)** — Verified Agents workflow | **Still relevant** | Blocked on owner DB-schema approval; DS-unaffected. |
| **Epic II (316–323)** — i18n hardening | **Still relevant** | DS only hardened *Storybook* i18n; runtime notif/email/toast/dynamic-key untouched. |
| **Epic Y (237, 238)** · **BB (243)** · **DD (246)** | **Still relevant** | Independent product features; DS-unaffected. |
| **I.3** listing-status helper API migration | **Deferred (valid)** | Trigger-based; DS-unaffected. |

> **🆕 Standing principle (owner 2026-06-05):** every still-open task consumes the global Design System (`docs/design-system.md`, Task 340) wherever UI/responsive/overlay surfaces are touched. `docs/rule-index.md` already mandates `design-system.md` as the first pre-read for UI/layout/admin task types — re-scope any pre-DS plan to it before execution.

## Active product backlog — open Epics (Y/BB/DD/HH/II/JJ; verified 2026-06-05). Closed epics → [`backlog-archive.md`](backlog-archive.md)

| Epic | Tasks | Source notes | Plan |
|---|---|---|---|
| **Y — Listing Form & Lifecycle UX** | **238** ✅ APPROVED (commit emitted) · **425** ✅ APPROVED (commit emitted) (236, 237 ✅, 239 ✅) | Y.3 edit side-panel + status control + dirty-state save ✅ — `docs/sessions/2026-06-14-task238-listing-form-side-panel.md`; 425 = `StatusChangeControl` mobile submit full-width already satisfied (Task 421) — `docs/sessions/2026-06-14-task425-statuschangecontrol-mobile-note-submit.md` | [`Epic_Y_…`](../tasks/Epics/Epic_Y_Listing_Form_and_Lifecycle_UX.md) |
| **BB — Listing Inquiries: Report & Message** | **243** ✅ APPROVED (commit emitted, pending owner commit run; Supabase SQL applied ✅ — "No rows returned") · **430** ✅ APPROVED & CLOSED (commit emitted, pending owner run) — drift-guard now covers `listing_inquiries`; owner native SQL run = "No rows returned" (no drift) (242 ✅) | BB.2 listing→owner inquiry — Path A shipped: new `listing_inquiries` table (idempotent SQL in session log), `submitListingInquiry` action (server-side owner-email resolve, IP rate-limit, typed errors), `listingInquiry.ts` Resend sender, shared `ListingInquiryDialog` (full-width bottom sheet <640), desktop + mobile triggers. Rework closed SQL idempotency + `email_transient` partial-success UX + `sm:max-w-sm` sheet fix + open-dialog matrix. Log: `docs/sessions/2026-06-15-task-243-listing-inquiry-flow.md`. Kickoff: [`Epic_BB_kickoff_prompt_Task_243_ListingInquiryFlow.md`](../tasks/Epics/Epic_BB_kickoff_prompt_Task_243_ListingInquiryFlow.md). | [`Epic_BB_…`](../tasks/Epics/Epic_BB_Listing_Inquiries_Report_and_Message.md) |
| **DD — Admin Audit & History Hygiene** | **246** ✅ APPROVED after RPC rework — **SQL Sections 1–3 APPLIED natively 2026-06-15** (table + `clear_user_history()` RPC + permission seed; "Success. No rows returned"); drift-check empty via Task 431. Commit pending owner run. | DD.1 admin can clear change history (gated + audited, per-row + per-entity); atomic `clear_user_history()` RPC; RLS via service_role-only GRANT (Task 275) + `hasPermission('audit.clear_history')`. Log: `docs/sessions/2026-06-15-task246-admin-clear-history.md` | [`Epic_DD_…`](../tasks/Epics/Epic_DD_Admin_Audit_and_History_Hygiene.md) |
| **HH — Admin UX System** | 310 (P4), 311 (P5, partial), 313 (P6) | 308/309 page-migration need remains — re-scope vs canonical primitives | [`Epic_HH_…`](../tasks/Epics/Epic_HH_Admin_UX_System.md) |
| **II — Global i18n Hardening** | 316–323 (planned) | P1 audit + scanner → P2 remediation → P3 CI gate | [`Epic_II_…`](../tasks/Epics/Epic_II_Global_i18n_Hardening.md) |
| **JJ — Design Variables (single-source tokens)** | 401–408 ✅, 407 strict flip landed — **Epic JJ CLOSED** | Project-wide `@theme` token layer + strict no-raw-value gate (report-mode → blocking, now blocking) | [`Epic_JJ_…`](../tasks/Epics/Epic_JJ_Design_Variables_Single_Source.md) |

> **Standing governance (codified in `/docs`):** Notes 18–23 (`ai-behavior.md`) + `agent-contract.md` (P0, clauses 1–14) + `rule-index.md` (task-type pre-reads) + Positive+Negative flow rule (`orchestrator-role.md`). Non-optional acceptance gates on every task.

## Archive

Completed tasks, sprints, and epics live in **[`docs/backlog-archive.md`](backlog-archive.md)**.
