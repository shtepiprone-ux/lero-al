### live wiring: package.json / .github / scripts (excluding the script itself)
exit=1
### docs (non-session, non-evidence, non-review) + open task files
docs/backlog.md:52:| **861** | **Sprint 78**, 🔴 `NEEDS REVISION` (review 2, 2026-09-20; re-enter §17) — **gates 846 and Wave B** | P1 Q4. **Implementation verified by review 2** — review 1's F1–F5 all closed; bell census 5/5 `manifest:yes story:yes className:0` exit 0, `check:rendered-scope` 0 new/0 stale, `check:story-coverage` 95/95, `check:surface-census:changed` 0/0, build 0 and current for the diff (all re-run by the reviewer, `win32`). **Two defects left.** **F6 P2 is the kickoff's own** — R4c's "chrome must match its pre-861 capture" is a GR-4 absolute that GR-0 forces the migration to violate (separators `#EBEBEB`→`#e4e7ec` via canonical `Divider` gray.2; row transition 150→200 ms, no 150 ms token exists) and no pre-861 capture exists; R4c + §13.3 tuples 5–7 rewritten to name both deltas — **no executor work**. **F7 P3, executor-owed:** clause 9 — `scripts/task319-qa-notification-templates.mjs:38` `STORY_ID` still points at the deleted `notifications-notificationitem--all-cases`; re-point to `mantine-primitives-notificationitem--default` or retire the script (new AC10). D9/D11/D13/D5/D6, the baseline edit, the stale tailwind allowlist entry, AC8's non-re-drive and AC4's re-expressed assertion all **ruled on and accepted** (§17.3); `check:stories-rendered` is retired `screenshots:assert` and must not be run. **AC9 partly closed (§17.5): owner accepted the visual half 2026-09-20 verbatim («візуально сторіси виглядаю чудово») — tuples 1/5/6/7 + 3-visual, which ratifies BOTH §17.1 deltas. Keyboard half (tuples 2, 3-keyboard, 4) still owner-owed — behaviour, not appearance; already machine-proven in Chromium + 61/61.** Neither half blocks the F7 revision. Session log + evidence `docs/sessions/evidence/task861/final-r1/`. |
tasks/Sprints/Sprint_78_Admin_And_Agent_Dashboards_On_Canonical_Mantine.md:114:| **861** | **`RangeDatePicker`'s trigger is a `readOnly` input inside a click-only `MantinePopover`, so the canonical date-range picker cannot be opened by keyboard** (WCAG 2.2 SC 2.1.1). Measured 2026-09-20: `MantinePopover.tsx:115-122` and `:137` attach `onClick` only, no `onKeyDown`/`onKeyUp` in either file; `RangeDatePicker.tsx:836-838` is the `<TextInput readOnly>`. Live today in the listings date-range filter — critical-flow row "Listings date-range filter" — and inherited by 846's `MantineDashboardPeriodControl`. **Owner-bound fix: a semantic `<button type="button">` trigger with `aria-expanded`/`aria-haspopup`, NOT an `onKeyDown` on the shared popover** (it would double-toggle the native-button consumers). Acceptance is the whole keyboard-only flow: open, select, validation feedback, close, focus returns. | **P1** | Q4 | — | 🔴 **NEEDS REVISION (review 2, 2026-09-20) — still PROMOTED: gates 846's approval and Wave B. Re-enter at §17.** Review 1's whole 16d block is **closed**: `NotificationCenter` + `NotificationItem` migrated, storied and enrolled inside 861 (Task 862 stays folded, never issued), the ARIA clone is guarded to native-button triggers and the bell's `ActionIcon` carries it, AC5's placeholder re-measured, AC7's absolute corrected. Reviewer re-ran the gates natively: bell census 5/5 exit 0, `check:rendered-scope` 0 new/0 stale, `check:story-coverage` 95/95, build 0 and current. **Owed now: only F7 (P3)** — clause 9, `scripts/task319-qa-notification-templates.mjs:38` still resolves the deleted `notifications-notificationitem--all-cases` story id (new AC10). **F6 (P2) is the kickoff's own GR-4 defect and is already fixed in it** — R4c demanded chrome "match its pre-861 capture" while GR-0 forces two deltas (`#EBEBEB`→`#e4e7ec` canonical `Divider`; 150→200 ms) and no such capture exists; §13.3 tuples 5–7 now name both for the owner. **AC9 partly closed (§17.5)** — owner accepted the visual half 2026-09-20, ratifying both §17.1 deltas; the keyboard half (tuples 2, 3-keyboard, 4) is still owner-owed and is behaviour, not appearance. D1 ratified (§16.9). Hosted here by owner decision 2026-09-20 → [`…_Task_861_…`](Sprint_78_kickoff_prompt_Task_861_Keyboard_Operable_RangeDatePicker_Trigger.md) |
exit=0
### package.json script names mentioning qa/319
email: email dev --dir src/modules/notifications/lib/emails
### does it still resolve? deleted story id vs index
scripts/task319-qa-notification-templates.mjs
storybook-static/index.json
old id present: false | new id present: true
