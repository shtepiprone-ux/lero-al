# Project Backlog

> ⚠️ **HARD LIMIT: this file holds ACTIVE state only (~80 lines).** Historical ledger → [`docs/backlog-archive.md`](backlog-archive.md); full per-task detail → `docs/sessions/`.
> "Last Session" = 2–4 lines. When a task is reviewed/closed, move it to ONE row at the TOP of the archive ledger. See `docs/ai-behavior.md` → "Backlog & Session Log Rules".
> Reconciled against git history 2026-07-17 (`main` == `origin/main`): everything committed is pushed. The former per-task "pending owner run" narrative was stale (its detail already lives in the archive) and has been removed.

## Last Session (2026-07-29)

- **681 — COMMITTED `d383f7c15` (AC9 defect named in commit msg). 684 — ✅ `APPROVED WITH NOTES` (orchestrator review 2026-07-29) — R1–R10 all VERIFIED, closes 681's AC9.** 681: Sonner retired onto Mantine notifications (`notificationVariants.ts`, `src/lib/toast.ts` 4-method adapter, 169 call sites/33 files, both mounts unmounted, `sonner.tsx` deleted); D4 `info:blueLight` RATIFIED. 684: `MantineRootProvider.tsx`'s `<Notifications>` gained `top={{base:97, sm:65}}` — the only `src/` diff — derived from a live 16-cell header measurement (97px <390, 65px ≥390; `sm`=640 chosen since Mantine breakpoints have no 390 key, verified in `parse-style-props.mjs`); before/after rect tables + `getComputedStyle` cascade proof (never `16px`) + 16/16 cells `toast.top≥header.bottom`, 0 overflow, persisted at `.screenshots/task684-ac9/`; `--mantine-only` 0 FAIL/1162 PASS/22 AMBIGUOUS, 0 changed cells vs `2026-07-29T06-49` baseline (1 transient font-loading-flake FAIL on first run, investigated + cleared on rerun); `build` 40/40 exit 0 (after fixing a self-inflicted `.next` corruption from concurrent dev+build); `vitest` 1161/1163 (2 documented full-run-only timeouts, isolated 39/39); `hydration` 4/0/3skip. **Review (reviewer re-derived all 16 cells + the 1184-cell manifest diff independently; owner-native gates re-run: `build` exit 0/40 pages/full route table, typecheck 0, 4/4, 41/41, storybook 0):** D3 extension to all widths **RATIFIED**; `sm`=640 cutover with 390–639 over-cleared **RATIFIED as shipped**; **D5** — kickoff §18's scrolled-state checkpoint **WAIVED** (header has no scroll handler, `sticky top-0`, container `position: fixed` → risk nil), documented limitation; **D6** — `.screenshots/` evidence accepted **local-only** per `.gitignore:55`, closing 681-F4 locally. P3/NOTE carried, no gate: §8 porcelain misquote, guest-only auth cells, `top` emits `calc(6.0625rem * var(--mantine-scale))` not px, 0px slack by construction, unnamed aborted `14-17` run. **685 — ✅ `APPROVED WITH NOTES` (orchestrator review 2026-07-29) + COMMITTED `ef05a92e5`** (Q4): added `check:stories` **Check 15** (registered-colour gate, `theme.ts`-derived set, scope = 3 Mantine story/pattern dirs incl. `src/stories/patterns/mantine/`); natural planted-violation proof = exactly 2 violations pre-fix at `MantineNotificationPattern.tsx:81`/`AdminSurfacePattern.stories.tsx:79`, 0 post-fix, synthetic `cyan` plant/revert proven clean; both `blue` sites → `blueLight`. `checksRan` 14→15, 127 files unchanged; `scripts/__tests__/check-stories.test.ts`'s `checksRan` assertion bumped in the same task (unlike Check 14's historical drift). `--mantine-only` 0 FAIL/1162 PASS/22 AMBIGUOUS unchanged, 0 verdict changes vs `2026-07-29T14-20`; PNG-md5: 28/32 target cells changed (AdminSurfacePattern desktop-1024×4 locales unchanged — Badge only renders in the <640 card layout, confirmed in `MantineAdminSurfacePattern.tsx`/`MantineDataTableToCards.tsx`). **Review (reviewer re-derived independently: gate re-run in a clean repo copy → exactly 2 pre-fix violations at the same lines; AC3 machine-proven by adding `teal` to `theme.ts` → planted `teal` passes with 0 script edits, removing it restores exit 1; 1184-cell md5 + verdict diff recomputed; i18n/design-tokens/story-coverage/file-integrity (6 files — records included)/mojibake all re-run; owner-native `tsc` 0, `vitest` 91/91, `build` 40/40):** **D10** — AC8's byte-level clause ("no cell outside the 2 stories changed") **RE-SCOPED**: the 84 outside cells sit *below* the harness noise floor — three zero-code-diff control pairs differ by 75/91/98 cells across the same animated/async story set, and the one story with no prior flake history (`CopyIdButton` sq/320, byte-stable across 16 run pairs) was resolved by pixel inspection to that story's own 1.5s "copied→revert" timer. Binding comparator is **0 verdict changes**, met exactly. `tsc`/`build` 40/40/`i18n` 2215×4/`design-tokens` 44/0-stale (0 in touched files)/`story-coverage` 15/15 all 0; `vitest` 1161/1163 (2 documented full-run-only timeouts, isolated 39/39 both). `orange` sites untouched. P3 carried to 686, no gate: **F2** gate blind to expression-valued props (`color={…}`, 28 in scope — all currently resolve to registered colours), **F3** latent false positives on `transparent`/`currentColor`/`oklch()`/gradients + single-quoted props; NOTE: R1's multi-line tag-collection replaced by an equivalent-or-broader line scan (site 1 still found); test-file scope deviation ratified under cl. 9. **686 — ✅ `APPROVED WITH NOTES` (orchestrator review 2026-07-29) + COMMITTED `043ceac33`.** `orange` registered in `theme.ts` (TailAdmin §4, idx 6+7=`#fb6514`); Check 15 widened to all `src/`, forms B (`var()`)+C (`*COLOR*` map) added, F3 passthrough rewritten; ladder **7→1→0** (first-observed, no adjustment); `ROLE_COLOR.agent`→`blueLight` (D12, closes 685's divergence). 106/106 unit tests (91+15 new Check-15 block), 21/21 smoke (20 pre-existing+1 new `data-color` assertion), `--mantine-only` 0 FAIL/0 verdict changes (1 md5-outlier `LightboxView` investigated — Modal-transition capture noise, both runs verdict=pass), `build` 40/40. **Review (reviewer re-derived independently, not from the report: ladder rebuilt from `git show HEAD:` tree states → exactly 7→1→0 at the same 7 sites; all 3 plants + all 7 negative controls re-run; `checksRan` 15/`storyFilesCount` 127 re-derived; 1184-cell verdict diff recomputed → 0 changes/0 FAIL/identical 22-ambiguous set; all 1184 PNG md5s recomputed → the executor's 64-cell/11-story table reproduced exactly; `file-integrity` 7/7, `mojibake` 0/2001, `i18n` 2215×4, `design-tokens` 44/0, `story-coverage` 15/15 all re-run by reviewer):** R1–R12 all VERIFIED. **D14** — the `LightboxView/Default` en/390 md5 outlier **RATIFIED as capture noise**, extending the documented set: `LightboxView.tsx:66-88` renders `Modal.Root`/`Modal.Content` (Mantine transition), zero `orange`/`blueLight`/`ROLE_COLOR` refs → no causal path, all 16 of its cells `pass` in both runs. **D12 CONFIRMED** (not overridden); AC9's "14 pre-existing" **re-scoped to the observed 20** — registry `:45` count is stale. P3 carried, no gate: **F1** session-log §9 left the `file-integrity`/`mojibake` counts as "see below" with nothing below (reviewer re-ran, both clean); **F2** `isComment` is line-start-only so trailing comments are scanned — machine-proved (dropping `orange` from `colors:{…}` with the tuple comments intact yields 9, not 7, the 2 extra at `theme.ts:92`); **F3** Form C silently `continue`s on an unbalanced block and counts braces inside strings; **F4** stock-set regex needs exactly 2-space indent + its loud-fail branch is untestable (deviation 3); **F5** registry `:45` still says "14 tests". Session log: `docs/sessions/2026-07-29-task686-registered-orange-widened-colour-gate.md`. **687 still reserved** = enrol `Admin/AdminUsersTable` into the rendered manifest (686's visual delta has no pixel gate). **688 — ✅ `APPROVED WITH NOTES` (orchestrator review 2026-07-30).** `PopularLocationsView` de-Tailwinded: `Flex direction/justify` for layout, new colocated `PopularLocationsView.module.css` (8 gradients + scrim + `:hover`/`:focus-visible` + content `z-index`, all `var(--token)`/`color-mix`, 0 contributed violations); dead `popular-locations` class removed. `computed-before/after` diff **empty** (incl. measured focus-ring = `var(--ring)`, not `currentColor`). `design-tokens` **44→43** exactly. `--mantine-only` 0 FAIL/22 AMBIGUOUS unchanged, 0 verdict changes on all 1184 cells — **but PNG-md5 on the 56 target cells is NOT byte-identical**: investigated via same-tree stability control (0/56 diff) + fresh same-session pre/post A/B (still 56/56 diff, ruling out CDN/time drift) + `sharp` pixel-diff (≤28px/1.5M, max channel delta 1/255, always gradient-card AA edges) — sub-visual rasterizer rounding from the gradient-syntax swap, not a redesign; flagged for owner/reviewer call, not self-resolved. `build` 40/40, `vitest` 1177/1179 (2 documented full-run-only timeouts, isolated 39/39). **Review (reviewer re-derived independently, not from the report: `compare-manifests.mjs` re-run `20-43` vs `17-50` → 56 md5 / 0 verdict changes / 0 FAIL / 0 missing-baseline; all 56 PNGs decoded with a from-scratch pure-JS decoder → worst 48px, **max channel delta 1/255 in every one of the 56**; ambiguous payloads byte-identical incl. `scrollWidth=241, clientWidth=114/142/149/207` → layout provably unchanged; before-capture authenticity proven by `from-badge-new`/`from-badge-premium`/`to-brand-950`/`from-primary/80` now appearing nowhere in `src/`; `design-tokens` 43/0 + `stories` 0/127 + `story-coverage` 15/15 + `i18n` 2215×4 + `mojibake` 0/2004 + `file-integrity` 4/4 all re-run; owner-native `typecheck` 0, `build` 40/40 with a route table byte-identical to the log):** R1–R6, R8–R10 **VERIFIED**. **D17** — the 56-cell md5 delta **RATIFIED as a sub-perceptual deterministic rasterisation delta** (NOT capture noise — the same-tree control proves determinism, so D10/D14 do not apply); binding comparator for these cells re-scoped to **0 verdict changes + byte-identical assertion payloads + max channel delta ≤1/255**; `2026-07-29T20-43/` is the new baseline. P2 carried, resolved by constraint: **F1** `.scrim`'s `var(--color-black)` is a Tailwind `@theme` variable kept alive only by `dialog.tsx:35`/`sheet.tsx:32` `bg-black/10` → **Task 690**, hard predecessor of 691. P3/NOTE, no gate: **F3** log misdescribes the 3 AC1 grep hits as "all `pos=absolute`" (`:72` is `styles.absoluteFill`) + trailing clause; **F4** pixel worst-case under-sampled (28 reported vs 48 actual); **F5** AC1's regex was mis-specified at task design (matches the `pos="absolute"` R3 mandates) — orchestrator defect, fix in the de-Tailwind template; **F6** `compare-manifests.mjs` records only `newFailReason`, never the baseline reason (reviewer verified identity separately); **F7** `inset={0}` is Mantine `type:"size"` → `calc(0rem * var(--mantine-scale))`, equal only at zero; **F8** the vitest timeout pair is **run-varying** across the documented trio `date-format-ssr-parity`/`RangeDatePicker`/`saveSavedSearch.dedup` (owner's two runs failed different pairs), so the log's fixed-pair naming is run-specific. Session log: `docs/sessions/2026-07-29-task688-popularlocations-detailwind-cssmodule.md`. **690 — 🛑 `BLOCKED` (Sonnet execution, 2026-07-30, uncommitted).** D18's mechanism (relocate `--overlay`/`--overlay-foreground` declarations `@theme inline`→`:root`, keep `--color-overlay*` in `@theme`, repoint `.scrim` to `var(--overlay)`) was implemented exactly as specified, but its own safety assumption (§3.3: "compiled utilities already dereference `var(--overlay)` at runtime") is **falsified**: Tailwind can no longer statically resolve `--overlay`'s value once it's outside `@theme`, so every `bg-overlay*`/`text-overlay-foreground*`/`border-overlay-foreground*` opacity-modifier utility's *static-fallback* rule degrades from a computed hex (e.g. `#0000004d`) to a bare `var(--overlay)` ref (opacity lost) — AC1's own gate (before/after bundle selector-set `diff` empty) fails, reproduced identically across a clean `rm -rf .next` rebuild. The 2nd-in-cascade `color-mix()` rule is unaffected and still wins in any `color-mix()`-supporting browser (incl. the project's Chromium QA harness), so the regression is real but scoped to non-`color-mix()` browsers — a new owner decision is needed on whether that's acceptable or a third mechanism is required. Stopped at I3 per the kickoff's own stop condition (A2); I4–I8 not attempted. `src/app/globals.css` + `PopularLocationsView.module.css` left uncommitted with the D18 edit in place for orchestrator inspection. Session log: `docs/sessions/2026-07-30-task690-overlay-root-relocation.md`. **Orchestrator review 2026-07-30: `BLOCKED` UPHELD — the stop was correct and the design defect is the orchestrator's.** Reviewer re-derived the fallback degradation from the persisted selector sets and the live bundle: `.bg-overlay/30` `#0000004d`→`var(--overlay)`, `/50` `#00000080`→, `/60` `#0009`→, `/85` `#000000d9`→, `/95` `#000000f2`→, `.border-overlay-foreground/20` `#fff3`→, `.text-overlay-foreground/40|70` `#fff6`/`#ffffffb3`→ — alpha not re-expressed but **gone**, confirmed by Tailwind collapsing the now-identical rules into `.bg-overlay,.bg-overlay\/30{...}`. Severity accepted as bounded **by scope** but sharpened **by kind**: in a non-`color-mix()` browser `bg-overlay/30` (avatar) and `/60` (listing cards) render **solid black over the photo** — content obscured, not cosmetic; `text-overlay-foreground/*` degrades harmlessly. **Two orchestrator defects named: M1** — kickoff §3.3 quoted `#0000004d` in its own evidence block and then concluded "the utilities already dereference `var(--overlay)` at runtime"; the first cascade tier was read and ignored. **M2** — 690 shipped **no AC proving F1 was actually fixed**: AC1 proved nothing broke, AC2–AC5 proved the scrim/pixels were unchanged, but nothing proved the variable survives utility removal, i.e. the entire objective. **D19** — **dual declaration**: restore `--overlay`/`--overlay-foreground` to `@theme inline` (Tailwind resolves statically → composited fallback returns) **while keeping** 690's `:root` copies (unconditional emission → F1 closed) and its scrim swap; duplicated literal accepted, guarded by comment now and Task 692's gate later. **693 KICKOFF FILED** (`tasks/kickoff_prompt_Task_693_Task690_Revision_Overlay_Dual_Declaration.md`, **Q3**), starting from 690's **manifested dirty worktree** (4 entries + md5 content witnesses: `globals.css` `c5afe03a…`, module `b721ecf9…`, backlog `c97915760…`, 690's session log kept verbatim). Fixes M2 with a **two-sided planted control**: strip every `overlay` utility → the variables must still emit on the fixed arrangement **and must vanish** on the pre-690 `@theme`-only arrangement, so the control can demonstrably fail. AC1's comparator is 690's own pre-change `overlay-selectors-before.txt`, marked read-only precisely so a post-edit regeneration cannot make it self-satisfying. Recorded limitation: **the Chromium Q3 harness supports `color-mix()` and therefore cannot see the fallback tier at all** — which is exactly how 690's regression would have reached review green; AC1's bundle-level identity is the only evidence covering it. **693 — 🛑 `BLOCKED` (Sonnet execution, 2026-07-30, D19's `globals.css` edit left in place, uncommitted).** R1–R4/R6/R7/R9 all VERIFIED clean: I2's dual declaration implemented exactly per D19; I3's bundle selector-set diff vs the pre-690 baseline is **empty** (restores the alpha fallback byte-for-byte, reproduced on a clean rebuild); I4's computed-style diff is **empty**, scrim byte-equal to the Task 688 string; module md5 unchanged, `design-tokens` 43/0 stale unchanged. **I5's two-sided planted control (R5/AC2) failed at its second half**: stripping all 33 `overlay` utilities from the 7 consumer files, then reverting `globals.css` to the pre-690 `@theme`-only arrangement, should show `--overlay`/`--overlay-foreground` **absent** — instead both are **still emitted**. Isolated by diagnostic (neutralizing an unrelated leaked `bg-overlay/30` string in dead script `scripts/task608-qa-listingcard-list-site.mjs:59` changed nothing): the true cause is that Tailwind's `@theme inline` usage-contingency also treats the non-Tailwind `var(--color-overlay*)` references in `LightboxView.tsx`/`MantineListingGalleryPattern.tsx` (A4-protected, untouched) as "in use" — meaning **F1's own premise (these files are invisible to Tailwind's scanner) may be false**, and may never have been a real production defect. D19's fix itself is not wrong (I2–I4 all pass), but AC2's objective proof cannot be demonstrated without editing files A4 forbids touching. Full plant/experiment cycle restored and re-verified byte-identical to `HEAD` (`git diff --stat` empty on all 8 transiently-touched files), final clean rebuild reconfirms I3's empty diff. Session log: `docs/sessions/2026-07-30-task693-overlay-dual-declaration.md`. **Orchestrator review 2026-07-30: `BLOCKED` UPHELD — stop correct, defect is the orchestrator's (M4).** Reviewer proved statically, no build needed, that **Tailwind v4 does not scan `.css` files**: `bg-gradient-to-br` exists as literal text in 2 `.module.css` files and nowhere in `.tsx`, and `.bg-gradient-to-br` is **absent** from the bundle, while `.tsx`-resident `bg-overlay` **is** emitted. Two opposite consequences. (1) **F1 is NOT invalidated** — `--color-black` has zero occurrences in scanned source (only the unscanned `.module.css`), is not declared or referenced inside `@theme` (Tailwind default-palette entry, absent from `globals.css`), so its sole lifeline really is `bg-black/10` at `dialog.tsx:35`/`sheet.tsx:32`; no confounder. (2) **The control's subject was structurally immune** — the overlay pair carries 2 extra lifelines a plant cannot remove: 6 literal `var(--color-overlay*)` occurrences in scanned `.tsx` (`LightboxView` ×5, `MantineListingGalleryPattern` ×1, both A4-protected) plus the intra-`@theme` self-reference `--color-overlay: var(--overlay)` (`globals.css:44-45`). So the original I5.3 could never return `ABSENT`; it was not a failed experiment but a test incapable of detecting its own effect — **M4**, same family as 690's **M2**. Executor's diagnosis was directionally right and its refusal to chase the cause into A4 files was correct; it could not have known, because the kickoff framed F1 through the overlay pair. **D20** — amend 693 in place: re-point I5 at **`--color-black`**, plant = remove `bg-black/10` from `dialog.tsx:35`+`sheet.tsx:32` only, two arms (unplanted → present, planted → **absent**), full revert proven via `git diff --stat`. Needs no A4-protected file and is the same temporary-plant mechanism already authorised. **D19's code ships unchanged** — I3/I4 passed on their own terms; execution **resumes at I5**, I0–I4 not re-run. Corrected in the kickoff: 690 and 693-rev1 both called the `.tsx` `var(--color-overlay*)` sites "invisible to Tailwind's scanner" — false; what is invisible is a `.css` file, which is precisely why the scrim's own reference could not sustain `--color-black`. **691 reserved** = `MantineListingCardPattern` (28) + `ListingCard` (8) de-Tailwind slice, blocked on **693**. **D19's `@theme` copy is transitional, with a named expiry — owner question 2026-07-30, "чи не лишаємо ми цю частину на Tailwind?".** It serves only the 33 surviving `bg-overlay*`/`text-overlay-foreground*`/`border-overlay-foreground*` utilities across 8 files (`MantineListingCardPattern` 6, `PerfDevOverlay` 10, `ListingGallery` 5, `LightboxView` 4, `MantineListingGalleryPattern` 3, `ImageUpload` 3, `AdminUserAvatar` 1). The fallback tier is a **Tailwind artifact, not a property of the token**: no opacity utilities → no second cascade tier → nothing to composite → the whole D18/D19 problem disappears rather than being worked around. Endpoint split into **694** (alias onto Mantine — `:root` currently holds 11 Mantine aliases vs 44 hand-authored literals, and the overlay pair belongs in the first group) and **695** (delete the `@theme` copy + namespace once utility count hits 0). 695 is the de-Tailwind **exit condition** for this pair and must not be lost as an implicit "someday". **692 reserved** = Q4 gate failing when a `.module.css` consumes an `@theme` variable whose last Tailwind-utility consumer disappears (the general form of F1; own blast radius, not a rider on 690). **689 reserved** = tokenise the shared section-heading `fz` triple `1.25/1.5/1.875rem` project-wide — deliberately NOT a 688 drive-by, since fixing one file alone desynchronises the homepage headings. **Site census CORRECTED 2026-07-30 (the original reservation was wrong twice):** the real set is **5 lines / 4 files = 15 of the 43 violations** — `page.tsx:49` + `:77`, `HowItWorksSteps.tsx:27`, `FeaturedListingsView.tsx:46`, `PopularLocationsView.tsx:46`. (1) `LatestListingsView.tsx` was named as a site but contains **zero** rem values — the "latest" heading lives at `page.tsx:49` via `tl('latest')`, which is likely what caused the misattribution in the 688 kickoff §17 grep claim. (2) `HowItWorksSteps.tsx:27` carries the identical triple and 3 violations, and was **missed entirely**. The old "`page.tsx` 14" was that file's *total* design-tokens count, not its triple count — only **6** of those 14 belong to 689. **Trap for the 689 executor:** `page.tsx` holds two other rem scales that must NOT be swept in — `:30` is the hero `<Title order={1}>` triple `1.875/2.25/3rem` (shares `1.875rem` with the section triple) and `:33` the hero subtitle `1.25/1.375rem` (shares `1.25rem`); `:27` is `4rem`/`6rem` section padding. A find-replace on either shared literal corrupts the homepage hero. Prior session logs (681/684/685, unchanged): `docs/sessions/2026-07-29-task681-sonner-retire-mantine-notifications.md`, `docs/sessions/2026-07-29-task684-notification-header-clearance.md`, `docs/sessions/2026-07-29-task685-unregistered-colour-gate.md`.

## Prior Session (2026-07-28)

- **671/675 — IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** `FiltersPanel.tsx` de-Tailwinded via canonical `MantineFilterSection` (17 sections, 0 raw elements/`className`, `check:story-coverage` 13/13); D3 divider derivation planted-violation proven; all 4 suites green pre-675 (38/38). **675 (2026-07-28) revision** (orchestrator review found 3 defects + 2 record errors, amended in place — code not reverted): label colour pinned `c="gray.5"` (`#667085`, D4 owner decision, clause 16a — no TailAdmin row for a 12px uppercase micro-heading); Drawer title `<p>`-in-`<p>` DOM-nesting warning fixed (`component="span"`); `isFirstVisible`→`withTopDivider` rename (predicate unchanged); Task 671's kickoff/session-log divider before-hex corrected (never-rendered value → measured `#E5E5E5`); R7/R8 downgraded `PARTIALLY IMPLEMENTED` (harness is 4-width `MANTINE_VIEWPORTS`, not the 14-width canon — Task 678 reserved) + owner native visual waiver recorded (480/560/680/768×sq/en/uk/it); R13 marked `NOT VERIFIABLE` (Storybook fixture renders raw enum labels, not localized — Task 679 reserved). `npx vitest run` 39/39 (38+1 new), planted-violation genuinely failed + reverted. `build` 40/40, `screenshots:assert --mantine-only` both stories 16/16, label measures `#667085`. Sessions: `docs/sessions/2026-07-28-task671-filterspanel-detailwind-filtersection.md` + `docs/sessions/2026-07-28-task675-task671-revision-label-color-dom-nesting.md`.
- **669 — IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** `MantineHomeSection`'s vertical rhythm (48/64/80px) moved from a CSS-module `@media(min-width:1536px)` step to a Mantine `py={{base,md,xxl}}` responsive prop consuming the same tokens, rebound onto Mantine's own `xxl`=90em/1440px (owner decision 2026-07-28, closes OQ3); `PopularLocationsView` retired its own Tailwind band for the canonical `MantineHomeSection variant="muted"` (manifest 13/13→14/14). New `MANTINE_STORY_EXTRA_VIEWPORTS` cells (1200/1440/1536) on `HomeSection`+`PopularLocationsView`: both 28/28 cells, 0 FAIL, 22 AMBIGUOUS unchanged (Combobox/Tabs pre-existing + PopularLocationsView's own intentional-ellipsis LongCityName story). Measured `HomeSection` muted-band height h(1200)=258px, h(1440)=290px (Δ32 exactly), h(1536)=290px (equal) — proves the new step, disproves any residual 1536 rule; emitted rule confirmed `@media(min-width: 90em)` on all 5 homepage bands via live SSR capture. `page.tsx`/`PopularLocations.tsx`/`theme.ts` absent from diff. `tsc`/`check:stories`/`check:story-coverage`(14/14)/`build-storybook`/`build`(40/40)/`check:hydration`(4/4 public routes PASS)/`check:i18n`(2215×4)/`check:design-tokens`(0 new in touched files)/`file-integrity`/`mojibake` all 0; `vitest` 1156/1156 (2 unrelated full-run timeouts, isolated-rerun 39/39). Session log: `docs/sessions/2026-07-28-task669-homesection-xxl-rhythm-popularlocations-band.md`.

## Prior Session (2026-07-26)

- **668 — IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** Featured/Latest homepage grids migrated to Mantine `SimpleGrid`/`Group`; large-desktop column step moved from Tailwind `2xl`(1536px) to Mantine `xxl`(1440px) — owner-approved adaptive change (Featured 3→4 cols, Latest 2→3 cols at 1440–1535px only; every other width/gap/locale unchanged). New canonical `Patterns/Mantine/HomepageListingGrids` story enrols both Views in `mantine-migration-scope.json` (`check:story-coverage` 10/10). New `task668-qa-grid-1440.mjs` `--baseline`/`--verify` harness (dual locator): 160/160 PASS both modes; before/after table confirms only the 1440–1535 band differs. `task420-qa-grid-step.mjs` given per-story expected-cols + per-story locator (Featured mechanism-agnostic, Similar unchanged Tailwind-token): 88/88 PASS. `tsc`/`build`/`check:stories`/`check:story-coverage`/`governance:screenshots`/`governance:components` all exit 0; full `screenshots:assert` manifest-delta: zero new FAIL on task-owned cells, total FAIL 953→952 (not higher); `--mantine-only` exit 0/0 FAIL both before+after (1048→1080 cells, 22 AMBIGUOUS unchanged); locale-leak delta clean (1 pre-existing unrelated leak, unchanged before/after). **Revision-7 remediation (2026-07-27, Phase D only, no product-code change):** F3 one-line `task668-qa-grid-1440.mjs` fix (missing-baseline branch now sets `row.pass=false`), re-verified 160/160 PASS; new `task668-qa-header-geometry.mjs` synthetic-`gap:0` harness proves the `Group` `column-gap:16px` change is absorbed by free space at every cell (12/12 PASS, 0 escalations, tightest case `uk@320` still 0px rect delta) — AC5/AC13 rewritten per orchestrator F1/F7 findings, both VERIFIED. Session log (same file) §12.
- **666 — IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** `check:mojibake` `run()` now partitions tracked-but-disk-absent paths out before the scan loop (existsSync guard, precedent `check-file-integrity.mjs:185`) instead of crashing `ENOENT`; skipped paths print visibly, present count unchanged. Live repro: before=crash, after=exit 0 (1886 scanned, 4 skipped = the Task 665 deletions). Planted-violation proof: exit 1 with plant, exit 0 reverted, plant absent from final `git status`. Scanned-set diff before/after = exactly the 4 absent paths, nothing else. `tsc`/`build`/`check:file-integrity` all pass. Unblocks 665's `check:mojibake` gate. Session log: `docs/sessions/2026-07-26-task666-mojibake-deletion-tolerance.md`.
- **665 — PARTIALLY IMPLEMENTED (route-parity unverifiable in this sandbox; §16 R13/R14 now closed).** Container/Presentational View split (no mocks) for `FeaturedListings`/`LatestListings`/`RecentlyViewedGrid`/`SimilarListings`; 4 rebuilt `System/*` stories statically import the real Views; deleted `System/ListingGrid` + `StoryListingCard` fake-card cluster (`rg`=0) + `MantineCardGrid` cluster (`rg`=session-logs-only). **Second pass (§16 post-review revision):** re-pointed screenshot-governance coverage from the deleted `ListingGrid` to `System/FeaturedListings/Default` across `responsive-screenshots.mjs`, `component-catalog.mjs`, `tailwind-entropy.allowlist.json`, and 6 governance docs (R13, `governance:screenshots`/`governance:components` now exit 0 — first-pass gate break fixed); added real `ASSERT_STORIES` rendered-proof entries for Featured/Latest/Similar, previously absent (R14) — `screenshots:assert:fast` now 1827/2116 PASS, 219 FAIL unchanged vs baseline, 0 FAIL among the 3 new stories. `tsc`/`check:story-coverage`/`check:stories`/build/`build-storybook`/`screenshots:responsive`/`check:file-integrity` all pass. **Still missing:** §13.7 pre/after live-route byte-identical DOM/computed-style baseline (AC1/AC8 — needs a routable dev server + seeded local/test DB + browser capture, unavailable here; use the §16.1 corrected JS `classList.contains` locator predicate for Featured/Latest — session log §10). `check:mojibake` now unblocked by Task 666; still needs owner to stage the deletions. Review 2026-07-26: `PARTIALLY VERIFIED`; §16.4's AC8 "0 FAIL … and the RVS cell" clause is unsatisfiable (RVS/Populated is 12/12 FAIL in the pre-change baseline too) and needs amending. Session log: `docs/sessions/2026-07-24-task665-storybook-listing-view-splits.md`.

## Prior Session (2026-07-23)

- **664 — IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** Reshaped the `AmbiguousOverlap` planted fixture (Task 663 finding N2, its own §5.3): popup offset to `left: 60` (was `left: 0`) so trigger/popup rects partially overlap without either containing the other (`[16,24,136,64]` vs `[76,24,196,64]` at 320/375/390 × en — byte-identical across all 3 widths), restoring Check 4's R1 `isAbsoluteOverOwnTrigger` branch instead of the Task-611 `isContained` short-circuit. OLD-fixture-vs-NEW-fixture proof against the unmodified committed harness: OLD (byte-identical/contained rects) = `pass`; NEW = `ambiguous-overlap` — the reshape alone flips it. `screenshots:assert -- --fast`: `Planted/AmbiguousOverlap` now 12/12 cells (320/375/390 × sq/en/uk/it) `ambiguous-overlap`, none FAIL; total run AMBIGUOUS 46→58 (+12 exactly), PASS 1831→1819 (−12 exactly), FAIL 219/OUT-OF-RANGE 12 unchanged — no other planted or Mantine fixture affected. `geometry-integrity.mjs` byte-for-byte unchanged (only fixture markup + one `ASSERT_STORIES` comment touched). `tsc`/build (40/40 static pages)/`check:stories`/file-integrity/mojibake all pass. Session log: `docs/sessions/2026-07-23-task664-ambiguousoverlap-fixture-reshape.md`.
- **663 — IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** `geometry-integrity.mjs` Check 4 cross-overlay-boundary branch now downgrades to a silent PASS only when a real, verified `.mantine-Overlay-root` backdrop covers the background element (new `isBackgroundCoveredByOverlayBackdrop`); otherwise unchanged `ambiguous-overlap`. Two new planted fixtures (`OverlayBackdropCovered`/`OverlayNoBackdrop`) + `ASSERT_STORIES` registration; OLD/NEW proof shows `OverlappingActions`/`ScrollVisibleOverlap`/`OverlayNoBackdrop` byte-identical (non-weakening), `OverlayBackdropCovered` flips ambiguous→pass. Full `--mantine-only`: backdrop-reason ambiguous rows 25→4 (Combobox/RangeDatePicker/NotificationBellView), `0 FAIL` both before/after, +21 passes = −21 ambiguous exactly. **Finding (out of scope, flagged for follow-up):** `AmbiguousOverlap` fixture's verdict is unaffected by this diff but was already silently broken pre-existing (Task-611 containment-guard interaction) — see session log §5.3. `tsc`/build/`check:stories`/file-integrity/mojibake all pass. Session log: `docs/sessions/2026-07-23-task663-harness-backdrop-overlap-downgrade.md`.
- **662 — IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** New canonical `MantineHomeSection` (CSS-module tokenized vertical rhythm 48/64/80px + `muted`/`brandFade` background) replaces the 4 homepage Tailwind band wrappers (`py-12 md:py-16 2xl:py-20`, `bg-muted/30`, `bg-gradient-to-br from-primary/10 to-primary/5`); live before/after computed-style capture on the real app route found **0/700 diffs** at 375/768/1440/1536/1920 × sq/en/uk/it. `Patterns/Mantine/HomeSection` story + `mantine-migration-scope.json` registration; `check:story-coverage` 8/8; `tsc`/build/`screenshots:assert --mantine-only` (full) all pass. Session log: `docs/sessions/2026-07-23-task662-mantine-home-section-tokenized-band.md`.
- **661 — IMPLEMENTED, AWAITING ORCHESTRATOR REVIEW.** Single-source brand color: new `src/design-system/brand.ts` (canonical tuple, `theme.ts` imports it); `globals.css` `--brand-*` now alias `var(--mantine-color-brand-*)` (light+dark), so `--primary`/CSS-var brand surfaces render true `#EC5447` (was `#D25656` drift, closes 660's open decision); `--brand-850` deleted (zero consumers), `--brand-950` kept hand-authored (non-tuple, documented); emails (`BaseEmail.tsx`, `auth-email-hook/route.ts`, `contactInquiry.ts`, + 3 files found beyond task scope: `listingInquiry.ts`/`emailChange.ts`/`sendTemplatedEmail.ts`) now import the constant instead of hardcoding hex. `tsc`/build/screenshots:assert/i18n/mojibake/stories all pass. Session log: `docs/sessions/2026-07-23-task661-single-source-brand-color.md`.

## Open — needs action

**Homepage → Mantine *band/hero chrome* migration ✅ COMPLETE** (A/646 · C/647 · B/648–649 · D/650 — all approved + archived); FavoriteButton (653) + SaveToCollectionButton trigger (654) close the last legacy-styled controls on that path.

**Homepage full render tree → Mantine: NOT complete** (orchestrator audit 2026-07-26 — a prior 651–654 claim of "100% Mantine" was retracted; 651–660 detail is in the archive ledger, 655 RETIRED/VOID). **668**/**669** are IMPLEMENTED, AWAITING REVIEW; **670**/**672** are ✅ APPROVED (see Last Session). Still outstanding: **671** `FiltersPanel` (27 raw els / 32 `className`, mounted unconditionally by `HeroSearchView`) · **673** `HeaderView`/`FooterView` = **hybrid** (real Mantine API + Tailwind visuals; `HeaderView` also `unstyled` everywhere per Task 629) — needs an owner-approved **definition of "migrated"** before any percentage claim is meaningful. **Task 681 (Sonner→Mantine notifications retirement) is reviewed `NEEDS REVISION`** — 11/12 verified, committed-with-known-defect pending **Task 684** (D3 narrow-width notification top offset + AC9 re-capture; kickoff `tasks/kickoff_prompt_Task_684_Task681_Revision_Notification_Header_Clearance.md`). Nothing pushed until 684 is approved; 684 starts from a clean tree. **673** (`HeaderView`/`FooterView`) is unblocked: owner ruled 2026-07-29 that "migrated" = **mechanism-only, as in 672** — raw elements → Mantine primitives, visuals preserved until a TailAdmin row exists (none exists for header/footer, confirmed by grep). **667** = route-oriented inventory only; manifest enrolment happens story-first inside each slice, since `check:story-coverage` fails any component enrolled without a `Mantine/Primitives/*` or `Patterns/Mantine/*` story. **683** reserved: Task 672's deferred TailAdmin bottom-nav conformance slice (no reference row exists yet).

> **Governance note (verified 2026-07-26):** no CI gate asserts the Mantine composition of a *route*. `--mantine-only` runs scope by **Storybook title prefix** (`scripts/lib/mantine-story-scope.mjs`), never by manifest; only `check:story-coverage` reads `mantine-migration-scope.json`, and treats anything absent from it as out of scope. Route-oriented checks exist (`check-hydration-console.mjs` walks `/en`,`/sq`,`/uk`; `check-header-id-parity`; `validate-production-lcp`) but none measures markup composition. `8/8` proved coverage of eight enrolled components — not the homepage.

**Deferred / on hold:** **560** admin suspension-as-date-range (DB/RLS/server action) · **463** (Epic BB) full admin report management — ON HOLD until Epic MM primitives complete, kickoff `tasks/Epics/Epic_BB_kickoff_prompt_Task_463_AdminReportFullManagement.md` · **Epic HH** 310 (P4 content/settings), 311 (P5 modals, partial), 313 (P6 Verified Agents, blocked on owner DB sign-off), 308/309 re-scope onto canonical primitives · **453** (Epic KK.2) remaining admin-manager freshness · **SaveToCollection dialog → Mantine** (follow-up from 654) — collections `Dialog`/`Input`/in-dialog `Button`s still shadcn.

## Pending Action Items (owner)

| Item | Notes |
|---|---|
| 🔐 Re-verify HIBP "leaked password protection" availability on Supabase Free tier (Auth → Password Security); enable now if available, else at Pro upgrade. | Security Advisor `auth_leaked_password_protection` WARN. `docs/integrations.md`. |
| 👁️ Eyeball-verify notification localization under `/sq` (data-only fix applied 2026-07-14; creation code already correct). | Re-open the bell under `/sq`. |
| 🐞 `/listings` Grid horizontal overflow <640px (FilterBar segmented `flex-1` + `min-w-35` Combobox push `scrollWidth` past the viewport at 320/375/390). Needs its own task. | Traced via DOM offender scan; out of Task 603 scope. |
| 🖋️ Verified Agents DB schema sign-off (Task 313) + verified-badge public visibility. | Epic HH blocker. |

**Task numbering — last used: 693. NEXT FREE: 696** (684 = Task 681 revision, D3 notification header clearance, ✅ APPROVED + COMMITTED `081c03e7f` + PUSHED 2026-07-29; **685** = unregistered-Mantine-colour gate + the two `color="blue"` sites, ✅ APPROVED + COMMITTED `ef05a92e5`; **686** = registered `orange` ramp + `ROLE_COLOR.agent`→`blueLight` + Check 15 widened to all `src/` (forms A/B/C), ✅ APPROVED WITH NOTES + COMMITTED `043ceac33` 2026-07-29 (D11/D13 owner, D12 derived from D4/D8, D14 reviewer); **687 reserved** = enrol `Admin/AdminUsersTable` into the `--mantine-only` rendered manifest (title-prefix enrolment adds 16 cells to a hard-blocking CI gate — own blast radius, not a rider on a colour fix); **688** = `PopularLocationsView` de-Tailwind onto Mantine props + a colocated CSS module, ✅ APPROVED WITH NOTES 2026-07-30 (D15/D16 owner, D17 reviewer: md5 comparator re-scoped to 0-verdict-change + ≤1/255); **689 reserved** = project-wide section-heading `fz` rem-triple tokenisation, census corrected 2026-07-30 to 5 lines / 4 files / 15 violations (`page.tsx:49,:77`, `HowItWorksSteps.tsx:27`, `FeaturedListingsView.tsx:46`, `PopularLocationsView.tsx:46`; `LatestListingsView` is NOT a site, `HowItWorksSteps` was missed — see Last Session for the shared-literal trap in `page.tsx`); **690** = `--overlay`/`--overlay-foreground` relocation `@theme`→`:root` + scrim swap, 🛑 BLOCKED 2026-07-30, **D18 SUPERSEDED by D19 — revision filed as 693**; **691 reserved** = `MantineListingCardPattern`+`ListingCard` de-Tailwind, blocked on **693**; **692 reserved** = Q4 `@theme`-dependency gate, extended by D19 to also assert the dual declarations stay in sync; **693** = 690 revision, dual declaration in **both** `@theme inline` and `:root`; first `BLOCKED` 2026-07-30 at I5, **kickoff amended in place by D20 and re-issued** — D19's fix itself verified clean (I2–I4 all green) and unchanged, only the control's *subject* was wrong (M4: the overlay pair has 2 lifelines a plant can't remove, so `ABSENT` was unreachable by construction); I5 re-pointed at `--color-black` with a 2-line plant on `dialog.tsx:35`/`sheet.tsx:32`, executor resumes at I5 without re-running I0–I4; **694 reserved** = alias `--overlay`/`--overlay-foreground` → `var(--mantine-color-black/white)` per the Task 660/661 no-second-hand-authored-source convention (`globals.css:311-315`); Mantine already emits both (bundle-verified) — independent of Tailwind, runnable straight after 693, but NOT foldable into it since it changes the value expression AC1/AC5 compare on; **695 reserved** = delete the `@theme inline` `--overlay*` copy **and** the `--color-overlay*` namespace entries once the last `bg-overlay*`/`text-overlay-foreground*` utility is gone — the de-Tailwind exit condition for this token pair, blocked on 691 + the remaining slices; 682/683 reserved). Awaiting review: 662–664, 666, 668, 669, **671/675**; **681 `NEEDS REVISION`** (blocked on 684); 670 + **672** approved + archived; 665 partially implemented; 667 + 673 reserved for the homepage-tree audit; 676 (globals.css stale hex comments), 677 (`<div>`-in-`<p>` pre-existing warning), 678 (14-width matrix enrolment), 679 (`usePropertyTypes` fallback localization), 680 (`check:locale-leak` lowercase blind spot) all reserved by Task 675 §8; **683** (Task 672's TailAdmin bottom-nav conformance slice) reserved by Task 672; **684** (681 revision, Q4) filed, **685** (`MantineNotificationPattern.tsx:81` info-trigger `<Button color="blue">` → registered colour, 681 review P3) and **682** (drop `sonner`/`next-themes` from `package.json` — sole `src/` consumer dies with 681) reserved by Task 681. 641/642 closed; 613/621/625/627/629/630 + 644–654 + 656–660 all APPROVED + COMMITTED + archived (detail → archive ledger). 655 RETIRED/VOID. 622/623 are not plain numbers (`Q0R`/`623R` are lettered IDs outside the numeric sequence); 628 reserved for the SB10 lint-debt fix (627 follow-up). **Governance: `npm run build` exit 0 mandatory for non-Q0 (`67340ff49`).**

## Reserved / deferred / retired

- Reserved: 310, 311, 313 (Epic HH), 453 (Epic KK.2).
- Deferred (no #): **I.3** listing-status helper API `(status) → (listing)` — see `docs/domain-rules.md` → "Future ListingStateMachine evolution trigger".
- Retired (never reuse): 465 (uk→ua migration, cancelled); 466/467/469–481 (legacy-primitive layout, superseded by the Mantine migration); 534 (superseded+closed by 535); 597/604 (superseded by 598/605); 655 (incorrectly-created task-design defect, no code shipped — superseded+reverted by 656).

## Active Epics — open (closed epics → archive)

| Epic | Status | Plan |
|---|---|---|
| **HH — Admin UX System** | OPEN — 310 (P4), 311 (P5 partial), 313 (P6, blocked on owner DB sign-off); 308/309 re-scope onto canonical primitives | [`Epic_HH_…`](../tasks/Epics/Epic_HH_Admin_UX_System.md) |
| **BB — Listing Inquiries: Report & Message** | ⏸️ ON HOLD — Task 463 held until Epic MM primitives complete; earlier BB tasks (242/243/430/435/458–462) done | [`Epic_BB_…`](../tasks/Epics/Epic_BB_Listing_Inquiries_Report_and_Message.md) |
| **II — Global i18n Hardening** | Implemented + reviewed (316–323); committed + pushed | [`Epic_II_…`](../tasks/Epics/Epic_II_Global_i18n_Hardening.md) |
| **KK — Admin Data Freshness** | 452 (KK.1) done; 453 (KK.2) reserved for remaining managers | [`Epic_KK_…`](../tasks/Epics/Epic_KK_Admin_Data_Freshness.md) |
| **MM — Mantine/TailAdmin Restyle** | IN PROGRESS — Sprints 37–44 landed + pushed (form controls, overlays, TailAdmin conformance, Combobox/DatePicker family, Header/app-shell decomposition, listing card/detail). Next primitives per tracker | [`Epic_MM_…`](../tasks/Epics/Epic_MM_Mantine_UI_Migration.md) · tracker `docs/mantine-tailadmin-migration-tracker.md` |

## Standing notes

> **Frozen/deferred (reviewed 2026-06-05):** Sprint 28 admin-mobile 308/309 must be re-scoped onto the DS + canonical primitives before resuming; Epic HH P4/P5/P6 per the table above; I.3 deferred (valid). Every open task consumes the global Design System (`docs/design-system.md`, Task 340) wherever UI/responsive/overlay is touched, and since the Mantine freeze (Task 482) new UI uses Mantine (`docs/mantine-responsive-design-system.md`); `docs/rule-index.md` sets the per-task pre-read.

> **🟡 Console NOISE (not bugs — do not re-triage as P0):** `[PRED] … preloaded`, `[LCP] … route`, `[Vercel Speed Insights] debug`, "speculation rule set … will be ignored", Turbopack dev-only CSS-chunk/`*.woff2` preload warnings — all dev/debug artifacts, gone in prod. OpenTelemetry `import-in-the-middle` resolved by Task 450 (`322c5d599`); Cloudinary LCP "preloaded but not used" by Task 437. A stale Turbopack `next dev` HMR cache can emit a one-off React `useId` `mantine-_R_…-target` hydration error that does NOT survive a clean `next build` + fresh dev restart and does NOT reproduce in prod — re-verify with `check:hydration` against a freshly restarted server before triaging any `_R_` id mismatch as a code bug (Task 582).

> **Commit emission policy:** the orchestrator emits explicit-path `git add`/`git commit` per task at review time (never `-A`/`-u`/wildcards, never pre-staged batches); the owner runs them in PowerShell. Before emitting, reconcile `git status --short` + the real diff + the session `Files Changed` table (STATUS/REPORT MISMATCH gate). Each commit is reconstructable from the session log.

> **Acknowledged advisor exceptions** (intentional, no task): `pg_net in public` (deferred); `email_change_tokens` RLS-enabled-no-policy — see `docs/rls-rules.md` → "Acknowledged Advisor Exceptions".

> **Standing governance:** `ai-behavior.md` Notes 18–23 + `agent-contract.md` (P0, clauses 1–15) + `rule-index.md` (task-type pre-reads) + the Positive/Negative flow rule (`orchestrator-role.md`). Non-optional acceptance gates on every task.

## Archive

Completed tasks, sprints, and epics live in **[`docs/backlog-archive.md`](backlog-archive.md)**.
