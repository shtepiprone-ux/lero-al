# Task 749 — unwaivable rule-compliance ledger

Completed from `docs/orchestrator-rule-compliance-ledger-template.md` on 2026-08-15, against the final kickoff text
and the current worktree (branch `codex/fix-task741-lint-baseline`).

| Rule source and exact clause | Applicability evidence | Exact mandatory outcome | Evidence artifact / command | Result |
|---|---|---|---|---|
| `docs/agent-contract.md:118` cl. 11 — mobile/overlay behavior | All three defects are text controls below 640px | "For in-scope UI below 640px, text controls use the full width" | AC3, AC4, AC6, AC7; `screenshots:assert -- --mantine-only` exit 0 | `COMPLIANT` |
| `docs/agent-contract.md:145` cl. 16a — no invented visual value | A 44px floor and a wrapping tab strip are visual values | Provenance, not invention | `--space-11` (`globals.css:145`, "44px — touch-target floor (§12a)") reused; wrapping is Mantine's own `styles.tab` slot; **no new token, no raw literal** | `COMPLIANT` |
| `docs/agent-contract.md:155` cl. 16c — canonical stories are the visual source | All three components are enrolled stories | Canonical story must exist and be used, not bypassed | `HeroSearch.stories.tsx`, `NotificationBellView.stories.tsx`, `AdminUsersTable.stories.tsx` all enrolled and asserted; `HeroSearchView.tsx` is in `scripts/mantine-migration-scope.json` | `COMPLIANT` |
| `CLAUDE.md` — "every task belongs to a sprint", kickoff at `tasks/Sprints/Sprint_NN_kickoff_prompt_Task_NNN_<Slug>.md` | New task 749 | Sprint first, then kickoff, never a root-of-`tasks/` path | `tasks/Sprints/Sprint_58_Rendered_Proof_Eighteen_Blocking_Cells.md` opened first; kickoff saved at the mandated path | `COMPLIANT` |
| `CLAUDE.md` — sprint chosen by **goal fit**, from `docs/backlog.md` "Sprints", never "highest number" | 4 sprints read as 🟠 OPEN: 46, 55, 56, 57 | Pick the sprint whose goal the task fits; if none, open the next number with its own plan file | Goal-fit table in the Sprint 58 plan file; 46 = ListingCard de-Tailwind/overlay, 55 = ARIA, 56 = enum leaks, 57 = removal — none fits | `COMPLIANT` |
| `.claude/skills/create-task/SKILL.md` — permanent Storybook story creation gate | The task touches three storied components | Record candidates + disposition; `create canonical`/permanent `extend` needs a named production consumer or quoted owner authorization | **No story file is written.** All three canonical stories already render the real production composition; disposition is `reuse` for all three (kickoff §3.5, §4) | `COMPLIANT` |
| `.claude/skills/create-task/SKILL.md` — no owner exception without a quoted decision, date and scope | D-2 supersedes Task 593 (2026-07-14) | Quote or precisely reference the actual decision, else `BLOCKED -- OWNER DECISION REQUIRED` | D-1/D-2/D-3/D-4 quoted verbatim with date 2026-08-15 in kickoff §2; the requirement that made this a blocked decision is cited at `Sprint_53_kickoff_prompt_Task_724R_FullWidthButtons_Revision.md:145` | `COMPLIANT` |
| `docs/backlog.md:74` — recurring orchestrator failure mode: every kickoff needs a two-armed plant that can demonstrably fail | This is a fix task with a live gate | Two-armed plant + pre-plant census | Six arms in kickoff §9, each row naming which AC consumes it; P1b and P2b are the "which half is load-bearing" arms | `COMPLIANT` |
| `docs/backlog.md:74` corollary (724 ②) — a gate's own exclusion selector is an attack surface; every exemption must be a condition the gate evaluates, never one an author applies | The failing assertion's `.mantine-Button-label` condition is exactly such a selector | Do not satisfy the gate by producing the property it reads | Kickoff §3.2(a) names and forbids the move by name; §4 makes `check-stories-rendered.mjs` zero-diff; AC10 measures it | `COMPLIANT` |
| `docs/backlog.md:74` corollary (709/721/694) — a kickoff's own measured facts are not exempt | Every count and line reference in §3 | Measure with the real tool; read what the config keys on | All manifest numbers read from `2026-08-15T05-29/manifest.json`; `MANTINE_STORY_EXTRA_VIEWPORTS` re-derived as **component**-keyed, so `band-700` applies to `HeroSearch/Default` **and** `/Fallback` = 8 cells (kickoff §10) | `COMPLIANT` |
| `docs/backlog.md:74` corollary (700) — when a requirement narrows a detector, state the class the narrowing excludes | The `<640` boundary and the `.mantine-Button-label` condition both narrow | Name the excluded class in the kickoff, before the executor builds | Kickoff §3.2(a) records that the label condition does **not** exclude a collapsed `MantineCountButton` and that the gate's comment is wrong; P2b proves the gate is blind to the label requirement itself | `COMPLIANT` |
| `docs/qa-profiles.md` — profile selection and per-story viewport policy | UI change on the rendered-proof path | Name one profile; read per-story widths from the manifest, do not infer from the union | `Q3` selected with reasons in kickoff §10, including why not `Q4`; band-700 warning stated | `COMPLIANT` |
| `docs/qa-profiles.md` `Q1` row — final `npm run build` hard gate | Source code changes | Zero-exit `npm run build` transcript; a failed/unrun build permits only `PARTIALLY IMPLEMENTED`/`BLOCKED` | AC14, contract ckpt 12 | `COMPLIANT` |
| `docs/orchestrator-ui-task-design.md` — visual source map + canonical UI decision record are mandatory artifacts | Changed visible artifacts | Both tables, with `reuse`/`extend`/`create canonical` dispositions and preserve/out-of-scope positive evidence | Kickoff §3.4 and §3.5; every `out of scope` row carries positive evidence (e.g. the bell `ActionIcon` cannot be selected by `check-stories-rendered.mjs:1301`) | `COMPLIANT` |
| `docs/critical-flow-registry.md:33` — authenticated header hydration, NotificationBell SSR shell | The chain names `NotificationBellView.tsx`; this task edits its child `NotificationCenter.tsx` | Automated regression evidence (CLAUDE.md, "Critical flows … require automated regression evidence") | Authoritative command from the row: `npm run test:header-hydration-id-parity`, exit 0, transcript retained (kickoff AC15) | `COMPLIANT` |
| `docs/critical-flow-registry.md:45` — verify/revoke agent, AdminUsersTable table actions | This task edits `AdminUsersTable.tsx` | same | Row command: `npx vitest run src/components/admin/__tests__/AdminUsersTable.smoke.test.tsx`, exit 0 (kickoff AC15) | `COMPLIANT` |
| `docs/critical-flow-registry.md:50` — listings filter controls, leaf sub-components + shell | The row command includes `src/components/shared/__tests__/heroSearch.smoke.test.tsx`, which asserts the `iconOnlyBelow={860}` wiring this task extends | same | Full row command set, exit 0 (kickoff AC15). `heroSearch.smoke.test.tsx:47` forces the below-threshold branch — it must still pass unchanged, proving `iconOnlyAbove` is additive | `COMPLIANT` |
| `docs/critical-flow-registry.md:39` — admin users list loads (hydration gate, owner-run with auth cookies) | Row engaged by file, not by mechanism | same | `NOT APPLICABLE` as a *new* obligation: the AdminUsersTable edit is a static `styles` prop — no hook, no `ssr:false`, no conditional render, so no server/client tree asymmetry is introduced. Positive evidence: `git diff` shows one prop; the general `npm run check:hydration` still runs (kickoff §11) | `NOT APPLICABLE` |
| `docs/critical-flow-registry.md:46` — admin-table pagination | `MantinePagination` is not in the write set | same | `NOT APPLICABLE` — file untouched, verified by AC10 | `NOT APPLICABLE` |
| `CLAUDE.md` — git policy | The task changes files | Mutating git is owner-only, native PowerShell | No agent runs `git add`/`commit`/`push`; the orchestrator emits an explicit-path owner handoff | `COMPLIANT` |
| `docs/component-rules.md` — container/presentational split | `HeroSearchView` is the presentational half of the Task 568 split | Keep the view hook-free of data concerns | `iconOnlyAbove={640}` is a static prop; the `useMediaQuery` lives in `MantineCountButton`, where it already lived | `COMPLIANT` |
| `docs/storybook-governance.md` §14.9.28 | Directly governs the `fullWidthButtonsAtMobile` adjudication | Follow the adjudication's mechanism decisions | Followed, **with a recorded contradiction**: its `:1842` row states Task 724's `NotificationCenter` retarget landed; `git log --all -- src/modules/notifications/components/NotificationCenter.tsx` ends at `50c40c2f8` and neither `bf343a297` nor `8113bc865` touches the file. Handed to the reviewer as a finding (kickoff §13); the doc is **not** edited by this task | `COMPLIANT` |

No row is `BLOCKED`. Rows link to `tasks/Sprints/Sprint_58_Task_749_execution_contract.md` for the single active
route. Recompute this ledger from the current text and worktree after any revision — a prior `COMPLIANT` does not
survive a changed plan, scope, command, baseline or owner decision.
