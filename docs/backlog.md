# Project Backlog

> ⚠️ **ACTIVE STATE ONLY.** A row belongs here only if it names something still to be **decided or done**. Closed tasks/sprints/epics → [`docs/backlog-archive.md`](backlog-archive.md), one ledger row each, newest first. Reserved-number full text → [`docs/backlog-reserved.md`](backlog-reserved.md). Full detail → `docs/sessions/`. Kickoff detail stays in `tasks/` and is **not** restated here.
> "Last Session" = 2–4 lines. On close, move the task to ONE archive row. Rules: `docs/ai-behavior.md` → "Backlog & Session Log Rules". **The budget is operational signal, not line count** — a row that fits the line limit but restates a kickoff, a session log or a ledger is still a violation.
> **No volatile facts in this file.** No `HEAD`/commit SHAs, branch names, divergence counts, tree cleanliness, or "N today" tallies — all of them are false within one commit. Read them live at the moment you need them (`git --no-optional-locks status` · `git --no-optional-locks rev-list` · the registry heading below). Durable Git lessons → `docs/orchestrator-procedures.md` → "Git state — durable lessons".
> **Decided is not active.** Binding decisions → [`docs/binding-decisions.md`](binding-decisions.md) · recurring orchestrator failure modes → `docs/orchestrator-procedures.md` · known console noise and assessed-harmless findings → `docs/maintenance-playbook.md` §14. Only **undecided** decisions live in this file.

## Last Session (2026-08-27)

Task 770 was independently reviewed, closed and archived; its session log and review ledger are cited in its archive row.

⚠️ **Commit-qualified:** 770 and this consolidation are in the worktree and **not** in `main`. Do not represent them as landed history until the owner pushes and the archive row carries the SHA.

Sprint 65: **771** is now filed (read-only `Q0` global-retirement decision record, kickoff under `tasks/Sprints/`); **D65-A** (`PerfDevOverlay`) stays owner-pending. No implementation task is open in the sprint.

Consolidated **2026-08-27** against the row-by-row audit (`docs/chat-gpt-reports/2026-08-27-backlog-row-by-row-audit.md` (**local-only — `docs/chat-gpt-reports/` is gitignored**, so this citation does not resolve in a fresh clone)) — four factual conflicts corrected, history and governance moved to their owning documents. What moved where is the archive row for that date. No active state was dropped.

## Open — needs action

| Item | State | Next step |
|---|---|---|
| **Cleanup step 3** | UNBLOCKED (696 = step 1, 701 = step 2, I-D landed in 703) | Delete 3 consolidated probes: `task420-qa-grid-step.mjs` · `task668-qa-grid-1440.mjs` · `task668-qa-header-geometry.mjs` (**1084 lines by `wc -l`, 946 non-blank** — say which you mean). **Must also update `check-homepage-grid.mjs`, which names all three in 18 provenance comments.** `scripts/` holds **13** task-numbered probes, all unwired; the other 10 are a separate call, incl. **670** |
| **665** | `PARTIALLY IMPLEMENTED / PARTIALLY VERIFIED` | **Only §13.7's live-route before/after DOM/computed-style baseline is outstanding** (AC1 — needs a routable dev server + seeded DB; use §16.1's corrected `classList.contains` locator). Also decide the orphan `listing.fixture.ts` |
| **Homepage ⓐ — Mantine component migration** | route-level scope still unmeasured | The only outstanding work is **667**, the route inventory of what `/[locale]` **actually mounts** — and it is BLOCKED (Sprint 59). `AppImage` and `ListingFeatureIcon` carry no Mantine **by design** and are out of scope as deliberate native wrappers. |
| **Homepage ⓑ — de-Tailwind (D28)** | **measured 0 on 2026-08-27** | `check:homepage-literal-utilities` reports **0 static class literals across its 3 guarded files** (run natively 2026-08-27). The six live occurrences **D63-B** was raised against were closed by Task 766, and `AppImage`'s variant strings by 763. ⚪ **OWNER: D63-B is now bookkeeping only** — confirm this row is the correction it asked for, then retire the decision. **Genuinely 0 — do not re-audit:** `HeroSearch.tsx` · `HeroSearchFallback` · `HowItWorksSteps` · `ViewAllLink` · `FiltersPanel` (671/675) · `MantineHomeSection`. |

## Pending Action Items (owner)

| Item | Notes |
|---|---|
| 🔐 Re-verify HIBP "leaked password protection" on Supabase Free tier (Auth → Password Security). | Advisor `auth_leaked_password_protection` WARN. `docs/integrations.md`. |
| 👁️ Eyeball-verify notification localization under `/sq` (data-only fix 2026-07-14). | Re-open the bell under `/sq`. |
| 🐞 `/listings` Grid horizontal overflow <640px (`ListingsSortBar`'s mobile filter trigger plus `min-w-35` sort `Combobox` can push `scrollWidth` past the viewport at 320/375/390). | Needs its own task; out of Task 603 scope. |
| 🖋️ Verified Agents DB schema sign-off (Task 313) + verified-badge public visibility. | Epic HH blocker. |
| 🗺️ **The Epic MM tracker is nominally the migration's source of truth and is hundreds of task numbers stale.** `docs/mantine-tailadmin-migration-tracker.md` last touched **2026-07-09** (Task 556); its "Current pointer" and all four "next" slices are already done. `tasks/Epics/Epic_MM_Mantine_UI_Migration.md` is worse — untouched since **2026-06-25**, highest task **484**. Measure the gap against the registry heading below, never against a number restated in prose. | Decide: refresh both as their own task, or demote them to historical and name a real successor pointer. Own blast radius. |
| ✅ **Ack that 689 is retired** — 699 repointed all 5 sites; the only surviving `1.875rem` is the hero triple 689 excluded by design. | Needs your ack only. |
| ⚪ **Close Sprints 63 and 64.** Both are fully delivered — 763, 764 and 765 are archived and no task remains open in either. They are deliberately **not** listed under "Sprints" while they hold no open work. | Close both to one archive row each, or record why they stay open. |
| 🧭 **No CI gate asserts the Mantine composition of a *route*** (first recorded 2026-07-26, still true). `--mantine-only` scopes by Storybook title prefix; only `check:story-coverage` reads `mantine-migration-scope.json` and treats anything absent as out of scope. **`15/15` proves coverage of fifteen enrolled components, not the homepage — re-measure it before quoting it.** | Decide: file a task, or accept it in writing as a known limitation. Same subject as 667. |

## Sprints

> **Owner rule, 2026-08-01: every task belongs to a sprint.** Kickoffs live at `tasks/Sprints/Sprint_NN_kickoff_prompt_Task_NNN_<Slug>.md`, never the root of `tasks/`. Closed tasks and sprints move to one archive row each.

**Sprint 46 — ListingCard de-Tailwind + overlay exit** (`tasks/Sprints/Sprint_46_…md`): completed delivery rows **691 · 694 · 695 · 700 · 702 · 741 · 742 · 748** are archived. 🟠 OPEN only for its retained follow-up scope: **743** (46.8), **744** (46.9) and **745** (46.10). **D28 + D34 + D36 + D37 bind the remaining work.**
**Sprint 55 — ARIA semantics no gate sees** (`tasks/Sprints/Sprint_55_ARIA_Semantics_No_Gate_Sees.md`): **730 only** — it **folds 731** (one kickoff, not two; the sprint's own execution-order note of 2026-08-08 and its Tasks table both say so). **731 is folded and must never be issued.** Opened 2026-08-08 — 46/52/54 all fail the goal-fit test (table in the plan file). Its transferable output is exit criterion 4: name the detector that would have caught these, or record in writing why none is worth building — **760 is a live candidate answer**. 🟠 OPEN, **zero landed tasks**.
**Sprint 56 — Raw enum leaks and the detector that cannot see them** (`tasks/Sprints/Sprint_56_Raw_Enum_Leaks_And_The_Blind_Detector.md`): **679** (folds 680). Opened 2026-08-08. The detector fix supplies the failing arm the leak fix needs, so the two-armed plant comes free — but only if the detector lands first. 🟠 OPEN.
**Sprint 57 — Delete what no longer earns its place** (`tasks/Sprints/Sprint_57_Delete_What_No_Longer_Earns_Its_Place.md`): **676** · **682**. Opened 2026-08-08. Pure removal, both proven inert before deletion rather than after. Owner cleanup step 3 stays in Sprint 46, not here. 🟠 OPEN.
**Sprint 59 — Route-level inventory before any migration claim** (`tasks/Sprints/Sprint_59_Route_Level_Inventory_Before_Any_Migration_Claim.md`): **667 only**, still `reserved` / **BLOCKED — OWNER DECISION REQUIRED**. **751 is archived** (`CLOSED AS HISTORY` 2026-08-17 — the owner rejected the direction; the measured FAIL is retained in its archive row and in `docs/reviews/artifacts/task-667/f1/FINDING.md`) and **does not decide 667**: 667 has **no task-level dependency** on it. Goal: establish what `/[locale]` **actually mounts** before any further migration is scoped against it. ⚠️ **OWNER DECISION PENDING:** with the F1 preflight shelved it will never be delivered, so either re-scope 667 onto another mechanism or close the sprint with it. D-A/D-C/D-H/D-J are approved; D-B/D-D/D-E/D-F/D-G/D-I remain unapproved recommendations in the 667 decision note. 🟠 OPEN, zero landed tasks.
**Sprint 62 — Tailwind runtime tokens outlive Tailwind** (`tasks/Sprints/Sprint_62_Tailwind_Runtime_Tokens_Outlive_Tailwind.md`): Task 762 is archived. 🟠 OPEN only for owner decision **D762-3**: whether `--text-*` joins a new task or remains separately scoped.
**Sprint 61 — The projection layer no gate reads** (`tasks/Sprints/Sprint_61_The_Projection_Layer_No_Gate_Reads.md`): Task 747 is archived. 🟠 OPEN for reserved P1 **761**; **750** fits the goal but is deliberately not assigned.
**Sprint 65 — Homepage finishes the Tailwind exit** (`tasks/Sprints/Sprint_65_Homepage_Finishes_The_Tailwind_Exit.md`): 766–770 are archived. **771** is 📋 **KICKOFF FILED 2026-08-27** — the read-only `Q0` Level-4 decision record (`NOT_READY` against five measured blocker classes; deletes nothing, authorises nothing). Its execution precondition is an empty `git status --porcelain` with Task 770 landed. ⚠️ **D65-A PENDING** (`PerfDevOverlay`) is the sprint's only open decision — D65-D/E/F were decided 2026-08-26 and their text lives in the sprint file. Binding rules for anything filed here: control ships with the fix unless D65-E explicitly says otherwise; no author-applied exemption; mechanism only, never restyle; new runtime values in `:root`; and no global Tailwind deletion or route certification in this sprint. 🟠 OPEN.

## Task registry — single source for every open number. Last used **771**, NEXT FREE **772**.

> Allocatable and reserved numbers only. A number leaves this table when its task is archived. Full justification for a reserved number → [`backlog-reserved.md`](backlog-reserved.md); retired and folded numbers → the same file's "Registry bookkeeping".

| # | State | What |
|---|---|---|
| **761** | reserved — **Sprint 61**, P1 | CommonMark fence detection for `check-ledger-claim-projection`, binding all five opener/closer cases. **Open owner decision:** whether an unclosed EOF fence is bad input (exit 2). Not a rework of the archived 747. Full text → `backlog-reserved.md`. |
| **667** | reserved — **BLOCKED — OWNER DECISION REQUIRED**, Sprint 59 | Route inventory of what `/[locale]` **actually mounts**, over the full render chain (D-A), DOM-evidenced (D-C). **No task-level dependency** — 751 is closed as history and does not decide it; the block is the Sprint 59 owner decision plus six unapproved recommendations (**D-G** and **D-I** hardest). **Not executable; must not be handed to an executor.** Full text → `backlog-reserved.md`. |
| **675 §8 family** | **676** → S57 · **679** → S56 · **677** owner | **676** stale hex comments in `globals.css` — documentation drift, no rendered consequence; must not re-touch `--brand-950` (694's) · **677** the `<div>`-in-`<p>` FiltersPanel warning (owner item) · **679** `usePropertyTypes` fallback localization, **folds 680**; Sprint 56 lands the detector fix first so the leak fix has a failing arm. Measured ΔE inventory → `backlog-reserved.md`. |
| **682 · 683** | **682** → Sprint 57 · **683** blocked | **682** drop `sonner` + `next-themes` from `package.json` — **confirm both are still unused at execution time, not from this row** · **683** TailAdmin bottom-nav conformance slice, blocked because no reference row exists yet. Full text → `backlog-reserved.md`. |
| **745** | reserved — **Sprint 46.10**, Q4 | A partially unstyled capture is admitted as a real width verdict. Detect that the **measured controls themselves** are unstyled, retry, then classify as `unstyled-render` — never as a product width regression, and never retry a valid `fullWidth=false` into a pass. Per **D37** the mechanism is not established: 745 must characterise it. Evidence → `backlog-reserved.md`. |
| **744** | reserved — **Sprint 46.9**, filed by D36 | Attribute `/[locale]`'s route-specific client JS — **the deliverable is the attribution, not a fix**, with the pattern barrel and the homepage client islands as the two named starting points. Independent of 691. **Re-measure every figure at execution; never quote one from a document.** Evidence → `backlog-reserved.md`. |
| **743** | reserved — **Sprint 46.8**, filed by 700's review | `check:css-vars` computes ownership live, so **deleting** a token from `globals.css` un-owns it together with its orphaned consumers and the gate goes silent (`0 violations, exit 0`). 743 must decide the scoping rule and prove it with a planted deletion; the naive fix reopens 112 Mantine false positives. Two reproductions → `backlog-reserved.md`. |
| 730 · 734 · 735 · 736 · 738 · **746** · **750** · **759** · **760** | reserved — full text in [`docs/backlog-reserved.md`](backlog-reserved.md) | **730** chip-row toggles announce no selected state (Sprint 55, folds 731) · **734** 25 N1 violations inside `src/design-system/mantine`, incl. the `zIndex: 100` sticky-layer escalation · **735** enrol `System/FeaturedListings` + a `wide-1440` extra-viewport entry · **736** classify the AdminUsersTable locale-leak findings, do **not** translate them · **738** the horizontal-scroll blind spot, 729's residual 40 · **746** P1, filed by 691's review — no control can see a hover-state or real-consumer (`AppImage`, not `DemoImage`) regression on the listing card; define the detector + its false-positive boundary first, then three failing plants· **750** P3, filed by 749's review — the `ambiguousOverlap` counter (`check-stories-rendered.mjs:1804`) prints as `\| ambiguous-overlap \| N \|` (`:1859`) but aggregates **five** reasons after 749 (measured: overlap 4 · offscreen 4 · ellipsis 16 · text-clipped-scrollable 18 · outside-scrollable 6); same family as **738**. Carries N1: record in `storybook-governance.md` that a `GEOMETRY_ALLOWLIST` row is strictly more permissive than a structural predicate. |

**Unnumbered follow-ups** — candidates, not tracked state; each becomes a numbered task or an owner row when scheduled: the `ListingCard`/`HomepageListingGrids` live relative-date fixture spuriously flags on any Q3 re-capture taken on a different calendar day · add `TextInput/Default`, `PopularLocationsView/Long City Name` (sq@390) and `LocationComboboxSubPanel/Default` (en@320) to `storybook-governance.md` §8.1 · no regression test for `check:mojibake` (666 F1) · the standing AMBIGUOUS cells have never been triaged once — owner-triage by design, **never citable as green proof**; count them at the moment you act, never from a number written here.

## Active Epics

**HH — Admin UX System** 🟠 310 (P4) · 311 (P5 partial) · 313 (P6, blocked on owner DB sign-off); 308/309 re-scope onto canonical primitives → [`Epic_HH_…`](../tasks/Epics/Epic_HH_Admin_UX_System.md) · **KK — Admin Data Freshness** 🟠 **453 only, and it is conditional** — it opens *only if* 452's investigation exposes a page needing special handling; no such page has been named → [`Epic_KK_…`](../tasks/Epics/Epic_KK_Admin_Data_Freshness.md) · **MM — Mantine/TailAdmin Restyle** 🟠 no live epic-level task; its one active item is the owner decision above. ⚠️ **Do NOT use the tracker as the "what's next" pointer** → [`Epic_MM_…`](../tasks/Epics/Epic_MM_Mantine_UI_Migration.md)

## Governance sources

> **Non-optional on every task.** `docs/agent-contract.md` (P0, clauses 1–15) · `docs/ai-behavior.md` Notes 18–23 · `docs/rule-index.md` (per-task pre-read) · `docs/orchestrator-procedures.md` (task design, review, Git policy, commit/push handoff, `index.lock` preflight, **recurring orchestrator failure modes**) · [`docs/binding-decisions.md`](binding-decisions.md) (**D19–D37**, closed and binding) · `docs/maintenance-playbook.md` **§14** (known console noise, advisor exceptions, assessed-harmless findings) · the Positive/Negative flow rule. **`npm run build` exit 0 mandatory for non-Q0.** **Design-system floor:** every task consumes `docs/design-system.md` wherever UI/responsive/overlay is touched; new UI uses Mantine (`docs/mantine-responsive-design-system.md`).
