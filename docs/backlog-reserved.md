# Backlog — Reserved Numbers (full text)

> Split out of `docs/backlog.md` on 2026-08-11, for the same reason `backlog-archive.md` was: the backlog holds
> **active state**, and a reserved number's full justification is task-design input, not state. Every number here
> is still live and still listed in the backlog's registry — that registry names it and points here; this file
> carries the measurement behind it.
>
> **These rows are the durable record.** Several explicitly say so — 734's per-file census exists because its
> per-line table is local-only (D6), 736 carries a measurement (`84 of 2257 sq strings`) taken once and not
> repeatable from the repo alone. Do not compress them away; move a row into its kickoff when one is written,
> then delete it here.

| # | State | What |
|---|---|---|
| **798** | reserved — **P3**, no sprint yet, filed by Task 791's review 2026-09-05 | **The map's third-party chrome is English in all four locales, and nothing could see it until a story was enrolled.** Measured from the owner's native `check:locale-leak:mantine-only` run (2026-09-05, 103 stories, 107 leaks): seven strings — `Marker`, `Close popup`, `Zoom in`, `Zoom out`, `A JavaScript library for interactive maps`, `Leaflet`, `OpenStreetMap` — leak on all three of `Patterns/Mantine/ListingDetailView`'s states × sq/uk/it = 21 cells. **They are not ours:** none of the seven appears in `messages/*.json` (grepped), and `src/components/shared/Map.tsx` configures only `attribution: '© <a …>OpenStreetMap</a>'` at `:31` — no `zoomInTitle`, `zoomOutTitle`, popup-close or marker aria strings. They are Leaflet's built-in English defaults and they render on the **live** `/[locale]/listings/[slug]` route, not merely in Storybook. **Pre-existing, newly visible** — the gap is exactly what Task 791's enrolment was predicted to surface, which is the argument for enrolment, not against it. The work is a decision per string, not a bulk translation: Leaflet exposes `zoomInTitle`/`zoomOutTitle` options and the attribution string is ours to write, while `Leaflet`/`OpenStreetMap` are brand names that must **not** be translated and belong in the detector's allowlist with a recorded reason. Follow Task 624's discipline — allowlist globally only where the string is genuinely a proper noun, otherwise `PER_STORY_TOKENS`. **Separate from the fixture cell:** `Elira Hoxha` (3 cells, same run) is a story fixture proper noun and takes a `PER_STORY_TOKENS` entry in Task 791's own revision, not here. **Re-measure at execution; the 107/24/83 split is a 2026-09-05 measurement, not a standing count.** |
| **797** | reserved — **P2**, no sprint yet, filed by Task 791's review 2026-09-05 | **A raw dimension written in Mantine's responsive object form is invisible to `check:design-tokens`.** Measured at source during the 791 review: the `raw-dimension-prop` arm (`scripts/check-design-tokens.mjs:271`) requires `prop=\{-?digits\}` — a bare numeric literal directly inside single braces — and the `raw-inline-dimension` arm (`:287`) keys on **CSS** property names (`padding`, `paddingBottom`, `width`…). Mantine's responsive form is `pb={{ base: 176, md: 80, lg: 32 }}`: the outer token is `{{`, and the object keys are breakpoint names, so **neither arm can match it**. Task 791 landed exactly that expression in `ListingDetailView.tsx` — a manifest-registered, `--scope=mantine` file — and the scoped run reported `0 violations, exit 0`. The value itself is correct (it preserves `pb-44 md:pb-20 lg:pb-8` = 176/80/32px, required by 791's R10), so this is a **detector** defect, not a 791 implementation defect. **Same family as 743 and 700's F1: the rule that makes a gate precise is the rule that makes it blind.** The work: add the arm, and state its false-positive boundary **first** — a responsive object whose values are Mantine spacing tokens (`{{ base: 'md', lg: 0 }}`, `{{ base: 12, md: 8 }}` on `Grid.Col` **span**, which is a column count and not a dimension) must never fire, and `span`/`order`/`offset` are data props that the existing arms already deliberately exclude. Prove it with a two-armed plant: the 791 expression must fail the gate, and its tokenized replacement must clear it. **Census at execution, never from this row** — `grep -rnE '\b(p|m)[btlrxye]?=\{\{' src/` for the current population. |
| **793** | reserved — **Sprint 71**, **P1**, filed 2026-09-05 | **`ListingContact` → `MantineListingContactPattern`, plus the stale 56px clearance Task 787 left behind.** The canonical contact pattern (261 lines, Task 616 D2, refined through D69-20/22/24/25) mirrors the production sidebar's five states but is **not** a drop-in — measured 2026-09-05, five divergences: ① it has no equivalent of `ListingContact.tsx:307-309`'s `lg:hidden fixed` **mobile bottom bar** (deleting a live mobile control is an owner decision, not a refactor); ② no slot for `SaveToCollectionButton`; ③ no loading state on the call/WhatsApp buttons (production disables them and swaps in a `Loader2` while `getListingOwnerContact` resolves); ④ no "link copied" share label toggle; ⑤ `color="green"` where production uses `bg-whatsapp`. **Also carries the 787 residue:** `ListingContact.tsx:309` and `ListingMobileCTA.tsx:70` still set `bottom-14` (3.5rem = 56px) for the bottom bar that 787 deleted on 2026-09-05 — 787's census grepped `--homepage-runtime-space-14` and these two are raw Tailwind utilities, so it could not see them. `globals.css:141`/`:380` already carry 787's corrected comments, so the tokens are right and only these consumers are stale. **Task 791 deliberately preserves `ListingDetailView`'s matching `pb-44 md:pb-20 lg:pb-8` page clearance** so this task changes both ends of the coupling in one diff. Gated on 791 (the sidebar column must be Mantine-owned first). Q3. |
| **792 · 794 · 795 · 796** | reserved — **Sprint 71**, filed 2026-09-05 | The rest of the detail + form family, each gated on **791** for the route frame. **792** (P2/Q2) detail-route chrome: `ListingBackButton` (44 ln), `ListingStatusBanner` (37 ln), `[slug]/loading.tsx` (73 ln, the last `@/components/ui/skeleton` consumer on this route) and the `SimilarListingsView`/`RecentlyViewedGridView` section wrappers. **794** (P2/Q3) gallery + lightbox: `ListingGallery` (165 ln, 15 `className`), `LightboxView` (174 ln, 12 — outer `Modal.Root` is already Mantine, the inner composition is not) and `GalleryStaticFrame`/`GalleryIsland`. **The LCP mechanism is the constraint, not the layout** — the static frame emits the `fetchPriority="high"` `<img>` into the SSR HTML and `ListingGallery`'s `useEffect` swaps it for `#gallery-interactive-shell`, keyed on three DOM ids and the shared `--listing-gallery-h-*` heights; `MantineListingGalleryPattern` reproduces none of it and lays the photos out differently, so this task decides whether the pattern adopts the mechanism or the production subtree keeps it. Must re-prove the Task 612 stacking contract **after** 793 moves the sticky card. **795** (P2/Q4) the three legacy dialogs — `ListingInquiryDialog`, `ListingReportDialog`, `SaveToCollectionButton`, all on `@/components/ui/dialog` + `input`/`textarea`/`label`; Q4 because inquiry and report are both critical-flow rows with existing smoke suites (`test:listings`) that must stay green and must keep asserting observable behavior. **796** (P2/Q3) `create` / `[slug]/edit`: `ListingFormShellView` (52 `className`, + its cancel-confirmation `Dialog`), 5 steps, 10 field components under `components/form/`, `ImageUpload` (28). **It is the first production consumer of `MantineFormSectionStack` / `MantineTwoColumnForm` / `MantinePageHeaderWithActions`** — all three had zero importers on 2026-09-05 — which is exactly the condition the owner's 2026-09-04 bare-pattern-gutter deferral is waiting on. **Re-run every census at execution; do not cite this row.** |
| **759** | reserved — **no sprint yet**, filed 2026-08-18 by the Sprint 60 reviews | **A detector for the unlayered-Mantine cascade trap.** `@mantine/core/styles.css` ships with no `@layer` wrapper; Tailwind utilities live in `@layer utilities`; unlayered always wins regardless of specificity or source order. So **any Tailwind class on a Mantine component that competes with a declaration that component's own root class sets has never rendered** — and a migration that "faithfully preserves" it into a working prop or CSS Module *introduces* a change. This shipped twice in one sprint and both times only review caught it: **752** (`justify-start`/`text-left` on `Button`, whose root is `display:inline-block` with `text-align:center` — the prop replacement moved labels 16.1px) and **756** (`leading-snug` on `Text`, whose root sets `line-height: var(--text-lh, …)` — moving it into `.cardTitle` cut every listing-card title from 20.02px to 19.25px). A repo-wide scan found **109** Tailwind classNames sitting on Mantine components, mostly in admin surfaces; `rounded-xl h-10 text-xs` on a `Button` and `text-xs px-1.5` on a `Badge` were measured completely inert. The detector: for each Tailwind class on a Mantine component, resolve the property it sets, read the component's own compiled CSS in `node_modules/@mantine/core/styles/<Component>.css`, and flag the pair when Mantine sets the same property. **This is also a live answer to Sprint 55's standing exit criterion** ("name the detector that would have caught these, or record in writing why none is worth building") — owner decides whether it lands there or in its own sprint. Task **757**'s AC9 makes the manual version mandatory for `AuthSheet`; this number is the automated version. |
| **760** | reserved — **no sprint yet**, filed 2026-08-18 by the Sprint 60 reviews | **Teach `scripts/check-stories-rendered.mjs` to separate NEW from CATALOGUED.** The runner already prints the path to `docs/governance-reports/2026-06-19-task467-storybook-visual-defect-inventory.md` but never compares against it, so every full run reports a raw failure count that a human must decompose by hand. This was done manually **three times** during the Sprint 60 reviews: 217 fail = 84 planted self-test + 97 catalogued + 36 stale registry rows; then 194 = the same minus 36 plus 12 new (`AddItemPanel`) plus 1 flake; then 193 → 181. Each decomposition took a reviewer and each was re-derivable from data the script already holds. Wanted output: `194 fail: 181 catalogued, 13 NEW` with the NEW list. The 12 `AddItemPanel` cells — a real defect shipped by **756** — would then have surfaced on their own instead of being found by subtraction. Also worth folding in: a documented non-determinism set (the six overlay cells plus `Primitives/Command`) so flaky cells stop being re-litigated per task. A second, cheaper source of the same noise: a story whose component has no production importers still paints cells every run — `src/components/ui/command.tsx` is one (zero importers, found 2026-08-18). Whether that is worth a deletion pass is a **Sprint 57** question for later, not migration work; recorded here only so the observation is not lost. |
| 734 | reserved — filed by 717 | **25 genuine N1 violations inside `src/design-system/mantine`, reserved not remediated (R7).** `typography.ts` (3, `SECTION_HEADING_FZ`) · `input-chrome.css` (7, padding matching `--space-2-5`/`--space-4` exactly) · `MantineListingCardPattern.module.css` (12: 4× `transition` matching `--duration-slow` exactly, 8× `.card:hover` box-shadow duplicating the Mantine-generated `--mantine-shadow-lg` var the sibling `.premium:hover` rule already references) · `MantineDialogDrawerPattern.tsx` + `responsiveBottomSheet.tsx` (1 each, `paddingBottom` matching `--space-2`) · `MantineResponsiveActionFooter.tsx` (1, `zIndex: 100` bypassing the semantically-named `--z-sticky`). Per-file counts above are the durable record; the per-line table is **local-only** (`.screenshots/task717-evidence/I3-classification.md`, D6) so **734 re-derives the census rather than depending on it** — the 711/728 lesson. **Review escalation (2026-08-08):** `MantineResponsiveActionFooter`'s `zIndex: 100` is not a plain tokenization item — it is `--z-toast` on a `position:sticky` element, above `--z-overlay`/`--z-modal`/`--z-popover`, while the repo's own sticky convention is `z-30` (`AdminMobileHeader.tsx:41`, `AdminPageShell.tsx:30`). Latent only because no production consumer passes `sticky`. Correcting it **moves the element down 70 layers**, so that item alone needs a rendered witness and a Q2/Q3 arm, and 734 must state whether the answer is `--z-sticky` or a new token. |
| 735 | reserved — filed by 678 | **Enrol `System/FeaturedListings` via the Task 678 per-story mechanism + a `wide-1440` `MANTINE_STORY_EXTRA_VIEWPORTS` entry**, closing the 4th of 5 `SECTION_HEADING_FZ` use sites (`FeaturedListingsView.tsx` renders the heading inside its own story tree, unlike its `System/LatestListings` sibling — see `storybook-governance.md` §14.9.30, that one is route-level and stays out of scope, owned by 667). Reserved rather than spent in 678 because the kickoff's objective named only `AdminUsersTable` + `HowItWorksSteps` as "the two things" the mechanism unblocks; `System/*` was OQ1, and reserving is an explicitly acceptable disposition. |
| 736 | reserved — **needs a sprint** | **Classify the AdminUsersTable locale-leak findings; do not translate them.** 678 enrolled the story and surfaced 13 leaks — 11 fixture proper nouns, plus sq `role_admin`/`role_moderator`, which 678 escalated as "untranslated". Measured 2026-08-09: **84 of 2257 sq strings (3.7%) are identical to en** (`Premium`, `WhatsApp`, `Email`, `Studio`, `Duplex`, `Panel`…), and `check-locale-leak.mjs:96-101` is Task 624's loanword allowlist whose comment records that it already verified "sq: admin/brand/panel … identical to en in **admin.*/role keys**". Follow 624's own discipline: verify against `messages/*.json`, then allowlist globally **or** via `PER_STORY_TOKENS` (which is where 624 put Studio/Penthouse precisely so a global entry could not mask a real mistranslation elsewhere). Translating them would "fix" strings that are already correct. |
| 738 | reserved, P3 — **needs a sprint** | **The horizontal-scroll blind spot — 729's residual 40.** After 729's vertical band scan, 40 candidates remain unreachable: lightbox thumbnails inside a horizontally-scrolling container. Structurally different from the vertical case (729 scrolls the document; these need their own container scrolled), so 729 named it rather than absorbing it. Same family as 729 and 733: a gate that reports a number which reads as coverage. |
| 746 | reserved, **P1** — **needs a sprint**, filed by 691's review 2026-08-12 | **No control in this repository can observe a hover-state or real-consumer regression on the listing card.** Both of 691's P0 regressions passed every gate for this reason, so the number exists to close the detector gap, not to re-fix 691 — **it is not a blocker for 691's re-review.** Measured during that review: the `--mantine-only` matrix captures **rest state only**, so a `:hover` rule is structurally unobservable; `ListingCardPattern.stories.tsx:22-24` renders `DemoImage`, a plain Mantine `Image` explicitly documented as a stand-in for the real `AppImage`, so `group-hover:*` utilities never enter the captured DOM at all; `check-homepage-grid.mjs` asserts structure and geometry, not interaction state; and 691's own computed-style capture pinned one story, `locale:en` and `1024x4000` (`capture-computed-styles.mjs:92,:96`). **Order matters: define the detector and its false-positive boundary FIRST** — hover capture is inherently flaky (transition timing, `prefers-reduced-motion`, pointer emulation), and a control that cannot state which diffs it must ignore will be disabled the first week. Only then build the failing plants, **three distinct ones**, each of which must genuinely fail before the fix and pass after: ① remove `group` from a Card root → the image's `scale`/`transform` loss is caught; ② strip the `(hover: hover)` guard → the coarse-pointer arm is caught (Task 605 already proved `hasTouch:true, isMobile:true` works on this surface); ③ the probe runs against the **real** `ListingCard` + `AppImage` composition, not a demo stand-in — `mantine-primitives-listingcard--default` already imports the real component and is the obvious host. **Do not fold in 747** — that one is about prose diverging from a ledger and shares no mechanism with this. |
| 747 | **NO LONGER RESERVED** — kickoff filed 2026-08-19 into **Sprint 61**; live state is the registry row in `docs/backlog.md`, and the executable detail is `tasks/Sprints/Sprint_61_kickoff_prompt_Task_747_Ledger_State_Projection_Gate.md`. Text below retained as the original filing (691's review, 2026-08-12, P1). | **Hand-written state and counts in markdown can contradict the machine-checked ledger, and nothing detects it.** Not a UI or hover problem — the subject is the projection layer: `review.coverage` in a v4 ledger is validator-derived and cannot lie, but `docs/backlog.md`, `tasks/Sprints/Sprint_NN_*.md` and session logs restate the same facts in prose that no gate reads. **Two measured instances, both from 691, one in each direction.** ⓐ The backlog registry row, the backlog Last Session line and the Sprint 46 Tasks row all said `2 P0` while the ledger's `openP0` was **4**; `check:review-ledger` was green throughout because the ledger itself was correct. The owner caught it by reading both, which is exactly the control this number must replace. ⓑ Task 691's session log §13 asserted a backlog edit (`landed count moved to 5 of 9`) that the diff never contained — recorded as finding `F-M`, and the same class as the four documented state-drift incidents in the backlog's standing notes, one of which cost a duplicate implementation round trip. **Define the machine source-of-truth and a stable assertion format before writing any checker.** A grep for `N P0` is precisely the brittle answer to avoid: counts appear inside historical prose, retracted claims and archive rows that must *not* be rewritten, so the design question is how a live state claim is marked as machine-checkable at all — a fenced block, a front-matter field, a generated region — not how to pattern-match English. **Failing plant:** change only the markdown P0-count or task state, leave the ledger byte-identical, and prove the new control rejects it; then restore and prove it passes. **Do not fold in 746.** |
| 730 | reserved — **Sprint 55**, folds 731 | **The chip-row toggles announce no selected state.** 726 restored `role="group"` + a name on `FilterMultiToggle`/`FilterRoomsRow`, but neither sets `aria-pressed` — selection rides only on the Mantine `variant` (`filled` vs `default`), so AT hears "Condition, group" and then N plain buttons with no on/off. The two consumers 726's own §12/A2 cites as the correct precedent — `FavoritesTypeFilter.tsx:31` and `ListingsTab.tsx:171` — both **do** set `aria-pressed`; 726 shipped half the pattern. 14 live sites (7 `ListingsFilters` + 7 `FiltersPanel`). The same task must soften `storybook-governance.md` §14.9.28's "announce as named groups", which overstates what landed. |
| **761** | reserved — **Sprint 61**, P1 | **CommonMark fence detection for `check-ledger-claim-projection`.** The checker must track the opener's character, length and indent: an indent ≥4 is not a fence; a mismatched delimiter or a shorter/indented closer cannot end a fence. Tests bind all five cases. Owner decision remains open on whether an unclosed EOF fence is bad input (exit 2). Task 747 is archived; 761 is its separate follow-up, not a rework. |
| **675 §8 family** | **676** → S57 · **679** → S56 · **677** owner | **676** `globals.css` stale hex comments — **694's preflight already classified the hardest slice, 2026-08-10:** of the 13 `--x: oklch(…); /* …#HEX… */` rows, **11 are wrong by ΔE00 > 1.0** — `--badge-new` **10.82** (renders `#00842C`, comment says `#16A34A`) · `--neutral-600` **10.26** · `--neutral-700` **9.96** · `--neutral-500` **9.52** · `--neutral-900` + `--neutral-0` **6.76** · `--neutral-400` **5.76** · `--badge-premium` **4.83** · `--neutral-300` **2.07** · `--neutral-200` **1.30** · `--neutral-100` **0.62**; only `--neutral-50` is exact. Documentation drift, no rendered consequence. **`--brand-950` is 694's, not 676's** — 676 must not re-touch it · **677** the `<div>`-in-`<p>` FiltersPanel warning (owner item) · **679** `usePropertyTypes` fallback localization (`:16` returns the raw enum as the label) — **folds 680**, the `check:locale-leak` lowercase blind spot at `:220` that structurally cannot see it. Sprint 56 runs the detector fix first so the leak fix has a failing arm. |
| **682 · 683** | **682** → Sprint 57 · **683** blocked | **682** drop `sonner` + `next-themes` from `package.json` (681) · **683** TailAdmin bottom-nav conformance slice, no reference row exists yet (672). |
| **745** | reserved — **Sprint 46.10**, filed by 742's review, Q4 | **A partially unstyled capture is admitted as a real width verdict.** Proven, no re-run needed: `mantine-primitives-textinput--default × it × mobile-390` captured raw UA inputs at default width in a serif face, passed `styleIntegrity` (threshold is `failCount < 2` over `[preflightOk, sheetsWithRules, fontOk]`, `:1101`) and landed as a hard `fullWidthControlsAtMobile: false`. **Not a font-load race** — a computed `fontFamily` of bare `"Times New Roman"` means author CSS never applied to that element; a merely unloaded webfont still computes the declared stack. **Preserve the `:690` invariant for VALID frames** — a genuine `fullWidth=false` must never be retried into a pass (Task 573's deliberate rule). The fix goes **before** the width verdict: detect that the **measured controls themselves** are unstyled, retry the capture, and only after retries are exhausted classify the cell as an uncited **`unstyled-render`** — never as a product width regression. **Do not build the rule on the global first-text-node `fontFamily`** (`:1078`, a TreeWalker over the whole document): that signal can be unrelated to the control being measured. A second, equally invalid frame in the same run (`homepagelistinggrids × sq × desktop-1024`) **passed** only because `fullWidthControlsAtMobile` is `null` at desktop — the guard admits invalid frames in both directions. **Measured incidence (742's three runs, 1204 cells each): 0 · 2 · 0 fallback-font captures** — intermittent and real. Per **D37** the mechanism is NOT established; 745 must characterise it rather than assume the 742 anomaly's explanation. |
| **744** | reserved — **Sprint 46.9**, filed by D36 | **`/[locale]` client-chunk attribution — the 435 kB nobody has attributed.** Route First Load JS **619 kB** against **184 kB** shared (measured at `0dac78755`), so **435 kB** is route-specific client JS. It has never been attributed to anything; the barrel suspicion (`@/design-system/mantine/patterns`, 671/675 F3) remains `NEEDS VERIFICATION` and is a hypothesis, not a finding. **Deliverable is the attribution, not a fix** — name what each chunk contains, with the barrel and the homepage client islands as the two named starting points, and say which of them a server/client boundary change could actually move. **Do not bundle a remediation into this number**; a fix built on an unverified cause is how 691 came to be blocked for weeks. **Independent of 691** — D36 explicitly separated them so neither waits on the other. |
| **743** | reserved — **Sprint 46.8**, filed by 700's review | **The ownership blind spot 700 cannot see (700 F1).** `check:css-vars` computes ownership live from `globals.css` (R3), so **deleting** a token there un-owns it *together with its orphaned consumers* and the gate goes silent. Reproduced in review: removing `--color-overlay-foreground`'s single declaration while leaving **6** live TSX consumers in `LightboxView.tsx`/`MantineListingGalleryPattern.tsx` gives `0 violations, exit 0`. 700 covers "declaration stays, stops shipping" (Task 690's case) and **not** "declaration deleted", which is at least as common and which nothing else catches. **The naive fix reopens the 112 Mantine runtime false positives**, so 743 must decide the scoping rule — was-owned-at-`HEAD`, an explicit third-party prefix set, or something better — and prove it with a planted deletion. Defect in 700's kickoff R3, authored by the orchestrator. **Second reproduction, 2026-08-24 (Task 765, evidence only — 765 is NOT blocked on 743 and must not be widened to fix it):** the gap also swallows a **sibling-preserved** deletion with a **static `.css`** consumer, not just a last-reference deletion with TSX consumers. Removing only `--motion-duration-slow` from `:root` while `--motion-duration-fast`/`--motion-duration-base` stay owned, leaving `AppImage.module.css`'s live `var(--motion-duration-slow)` dangling, gives `0 violations, exit 0` — reproduced twice natively (`docs/sessions/evidence/task765/phase4-p3-plant.txt`). **This also kills the assumed mitigation:** a surviving sibling sharing the `--motion-duration-` prefix rescues nothing, because the prefix test at `:530` is computed only inside `scanArmB:528`'s `!isCss` branch for `findDynamicVarSites` template sites. So 743's scoping rule must cover a name that was owned at `HEAD` and is referenced from CSS — the arm with no dynamic-site fallback at all. |
| **750** | reserved, **P3** — filed by 749's review; **Sprint 61 fits its goal but it is deliberately not assigned** | P3, filed by 749's review — the `ambiguousOverlap` counter (`check-stories-rendered.mjs:1804`) prints as `\| ambiguous-overlap \| N \|` (`:1859`) but aggregates **five** reasons after 749 (measured: overlap 4 · offscreen 4 · ellipsis 16 · text-clipped-scrollable 18 · outside-scrollable 6); same family as **738**. Carries N1: record in `storybook-governance.md` that a `GEOMETRY_ALLOWLIST` row is strictly more permissive than a structural predicate. |

## Registry bookkeeping

> Moved out of `docs/backlog.md` on **2026-08-27** by the row-by-row audit: retired/folded numbers and their
> source citations are registry **history**, and the active registry needs only allocatable and reserved numbers.
> Nothing here is live work. Never reuse a number listed as retired or folded.

*Described once elsewhere:* 665 · 691 (above) · 310 · 311 · 313 · 308–309 (HH) · 453 (KK.2) · 463 (BB) in Epics. *Standalone deferred:* **560** admin suspension-as-date-range — **sourced, keep** (split out at `Sprint_41_kickoff_prompt_Task_557_…md:26,:36`, DEFERRED 2026-07-06), blocked on the owner defining suspension-window semantics · **SaveToCollection dialog → Mantine** (654 follow-up, still shadcn). *Not plain numbers:* 622/623 (`Q0R`, `623R`) · **709-R** (closed with 709) · **628** SB10 lint-debt. *Retired, never reuse:* 465 · 466/467/469–481 · 534 · 597/604 · 655 · **667** (Sprint 59 closed as mechanism rejected, owner 2026-08-27 — the route-inventory mechanism was measured FAIL and the direction rejected; see the 2026-08-27 archive row) · **689** (acknowledged retired, owner 2026-08-27). *Folded into another number, never reuse:* 732 → 722 · 728 → 721 · 687 → 678 · 731 → 730 · 680 → 679 — each folded only after its detail moved into the receiving sprint's plan file; none was dropped.
