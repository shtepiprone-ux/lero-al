# Project Backlog

> ⚠️ **ACTIVE STATE ONLY (~80 lines).** Closed tasks/sprints/epics → [`docs/backlog-archive.md`](backlog-archive.md), one ledger row each, newest first. Full detail → `docs/sessions/`. Kickoff detail stays in `tasks/` and is **not** restated here.
> "Last Session" = 2–4 lines. On close, move the task to ONE archive row. Rules: `docs/ai-behavior.md` → "Backlog & Session Log Rules".

## Last Session (2026-08-22) — **Task 763 Revision 1 `APPROVED WITH NOTES`. F-1 closed. Sprint 63 Phase 1 done except the carve-out.** Ledger: `docs/reviews/2026-08-22-task763-revision1-class-naming.review-ledger.json` (6/6 VERIFIED, 0 open P0/P1/P2, `check:review-ledger --file` exit 0). All ten utility-shaped classes renamed to role names (`.relative`→`.frame`, `.absoluteFill`→`.imageLayer`, …), hover re-rooted to `.frame:hover .hoverBrightness`, consumers + the 16-key test + every mapping comment updated. **Declaration set is byte-identical** — reviewer read all 103 lines against the pre-revision text: same properties, values, order, `@layer utilities`, `@media (hover: hover)`. **Rendered parity 0/2896** on a new pre-rename-vs-post-rename comparator, and that zero was checked for vacuity two ways: the two input captures differ by md5, and the *same* 2896-cell comparator returns 48 on the I2 pairing — a built-in positive control, no extra plant needed. The 48 are the already-accepted A3 `--tw-gradient-*` `transitionProperty` drops and the reviewer proved the sets are **element-for-element identical** (0 only-in-A, 0 only-in-B), so zero new. Gates re-run by the reviewer, not read from transcripts: `check:tailwind-runtime-tokens` 24/14/14/0-new/0-stale, `check:design-tokens --strict` exactly 2 findings out of 453 files, both in `AppImage.module.css`. `typecheck` 0, `test:listings` 45/45, class assertions 9/9, `build` exit 0. **4 notes, 0 blocking.** N-1: the brief's §6 "zero deltas against the retained I2 baseline" **was wrong and is the orchestrator's defect** — I2 predates 763's own accepted change, so it can never return zero; the executor ran the literal instruction, showed it reproduces the identical 48, built the correct comparator, and **flagged it instead of substituting silently**. That is the third consecutive orchestrator artifact with the same defect class (763 §3.5 → 764 AC6 → R1 §6): *a criterion written against an assumed baseline rather than the state that exists.* N-2: **D63-F still open**, `check:design-tokens` stays red until the owner decides. N-3: `.frameWidth`/`.frameClip` still lean on the CSS effect — orchestrator-suggested names, recorded not charged. N-4: **`listing`'s `hoverClass`/`'group'` pair stays open by design** — Task 764 (D63-E route c) closes it; 763 is approved with that carve-out explicit, not silently closed.

- **Backlog consolidated 2026-08-11** (94 → this): 742 archived, 5 reserved rows moved to
  [`backlog-reserved.md`](backlog-reserved.md), registry bookkeeping merged. See the archive row for what moved where.

## Open — needs action

| Item | State | Next step |
|---|---|---|
| **Git** | On `codex/merge-main-20260820` (remote `origin`, upstream `origin/main`) — re-read 2026-08-21; Sprint 60 delivery (`9a69aef9e`…`907354bd3`) is committed and pushed there but **not yet merged** — the archive rows for 752-758 record the SHAs. Three unreviewed `feat(ui): tokenize homepage hero background` commits sit on top of it (three different hex values in sequence); review or squash before the PR. Previously on `task/q0-ci-rendered-locale-split`. **The branch had silently diverged from `origin` by rewritten hashes — resolved 2026-08-10.** A `--cherry-mark` pass measured **zero** remote commits lacking a local patch-equivalent (the unmatched local ones were the PR-#3 merge, which has no patch to match, and three weekly governance reports), so the owner republished with `--force-with-lease` after a fresh fetch. **The durable lesson is the failure mode, not the fix:** a plain `git push` had been rejected on every attempt and nothing surfaced it, so approved work sat unpublished while the backlog asserted `origin` contained it. | ⚠️ **Never record divergence counts or tree cleanliness here** — stale within one commit. Read from `git status` / `git rev-list`. **After every push, confirm it actually published** — do not infer it from the command not erroring in your scrollback. If a future fetch shows any remote-only commit, `--force-with-lease` is no longer safe; stop and re-measure. Bridge sessions must use `git --no-optional-locks …`; a plain `git status` there leaves an `index.lock` the sandbox cannot unlink, which then blocks the owner's next `git add` |
| **Cleanup step 3** | UNBLOCKED (696 = step 1, 701 = step 2, I-D landed in 703) | Delete 3 consolidated probes: `task420-qa-grid-step.mjs` · `task668-qa-grid-1440.mjs` · `task668-qa-header-geometry.mjs` (**1084 lines by `wc -l`, 946 non-blank** — say which you mean). **Must also update `check-homepage-grid.mjs`, which names all three in 18 provenance comments.** `scripts/` holds **13** task-numbered probes, all unwired; the other 10 are a separate call, incl. **670** |
| **665** | `PARTIALLY IMPLEMENTED / PARTIALLY VERIFIED` | **Only §13.7's live-route before/after DOM/computed-style baseline is outstanding** (AC1 — needs a routable dev server + seeded DB; use §16.1's corrected `classList.contains` locator). Also decide the orphan `listing.fixture.ts` |
| **748** | ✅ `APPROVED WITH NOTES` (Opus review 3, 2026-08-13) — Sprint 46.11 | **Three rounds: the migration, then the two rendered regressions it hid, then the apparatus that could not see them.** Final state: all 24 overlay utilities migrated, census **0**, `globals.css` untouched, F-A and F-B fixed and independently verified, and an evidence apparatus that now fails closed — a signature-keyed D34 gate proven bidirectional by four adversarial mutations, and a two-phase comparator (176 cells) that refuses to score an unresolved custom property as a match. **Durable lesson:** every defect in this task was the same shape — a check that could not have come out wrong. A synthetic probe compared against its own intention; an id-keyed whitelist over the three sites it existed to watch; a witness agreeing with itself about a colour neither side could resolve. Ask of each artifact what would have to break for it to redden. Open: 1 P3 (no gate transcripts retained this round) + 4 notes; H1 and H6 fixed before commit. **Historical review context:** 695 was blocked on an owner decision about the 7 live `var(--color-overlay*)` refs; it is now archived, so 748 has no active 695 dependency. Ledger `docs/reviews/2026-08-13-task748-rework2-evidence-apparatus.review-ledger.json` |

**Homepage — TWO SEPARATE AXES.** ⓐ **Mantine component migration**: ✅ COMPLETE for the `page.tsx` **body** — Task 650. **Corrected 2026-08-16: not complete for the whole `/[locale]` tree.** Re-reading each file's imports found three components in the render graph that import nothing from `@mantine/*` — `CaptchaWidget`, `AuthRedirect`, `PasswordRequirementsHint` — all reached through `layout.tsx` → `Header` → `AuthSheet`, i.e. outside the page body this row was measured against. **Task 753 (Sprint 60) closes them.** `AppImage` and `ListingFeatureIcon` also have no Mantine and are deliberate native wrappers, out of scope by design. ⓑ **De-Tailwind (D28): ✅ COMPLETE for the homepage card pair.** Tasks 691, 695, 702 and 741 are archived; `CLOSED_OVERLAY_STYLE` was the final `ListingCard` migration. Everything else landed across Sprints 47–51. **Genuinely 0 — do not re-audit:** `HeroSearch.tsx` · `HeroSearchFallback` · `HowItWorksSteps` · `ViewAllLink` · `FiltersPanel` (671/675) · `MantineHomeSection`. 667 stays the route-level inventory. ⚠️ **ⓑ is CONTRADICTED as of 2026-08-21 (`201683f9d`), flagged not rewritten — owner decision D63-B.** `MantineListingCardPattern.tsx:304-306` still passes `'group'`, `'flex flex-col'` and `'grayscale opacity-60'` into `cn()` in production, and `AppImage`'s 21 variant strings reach this card through it. Measurement and phase map: `tasks/Sprints/Sprint_63_Homepage_Exits_Tailwind.md`.

## Pending Action Items (owner)

| Item | Notes |
|---|---|
| 🔐 Re-verify HIBP "leaked password protection" on Supabase Free tier (Auth → Password Security). | Advisor `auth_leaked_password_protection` WARN. `docs/integrations.md`. |
| 👁️ Eyeball-verify notification localization under `/sq` (data-only fix 2026-07-14). | Re-open the bell under `/sq`. |
| 🐞 `/listings` Grid horizontal overflow <640px (`ListingsSortBar`'s mobile filter trigger plus `min-w-35` sort `Combobox` can push `scrollWidth` past the viewport at 320/375/390). | Needs its own task; out of Task 603 scope. |
| 🖋️ Verified Agents DB schema sign-off (Task 313) + verified-badge public visibility. | Epic HH blocker. |
| 🗺️ **The Epic MM tracker is at least 194 task numbers stale and is nominally the migration's source of truth.** `docs/mantine-tailadmin-migration-tracker.md` last touched **2026-07-09** (Task 556), while the active registry's last issued number is **750**; its "Current pointer" and all four "next" slices are already done. `tasks/Epics/Epic_MM_Mantine_UI_Migration.md` is worse — untouched since **2026-06-25**, highest task 484. | Decide: refresh both as their own task, or demote them to historical and name a real successor pointer. Own blast radius. |
| ✅ **Ack that 689 is retired** — 699 repointed all 5 sites; the only surviving `1.875rem` is the hero triple 689 excluded by design. | Needs your ack only. |

## Sprints

> **Owner rule, 2026-08-01: every task belongs to a sprint.** Kickoffs live at `tasks/Sprints/Sprint_NN_kickoff_prompt_Task_NNN_<Slug>.md`, never the root of `tasks/`. Closed tasks and sprints move to one archive row each.

**Sprint 46 — ListingCard de-Tailwind + overlay exit** (`tasks/Sprints/Sprint_46_…md`): completed delivery rows **691 · 694 · 695 · 700 · 702 · 741 · 742** are archived. 🟠 OPEN only for its retained follow-up scope: **743** (46.8), **744** (46.9), **745** (46.10) and the separately filed 748 work. **D28 + D34 + D36 + D37 bind the remaining work.**
**Sprint 55 — ARIA semantics no gate sees** (`tasks/Sprints/Sprint_55_ARIA_Semantics_No_Gate_Sees.md`): **730** · **731**, both from 726’s review. Opened 2026-08-08 — 46/52/54 all fail the goal-fit test (table in the plan file). Its transferable output is exit criterion 4: name the detector that would have caught these, or record in writing why none is worth building. 🟠 OPEN, **zero landed tasks**.
**Sprint 56 — Raw enum leaks and the detector that cannot see them** (`tasks/Sprints/Sprint_56_Raw_Enum_Leaks_And_The_Blind_Detector.md`): **679** (folds 680). Opened 2026-08-08. The detector fix supplies the failing arm the leak fix needs, so the two-armed plant comes free — but only if the detector lands first. 🟠 OPEN.
**Sprint 57 — Delete what no longer earns its place** (`tasks/Sprints/Sprint_57_Delete_What_No_Longer_Earns_Its_Place.md`): **676** · **682**. Opened 2026-08-08. Pure removal, both proven inert before deletion rather than after. Owner cleanup step 3 stays in Sprint 46, not here. 🟠 OPEN.
**Sprint 59 — Route-level inventory before any migration claim** (`tasks/Sprints/Sprint_59_Route_Level_Inventory_Before_Any_Migration_Claim.md`): **751 closed as history 2026-08-17** (owner rejected the direction; measured FAIL retained in the archive row and in `docs/reviews/artifacts/task-667/f1/FINDING.md`) · **667** (the inventory itself, still `reserved`/BLOCKED). Opened 2026-08-16. Goal: establish what `/[locale]` **actually mounts** before any further migration is scoped against it. **Owner decisions D-A/D-C/D-H/D-J recorded in the 667 decision note; D-B/D-D/D-E/D-F/D-G/D-I remain unapproved recommendations.** ⚠️ **OWNER DECISION PENDING:** with 751 shelved, 667's feasibility preflight will never be delivered — the sprint now holds one blocked task and zero landed ones. Either re-scope 667 onto another mechanism or close the sprint with 667.
**Sprint 62 — Tailwind runtime tokens outlive Tailwind** (`tasks/Sprints/Sprint_62_Tailwind_Runtime_Tokens_Outlive_Tailwind.md`): **762** ✅ `APPROVED WITH NOTES` 2026-08-21 (Revision 1). Opened 2026-08-21 — 46/55/56/57/59/60/61 all fail the goal-fit test. Binding rule: the control ships before or with the fix. 763/764 indicative, not reserved. 🟠 OPEN.
**Sprint 61 — The projection layer no gate reads** (`tasks/Sprints/Sprint_61_The_Projection_Layer_No_Gate_Reads.md`): **747**. Opened 2026-08-19 — 46/55/56/57/59/60 all fail the goal-fit test (table in the plan file). Every gate here reads code or rendered output; none reads the documents that describe the state of the work, which is what the next session acts on. 🟠 OPEN — 747 Phase 2 built 2026-08-20, `IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW` (AC7 met, `npm run test` green); zero tasks approved yet. **750** fits the same goal but is deliberately NOT assigned.
**Sprint 63 — Homepage exits Tailwind** (`tasks/Sprints/Sprint_63_Homepage_Exits_Tailwind.md`): **763** ✅ `APPROVED WITH NOTES` 2026-08-22 (Revision 1, rename-only — F-1 closed; 4 notes, 0 open P0/P1/P2; ledger `docs/reviews/2026-08-22-task763-revision1-class-naming.review-ledger.json`). **Carve-out left explicitly open:** `listing`'s `hoverClass`/`'group'` pair → Task 764. 20/21+4 strings migrated and accepted; `listing`'s `group-hover:scale-105`/`'group'` pair correctly `BLOCKED` and moved to Phase 2. Opened 2026-08-21 — 46/55/56/57/59/61/62 all fail the goal-fit test (table in the plan file; 62 fails by its own written out-of-scope boundary). Measured on `201683f9d`: the `/[locale]` render graph is **110 files** (barrel-resolved; a naive import walk says 143) and carries Tailwind in **three** classes — 6 files with utility strings, 20 references to Tailwind's own token names, and **43 `@theme inline` references the 762 gate classifies `project` and does not flag**. ⚠️ **Two audit claims measurably false at `201683f9d`:** line 20's "ⓑ De-Tailwind (D28): ✅ COMPLETE for the homepage card pair" — `MantineListingCardPattern.tsx:304-306` still ships `'group'`, `'flex flex-col'`, `'grayscale opacity-60'`; and "runtime debt = 14 baseline pairs" — the baseline covers one ownership bucket over 23 of the repository's files. **D63-E DECIDED 2026-08-21 — route (c), filed as Task 764** (`Sprint_63_kickoff_prompt_Task_764_Listing_Hover_Fold.md`, Phase 2a, blocked until 763 R1 is approved): fold the hover into `MantineListingCardPattern.module.css:68` as `scale(1.1025)`, drop `listing.hoverClass` and `'group'`. Filing found a second never-equivalent dimension the decision predates — `scale` is not in `.imageSection img`'s transition list (`:53`), so the zoom snaps to 1.05x before easing to 1.1025x; 764 Phase A2 samples mid-flight so a rest/settled comparator cannot report a false zero. Also corrected there: `MantineListingCardPattern.module.css:22-24` claims Tailwind's `hover:` guard matches its own `(hover:hover) and (pointer:fine)` — the compiled `group-hover:` carries `(hover:hover)` only, which is the whole source of the coarse-pointer gap. **Owner also pre-accepted, 2026-08-21, the two behaviour changes the fold produces — the lost coarse-pointer zoom and the changed animation shape — as intentional**; 764 records them and never restores them, and the acceptance does not extend to any other delta. **764's kickoff was revised the same day after owner review:** AC6/AC7/§10.4 now compare the **effective rendered scale** (`getBoundingClientRect()` ratio), because the first filing demanded a byte-identical computed `transform` — which a correct fold must change (`matrix(1.05…)`→`matrix(1.1025…)`, `scale:1.05`→`none`), so it would have failed the right answer and passed nothing; and A2 is now a hard `BLOCKED` if the probe cannot report `(hover:hover)` and `(pointer:coarse)` together. **Ninth recorded instance of the standing kickoff-fact failure mode, second one authored by the orchestrator.** Owner decisions **D63-A…D63-D** and **D63-F** (the two design-token literals) remain open in the plan file. Phase 3 (gate → `.tsx`) files in **Sprint 62**, Phase 5 (`globals.css`) needs its own sprint, Phase 6 is Sprint 59's blocked 667. 🟠 OPEN.
## Task registry — single source for every open number. Last used **764**, NEXT FREE **765**.

| # | State | What |
|---|---|---|
| 747 | **`APPROVED WITH NOTES`** 2026-08-20 — orchestrator review passed; the one note is **P1 761** (fence detection, filed above), not a rework of 747. Phase 1 (`REVISION 5`) owner-approved 2026-08-20; Phase 2 built the same day, reworked across four review rounds that found **5** parser defects total (all fixed, regression-tested — the marker is now identified only by a tokenized attribute key, never a text/regex scan, and the tokenizer keeps scanning past a leading unparseable token instead of stopping before it reaches the key). **AC7 met** — `npm run test` 1400/1400 after owner commit `2819d0962` (external to this diff) resolved the one pre-existing failure. Not self-approved. P1. | The live claim is a **visible inline `<span data-ledger-claim>` element** whose body is the whole compared value (fixes B·P0-1: value no longer detached from prose). `SOURCE-RETIRED` is **removed from v1 entirely** — a missing source is bad input, exit 2 (fixes the 5-of-5/3-of-5 factual defect that sank `REVISION 4`). A `SHALLOW-REPOSITORY` preflight check runs before any source is evaluated (fixes B·P0-3). Marker lifecycle is now a firm rule, not a `D3` recommendation: every marker is stripped from a carrier **before** it closes or archives, so no marker outlives its document (fixes B·P1-8/P1-9; no owner decision left open on this point). **Owner decision D1 (2026-08-20) is CLOSED**, quoted in-document verbatim. `openP0`/`openP1`/`openP2` still derive off `findings` (`check-review-ledger.mjs:927-929`), unchanged. **The owner's quoted, dated approval (2026-08-20) is recorded in the decision document; the `scripts/` write block is lifted and Phase 2 may begin under `REVISION 5`.** The owner explicitly accepted one Phase 1 evidence variance: no contemporaneous pre-edit `git hash-object` for the decision document and no hash row for the session log; the retained `revision5-*` validation logs are the accepted Phase 1 evidence. Session log `2026-08-20-task747-phase1-revision5-rework.md`; adversarial review dossier for the rejected `REVISION 4` brief at `docs/reviews/artifacts/2026-08-19-task747/revision4-brief-adversarial-reviews.md`. Kickoff `tasks/Sprints/Sprint_61_kickoff_prompt_Task_747_Ledger_State_Projection_Gate.md`; rework brief `tasks/Sprints/Sprint_61_Task_747_phase1_rework_brief.md`. **Phase 2 (2026-08-20):** `scripts/check-ledger-claim-projection.mjs` + `scripts/__tests__/check-ledger-claim-projection.test.ts` (45 tests) + 3 fixtures + `npm run check:ledger-claim-projection`; one real marker planted on `docs/reviews/2026-08-16-task741-final-closeout.review-ledger.json`'s `decision` field in the session log below; AC4/AC5 plants both hash-proven restored; AC3 reworked to run through the real parser/scanner path; isolated lifecycle proof added. Not wired into CI. Evidence: `docs/sessions/2026-08-20-task747-phase2-ledger-claim-projection.md`. **Do not fold in 746 or 750.** |
| **762** | ✅ `APPROVED WITH NOTES` 2026-08-21 (Revision 1) | **Tailwind runtime tokens in CSS Modules.** Gate `check:tailwind-runtime-tokens` derives ownership from Tailwind's own version-pinned `theme.css`/`index.css` plus the `--tw-` prefix, which **wins over `globals.css`** — the fix for the two bypasses the first review reproduced (a one-line `@theme inline` silencer; new Category-B debt passing green). Scan covers `var()` reads, declarations and `transition-property`/`will-change` name lists. Category C+D removed from 9 files; zero rendered delta except 7 enumerated inert cells (transparent box-shadow layers, never-set `--tw-gradient-*` names). Baseline 14 = Category B only, now visible. **Owner decision D762-3 still open:** whether `--text-*` joins 763 or is scoped separately. Recorded for 763: `src/app/[locale]/page.tsx:31,34` reads `var(--text-3xl/4xl/5xl/xl/2xl)` from TSX, outside any CSS-Module scan; the 7 `*-chrome.css` files are clean today but unscanned. Ledger: `docs/reviews/2026-08-21-task762-revision1-category-c-and-gate-bypass.review-ledger.json`. Brief: `tasks/Sprints/Sprint_62_Task_762_revision_1_Category_C_And_Gate_Bypass.md`. Sessions: `docs/sessions/2026-08-21-task762-revision1-category-c-and-gate-bypass.md`, `docs/sessions/2026-08-21-task762-tailwind-runtime-tokens.md`. |
| **761** | reserved — **Sprint 61**, filed by the 747 Phase 2 review, **P1** | **`check-ledger-claim-projection` fence detection is author-reachable in both directions.** `markFencedLines` (`scripts/check-ledger-claim-projection.mjs:111`) toggles on any run of three-or-more backticks or tildes with any leading whitespace, remembering neither the opener's character, its length, nor its indent. **Fail-open arm:** a backtick run indented by 4 spaces is CommonMark *indented code*, not a fence opener, yet it flips the flag — a live marker after it is silently unscanned, not even syntax-checked (reproduced: `flags=[true,true]`, 0 results). The same holds for any file with odd delimiter parity; **4 are already in scan scope** (`Opus_Task_253.md`, `Sprint_17_kickoff_prompt_Task_288.md`, `Sprint_35_kickoff_prompt_Task_421_Slice6_HarnessButtonAndPopupAssertions.md`, `Sprint_43_kickoff_prompt_Task_572_HeroSearchSearchButtonWrap.md`). **False-positive arm:** a `~~~` line inside a backtick fence, or a closer shorter than its opener, exits the fence early and makes genuine code-block content scannable (reproduced: 1 result each). Both arms share one root and a one-sided fix closes only half. This contradicts `REVISION 5`'s **"a checker-evaluated syntax rule, not an author exemption"**. Fix must track opener character, length and indent per CommonMark. **Tests required:** indent >=4 · indent <=3 control · mismatched delimiter type · closer shorter than opener · indented closer. **Open owner question:** should an unclosed fence at EOF be bad input (exit 2) instead of silently swallowing the rest of the file — otherwise one stray delimiter permanently silences the gate for that document. No live marker is affected today: `check:ledger-claim-projection` exits 0 on the current tree with 1 marker, 0 drift, 0 bad input. **Do not fold into 747** — 747 is closed `APPROVED WITH NOTES` on this note. |
| **757** | ✅ `APPROVED WITH NOTES` 2026-08-21 (with **757R**) | **`AuthSheet` de-Tailwind** — 46 sites / 168 tokens → 0, canonical Story added and registered, `MantineAddItemPanel` consumed unmodified, separator reproduced 1:1. Two defects corrected by 757R (see below). Ledger superseded: `docs/reviews/2026-08-21-task757-authsheet-detailwind.review-ledger.SUPERSEDED.json`. Decisions D757-1…9: `tasks/Sprints/Sprint_60_owner_decisions_Task_757.md`. Session: `docs/sessions/2026-08-20-task757-authsheet-detailwind.md`. |
| **757R** | ✅ `APPROVED WITH NOTES` 2026-08-21 | **`AuthSheet` runtime-token correction** — `h3` → `lh={1.25}` (22.5px, unitless, live-measured); 8 `var(--default-transition-*)` → literal `150ms` / `cubic-bezier(0.4, 0, 0.2, 1)` (`--ease-standard` is `@theme inline`, absent from every built stylesheet — must NOT be used); Revision 1 restored the `[data-perf-tier="low"]` override 757 had broken, probed per class in both attribute states. 78/78 zero-delta matrix against a hash-verified preserved baseline. Ledger: `docs/reviews/2026-08-21-task757R-authsheet-runtime-tokens.review-ledger.json`. Session: `docs/sessions/2026-08-21-task757R-authsheet-runtime-tokens.md`. |
| 667 | reserved — **BLOCKED**, Sprint 59, gated behind 751 | Route inventory of what `/[locale]` **actually mounts**. **Method superseded by owner decision D-C (2026-08-16): route-level DOM on a real dev server is the mount evidence; Storybook is supplementary only.** Story-first *enrolment per migration slice* is untouched and still governs migration tasks. Boundary is the full render chain `app/layout.tsx` → `app/[locale]/layout.tsx` → `app/[locale]/page.tsx` (D-A), tagged `global shared UI` / `locale shared UI` / `route body`. Six decisions remain unapproved (D-B/D-D/D-E/D-F/D-G/D-I) — **D-G** (the `deliberate native wrapper` allowlist: intent is not in the DOM) and **D-I** (non-rendering mount points: providers, head scripts, resource hints, third-party — the three-way classifier cannot express them) block hardest. Decision note: `tasks/Sprints/Sprint_59_kickoff_prompt_Task_667_Homepage_Route_Semantic_Inventory.md` |
| 675 §8 family | **676** → S57 · **679** → S56 · **677** owner | **676** `globals.css` stale hex comments — **694's preflight already classified the hardest slice, 2026-08-10:** of the 13 `--x: oklch(…); /* …#HEX… */` rows, **11 are wrong by ΔE00 > 1.0** — `--badge-new` **10.82** (renders `#00842C`, comment says `#16A34A`) · `--neutral-600` **10.26** · `--neutral-700` **9.96** · `--neutral-500` **9.52** · `--neutral-900` + `--neutral-0` **6.76** · `--neutral-400` **5.76** · `--badge-premium` **4.83** · `--neutral-300` **2.07** · `--neutral-200` **1.30** · `--neutral-100` **0.62**; only `--neutral-50` is exact. Documentation drift, no rendered consequence. **`--brand-950` is 694's, not 676's** — 676 must not re-touch it · **677** the `<div>`-in-`<p>` FiltersPanel warning (owner item) · **679** `usePropertyTypes` fallback localization (`:16` returns the raw enum as the label) — **folds 680**, the `check:locale-leak` lowercase blind spot at `:220` that structurally cannot see it. Sprint 56 runs the detector fix first so the leak fix has a failing arm. |
| 682 · 683 | **682** → Sprint 57 · **683** blocked | **682** drop `sonner` + `next-themes` from `package.json` (681) · **683** TailAdmin bottom-nav conformance slice, no reference row exists yet (672). |
| 745 | reserved — **Sprint 46.10**, filed by 742's review, Q4 | **A partially unstyled capture is admitted as a real width verdict.** Proven, no re-run needed: `mantine-primitives-textinput--default × it × mobile-390` captured raw UA inputs at default width in a serif face, passed `styleIntegrity` (threshold is `failCount < 2` over `[preflightOk, sheetsWithRules, fontOk]`, `:1101`) and landed as a hard `fullWidthControlsAtMobile: false`. **Not a font-load race** — a computed `fontFamily` of bare `"Times New Roman"` means author CSS never applied to that element; a merely unloaded webfont still computes the declared stack. **Preserve the `:690` invariant for VALID frames** — a genuine `fullWidth=false` must never be retried into a pass (Task 573's deliberate rule). The fix goes **before** the width verdict: detect that the **measured controls themselves** are unstyled, retry the capture, and only after retries are exhausted classify the cell as an uncited **`unstyled-render`** — never as a product width regression. **Do not build the rule on the global first-text-node `fontFamily`** (`:1078`, a TreeWalker over the whole document): that signal can be unrelated to the control being measured. A second, equally invalid frame in the same run (`homepagelistinggrids × sq × desktop-1024`) **passed** only because `fullWidthControlsAtMobile` is `null` at desktop — the guard admits invalid frames in both directions. **Measured incidence (742's three runs, 1204 cells each): 0 · 2 · 0 fallback-font captures** — intermittent and real. Per **D37** the mechanism is NOT established; 745 must characterise it rather than assume the 742 anomaly's explanation. |
| 744 | reserved — **Sprint 46.9**, filed by D36 | **`/[locale]` client-chunk attribution — the 435 kB nobody has attributed.** Route First Load JS **619 kB** against **184 kB** shared (measured at `0dac78755`), so **435 kB** is route-specific client JS. It has never been attributed to anything; the barrel suspicion (`@/design-system/mantine/patterns`, 671/675 F3) remains `NEEDS VERIFICATION` and is a hypothesis, not a finding. **Deliverable is the attribution, not a fix** — name what each chunk contains, with the barrel and the homepage client islands as the two named starting points, and say which of them a server/client boundary change could actually move. **Do not bundle a remediation into this number**; a fix built on an unverified cause is how 691 came to be blocked for weeks. **Independent of 691** — D36 explicitly separated them so neither waits on the other. |
| 743 | reserved — **Sprint 46.8**, filed by 700's review | **The ownership blind spot 700 cannot see (700 F1).** `check:css-vars` computes ownership live from `globals.css` (R3), so **deleting** a token there un-owns it *together with its orphaned consumers* and the gate goes silent. Reproduced in review: removing `--color-overlay-foreground`'s single declaration while leaving **6** live TSX consumers in `LightboxView.tsx`/`MantineListingGalleryPattern.tsx` gives `0 violations, exit 0`. 700 covers "declaration stays, stops shipping" (Task 690's case) and **not** "declaration deleted", which is at least as common and which nothing else catches. **The naive fix reopens the 112 Mantine runtime false positives**, so 743 must decide the scoping rule — was-owned-at-`HEAD`, an explicit third-party prefix set, or something better — and prove it with a planted deletion. Defect in 700's kickoff R3, authored by the orchestrator. |
| 730 · 734 · 735 · 736 · 738 · **746** · **750** · **759** · **760** | reserved — full text in [`docs/backlog-reserved.md`](backlog-reserved.md) | **730** chip-row toggles announce no selected state (Sprint 55, folds 731) · **734** 25 N1 violations inside `src/design-system/mantine`, incl. the `zIndex: 100` sticky-layer escalation · **735** enrol `System/FeaturedListings` + a `wide-1440` extra-viewport entry · **736** classify the AdminUsersTable locale-leak findings, do **not** translate them · **738** the horizontal-scroll blind spot, 729's residual 40 · **746** P1, filed by 691's review — no control can see a hover-state or real-consumer (`AppImage`, not `DemoImage`) regression on the listing card; define the detector + its false-positive boundary first, then three failing plants· **750** P3, filed by 749's review — the `ambiguousOverlap` counter (`check-stories-rendered.mjs:1804`) prints as `| ambiguous-overlap | N |` (`:1859`) but aggregates **five** reasons after 749 (measured: overlap 4 · offscreen 4 · ellipsis 16 · text-clipped-scrollable 18 · outside-scrollable 6); same family as **738**. Carries N1: record in `storybook-governance.md` that a `GEOMETRY_ALLOWLIST` row is strictly more permissive than a structural predicate. |

- **Registry bookkeeping.** *Described once elsewhere:* 665 · 691 (above) · 310 · 311 · 313 · 308–309 (HH) · 453 (KK.2) · 463 (BB) in Epics. *Standalone deferred:* **560** admin suspension-as-date-range — **sourced, keep** (split out at `Sprint_41_kickoff_prompt_Task_557_…md:26,:36`, DEFERRED 2026-07-06), blocked on the owner defining suspension-window semantics · **SaveToCollection dialog → Mantine** (654 follow-up, still shadcn). *Not plain numbers:* 622/623 (`Q0R`, `623R`) · **709-R** (closed with 709) · **628** SB10 lint-debt. *Retired, never reuse:* 465 · 466/467/469–481 · 534 · 597/604 · 655. *Folded into another number, never reuse:* 732 → 722 · 728 → 721 · 687 → 678 · 731 → 730 · 680 → 679 — each folded only after its detail moved into the receiving sprint's plan file; none was dropped.

**Unnumbered follow-ups:** the `ListingCard`/`HomepageListingGrids` live relative-date fixture spuriously flags on any Q3 re-capture taken on a different calendar day · add `TextInput/Default`, `PopularLocationsView/Long City Name` (sq@390) and `LocationComboboxSubPanel/Default` (en@320) to `storybook-governance.md` §8.1 · no regression test for `check:mojibake` (666 F1) · `check-homepage-grid.mjs`'s missing `emulateMedia` (704/705 F1) stays **assessed harmless — do not re-escalate** · one-time triage of the standing AMBIGUOUS cells (43 at 661's review, 22 today) — owner-triage by design, never citable as green proof, but never actually triaged.

## Active Epics

**HH — Admin UX System** 🟠 310 (P4) · 311 (P5 partial) · 313 (P6, blocked on owner DB sign-off); 308/309 re-scope onto canonical primitives → [`Epic_HH_…`](../tasks/Epics/Epic_HH_Admin_UX_System.md) · **KK — Admin Data Freshness** 🟠 452 done. **453 is conditional** — it opens *only if* 452's investigation exposes a page needing special handling; no such page has been named → [`Epic_KK_…`](../tasks/Epics/Epic_KK_Admin_Data_Freshness.md) · **MM — Mantine/TailAdmin Restyle** 🟠 Phase-1 primitives COMPLETE; Sprints 37–44 landed, the structure lapsed, resumed at 46. ⚠️ **Do NOT use the tracker as the "what's next" pointer** (owner item above) → [`Epic_MM_…`](../tasks/Epics/Epic_MM_Mantine_UI_Migration.md)

## Standing notes

> **Binding decisions:** **D37** (2026-08-11, owner) **one passing re-run does not retro-explain a failing one.** When a rendered cell fails once and passes on re-run, the acceptance criterion closes on the **result of the final run**, not "with a changed set documented as a flake". The first artifact is preserved and labelled **`UNATTRIBUTED`**: a single passing repeat demonstrates instability, it does **not** establish the mechanism, and naming a mechanism it did not measure is the same substitution of hypothesis for evidence that a comparator exists to prevent. · **D36** (2026-08-10, owner) **691 is allowed to run; the perf gate is "no increase", not "no excess".** Owner's reasoning, recorded because it corrects a category error the backlog carried for weeks: 691 is a mechanical de-Tailwind of an already-`'use client'` `MantineListingCardPattern` — it adds **no new client boundary and no new page dependency**; the barrel suspicion is still only `NEEDS VERIFICATION — not blocking`; and **185 kB shared JS and 618 kB route First Load JS are not comparable quantities**, so their difference was never a valid pre/post base for 691. Blocking a local, visually-risky refactor on an unattributed pre-existing number also freezes **741** and **695** with no guarantee of any perf benefit. **Condition:** record `/[locale]` First Load JS before and after, stop 691 on any increase. The route-specific client JS above shared — **435 kB measured**, not the ~433 kB estimated — is a separate attribution problem, filed as **744**. *(Both figures in the owner's statement were re-measured against the build at `0dac78755`: route **619 kB**, shared **184 kB**. The condition binds to the I0 re-measurement, never to a number quoted from a document.)* · **D19 — CLOSED 2026-08-13 (695).** Overlay pair no longer declared in `@theme inline`; `:root` is the single source (694 no longer touches the pair — D35). · **D35** (2026-08-10, owner) **a `@theme inline` value that feeds an opacity-modifier static fallback may not be aliased to a runtime `var()`.** Measured on the repo's own `tailwindcss@4.3.0`: `--overlay: var(--mantine-color-black)` collapses `.bg-overlay\/30{background-color:#0000004d}` to a bare `var(--overlay)` — Task 690's regression exactly, compound-selector collapse included, across **12** opacity-modifier utilities. This is why 694's original alias scope is closed, not deferred. The 660/661 alias convention is unaffected: `--brand-*` lives in `:root`, never `@theme`, and the built CSS has **zero** `bg/text/border-brand-*/NN` utilities — the convention has never once been applied to a token consumed with an opacity modifier. · **D25** frozen Storybook preview clock. · **D26** sub-perceptual delta **≤2/255** is an attribution path under 4 conjunctive conditions only (`storybook-governance.md` §14.11). · **D27** Skeleton `::after` fill = gray-3 `#d0d5dd`, token not hex. · **D28** (2026-08-01) de-hybrid = **mechanism-only, zero visual delta**; authorizes no restyle, token, spacing or typography change. · **D32** a migration may not be proven against a comparator not shown to fail. · **D33** re-anchor a gate onto a de-Tailwind-stable hook, never another utility class. · **D34** (2026-08-05) a D28 module reproduces the utility's cascade **layer** — wrap in `@layer utilities`; the inverse 602/629/650/651/653/654/656 family stays **unlayered** on purpose (a migration reproduces; a cascade-trap fix overrides).

> **Recurring orchestrator failure mode — read before writing any kickoff (M1 · M2 · M4 · M5).** Four blocked/revised tasks failed identically: the *fix* was right, the *control* could not detect its own effect. **Every kickoff needs a two-armed plant that can demonstrably fail, plus a pre-plant census proving no further lifeline.** Reconcile git state from `git log`/`git status`, never a session log's self-description. **Corollary (709):** a kickoff's own "measured" facts are not exempt. **Corollary (710–714, 2026-08-06):** four consecutive kickoffs shipped a factual defect the executor had to correct — a stale line reference, a `grep` that counted `.split(` as a test, a crude literal census, and a requirement that was *unsatisfiable* because the detector could not see the target syntax. **Measure with the real tool, not an ad-hoc grep**; see `orchestrator-procedures.md` → "Detector-aware requirements and migrations" (`6c3a2054e`). **Corollary (724, 2026-08-07) — two defects, one kickoff.** ① *Never fence an artifact off as zero-diff and simultaneously require the aggregate gate that measures it to go green.* State the scoped condition — "no failure outside X" — and assign the exit code to the task that owns X. ② *A gate's own exclusion selector is an attack surface.* When a kickoff re-expresses an exclusion as a DOM property (711's `[role="group"]`), the next task can satisfy the gate by **producing** that property. Say so in the kickoff, and require every exemption to be a condition the gate evaluates — never one an author applies. **Corollary (726, 2026-08-08) — story markup that exists only to exercise a selector, gate or measurement is a probe, not a permanent artifact.** A missing API-specific story is not authorization to add one; a permanent addition needs a named in-scope production consumer or quoted owner authorization. Blocking in both `create-task/SKILL.md` and `execute-task/SKILL.md`: it stopped 726 draft 2, and 726 then proved the probe/revert path end-to-end. **Corollary (661, recorded 2026-08-08) — a verdict that is not written down did not happen.** 661 was `APPROVED WITH NOTES` and committed on 2026-07-23; the same session then pivoted straight into writing 662's kickoff, 662's outcome got recorded and 661's did not, and the backlog still read `AWAITING ORCHESTRATOR REVIEW` sixteen days later — long enough that the orchestrator proposed re-reviewing already-approved, already-committed work. This is the **second** occurrence of the identical failure (703/704/705, 2026-08-01). **Write the verdict, the archive row and every spawned follow-up before starting the next artifact, not after.** Follow-ups a review names but does not file are lost the same way: 661's ΔE sync-check went unfiled for sixteen days. **Corollary (721, 2026-08-08) — the kickoff's own measured facts are still not exempt, now a fifth time.** 721's kickoff stated band-700's applicable set as "4 locales × 1 viewport = 4 cells"; `MANTINE_STORY_EXTRA_VIEWPORTS` keys by **component**, and `HeroSearch` has two stories, so it is 8. The executor measured it correctly and the orchestrator did not. When a kickoff derives a count from a config, **read what the config keys on**, not what it appears to key on. **Corollary (717 · 721 · 722) — three consecutive executors misreported `docs/backlog.md`'s line count** by measuring it after their own edit and calling the result pre-existing. Take the baseline from `git show HEAD:docs/backlog.md | wc -l` before writing. **Corollary (694, 2026-08-10) — the kickoff-fact defect is now a sixth occurrence, and this one was the orchestrator's own, written the same day it was executed.** §13.2's plant matrix listed P1's failing set as "B, C, E"; assertion C compares `--accent` to `brand[0]` and P1 edits `brand[7]`, so C structurally cannot fire. The executor measured B+E, said so, and did **not** widen C to react to an unrelated token — the correct disposition, and the reason a plant matrix must state *which assertion consumes which input*, not merely which assertions exist. **Freshness is not accuracy: a fact measured an hour ago is exempt from nothing.** The same task read its backlog baseline from `HEAD` correctly — a third consecutive clean read after 717/721/722. **Corollary (702, 2026-08-10) — seventh occurrence, and the tell is now specific enough to act on: all three of 702's defects were *derived* claims, not measurements.** §3's numbers survived re-derivation; what failed was the reasoning laid over them — "688 already reproduces this composition" (it flattens instead), "no markers needed" (true of the `.tsx`, false of the module it creates), and a command list whose flag silently skips the phase its own AC depends on. **Inspecting a value is not inspecting the claim built on it: open the cited file and the cited flag's code path, not just the number.** §15's quality-gate row asserting nothing went uninspected was itself the defect. **Corollary (702, same day) — the unwritten-verdict failure is now a THIRD occurrence** after 661 and 703/704/705, and this time the gap was hours, not weeks: 702 was approved and committed while `docs/backlog.md` still read `IMPLEMENTED - AWAITING ORCHESTRATOR REVIEW`, so the next session handed it to an executor as live work. It was refused on artifact evidence, which is the only reason this cost a round trip instead of a duplicate implementation. **Sixteen days and four hours produce the same defect — approve, archive and update the backlog as one indivisible step.** **Corollary (2026-08-10, same day, FOURTH occurrence) — "the backlog" is not one file, and updating it is not one edit.** 702's verdict was recorded in `docs/backlog.md` and its sprint plan's **Tasks table was left at `KICKOFF FILED`**, so an executor was again offered approved-and-committed work — this time it surfaced the row as a menu option instead of acting on it, which is the only reason it cost nothing. **Root cause: `Sprint_NN_*.md` carried task state in two tables and only one was maintained.** Fixed structurally rather than by another reminder — the Tasks table is now the single state source, the execution-order table is order-and-gating only, and both say so in-file. **When closing a task, enumerate every artifact that names its state — backlog registry, backlog Last Session, sprint Tasks table, sprint order table, archive — and change them in the same edit.** **Corollary (700, 2026-08-10) — a gate's scoping rule is also its blind spot, and the kickoff must say which half it does not cover.** 700's R3 scopes the gate by ownership computed live from `globals.css`, which is what makes it precise (the unscoped form reports 112 Mantine false positives) and simultaneously what makes it blind: deleting a token there un-owns it **together with its orphaned consumers**, so the gate goes silent on `0 violations, exit 0` — filed as **743**. **When a requirement narrows a detector to make it usable, state the class the narrowing excludes, in the kickoff, before the executor builds it.** Also 700: the same rule silently made two of the kickoff's own plants self-immunizing, and the executor caught it — **write plants against the observable defect, not against the mechanism you assume produces it.** And its re-scope is the counterexample worth keeping: the reserved premise was measured false *before* the kickoff was written, which is the only reason a gate that could never fail did not ship.

> **No CI gate asserts the Mantine composition of a *route*** (2026-07-26, still true). `--mantine-only` scopes by Storybook title prefix; only `check:story-coverage` reads `mantine-migration-scope.json`, treating anything absent as out of scope. `15/15` proves coverage of fifteen enrolled components — not the homepage.

> **🟡 Console NOISE (not bugs — do not re-triage as P0):** `[PRED] … preloaded`, `[LCP] … route`, `[Vercel Speed Insights] debug`, "speculation rule set … will be ignored", Turbopack dev-only CSS-chunk/`*.woff2` preload warnings. OpenTelemetry `import-in-the-middle` fixed by Task 450; Cloudinary LCP preload by Task 437. A stale Turbopack HMR cache can emit a one-off `useId` hydration error that does not survive a clean `next build` (Task 582). **Advisor exceptions** (intentional, no task): `pg_net in public`; `email_change_tokens` RLS-enabled-no-policy — `docs/rls-rules.md`.

> **Standing governance:** `ai-behavior.md` Notes 18–23 + `agent-contract.md` (P0, clauses 1–15) + `rule-index.md` + `orchestrator-procedures.md` (git policy, commit/push handoff, index.lock preflight) + the Positive/Negative flow rule. Non-optional on every task. **`npm run build` exit 0 mandatory for non-Q0.** **Design-system floor:** every task consumes `docs/design-system.md` wherever UI/responsive/overlay is touched; new UI uses Mantine (`docs/mantine-responsive-design-system.md`), and `docs/rule-index.md` sets the per-task pre-read.
