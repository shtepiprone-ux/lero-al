# Task 495 — Select primitive → TailAdmin (Sprint 38, P1.03)

**Date:** 2026-06-27  
**Sprint:** 38, Batch B  
**Status:** CODE COMPLETE — awaiting orchestrator rendered-proof review

## Summary

Select input chrome (resting/focus/error) added to `input-chrome.css` mirroring TextInput.  
Disabled state already covered by Task 507 — not duplicated.  
New story with 4 sections + `sel_*` locale keys ×4.  
Dropdown bottom-sheet `<640` deferred to Batch C (documented in story caption).

## Changes — Files Changed

| File | Change |
|------|--------|
| `src/design-system/mantine/input-chrome.css` | Added Select resting/focus/error rules (before existing disabled block): `border-color: gray-2`, `box-shadow: shadow-xs`, `::placeholder gray-4`, `:focus brand-3 + ring`, `[data-error] red-6` |
| `messages/en.json` | +8 `sel_*` keys: `sel_label`, `sel_placeholder`, `sel_hint`, `sel_option_apartment`, `sel_option_house`, `sel_option_commercial`, `sel_option_land`, `sel_error` |
| `messages/uk.json` | +8 `sel_*` keys (Cyrillic; `sel_option_commercial` = "Комерційна нерухомість" — long, wraps at 320) |
| `messages/sq.json` | +8 `sel_*` keys (Albanian) |
| `messages/it.json` | +8 `sel_*` keys (Italian) |
| `src/stories/mantine/primitives/Select.stories.tsx` | New story: 4 sections (basic / open-state / error / disabled); `skipCanvas:true`; all strings via `storyT`; Batch C deferral caption |

## theme.ts

No change needed — `Select: { defaultProps: { radius: 'lg', size: 'sm' }, styles: { input: { minHeight: '2.75rem' } } }` already correct (flat props only, no border/state keys).

## Selector notes (for orchestrator DevTools verification)

Mantine Select renders its trigger as `.mantine-Select-input` (an `<input>` element):
- Resting: `border-color: gray-2` via `.mantine-Select-input`
- Focus: `border-color: brand-3` + ring via `.mantine-Select-input:focus`
- Error: `border-color: red-6` via `.mantine-Select-input[data-error]` (NOT `data-invalid`)
- Disabled: already handled by Task 507 (`.mantine-Select-input:disabled` + `[data-disabled]`)

Chevron (`rightSection`): Mantine's default — color inherits from Mantine vars (gray-5 region), not hardcoded.

## Gate results

- tsc = 0 ✅
- `check:stories` = 85 files, 0 violations ✅
- `check:i18n` = 1997 keys (parity en/uk/sq/it) ✅ (+8 from 1989)
- `check:design-tokens` = 0 violations ✅
- `check:mojibake` = 0 artifacts ✅

## Rendered proof required (orchestrator)

- uk@320/375/390: trigger full-width, gray-2 border, brand-3 focus, red-6 error, faded disabled
- Long uk option "Комерційна нерухомість" in dropdown: no clip, no h-scroll at 320
- Planted-violation: remove `border-color: var(--mantine-color-red-6)` from error rule → border stays gray → revert
- Dropdown bottom-sheet caption visible in story

## NO git add/git commit — orchestrator emits at review
