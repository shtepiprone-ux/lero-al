# Project Backlog

> ⚠️ **ACTIVE STATE ONLY.** A row belongs here only if it names something still to be **decided or done**. Closed tasks/sprints/epics → [`docs/backlog-archive.md`](backlog-archive.md), one ledger row each, newest first. Reserved-number full text → [`docs/backlog-reserved.md`](backlog-reserved.md). Full detail → `docs/sessions/`. Kickoff detail stays in `tasks/` and is **not** restated here.
> "Last Session" = 2–4 lines. On close, move the task to ONE archive row. Rules: `docs/ai-behavior.md` → "Backlog & Session Log Rules". **The budget is operational signal, not line count** — a row that fits the line limit but restates a kickoff, a session log or a ledger is still a violation.
> **No volatile facts in this file.** No `HEAD`/commit SHAs, branch names, divergence counts, tree cleanliness, or "N today" tallies — all of them are false within one commit. Read them live at the moment you need them (`git --no-optional-locks status` · `git --no-optional-locks rev-list` · the registry heading below). Durable Git lessons → `docs/orchestrator-procedures.md` → "Git state — durable lessons".
> **Decided is not active.** Binding decisions → [`docs/binding-decisions.md`](binding-decisions.md) · recurring orchestrator failure modes → `docs/orchestrator-procedures.md` · known console noise and assessed-harmless findings → `docs/maintenance-playbook.md` §14. Only **undecided** decisions live in this file.

## Last Session (2026-09-18)

Task **837** ✅ `APPROVED` 2026-09-18 (Opus review 4, Revision 1 / run3) and archived: the detail pattern Story renders the real borderless `FavoriteButton`, the unconsumed pill shape + `favoritePill` token + pill test are deleted, and the badges row is `align="flex-start"` by owner decision 2026-09-18 (supersedes D69-27). Owner accepted all visual tuples. Every Sprint 75 Tasks-table row is now approved; the sprint stays open until the owner closes it (exit criteria in the plan file). **836** and **838** stay reserved.

## Open — needs action

| Item | State | Next step |
|---|---|---|
| **Bare-pattern gutters** | **DEFERRED by owner, 2026-09-04** — revisit when these patterns get a production consumer | `TwoColumnForm` and `PageHeaderWithActions` render flush to the viewport edge, and `FormSectionStack`'s action row sits outside its sections' `<Paper p="md">` so mobile buttons run wider than the fields. **All pre-existing** — the roots are byte-identical at `HEAD` and carry no horizontal padding. These patterns set `skipCanvas: true`, which deliberately bypasses the `.container-wide py-6` decorator, and **none has a production consumer yet**, so there is no observed page shell to infer the convention from. Owner decision: leave both untouched for now — with no production consumer there is no page shell to infer the convention from, and padding the pattern now risks double padding later. When a consumer exists, decide then between padding the *stories* (if gutters are inherited from a page shell) or the *patterns* (if they are self-contained). The `FormSectionStack` inset mismatch is **not** deferred — it is 785's R8. Re-measure at execution. |
| **786** | reserved — **P1**, filed by the 2026-09-04 production incident | **No control can see a React hook called in a Server Component**, and one took `lero.al` down (Task 784, `FooterView.tsx`). `next build` cannot catch it for a `ƒ` dynamic route, `tsc` types hooks as valid anywhere, `eslint` has no such rule, and Storybook renders every component as a client component so the hook always works there. 786 must define the detector **and its false-positive boundary** (a `'use client'` file may call hooks freely; a hook inside a nested client-only helper is fine), then prove it with a **two-armed plant** — a hook added to a Server Component must fail the gate, and removing it must clear it. Census first: `grep -rl useMantineTheme src/ --include=*.tsx` cross-referenced against `'use client'`, and widen to all `use*` imports from React. **Re-measure at execution; do not cite this row.** |
| **665** | `PARTIALLY IMPLEMENTED / PARTIALLY VERIFIED` | **Only §13.7's live-route before/after DOM/computed-style baseline is outstanding** (AC1 — needs a routable dev server + seeded DB; use §16.1's corrected `classList.contains` locator). Also decide the orphan `listing.fixture.ts` |

## Pending Action Items (owner)

| Item | Notes |
|---|---|
| 👁️ **Notification localization — one production QA pass.** ① Confirm `template_id` and `template_params` exist in Supabase. ② Open one template-driven notification under `/sq`, then the same notification under `/uk`. ③ Confirm the **same** notification localizes in **both** locales. | **PASS → archive this item.** **FAIL → file one P1 bug**, nothing else; no speculative task before a measured FAIL. Supersedes the old open-ended "eyeball `/sq`" item (owner decision 2026-08-27). |
| 🔬 **781R2 residue — four findings that only the owner's machine can close.** **F5 · F12 · R8 · R13** were deliberately left open when 781R2 was archived (2026-09-04). Their precondition table and the exact command block are in `docs/sessions/2026-09-04-task781r2-residual-closure.md` §5. A candidate `playwright/.auth/admin-storage-state.json` exists but has **never been verified for this purpose** — check it before trusting it. | Run the §5 block → each finding closes or becomes a numbered task. Do not re-derive the preconditions from this row. |
| 🖋️ **Task 313 (Verified Agents) — sign the schema contract to authorize execution.** The contract is recorded verbatim at `tasks/Epics/Epic_HH_Admin_UX_System.md` → "Task 313 — owner schema contract": `verification_state`; immutable `user_verification_events`; a DB transaction that validates allowed state transitions **and** writes the audit event; public output exposes **only** `is_verified`; admin-only by default, moderator explicitly denied until separately granted. | Owner signs the contract → 313 becomes executable. Until signed, no kickoff and no implementation. |

## Sprints

> **Owner rule, 2026-08-01: every task belongs to a sprint.** Kickoffs live at `tasks/Sprints/Sprint_NN_kickoff_prompt_Task_NNN_<Slug>.md`, never the root of `tasks/`. Closed tasks and sprints move to one archive row each.

**Sprint 46 — ListingCard de-Tailwind + overlay exit** (`tasks/Sprints/Sprint_46_…md`): 🟠 OPEN for **741 Revision 2** (`NEEDS REVISION`, reopened by the owner 2026-09-17: one canonical "Sold"/"Rented" style on every card, hardcode removed — kickoff §16) and its retained follow-up scope: **744** (46.9) and **745** (46.10) — **743 moved to Sprint 75** 2026-09-11, where its blind-detector family belongs. **D28 + D34 + D36 + D37 bind the remaining work.**
**Sprint 55 — ARIA semantics no gate sees** (`tasks/Sprints/Sprint_55_ARIA_Semantics_No_Gate_Sees.md`): **730 only** — it **folds 731** (one kickoff, not two; the sprint's own execution-order note of 2026-08-08 and its Tasks table both say so). **731 is folded and must never be issued.** Opened 2026-08-08 — 46/52/54 all fail the goal-fit test (table in the plan file). Its transferable output is exit criterion 4: name the detector that would have caught these, or record in writing why none is worth building — **760 is a live candidate answer**. 🟠 OPEN, **zero landed tasks**.
**Sprints 56 and 57 — raw enum leaks, and deleting what no longer earns its place** (`tasks/Sprints/Sprint_56_Raw_Enum_Leaks_And_The_Blind_Detector.md`, `tasks/Sprints/Sprint_57_Delete_What_No_Longer_Earns_Its_Place.md`): **56 — 679** (folds 680); the detector fix supplies the failing arm the leak fix needs, so the two-armed plant comes free, but only if the detector lands first. **57 — 676 · 682**; pure removal, both proven inert before deletion rather than after. Both opened 2026-08-08, both 🟠 OPEN with zero landed tasks. Merged into one row 2026-09-11 to hold the 80-line budget while Sprint 75 opened — no state dropped.
**Sprints 61 and 62 — the projection layer no gate reads, and Tailwind runtime tokens outliving Tailwind** (`tasks/Sprints/Sprint_61_The_Projection_Layer_No_Gate_Reads.md`, `tasks/Sprints/Sprint_62_Tailwind_Runtime_Tokens_Outlive_Tailwind.md`): **61 —** 🟠 OPEN for reserved P1 **761**, and **750** fits the goal but is deliberately not assigned. **62 —** 🟠 OPEN only for owner decision **D762-3**, whether `--text-*` joins a new task or stays separately scoped. Merged into one row 2026-09-11 to hold the 80-line budget while Task 816's governance decision took an owner-action row — no state dropped.
**Sprints 68 and 69 — `/listings` leaves Tailwind, then finishes the Mantine migration** (`tasks/Sprints/Sprint_68_Listings_Leaves_Tailwind_One_Surface_At_A_Time.md`, `tasks/Sprints/Sprint_69_Listings_Finishes_The_Mantine_Migration.md`): all tasks archived. 🟠 OPEN with **zero open tasks each**, for one shared reason: closure is an owner decision, and **D68-2** (differential rendered acceptance: `P \ B = ∅` plus a PASS on every new cell), **D69-3** (no review ledger for frontend work) and **D69-6** (canonical dimension scale) still live in their sprint files rather than `binding-decisions.md`. The deferred bare-pattern gutter question is its own row above; 781R2's owner-native residue is an owner action item. Merged into one row 2026-09-10 to stay inside the 80-line limit — no state dropped.
**Sprint 70 — the site chrome leaves Tailwind, and the mobile bar goes away** (`tasks/Sprints/Sprint_70_The_Site_Chrome_Leaves_Tailwind.md`): Goal-fit table in the sprint file rules out 46/55/56/57/61/62/68/69. Carries **D70-1** (a green `npm run build` is not evidence for `/[locale]`; a real `next start` request is the AC until **786**'s detector lands) and **D70-2** (the owner's visual matrix is the visual criterion). 🟠 OPEN for **789** only — the live cabinet filter bar. 788's consumer trace is what filed both **789** and **790**.
**Sprint 71 — the listing-detail route leaves Tailwind** (`tasks/Sprints/Sprint_71_The_Listing_Detail_Route_Leaves_Tailwind.md`): 🟠 OPEN: **794 · 795 · 796** reserved. Execution order **792 → 794 → 795 → 796**. Goal-fit table in the sprint file rules out 46/55/56/57/61/62/68/69/70. Carries **D71-1** (a green build is not evidence for a `ƒ` route — a real `next start` request is the AC until **786**), **D71-2** (the owner's visual matrix is the visual criterion), **D71-3** (a slot is a preservation device, not an incomplete migration) and **D71-4** (raw px never acceptable — create the token). 791's open residue: one documented `className="hidden"` exception (→ **794**) and the admin-preview route (R7/AC7), unverifiable without a staff session. 793's owner §18.4 run is still owed.
**Sprint 72 — the similar-listings block becomes a real entry point into search** (`tasks/Sprints/Sprint_72_Similar_Listings_Becomes_A_Real_Search.md`): opened 2026-09-09 by owner request during 792's review. 🟠 OPEN: **804** only (reserved). Goal-fit table in the plan file rules out 46/55/56/57/61/62/68/69/70/71 — 71 is ruled out by its own de-Tailwind goal sentence, the same rule the 801/802 row already applied. Carries **D72-1** (the view-all href is built from the predicate that actually produced the rendered rows), **D72-2** (`applyPublicVisibility` and the current-listing exclusion are never relaxable — registered critical flow), **D72-3** (one similarity source, two consumers), **D72-4** (the relaxation ladder is capped at four queries) **D72-5** (the row scrolls horizontally at every width, not scroll-below-sm/grid-above) and **D72-6** (2026-09-09 — the block's header takes the Featured shape: column below 640px, `justify="space-between"` row above; `page.tsx:57`'s nowrap-at-every-width variant was the rejected alternative).
**Sprint 73 — a sold listing is reachable by link, but never listed** (`tasks/Sprints/Sprint_73_A_Sold_Listing_Is_Reachable_But_Never_Listed.md`): opened 2026-09-09 by 792's review. 🟠 OPEN: **805** `KICKOFF PENDING`. Carries **D73-1** (direct-link visibility and list inclusion are two policies — `PUBLIC_VISIBLE_STATUSES` must not change), **D73-2** (fix at the RLS layer), **D73-3** (`[slug]/page.tsx:151`'s `.in(...)` is the declaration of record) and **D73-4** (the acceptance instrument is an anonymous HTTP request, never a signed-in browser).
**Sprint 74 — one card width for the whole site** (`tasks/Sprints/Sprint_74_One_Card_Width_For_The_Whole_Site.md`): opened 2026-09-10 by owner instruction after he compared the live homepage and listing page against Booking.com. Measured defect: five surfaces render `ListingCard` with five different width contracts — Featured `cols={{base:1,sm:2,xl:3,xxl:4}}`, Latest `{{base:1,md:2,xxl:3}}`, RecentlyViewed a 192px rail then a 2/3/4 grid, Similar a 83.3%→23.8% rail, `/listings` `{{base:1,sm:2,xl:3,xxl:4}}` — so at 560px the same card is 192px in one section and ≈433px in another, and Featured/Latest pass no `layoutContext` at all. 🟠 OPEN for **811 · 814** (registry below). Carries **D74-1** (one shared track: `repeat(auto-fill, minmax(var(--listing-card-min), 1fr))` for grids and the same width for rails; per-surface column ladders are removed, not re-tuned), **D74-2** (`--listing-card-min` = **280px**; 260 and 300 were the rejected alternatives) and **D74-3** (below 640px: section rails scroll with a visible peek, `/listings` stays a single full-width column) and **D74-4** (2026-09-10 — Featured and Latest are rails at every width too, so `/listings` is the only `grid` consumer). Goal-fit table in the plan file rules out 46/55/56/57/61/62/68/69/70/71/72/73. **D74-6** (2026-09-10 — on a rail `--listing-card-min` is a **maximum**: the card shrinks to a fraction of the track so a tail of the next card is always visible when more cards exist than fit; measured trigger — a 872px container fits exactly three 280px cards and leaves 0px of the fourth. In the grid it stays the `minmax()` minimum). **D74-7** (`offset`=36; the reference's 30 leaves 0.64px at 1344px — rejected), **D74-8** (a click scrolls `floor(clientWidth/(card+gap))×(card+gap)`, a whole snap-aligned page, so AC3 stands and the code moves to it) and **D74-9** (the count ladder keys on the **container**, not the viewport: `@container` on a `container-type: inline-size` wrapper at the same 30/48/64/80em rungs — D74-5 stays a media query) were taken on 810's review 2026-09-10. **D74-10** (Favorites is a **grid** consumer of the track — it is paginated; amends D74-4's "only grid consumer" sentence), **D74-11** (`'3-col-xl'` deleted in 809 — exactly two consumers, both in its scope) and the owner's full-de-Tailwind scope decision were taken on 809's kickoff 2026-09-10.
**Sprint 75 — the gates that report green on what they cannot see** (`tasks/Sprints/Sprint_75_The_Gates_That_Report_Green_On_What_They_Cannot_See.md`): opened **2026-09-11** after 809 closed. Goal: a gate's scoping rule is also its blind spot — each measured instance gets a detector with a proven false-positive boundary, or a written record that it is deliberately undetectable. 🟠 OPEN with **zero open tasks** — 837, the last Tasks-table row, was approved 2026-09-18. Exit criterion 2 is met; closing the sprint against criteria 1 and 3-5 is an owner decision. **786 is deliberately excluded.** Every closed task of this sprint is in `docs/backlog-archive.md`; order and goal-fit live in the plan file.

## Task registry — single source for every open number. Last used **838**, NEXT FREE **839**.

> Allocatable and reserved numbers only. A number leaves this table when its task is archived. Full justification for a reserved number → [`backlog-reserved.md`](backlog-reserved.md); retired and folded numbers → the same file's "Registry bookkeeping".

| # | State | What |
|---|---|---|
| **805** | **Sprint 73**, P1, filed 2026-09-09 by 792's review | **An anonymous visitor gets a 404 on a `sold` listing; a signed-in one renders it.** Measured: anonymous `Invoke-WebRequest` returned `NEXT_HTTP_ERROR_FALLBACK` at 190,327 bytes on the same URL the owner's browser rendered. So the status banner and its "see similar listings" control — the thing 792 fixed — are unreachable for the public. Three layers disagree: `visibility.ts:16-22` marks every non-active status `publicEligible:false`, `[slug]/page.tsx:151` deliberately admits `['active','sold','rented','archived']`, and RLS wins. **Owner decision 2026-09-09: sold/rented/archived MUST be publicly reachable by link.** The fix is RLS, not `visibility.ts` — all seven `applyPublicVisibility` consumers are list queries (search, `/api/listings`, saved-search cron, `SimilarListings`, 3× `lib/queries.ts`), so flipping the policy would push sold listings into search results and saved-search emails. No `supabase/` dir: policy SQL ships as `scripts/task-805-*.sql` and the owner applies it. Open owner decision: whether `expired` joins the reachable set. |
| **804** | **Sprint 72**, filed 2026-09-09 by owner request | **804** reserved — the filter surface cannot express what a listing is: `bedrooms`/`bathrooms`/`toilets` have **no** parameter anywhere (measured: absent from `ParsedFilters`, `FilterValues`, `parseSearchParams`, `applyListingFilters`), while `heating`/`wall_type`/`offer_type` parse and apply end-to-end but have **no control** in `ListingsFilters.tsx`. Two different defects, one surface. Kickoff → `filterEngine.ts:178-212` is the measured ceiling. **Fold in (803's review, F7):** `rooms` disagrees across the two layers — `similarity.ts` emits `.eq('rooms', n)` while `applyListingFilters` (`filterEngine.ts:263-271`) reads `rooms=5` as "5 or more" and applies **no** rooms filter at all for `rooms>5`, so a similar-listings link is silently broader than the block it came from. 803 carries only a comment; the reconciliation is 804's. |
| **811 · 814** | **Sprint 74**, open | **811** **Two page-frame widths, and nobody decided which is canonical.** Measured at a 1440px viewport: `/favorites` container **1344px**, `/listings` **1312px**, identical 4-column count — the 32px gap divides into the **8px** card-width difference 809's probe hard-fails on at 1440 (`runs/reverted-clean-1`, every clean run). Task 810's `rev1-baseline` measured the **homepage** at 1344 as well, so `ListingsPageFrame` is the outlier, not `/favorites/page.tsx:83`'s legacy `.container-wide`. **D74-12: 1344 is canonical and `/listings` adopts it.** Scope: `ListingsPageFrame` only — and re-measure all three surfaces before and after, because the acceptance test is that 809's probe's 1440 cell goes to 0px, not that a number in this row was reproduced. Sprint 74's exit criterion 3 is what this closes.  · **814** filed 2026-09-10 by 809's second owner rejection — `src/modules/listings/` still imports `@/components/ui/{button,input,dialog}` 27 times outside the FavoritesShell tree (listing-creation wizard `form/`/`steps/`, `ListingGallery`, `ListingInquiryDialog`, `ListingReportDialog`, `ClearRecentlyViewedButton`, `ListingMobileCTA`) — explicitly out of 809's scope per the owner's own "на сторінці" (on the page) wording, named here instead of silently dropped.  |
| **801 · 802** | reserved — **needs its own sprint**, filed 2026-09-06 by owner request | **Listing-page feedback, modelled on dom.ria.** **801** the public blocks on `/[locale]/listings/[slug]`: "оголошення неактуальне чи інформація неточна?", "оцініть сторінку пропозиції" (1-10 + reason chips + free text + optional contact), and "надсилати нові пропозиції за цим пошуком?"; owns the data model (rating, reason tags, comment, reporter, listing, status) and its RLS. **802** the admin surface: a ratings popup from the admin listings row, and a ratings page where each row links to the rater's profile and carries rating + comment + a **status** (new / in progress / closed). 802 depends on 801's schema. **Not Sprint 71** — that sprint's goal sentence is a de-Tailwind migration and mixing a feature in breaks its own goal-fit rule. Open a sprint when the migration slices are done or run it in parallel; the owner decides. |
| **799** | reserved — **P1**, filed 2026-09-06 by the owner's visual review | **The Storybook viewport switcher does not resize the preview, and it never has for any owner review.** Reproduced twice: the owner via the toolbar, the reviewer via `?globals=viewport.value:mobile390` — `#storybook-preview-iframe` stays at `width: 1280px`, wrapper `transform: none`. `.storybook/preview.tsx`'s `VIEWPORTS` map is correctly shaped (every entry has `styles:{width,height}`), so the fault is the addon wiring, not the values. **Blast radius:** D71-2 and `docs/qa-profiles.md` make rendered width the core of every `Q2`-`Q4` owner review, so the viewport dimension of every owner visual matrix to date was unreviewable. Automated evidence is unaffected (the Playwright scripts set real viewport sizes and never touch the addon). Fix the wiring, then **prove it with a two-armed check**: a story that must reflow at 390 and must not at 1280. |
| **800** | reserved — **P1**, filed 2026-09-06 by the owner's question *"нахуя Storybook, якщо він не виконує свою місію"* | **Stories share the component but not the state, and nothing keeps the two in step.** Measured: `ListingDetailView.stories.tsx` imports the **real** `ListingDetailViewBody` (`:5`) — that link is real and gated by `check:story-coverage`. What is hand-written and unchecked is the **props**: all three states set `isGuest:true, listingId:undefined`, so the authenticated view — the only one with a favorite — has never been rendered anywhere but production. The same prop object is assembled independently in `[locale]/listings/[slug]/page.tsx`, `admin/listings/[id]/preview/page.tsx` and the story fixture: **three constructions, one gate, no shared source.** Deliverable: one exported view-model builder consumed by both routes **and** the story fixtures, plus story states for the real production variants (guest · authenticated+favorited · owner-deleted · closed · archived · staff-preview). Fold in: story fixtures point at `https://example.com/*` so every gallery renders as grey placeholders — deliberate (no network dependency) but it makes a gallery visually unreviewable; replace with local static fixture assets. |
| **798** | reserved — **P3**, filed by 791's review 2026-09-05 | **Leaflet's own control and attribution chrome is English on every locale of the detail route.** `Marker` · `Close popup` · `Zoom in` · `Zoom out` · `A JavaScript library for interactive maps` · `Leaflet` · `OpenStreetMap` — none exists in `messages/*.json`, and `src/components/shared/Map.tsx:31` sets only the OSM `attribution`, configuring no title/aria strings. Pre-existing; **newly visible** only because Task 791 enrolled `Patterns/Mantine/ListingDetailView` into `check:locale-leak --mantine-only` (21 of its 107 cells). Decide per string: localize via Leaflet's options, or record as an accepted third-party exception in the detector's allowlist with the reason. **Do not translate the `Elira Hoxha` fixture** — that one is a `PER_STORY_TOKENS` entry (Task 624). |
| **794 · 795 · 796** | reserved — **Sprint 71** | **794** gallery (migrating `ListingGallery.tsx` breaks `check:surface-census:changed:verify` arm 12, which uses it as the unmigrated `.tsx` control — swap the control in the same diff; 831 note) (`GalleryStaticFrame`/`ListingGallery`; `LightboxView` moved to 825 on 2026-09-16), **LCP static-frame/island swap is the constraint** · **795** the three legacy `@/components/ui/dialog` dialogs, Q4 (inquiry + report are critical-flow rows) · **796** create/edit form family — **first consumer of the three form patterns**, which is what the owner's deferred bare-pattern gutter decision is waiting on. Full text → `backlog-reserved.md`. |
| **790** | reserved — P1, filed by 788's review 2026-09-05 | **The full `npm run test` suite is red and no task noticed.** 5 failures / 4 files, all pre-dating 788 (proved read-only: none of the failing suites touch a path 788 changed). The named one: `theme.d69-18.test.tsx:257-259` asserts `FooterView.tsx` contains the literal `theme.other.layout.footerGridGap`, but the Task 784 outage hotfix (`34faa47a9`) wrote `theme.other!.layout!.footerGridGap` — the non-null assertions break a substring match while the sibling runtime check at `:134` still passes, so the contract holds and only the mechanical check is wrong. Also red: `css-var-resolvability.test.ts` (296 vs an asserted 257), `appimage-config-class-assertions.test.ts` (self-declared BLOCKED), `ListingCard.smoke.test.tsx` ×2 (archived badge); and (822's review) `ListingGallery.portal.smoke.test.tsx` ×4 — jsdom has no `ResizeObserver` (`useSwipeTrackSync.ts:101`), red since at least 803 — **this is 825's 612 regression set, so 825 starts red**. **Root cause of the blindness: no kickoff's verification plan runs the full suite** — 787 ran `vitest run src/components/layout/__tests__` only, and its review (mine) accepted that. 790 decides per failure whether the test or the source is wrong, and whether the full suite joins the standing gate set. **Widened by 810's Revision 1 review, 2026-09-10 — the redness is not one set.** The full suite was run four times with **no source change**: 5 / 9 / 9 / 10 failures over 4 / 6 / 7 / 7 files. The five above are in **every** run; a second group — `filtersPanelShell.smoke`, `filtersRangeDatePicker.smoke`, `heroSearch.smoke` — appears in some and not others, at 26-31s durations (timeout-shaped), and none is reachable by an import path from 810's diff. 790 must therefore decide per failure **and** characterise the non-deterministic group (test-order pollution vs real timeout), then decide whether the full suite joins the standing gate set. Re-measure the variance at execution; do not cite these counts. |
| **789** | reserved — **Sprint 70**, P2 | Migrate the **live** hand-rolled filter bar in `src/modules/cabinet/components/ListingsTab.tsx` (rendered by `CabinetShell.tsx:137`) onto canonical Mantine: 3 raw `<button>` and 49 `className` in the user cabinet. Carries two named hazards — `visibility.test.ts:357` reads that file **by path**, and `:170` declares a local constant named `FilterBar`. Filed by 788's consumer trace, which found the component of that name unconsumed while this bar is live. |
| **761** | reserved — **Sprint 61**, P1 | CommonMark fence detection for `check-ledger-claim-projection`, binding all five opener/closer cases. **Open owner decision:** whether an unclosed EOF fence is bad input (exit 2). Not a rework of the archived 747. Full text → `backlog-reserved.md`. |
| **675 §8 family** | **676** → S57 · **679** → S56 · **677** owner | **676** stale hex comments in `globals.css` — documentation drift, no rendered consequence; must not re-touch `--brand-950` (694's) · **677** the `<div>`-in-`<p>` FiltersPanel warning (owner item) · **679** `usePropertyTypes` fallback localization, **folds 680**; Sprint 56 lands the detector fix first so the leak fix has a failing arm. Measured ΔE inventory → `backlog-reserved.md`. |
| **682 · 683** | **682** → Sprint 57 · **683** blocked | **682** drop `sonner` + `next-themes` from `package.json` — **confirm both are still unused at execution time, not from this row** · **683** TailAdmin bottom-nav conformance slice, blocked because no reference row exists yet. Full text → `backlog-reserved.md`. |
| **745** | reserved — **Sprint 46.10**, Q4 | A partially unstyled capture is admitted as a real width verdict. Detect that the **measured controls themselves** are unstyled, retry, then classify as `unstyled-render` — never as a product width regression, and never retry a valid `fullWidth=false` into a pass. Per **D37** the mechanism is not established: 745 must characterise it. Evidence → `backlog-reserved.md`. |
| **744** | reserved — **Sprint 46.9**, filed by D36 | Attribute `/[locale]`'s route-specific client JS — **the deliverable is the attribution, not a fix**, with the pattern barrel and the homepage client islands as the two named starting points. Independent of 691. **Re-measure every figure at execution; never quote one from a document.** Evidence → `backlog-reserved.md`. |
| 730 · 734 · 736 · 738 · **746** · **750** · **759** · **833** · **834** · **835** · **836** · **838** · **760** | reserved — full text in [`docs/backlog-reserved.md`](backlog-reserved.md) | **730** chip-row toggles announce no selected state (Sprint 55, folds 731) · **734** 25 N1 violations inside `src/design-system/mantine`, incl. the `zIndex: 100` sticky-layer escalation · **736** classify the AdminUsersTable locale-leak findings, do **not** translate them · **738** the horizontal-scroll blind spot, 729's residual 40 · **746** P1, filed by 691's review — no control can see a hover-state or real-consumer (`AppImage`, not `DemoImage`) regression on the listing card; define the detector + its false-positive boundary first, then three failing plants· **750** P3, filed by 749's review — the `ambiguousOverlap` counter (`check-stories-rendered.mjs:1804`) prints as `\| ambiguous-overlap \| N \|` (`:1859`) but aggregates **five** reasons after 749 (measured: overlap 4 · offscreen 4 · ellipsis 16 · text-clipped-scrollable 18 · outside-scrollable 6); same family as **738**. Carries N1: record in `storybook-governance.md` that a `GEOMETRY_ALLOWLIST` row is strictly more permissive than a structural predicate. · **834** `ViewAllLink` has no Story of its own and no manifest entry, baselined in 6 census rows (filed by 827 review 5). · **835** white `Loader` on a disabled pending `Button` in `SaveSearchButton`/`SaveToCollectionButton` (filed by 829 review 1). · **836** P2, filed by 826's review 1, re-measured 2026-09-17 by 837's review — `check:locale-leak:mantine-only` is **non-deterministic run to run with no diff to explain it**. The two retained transcripts: `task826/15-check-locale-leak-rerun.txt` = 392 leaks / 24 stories (output `...T19-22`Z) vs `task837/13-check-locale-leak.txt` = 164 / 22 (`...T20-02`Z). Both print `Mantine selected: 142`, so scope is identical; the -228 is **two whole stories going silent** — `NotificationPattern/Default` (210 leak lines) and `PageHeaderWithActions/Default` (15) — plus the 3 `FavoriteButton` pill lines 837 deleted (392-225-3=164). **This row's old '+225 = `CopyIdButton`/`CountButton` manager chrome' claim was wrong**: neither transcript supports it (`04-check-locale-leak.txt` is a 32-line tail excerpt with no count line). The iframe-404/manager-UI fallback stays a hypothesis to prove, not a measured cause. Deliverable unchanged — fail closed + ordering decision + two-armed plant, with run-to-run determinism as the first arm. **Re-measure at execution; do not cite these counts.** · **838** P2, filed 2026-09-17 by 837's design census — `ListingShareButton.tsx` has no manifest entry and no Story of its own (`manifest:no story:no`), baselined under three surface keys; it is the `share` slot sitting next to the favorite in the detail badges row, and 837's R3 dispositions its stand-in but does not enrol it. |

**Unnumbered follow-ups** — candidates, not tracked state; each becomes a numbered task or an owner row when scheduled: the `ListingCard`/`HomepageListingGrids` live relative-date fixture spuriously flags on any Q3 re-capture taken on a different calendar day · add `TextInput/Default`, `PopularLocationsView/Long City Name` (sq@390) and `LocationComboboxSubPanel/Default` (en@320) to `storybook-governance.md` §8.1 · no regression test for `check:mojibake` (666 F1) · the standing AMBIGUOUS cells have never been triaged once — owner-triage by design, **never citable as green proof**; count them at the moment you act, never from a number written here. · **from 810's Revision 1 review (all P3, all in `scripts/task810-rail-controls-probe.mjs` or the track CSS):** the hit-test assertion fails open on `inViewport:false` (same shape as the Revision 0 gap it replaced); `.grid > a {height:auto}` is provably inert — planted and reverted grid cells are byte-identical, because a percentage block-size resolves against a definite grid area, so the stretch defeat is flex-only — decide whether to narrow the selector or keep it as a documented guard; AC11's exit-2 usage path retains no transcript; the probe's `RUNG_THRESHOLDS` are px while the CSS rungs are `em`, agreeing only at a 16px root. · **from 808's review (P3):** `scripts/task808-key-warning-probe.mjs` is not wired into `package.json`, unlike its own cited precedent `check:hydration` (four entries) — a two-armed proof, not a standing guard; decide whether it becomes `check:key-warning` and joins the gate set, noting it needs a `LINK_HREF` that is actually rendered on the index (it exits 2 when it is not, so it fails closed). · **from 809's review (P3), for Task 790:** `scripts/__tests__/overlay-dual-declaration.test.ts` is **order-dependent** — it reads `.next/static/css` and throws `not found — run "npm run build" first` whenever the full suite runs before a build (809's `i7` fails on it, its post-build `i7b` does not). That is a third, distinct class of full-suite redness alongside the five deterministic failures and the non-deterministic pair.

## Active Epics

**HH — Admin UX System** 🟠 310 (P4) · 311 (P5 partial) · 313 (P6 — **schema contract recorded 2026-08-27, awaiting the owner's signature**; see Pending Action Items); 308/309 re-scope onto canonical primitives → [`Epic_HH_…`](../tasks/Epics/Epic_HH_Admin_UX_System.md) · **KK — Admin Data Freshness** 🟠 **453 only, and it is conditional** — it opens *only if* 452's investigation exposes a page needing special handling; no such page has been named → [`Epic_KK_…`](../tasks/Epics/Epic_KK_Admin_Data_Freshness.md) · **MM — Mantine/TailAdmin Restyle** 🗄️ **HISTORICAL as of 2026-08-27 (owner decision).** `docs/mantine-tailadmin-migration-tracker.md` and `tasks/Epics/Epic_MM_Mantine_UI_Migration.md` are **not** refreshed and are **not** a "what's next" pointer. **Live work = this file. File-level migration reference = `docs/mantine-responsive-design-system.md`.**

## Governance sources

> **Non-optional on every task.** [`docs/golden-rules.md`](golden-rules.md) **GR-1…GR-6 — receipt-enforced, non-negotiable; a response missing a required receipt is void** · `docs/agent-contract.md` (P0, clauses 1–15) · `docs/ai-behavior.md` Notes 18–23 · `docs/rule-index.md` (per-task pre-read) · `docs/orchestrator-procedures.md` (task design, review, Git policy, commit/push handoff, `index.lock` preflight, **recurring orchestrator failure modes**) · [`docs/binding-decisions.md`](binding-decisions.md) (**D19–D37**, closed and binding) · `docs/maintenance-playbook.md` **§14** (known console noise, advisor exceptions, assessed-harmless findings) · the Positive/Negative flow rule. **`npm run build` exit 0 mandatory for non-Q0.** **Design-system floor:** every task consumes `docs/design-system.md` wherever UI/responsive/overlay is touched; new UI uses Mantine (`docs/mantine-responsive-design-system.md`).
